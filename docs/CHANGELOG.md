# Changelog

All notable changes to the agent-board-toolkit are documented here. The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and the project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html) (pre-1.0: features minor-bump, fixes/refactors/docs patch-bump). See [`../VERSIONING.md`](../VERSIONING.md) for the full policy.

> **This changelog begins at [0.8.2].** Earlier releases (v0.4.x–v0.8.1) predate the changelog and are recorded only as git tags — run `git tag -l 'v*' --sort=-version:refname` and inspect each tag's release commit for their contents. They were not retro-documented here to avoid reconstructing summaries after the fact.

## [Unreleased]

## [0.27.0] - 2026-08-11

**Minor — a new host-local instrument that asks whether the fix a CLOSED Dependabot alert claims is actually present in what is deployed and running, wired as a fourth advisory leg of `board-session-close`.** 2 PRs since v0.26.0 (#230–#231): one feature, plus a docs-only solo-orientation sync (coord v0.33.0, #230). **Minor** per `VERSIONING.md § Bump sizing` — a new `bin/` tool plus new user-visible output on an existing one.

**⚠ UPGRADING — re-run the `PATH` symlink loop (`VERSIONING.md` step 11).** This release ADDS a bin, and a symlink tracks its target's *content*, not the *set* of tools: without the loop `dependabot-deploy-reconcile` never lands on `PATH`, and `agent-board-toolkit-runtime-check` — which gains it in its `TOOLS` set — prints a new warn line naming exactly that. The warn line is the check doing its job; the fix is the re-symlink it already prescribes. **Two new preflight entries in `docs/INSTALL.md` §0, neither fatal:** `ps` (absent ⇒ the new tool reports an instrument failure rather than a wrong answer) and coreutils `timeout` (absent — the default on macOS, and possible under the MSYS install — ⇒ `board-session-close`'s advisory legs still run, each printing a one-line warning that it is running **unbounded**).

**No narrowings this release, and no interface change for action-pinning consumers** — `promote/action.yml` and `release-artifacts/action.yml` are untouched, as are the board-writing CLIs (`bin/kbcard`, `bin/board-card-start`, `bin/next-dl`, `bin/promote-released-cards`, `bin/release-pr-body`). Two existing bins do change what a consumer sees: **`board-session-close`** grows a `── Dependabot fixed-alert vs deployed-tree reconciliation ──` section with its own warnings, runs **both** advisory legs under a 60s wall-clock bound (a leg killed at the budget prints a ⚠ and still never blocks the close), and reworded the archive leg's missing-sibling warning slightly — which matters only to anything matching on its exact text; **`agent-board-toolkit-runtime-check`** gains one `TOOLS` entry. New files: `bin/dependabot-deploy-reconcile` + `bin/_dependabot-reconcile.py` (host-local by design — fixed scope, not a vendorable per-repo tool).

### Added

- **card#6277** — **`bin/dependabot-deploy-reconcile`: does the fix a CLOSED Dependabot alert claims actually exist in what is deployed and RUNNING?** An alert closes when its fix reaches the repository's default branch. Production serves something else — a tag, a release clone, an installed dependency tree — cut by a separate process on its own cadence, and nothing in the toolkit compared the two. A repository whose alert list reads 0-open can therefore be serving the vulnerable version indefinitely, with no symptom anywhere and no artifact recording that nobody checked. The new tool implements **one leg**: an installed npm dependency tree, whose evidence class is `installed-file` — the identity IS the version of the file the runtime loads, so no git tag is read and none is claimed. It resolves the deployed directory from the MCP config that **launches** the server (never a hardcoded path — that resolution is what ties "the tree I read" and "the tree it is running" to the same claim), reads each alerted package's installed `package.json` version, and compares it against the alert's own `first_patched_version` under **SemVer 2.0 precedence**. **The comparator is an explicit conservative proxy — `installed >= first_patched_version`, and `vulnerable_version_range` is never parsed:** each ecosystem's range grammar differs with no single published spec, and a range-parsing bug is a silent FALSE NEGATIVE (a genuinely vulnerable version cleared), where the proxy can only ever over-report. `>=` and not `>`, so a version EQUAL to the first patched version is FIXED — a boundary the suite exercises through the shipped argument path in both directions (equal ⇒ `FIXED_CONFIRMED`, one patch below ⇒ `STILL_EXPOSED`), because a comparator accidentally spelled `>` passes every healthy-tree case. **Four dispositions, and only two of them are answers:** `FIXED_CONFIRMED` / `STILL_EXPOSED` / `UNDECIDABLE` (the read is fine, the comparison cannot be made — no `first_patched_version`, a non-semver version, the package is not in the deployment) / `UNVERIFIABLE` (the read itself cannot be trusted — no running process for this install, a `package.json` newer than the running process, a nested `node_modules` copy making the loaded copy ambiguous, an alert outside the leg's declared ecosystem/manifest set). Nothing is ever silently skipped: an alert this leg does not cover gets a row naming why. **The tree's own readability is gated before any per-package read, and that gate is the difference between a finding and a fiction:** with `node_modules` absent or unreadable, every alerted package's `package.json` is missing, and answered one package at a time that renders as `UNDECIDABLE: package not in deployment` — a decidable FACT about the deployment minted out of a read that never happened, at rc 0, beside a coverage entry still claiming the install is covered. The window is the ordinary one (`npm ci` REMOVES `node_modules` before repopulating it, and this runs at session close), so every alert now resolves `UNVERIFIABLE: node_modules not readable at <path>` and the coverage entry flips to `{covered: false}` carrying the **same reason string** — rows and coverage statement are one claim about one install, and an instrument reporting both ways leaves a reader no way to tell which half to believe. Absent and unreadable are named apart because the operator remedies differ (wait out the reinstall vs. fix the permissions). **That predicate is now one primitive applied at every directory read rather than a check written per site** — a sibling audit found the same shape open at two more of them, and both mattered: the nested-copy scan (`os.walk` swallows every listing error by default, and a subtree it cannot enter reads as "no nested copy here", which is precisely what LETS the top-level read stand — so an unreadable subtree did not degrade the verdict, it UPGRADED it to `FIXED_CONFIRMED`), and the per-package read (`os.path.exists` answers False for a package that is missing AND for one whose directory cannot be traversed, so the code contradicted its own docstring's claim to separate the two). The readability test is read+execute, not read: a directory with read but no execute lists its children and then reports every one of them as a non-directory, which is the quietest form of the same lie. The nested-copy scan now classifies **every** directory entry positively — a plain directory, a plain file, or a symlink resolving to a regular file are the only outcomes confirmed harmless, and everything else is recorded — because enumerating the unsafe shapes kept missing one: a symlinked subtree escaped the walk entirely, and then a symlink whose *target* could not be stat'd escaped the symlink check too (`os.walk` swallows the error and files it as a regular file). A symlinked subtree is recorded rather than followed — under the default `followlinks=False` a symlinked directory is listed but never walked, so neither the error handler nor the per-directory check ever sees it, and an unwalked subtree reads as "no nested copy here" (file-symlinks, as in `node_modules/.bin`, correctly do not fire it). **No count of these routes is claimed in prose any more** — that claim has been falsified twice — so the code points at the fixture set that asserts them instead. **Every alert query is gated, because this API fails in ways a status code does not show** — measured, not assumed: an **unknown** state value answers **200 with `[]`** (`?state=all` and `?state=bogus` both do), so a typo'd filter yields an empty population at a success status, indistinguishable from a real "no alerts in this state". A **mis-cased known** value is not that case — the filter is applied case-insensitively (`?state=Fixed` returns the same set as `?state=fixed`) — so it is the enum guard, not casing luck, that keeps a typo out; the per-response `.state` re-assert is a **boundary check on a remote answer**, kept because no gate should rest on the absence of a currently-known bad shape. So each query checks the command's rc, asserts the body is a JSON array, sends only a literal from a validated enum, and **re-asserts every returned alert's own `.state`** against the one queried; the three excluded states are **counted** and reported (so `state=fixed` is a named inclusion with measured exclusions rather than a silent filter) and the integrity control sums **all four** per-state counts — the included state as well as the three excluded — against an unfiltered query, so a short read of the population itself is caught and not only a short read of what was excluded, with a zero-length population treated as an **instrument failure (rc 1)** — not as an `UNVERIFIABLE` disposition, which is a per-alert verdict and cannot be emitted over zero rows — unless that unfiltered query confirms the repository genuinely has zero alerts in every state. **The staleness input is each READ `package.json`'s mtime against the running process's start time**, not the `node_modules` directory's: a reinstall that rewrites one package leaves the directory mtime saying nothing about the file the process actually loaded, and this tool runs at session close on a host where the tree may have just been reinstalled. Process detection reads `ps` whole and filters in-process — `pgrep -f`/`pkill -f` are refused rather than escaped, since their pattern is matched against the very argv that carries it. Installs on the host **other** than the one read are enumerated and reported `{covered: false}` with the reason, so the run states its own coverage instead of implying it — and that detection is keyed on the `channel-servers/<file>.mjs` path shape alone rather than on the repository name, because an install cloned into a differently-named directory is still one this run does not cover, and a coverage statement that silently omits an install reads as a complete one. **Fixed scope, host-local by design:** alerts are queried for one named repository, and the deployed directory is resolved from this host's own MCP server entry — it is the agent host's session instrument, not a vendorable per-repo tool. Output is one line per alert plus a **scope object** (run identity, states queried with their counts, per-install coverage, denominator, any injected identity) emitted into **both** stdout and a JSON artifact under `~/.cache/coord/dependabot-reconcile/`. **Exit contract, and it is the point of the tool being advisory:** rc 0 = the instrument ran and every alert carries a disposition, INCLUDING `STILL_EXPOSED` and `UNVERIFIABLE` rows, which are findings reported in the output and deliberately not folded into the exit code; rc 1 = an input was unreadable, a gate failed, **or the evidence artifact could not be written** — so the dispositions are absent, untrustworthy, or unrecorded (where a gate is what failed, the artifact still records what was measured); rc 2 = usage error. The 1-vs-2 split follows `agent-board-toolkit-runtime-check`, the closest sibling. **That three-value contract is measured on the path `board-session-close` takes, and is not yet universal on a hand-run one:** the advisory leg captures the tool's output, so its stdout is never closed early, but a **manual** invocation piped into a reader that exits first escapes as a `BrokenPipeError` at **rc 120** — the top-level backstop wraps the measurement phase and not the output phase. Known and tracked at release, bounded to piped manual invocation; the session-close leg is unaffected (verified rc 0 on that path). **A host that is not configured for this tool is not a broken one:** with no `~/.mcp.json`, or one declaring no `mcpServers.kanbanboard-agent` entry, the run prints a single not-configured line, writes **no artifact**, and exits **0** — previously this was a rendered INSTRUMENT FAILURE at rc 1 at every session close, forever, on every host that was never in scope, and a warning that always fires is one nobody reads on the day it means something. A declared entry that launches no `.mjs` is the opposite case — something is meant to run here and cannot be located — and stays rc 1, as does a present-but-unparseable config. **A package name is validated POSITIVELY — accepted only in npm's two legal shapes — and a bad one costs only its own alert.** Every earlier version of this check enumerated BAD shapes, and each review round found one the list had not named; the last two were the expensive kind. A `None` name (through an `or ""`) path-joined to the `node_modules` ROOT and reported `FIXED_CONFIRMED` about a package that does not exist, and a `../`-bearing name read a file OUTSIDE the deployed tree and minted `FIXED_CONFIRMED` from it. The rule is now what a name IS — one path component, or `@scope/name`, with no empty, relative, separator- or NUL-bearing segment — and everything else is refused. It is checked only for alerts this leg actually handles, since the grammar is npm's and another ecosystem's names are legal in their own (composer's `vendor/package` is the live case). Backing it, independent of it: a containment invariant asserting every path this leg reads resolves strictly under the deployed tree — the check that holds when a grammar turns out to be wrong about a shape, which is the thing that has actually kept happening. A malformed name now yields an `UNVERIFIABLE` row naming the reason at rc 0, rather than failing the run: one bad alert must not destroy the other eighteen measurements. **An unexpected exception anywhere in the run now still produces an artifact.** A top-level backstop turns anything unanticipated into an instrument failure in the tool's own vocabulary — rc 1, `INSTRUMENT FAILURE` framing, the rows and scope measured up to the fault, and the traceback recorded under `scope.unhandled_traceback` — instead of a raw traceback with no artifact at all, which made the exit contract's promise that a failed gate still records what was measured false exactly when it mattered. Three wrong-typed scalars in an alert payload were escaping that way (a `number` mixing int and str so the population could not be sorted, an unhashable package name, a non-string name reaching a path join); each also gets a NAMED cause in the query gate, because the backstop can only say "unhandled TypeError" and a named cause is what makes a report actionable. **Malformed API and file payloads now degrade to dispositions instead of tracebacks.** An alerts array whose ELEMENTS are not objects passed the array check and then killed the first field read — an `AttributeError`, no `INSTRUMENT FAILURE` framing and no artifact, contradicting the exit contract's promise that a gate failure still records what was measured; the element shape is asserted in the query gate so no consumer repeats the check. The same assumption one level down (`dependency`, `dependency.package`, and a `package.json` parsing to a JSON array) was closed with it: the `x.get(k) or {}` idiom guards against null and a missing key, but a present-but-wrong-typed value is truthy and sails through, so all three crashed. They now resolve to a named `UNVERIFIABLE` row, which is safe only because every field read out of those objects feeds a fail-closed disposition. A `--test-mode` flag routes artifacts to a **separate** directory so a synthetic run is never mixed into the evidence trail, and `--inject-identity` (accepted **only** alongside it — a stray one is a hard error, never a silent override) drives the disposition engine through the real argument path rather than a side harness. Wired as a fourth advisory leg of `board-session-close` on the same contract as the archive leg: a non-zero exit warns and never blocks the close. **New `tests/dependabot-reconcile-selftest.sh`** — network-free (`gh` and `ps` are stubbed from shellchecked `tests/_dependabot-*-stub.sh` files), each check pointed at a fixture that drives its failing branch with a control differing in one input — with one stated exception, below. **The figures are the suite's own measured output, re-derivable rather than recorded:** `bash tests/dependabot-reconcile-selftest.sh | grep -c '^  ok'` reports the assertion count (257 at this writing), and the suite's exit status is the only pass claim it makes. **The warranty is deliberately weaker than "every check can fail":** an adversarial re-read of this change mutated the shipped code and found two checks that could not — the excluded-state counts (every fixture held 0 in all three excluded states, so hardcoding the emission to 0 survived) and `gh_alerts`' call to the `state=` validator (for the only literals a real run passes, the validated and unvalidated URLs are byte-identical). Both are now pinned — the first by a fixture carrying **distinct non-zero** counts in all three excluded states plus an all-zero control over the same keys, the second by a unit probe that drives `gh_alerts` with `subprocess` replaced by a recorder — and each was watched red under the mutation it closes and green on restore. **A third gap is stated rather than papered over:** three arms of the nested-copy classifier — the “neither a regular file nor a directory” fault append, and the two `OSError` handlers recording an entry that could not be classified or a directory that could not be listed — carry no fixture that reds them, so the per-check warranty above does not extend to them. The behaviour behind them was exercised by hand (a FIFO, a unix socket, a symlink to a character device, and an `ELOOP` chain), and each recorded a fault as designed; what is missing is the assertion, not the behaviour. Tracked for a follow-up fixture. `board-session-close`'s own suite gains the new leg's rc/indent/absent-sibling contract. **The advisory-leg shape is extracted rather than duplicated (canon #5, at the second caller):** `board-session-close` now delegates both its archive-eligible and its Dependabot legs through one `_bsc_advisory_leg` helper — sibling resolution beside the bin, an existence check whose predicate stays per-leg (one sibling ships mode 644 and is run through `python3`, so requiring `-x` would report the correctly-installed file as missing), the `2>&1` fold that keeps a delegate's stderr from reading as a ritual-level warning, the two-space indent, and a non-zero exit that warns without propagating. The per-leg wording stays distinguishable, and with both siblings present-and-quiet in each fixture the suite's absence assertions are back to their broad form: a healthy run must leave **every** leg silent about a missing sibling, not just the one a needle happened to name. **Consumer impact — the composite actions are untouched; two bins do change.** `promote/action.yml` and `release-artifacts/action.yml` are unchanged, so action-pinning consumers see no interface change, and the new tool is itself a host-local session instrument rather than a vendorable one. But "no existing `bin/` behaviour changes" would be false, so it is not claimed: **`board-session-close`** grows a new `── Dependabot fixed-alert vs deployed-tree reconciliation ──` section with its own warnings, and both its advisory legs now run under a **60s wall-clock bound** (a leg killed at the budget prints a ⚠ and still does not block the close) — and where coreutils `timeout` is absent, as on a default macOS or MSYS host, each leg still RUNS and prints a one-line warning that it is unbounded, because that bound was added to a helper extracted from two legs, one of which pre-dates it: making it a hard dependency would have broken a working leg to add a guarantee those hosts cannot have. `docs/INSTALL.md` §0 gains `timeout` and `ps` to its preflight — the archive leg's missing-sibling warning is also reworded slightly by that extraction, which matters only to anything matching on its exact text. **`agent-board-toolkit-runtime-check`** gains one `TOOLS` entry, so an install that took the upgrade **without** re-symlinking `bin/` onto `PATH` gets a new warn line naming the missing tool — that line is the check doing its job, and the fix is the re-symlink it already prescribes.

## [0.26.0] - 2026-08-10

**Minor — `next-dl --peek` stops handing back an already-allocated DL number, and a repo whose commit subjects say `card#NNNN` can finally have its release-PR card-coverage gate fire.** 5 PRs since v0.25.0 (#224–#228). Minor per `VERSIONING.md § Bump sizing` — new user-visible surface (`promote-released-cards --cards`, the optional `.release-pr.json` `card_token_regex` key) alongside the fixes.

**⚠ UPGRADE FIRST IF ANYONE USES `next-dl --peek` TO CHOOSE A DECISION-LOG NUMBER.** Before this release `--peek` answered from an offline scan of the DL numbers that had reached a *card*, so any number already **claimed** from the board's DL counter but never stamped onto a card was invisible to it: `--peek` handed back a number the server had already issued, once per burned claim. A DL is a correlation key, so a reused one silently merges two unrelated work items, and the offline scan is structurally unable to detect that state — it is a floor, not the truth. `--peek` now reads the board's non-consuming DL-sequence counter and falls back to the offline scan only where that endpoint is absent, so a board whose kanban predates the inspect route sees no change and a board that has it starts getting the authoritative answer. If your `--peek` and a direct counter read disagree, your `next-dl` predates this fix. The default (claim) path is untouched.

**No narrowings this release.** Changed vendorable files: `bin/next-dl`, `bin/promote-released-cards`, `bin/release-pr-body`, plus a description-only correction to `release-artifacts/action.yml`; `promote/action.yml` and the board-writing CLIs (`bin/kbcard`, `bin/_kb-board-lib.sh`) are unchanged.

### Added

- **card#5877** — **`.release-pr.json` gains `card_token_regex`, and `promote-released-cards` gains an opt-in `--cards`, so a repo whose commit subjects say `card#NNNN` can have its release-PR card-coverage gate actually fire.** `release-pr-body`'s `## Card coverage` section derived its manifest from `ref_token_regex` alone and short-circuited an empty one with a confident `_No shipped DL refs in range._`. In a repo whose subjects have moved to `card#NNNN` while that key still says `DL-`, the manifest is unconditionally empty, so the section reported clean **without checking anything** — a check that cannot fail. Reproduced before being fixed: over a range carrying a `card#` ref, with a board token present and a promoter that reports every ref it is handed as cardless, the section still rendered the clean line; the same range under a matching regex rendered the warning, so the reproduction discriminates. **The one-line config edit is a worse bug, and this is measured, not argued:** `promote-released-cards` reads the *same* key and matches the token's numeric part against `payload.dl_number`, so under a `card#[0-9]+` spelling a range naming `card#42` correlated with the card carrying **DL-42** — it moved that card, left card 42 alone, and summarised `1 moved, 0 no-card`. The two spellings are therefore two **id spaces** with two keys: `ref_token_regex`'s numeric part is a decision-log number matched against `payload.dl_number`; `card_token_regex`'s is a **card id** matched against the card's own `id`. **Consumer impact — additive, and nothing changes for a repo that adopts neither key.** `card_token_regex` is optional, and an existing consumer that does not add the key sees no change at all; note that `examples/release-pr.json.example` **does** set it, so a consumer copying that template starts with the coverage section enabled; `promote-released-cards --cards "12,34"` is opt-in and, with the flag absent, produces byte-identical stdout, stderr and PATCH set (diffed against the pre-change script across five invocation shapes: multi-DL with a cardless ref, the stage guard, an already-released card, a no-match set, and `--dry-run`). Two visible changes for a repo that **does** set the new key: the coverage section's prose now says "refs" rather than "DLs" (`_No shipped refs in range._`, `_All shipped refs have a tracking card._`), and the body gains a second footer `<!-- release-manifest:shipped-cards=5877,5874 -->` alongside the unchanged `shipped-refs` one — **bare ids, not tokens**, since the existing `tr '[:lower:]' '[:upper:]'` fold would otherwise deliver `CARD#5877` to a consumer. The bundled list also renders a card token where a commit has no DL token, DL-first so an existing repo's rendering is untouched. **Card ids are deliberately never derived from commit subjects on the MOVE path:** a descriptive citation ("supersedes card#1234") is common, and where a DL token's misfire is bounded by the card also needing a matching `dl_number` stamp, an id *is* the match — so a passing mention would relocate an unrelated card. `--cards` takes them explicitly and is strictly numeric (`--cards "card#12"` is a config error, not a lenient parse); only `release-pr-body`'s read-only coverage call derives them, under `--dry-run`, where the worst outcome is a line reading "covered". Both selftests were **watched red on the pre-change tree** — the coverage assertions fail and the run dies at the unknown `--card-manifest` — each with a control that flips only the board's contents, so "reports every ref" cannot satisfy them. Three hazards found by the reread of this change's own diff are closed with it. A `set -o pipefail` one in `bundle_lines`: a subject with no token makes its `grep` exit 1 and the assignment inherit it, and only bash's errexit suppression for a command substitution inside an assignment kept the bundled list from truncating at the first token-less commit — the no-match case is now explicitly valid rather than valid by where the caller happens to put the call. The card id is the matched token's **trailing** digit run rather than a `grep -oE '[0-9]+'` over the whole stream, because `card_token_regex` is operator-supplied and need not be `card#…`: a prefix carrying its own digits (`PROJ2-card#77`) yielded **two** ids, `2` and `77`, and `2` is a real id belonging to an unrelated card — a token whose id is not trailing now yields nothing (visible in the coverage section) rather than minting one. And `--manifest --card-manifest` together is now rc 2 instead of printing one list and dropping the other in silence — the card#5429 shape, and worse in a query mode whose output is machine-read; the rc assertion for it carries a control that each flag alone still answers, after the first spelling of that assertion was observed to pass for the wrong reason (a fixture with no origin makes the baseline fetch fail at rc 2 as well).

### Fixed

- **card#6232** — **`next-dl --peek` no longer reports an already-allocated DL: it reads the board's non-consuming DL-sequence counter, and falls back to the offline `max+1` scan only when that endpoint is absent.** `--peek` went straight to the offline scan, which takes its maximum over DL numbers that reached a **card** — so a number that was CLAIMED but never STAMPED is invisible to it and the answer under-reports by one per burned claim. Measured on a real board before the fix: `--peek` returned `DL-0220` while the counter's authoritative `next` was **222** (`last_value: 221`, `live_max: 219` — two allocations that never reached a card), i.e. the tool handed back a number the server had already issued. A DL is a **correlation key**, so a reused one silently merges two unrelated work items, and the offline scan cannot ever detect that state: it is a floor, not the truth. Only the counter knows the claimed-but-unstamped numbers. **What makes `--peek` non-consuming is skipping the CLAIM, not skipping the read.** The two DL-sequence routes differ only in method, path and response field, so rather than ship a second copy of the transport they are **one `dl_sequence_call` primitive** with the wrappers passing those three plus their own error wording (canon #5, at the second caller rather than the Nth): the first draft of this fix was ~18 duplicated lines held in agreement by a comment asking the next editor to keep them in sync, and a drift there would have failed no test — it would just have made one route quietly answer a `500` from the offline scan. The consolidation is covered from **both** sides, and the claim path's error arms are now asserted for the first time (they were previously exercised only on the happy path): `404 ⇒ fallback`, `500 ⇒ rc 1`, and the message must carry the **claim's** own label and consequence clause rather than the peek's — watched red under an argument-swap mutation (3 assertions) and under `exit 3 → exit 1` (6 assertions, both routes). While consolidating, a stale contract comment was corrected: it assigned "a 2xx with no positive value" to rc 3, but the code has always exited **1** there, and the contract is now stated once on the primitive instead of restated per wrapper. The shared behaviour: **404 or unreachable ⇒ benign fallback** to the scan (pre-endpoint behaviour, unchanged), **a present-but-errored endpoint ⇒ rc 1 with the cause on stderr**, never a quiet answer from the floor — a plausible wrong number minted into a decision log on a bad token is the worst of the three outcomes, and the pre-change tool did exactly that (it answered `DL-0220` on an HTTP 500). A `2xx` carrying no usable value stays a benign fallback, deliberately matching the claim path rather than the error path, since it is indistinguishable from an older response shape. **Consumer impact:** a board whose kanban lacks the inspect route sees **no change**; a board that has it sees `--peek` start reporting the counter's value, which is the point — if your `--peek` and the inspect call disagree, your `next-dl` predates this fix. The default (claim) path is untouched and is asserted so. The new coverage is a **control pair, not two passes**: both cases run against byte-identical board contents (one card stamped `DL-0219`, so the scan's answer is 220 either way) and vary only whether the endpoint answers — 222 is not derivable from the board contents at all, so a fix that ignored the endpoint reds the first assertion and one that broke the fallback reds the second. Watched red on the pre-change tree: **8 assertions failed**, including `mints nothing → got 'DL-0220'` on the errored endpoint, while the fallback and claim-path controls stayed green on both trees. `docs/DL-COUNTER-RECOVERY.md` is corrected in the same change because the code fix is what makes it true — step 1 now notes that the **old** `--peek` returned `liveMax + 1`, the correct-looking low value, and so **hid** the stranding symptom that step exists to find; and step 5 no longer presents `--peek` as agreeing corroboration, since it now reads the same route — one measurement displayed twice. What that line actually checks is the leg the inspect call cannot: that the operator's own board env, token and `api_base` resolve to the board they just fixed. Verified by executing the runbook step rather than asserting it.

- **card#6062** — **the `changelog-card-entry` gate moves out of `ci.yml` into its own workflow so a PR-TITLE edit re-runs it.** The gate's obligation set is derived from the commit subjects since the last tag **plus the PR title** — the title half is the entire reason the check is pre-merge, since under squash-merge the title *is* the coming subject. But editing a title fires `edited`, and `ci.yml` subscribes GitHub's default `pull_request` set (`opened`/`reopened`/`synchronize`), which does not include it: adding the `card#NNNN` token to a title after the last push left the gate's newest run answering about the **pre-edit** title, so the PR showed green with no entry owed and the omission reddened `dev` on the push after the merge — exactly the state the title read exists to prevent. A job cannot carry its own `types:`, so the fix is a workflow, not a trigger tweak: `.github/workflows/changelog-card-entry.yml` carries the job verbatim (`fetch-depth: 0`, `PR_TITLE` through the environment) under `types: [opened, edited, reopened, synchronize]`. This is the **same class** the release-artifact gate closed above — a gate whose verdict is a function of a PR field that only `edited` reports — with a different field (title, not base); `edited` also fires on body edits, accepted at one checkout plus a `git log` and a grep. The job **keeps its name**, so the `changelog-card-entry` status-check context is unchanged for anything keyed on it, and it keeps `ci.yml`'s `push: [main, dev]` leg, so the post-merge backstop on the integration branch is not traded away for the pre-merge fix. **`ci-matrix-parity-selftest.sh` had to be generalised in the same change or it would have called a genuinely-run test unrun:** its population was `ci.yml` alone, and it is now the **workflow directory filtered by trigger** — a workflow counts only if its `on:` subscribes `pull_request`, so a test named only by the push-only `auto-tag-version`/`release-promote-cards` workflows still reads as unrun rather than the rule loosening to "named somewhere". That predicate is exposed as its own projection and asserted in **both** directions against the real directory (admits `ci.yml`, rejects `release-promote-cards.yml`) plus a twin-fixture pair differing only in the trigger, and it was watched red under three mutations (predicate always-true, always-false, and dropping the `on` → `True` lookup that YAML 1.1 makes necessary — pyyaml resolves a bare `on:` key to the boolean, so reading only `doc.get('on')` admits nothing).

- **card#6054** — **the release-artifact gate's own trigger and the `INSTALL.md` §6c recipe gain `edited`, and two surfaces stop claiming artifact contents are read at both ends.** Retargeting a PR's base fires `edited`, not `synchronize`, and the gate's verdict is a function of the base — the merge base drives both the classification and the asserted diff — so a stale verdict survived a base retarget. Both consumer adoptions (kanban, bridge) added `edited` independently at adoption review; the write-site — the §6c recipe both consumers copied — and this repo's own `release-artifacts-gate.yml` still carried the omission, so the next consumer copying the recipe would re-mint it. Also synced: the gate's fetch-depth comment and the `release-artifacts/action.yml` description said the script reads "the version file plus the artifact contents at both ends"; it reads the version file at both ends and artifact contents at **head only** (`git show`/`cat-file` on head) — now stated exactly, matching the wording both consumers already corrected locally. The §6c recipe's fetch-depth comment ("both ends of the range are read") was vague rather than false — true of the version file — and is tightened in the same pass. The check script and the action's inputs are unchanged.

### Docs

- **card#6169** — **`.github/dependabot.yml` now records why this repository's Dependabot *security* updates are off, as a permanent posture rather than an unexplained gap.** Security updates ignore `target-branch:` and open against the repository's **default** branch, which here carries the version/tag invariant (`VERSION` + `auto-tag-version.yml`) that a dependency PR structurally cannot satisfy: it never bumps `VERSION`, so merging one on the default branch leaves it at a version an older tag already claims and auto-tag correctly refuses to re-mint it, leaving the branch untagged. Vulnerability **alerts remain enabled** — detection is intact, only the automatic security-PR channel is off — and the *version* updates configured in that file continue to honour `target-branch: dev`, so bumps keep arriving on the integration branch on cadence; anything urgent is hand-cut as a PR to `dev` and reaches the release branch through a normal release, never auto-patched onto it. The comment states that the posture is **permanent and does not lift**, because an unexplained disabled channel invites a later reader to re-enable it as tidy-up and re-arm the failure. It also pins the precondition a reader should **re-check rather than trust**: the posture holds only while the alert channel on the default branch is non-vacuous — verified for this repo as no package manifests at all, 4 workflow files, and `github-actions` as the sole declared ecosystem, so the alert channel covers the entire scanned surface here. **Consumer impact: none.** No `bin/` CLI, no `promote`/`release-artifacts` action surface, and no vendored file changes — comments only, with the parsed YAML asserted identical to before and the assertion watched to fail under both a syntax break and a `target-branch` mutation. The reasoning generalises to any repo whose default branch carries a version/tag invariant; the setting itself is per-repository and is inherited by nothing.

## [0.25.0] - 2026-08-07

**Minor — the declared release-artifact set becomes an assertion instead of a checklist, and `board-card-start` gains `-h`/`--help`.** 5 PRs since v0.24.0 (#217–#221). Minor per `VERSIONING.md § Bump sizing` — new user-visible surface (`bin/release-artifacts-check`, the `release-artifacts/` composite action, `board-card-start -h`) alongside the fixes. **⚠ UPGRADING — one narrowing:** `agent-board-toolkit-drift-check` now refuses a third positional at rc 2 where it previously read the first two and reported `drift-check: OK`. Changed vendorable files this release: `bin/agent-board-toolkit-drift-check`, `bin/agent-board-toolkit-runtime-check`, `bin/board-card-start`, `bin/release-pr-body`, plus the new `bin/release-artifacts-check` and `release-artifacts/action.yml`; `promote/action.yml` and the board-writing CLIs (`bin/kbcard`, `bin/_kb-board-lib.sh`, `bin/promote-released-cards`) are unchanged.

### Added

- **card#5910** — **`bin/release-artifacts-check` + the `release-artifacts/` composite action: `.release-pr.json`'s `artifacts` array stops being a checklist and becomes an assertion.** That array is the explicit **must-move-together** release set, and its only consumer was `release-pr-body`, which **prints** it as `- [ ]` lines in the generated body. A printer is not an asserter, so the class defect — *a guard that reads a proper subset of a must-move-together file set cannot fail on the member it never reads* — held here with a subset of size **zero**: nothing in this repo checked that `VERSION`, `docs/CHANGELOG.md`'s `[X.Y.Z]` section and `CLAUDE.md § Recent releases`'s row moved together, so a forgotten one shipped in silence. The new tool takes `--base`/`--head` and asserts the PR's **own** changes: the fork point `merge-base(base, head)` is resolved once and drives both the version read and the diff, never base's tip — a base branch that moves after the fork can otherwise spuriously satisfy the moved-leg for a member the PR never touched, or make a real release PR read as "version unchanged" and skip the whole gate. It expands each entry's `{{version}}` and any single-level `{a,b}` brace set, and asserts three things per member: it is an exact line of the captured diff, it still **exists** at head (a deletion appears in a diff, so the moved-leg alone passes one), and it **agrees** with the version — a `## [X.Y.Z]` section line for the `[{{version}}] section` shape, existence alone where the filename carries the version, else the version as a fixed string in the content. **Every** failing member is reported (`::error::` annotations plus an `N of M` summary), not just the first. Release-PR classification is by version **VALUE**, deliberately not "the version file appears in the diff": a repo may point `version_file` at a whole config file (kanban's is `config/app.php`), where the file-moved test misfires on any unrelated edit — base value == head value means "not a release PR", exit 0, so the gate is safe to run on every PR with no `paths:` filter. It **fails closed** (rc 2, naming which end of the range, the path and the `fetch-depth: 0` precondition) when the version file cannot be read at either end or the merge base cannot be resolved at all, rather than reading an unreachable base as "not a release PR" — that misclassification is a silent non-run of the whole gate. Adopted by this repo in [`release-artifacts-gate.yml`](../.github/workflows/release-artifacts-gate.yml) — which makes the check **red**, not unmergeable: it becomes binding through `VERSIONING.md` § Release flow step 8 ("wait for ALL CI checks"), or mechanically once its job is added to `main`'s required status checks. Post-merge `auto-tag-version.yml` stays **fail-soft by design**, because at that point the fix is a retag rather than a commit. `tests/release-artifacts-selftest.sh` builds a throwaway fixture repo under `mktemp` and watches each defect class red on its own branch (member not moved · CHANGELOG moved without its section · member deleted · unreadable version file at the fork point · a base branch that moved after the fork, in both the spurious-moved and version-masking directions · an unresolvable merge base · two failures reported as two), with the version-unchanged and fully-correct branches as the controls that keep "fails on X" from also being true of a tool that fails on everything.

- **card#5354** — **`board-card-start` gains `-h`/`--help`, printing its usage line to stdout at rc 0.** Before this, a hand-typed `board-card-start --help` fell through to the `-*` unknown-option arm, whose remedy line — *"for a branch name that starts with `-`, pass it after `--`"* — reads as advice to run `board-card-start -- --help`, which would target a **branch literally named `--help`**. That refusal text was also the only usage surface a hand-typed invocation ever reached. The arm sits **inside** the end-of-options guard, so `--` keeps claiming its argument: `board-card-start -- --help` still means the branch `--help`, not a help request (a selftest asserts exactly this, and goes red if the arm is hoisted above the guard). The refusal text is deliberately **unchanged** — it is phrased conditionally (*"for a branch name that starts with `-`"*), so it never instructed anyone to run that command; the actionable defect was the **absent** help surface, not the wording. Registration in `help-output-selftest.sh` was **machine-forced, not remembered**: adding the arm put the literal `--help` into the file, which immediately reddened the card#5339 completeness leg (`unaccounted bins with --help … got 'board-card-start'`) until the bin was named in `EXCLUDED` — the one-liner form, which cannot satisfy a contiguous-header line-count equality. That leg's registry arithmetic is now phrased against the arrays rather than restating their lengths, so it cannot go stale as the registry grows.

### Fixed

- **card#5429** — **`agent-board-toolkit-drift-check` no longer discards a third positional in silence, and it now names which argument is empty.** ⚠ **UPGRADING — a narrowing:** `agent-board-toolkit-drift-check <toolkit> <repo> <anything-else>` previously exited **0** with `drift-check: OK`, reading the first two arguments and dropping the rest without a word; it now exits **2** with `unexpected extra argument: <arg>`. This is the failure mode a gate must never have — the tool *is* the drift gate, so a caller who believed they passed a scoping or strictness flag got a green drift report about something other than what they asked for. The other half of the same rule was implemented but **anonymously**: an empty positional was refused at rc 2 behind a bare usage line that never said *which* of the two slots was empty, where the three bins card#5343 consolidated all name it (`<repo-dir> is empty (an unexpanded variable?)`) — with two positionals the naming is worth more, not less. Both halves now match the shared wording. **Found by the canon-#7 sibling audit run for card#5343's consolidation PR, not by report:** that consolidation covered the three sites the card named, and `bin/install-board-hooks`' own arg-loop comment had already written down this exact rationale ("Silently ignoring an extra positional is the same hazard one step further out") without it ever being applied to the sibling. **Deliberately not a fourth adoption of `kb_require_positional`,** on the ground the repo already applies to `agent-board-toolkit-runtime-check`: this tool is a **detector of `_kb-board-lib.sh`** — the content-drift loop diffs it, the MISSING-LIB leg reads it — so sourcing the lib would make the detector depend on the artifact under test, and a drifted or broken lib would take out the tool that exists to report it. The primitive is also single-slot by construction, and generalising it to N slots would cost the property that makes it simple in order to serve the repo's only two-positional bin — extraction belongs at the second real caller and there is no second. With fixed arity and no argument loop, `"$#"` answers both halves directly; the reasoning is recorded in `docs/CONSOLIDATION-PLAN.md` Stage C and the shared diagnostic wording is pinned by a header comment. **No caller is affected:** every invocation of the tool anywhere — the `drift-check-selftest` CI job (`. .`), the selftest, and the three documented examples in `INSTALL.md` §6b / `UPGRADE.md` §3 and §4 — passes exactly two positionals, and no consumer repo invokes it at all since both vendored drift-gates were retired for the SHA-pinned composite action. `tests/drift-check-fixture-selftest.sh` gains coverage of all nine input classes (arity 0/1/2/3+ crossed with which slot is empty), asserting on the **message** rather than the rc, because every one of these shapes already exited 2 — an rc-only assertion passes just as well when the refusal comes from the wrong guard, which is exactly how the anonymous case survived a green suite. Each new assertion was **observed red** under a mutation that removes the guard it covers, including the pre-card guard restored verbatim (which reds the three naming assertions and nothing else) and the empty-before-extra ordering that no other case can witness; the mutation harness positive-controls itself on both axes — the edit must actually change the file, and the suite must still run the full baseline assertion count, which caught one mutation whose pattern never applied. A repo-wide sibling audit over every `bin/` and `hooks/` file found **no other unguarded positional site** (n=1, no consolidation owed): the rest either adopt the primitive, carry `install-board-hooks`' standalone mirror, or refuse every unrecognised token through a `*)` arm — the audit method positive-controlled against the pre-fix bin, which it flags. `promote/action.yml` vendor surface unchanged.
- **card#5874** — **`release-pr-body` no longer reports a correct body as a failed generation when the commit range contains no ref-token match.** Its final statement was `[ -n "$MANIFEST" ] && printf '<!-- release-manifest… -->'`; with an empty manifest the test fails, and because it was the **last command the script ran**, its status became the script's exit status — a fully-written, correct body returned at **rc 1**. `set -e` cannot catch this: the left arm of an `&&` list is exempt, which is why the script ran to completion first. It contradicted the tool's own stated contract in two places (the header's "an empty range yields an empty (but valid) bundled section, not a failure", and `emit_manifest`'s "a release with no ref tokens is valid, not a failure" — that function already guarded itself with `|| true`; the final statement was the one place the intent was not carried through). Found while cutting the v0.24.0 release PR, and it had become **unconditional** for this repo rather than occasional: commit subjects migrated to `card#NNNN` while `.release-pr.json` still sets `ref_token_regex` to `DL-[0-9]+`, so the manifest is now always empty here and every future release cut would have tripped it. The optional-footer branch is now an `if` block, and the script closes on an explicit `exit 0` so the tail cannot leak a status at all — the narrower fix would have left the next edit free to re-introduce it. Verified on the real repo, not only the fixture: same command and same range exit **1** before and **0** after, with a byte-identical body. The selftest gained the no-token-range case it never had — **watched RED on the pre-fix tree**, with the same range under a *matching* regex asserted as the discriminating control, so a vacuous pass is not available to it. A repo-wide sibling audit over all 48 tracked shell files (both sites: script tail and function tail, the scan positive-controlled against a known-leaky fixture) found **no other instance** — every other `&&`-tail is a deliberate predicate function, an assertion helper that ends in `&& ok || bad`, or a fail-soft `|| true`, and the two scripts whose last textual line is a bare `fi` were read in full (they close on `return "$rc"` and `exit 0` respectively). The stale `ref_token_regex` itself is deliberately **not** changed here: switching it interacts with `promote-released-cards`, which matches the token's numeric part against `payload.dl_number`, so a `card#` spelling would compare card ids to DL numbers — tracked separately.
- **card#5910** — **the `promote-action-selftest` flag-parity check now DERIVES the wrapper's flag list instead of enumerating three literals.** It was three hand-written `grep -q -- '--dls)'` lines, i.e. the same subset-of-a-must-move-together-set shape as the entry above: a **fourth** action input would have been guarded by nothing, and no assertion can fail on the flag it never reads. The adjacent python step that already parses `promote/action.yml` now extracts every `--flag`-shaped token appearing **anywhere on an `args+=( … )` append line**, plus any on the exec line, prints the derived list, and asserts each has a `--flag)` case arm in `bin/promote-released-cards`. The scan is per line, so a multi-line append would derive nothing from its continuation lines (today's wrapper writes one append per line). Scanning the whole line rather than anchoring on the append's first token is what closes the two spellings a first-token pattern let through in silence — `args+=("--quoted-flag" …)` and `args+=(--a --b)`; it deliberately **over**-collects (a `--`-leading value literal would also demand an arm), which errs red rather than green. The derivation carries its own control (a non-empty list containing `--dry-run`), so a parser that silently derives zero flags **fails** rather than passing vacuously. Four controls were watched to fire: a quoted flag, a second flag in one append, an exec-line flag, and an emptied run block. **The exec-line half collects nothing from today's wrapper** (which passes only `"${args[@]}"`) — it guards a future direct-flag spelling and is verified by that mutation, not by the live file. A sibling `release-artifacts-action-selftest` job gives the new composite action the same wiring guard, deriving its assertion set from the action's own declared `inputs` so a fourth input cannot escape the loop.

- **card#5876** — **every surface that told a reader to `git checkout <tag>` now says which topology that is correct for, because on the recommended one it is not.** `docs/UPGRADE.md` §5 offered `git -C ~/agent-board-toolkit checkout <previous-tag>` as the host rollback and asserted on the same line that "rollback is always safe" — true about *state* (the toolkit stores none) and false about *topology*: on the INSTALL §1 **Option A** clone, whose whole contract is that "upgrades are then just `git pull`", it **detaches HEAD**, and §2's own `git pull --ff-only` then exits **1** (`You are not currently on a branch.`) — measured on a throwaway clone against an attached-HEAD control that exits 0. So the doc walked the reader into precisely the pinned topology INSTALL §2's warning box exists to keep them out of, where the silent cost is that the next release simply never arrives. Fixed on all four surfaces the sibling audit found, not just the reported one: §5 now states that rollback IS a deliberate temporary pin and gives the return path (and names `reset --hard` as the wrong escape — it diverges the branch and breaks `--ff-only` too); §2 names the detached case at the point of use; `VERSIONING.md` step 11 — where the same command is **correct**, its target being a deliberately-detached second checkout — now carries that scope on the line itself, since a line whose correctness lives only in a `<pinned-runtime>` placeholder is one copy-paste from being wrong somewhere else. **And `agent-board-toolkit-runtime-check`'s own remediation, the highest-traffic instance** (it fires at every SessionStart on a stale host, and it fired on the reporting seat), **now branches on topology** rather than assuming a pin: a checkout on a branch is told to `pull --ff-only` and why a tag checkout would detach it; only an already-detached one is told to advance the pin. Reported by a downstream adopter against a v0.24.0 announcement snippet that had relayed the maintainer deploy line to a clone install; the adopter ran the pull instead and verified the identical commit landed. The selftest gained the clone-tracking-a-branch topology it never had — watched RED on the pre-fix tree, with the existing detached-pin case asserted as the discriminating control so the two arms cannot collapse into one.

## [0.24.0] - 2026-08-04

**Minor — `patch` can finally correct the five fields that were settable only at birth, swimlanes gain their write and laneless-read sides, and the release's largest theme is that a dozen surfaces which nothing asserted on now have gates that were watched to fail.** 37 PRs since v0.23.1 (#178–#214). Minor per `VERSIONING.md § Bump sizing` — new user-visible surface (`patch --name/--tags/--type/--external-id/--origin`, `create-card --swimlane`, `list --swimlane none`, a new `board-session-close` leg) alongside the fixes.

**⚠ UPGRADING — four things narrow what the tools accept.** Each was a case where a wrong input was answered with a default path at rc 0. (1) `kbcard "" <verb>` printed usage and exited **0**; an empty first argument is now a failed expansion, not a help request, and exits non-zero — a caller that checked `rc` was reading a no-op as a completed write (card#5276). (2) An explicitly-empty **positional** argument is refused across the three bins that took one, where it was previously invisible and let the next argument silently take its place — `install-board-hooks "" <repo>` installed into `<repo>` (card#5343, card#5200). (3) `board-card-start` now refuses an empty/unknown/extra argument and honours `--lint` in any position; it previously dropped the flag and moved a card for real (card#5333). (4) Guards spelled as bash bracket **ranges** are pinned to ASCII, so under a UTF-8 shell they no longer match the wider set the locale's collation admitted (card#5409). One further observable: `patch --triaged` no longer re-**sorts** the card's tags (card#5776). `promote/action.yml`'s vendor surface is unchanged.

### Added
- **card#5776** — **five card fields were settable only at birth: `kbcard create-card` wrote `name`, `tags`, `card_type_id`, `external_id` and `payload.origin`, and `patch` could never change any of them.** A card minted wrong stayed wrong. The shipped consequence was a **board record asserting something the code never did**: a consumer's Dependabot cards take their name once from the PR title, Dependabot then **retargets the PR in place**, and nothing re-reads the title at promotion — so a card sat in Shipped-to-dev claiming TypeScript 7.0.2 shipped while the merged diff landed 6.0.3, directly contradicting a sibling card saying TS 7 was blocked, with the *wrong* one reading as resolved. With no `--name` neither title could be corrected, and the correction lived in each description behind a stale-title header. `patch` now takes **`--name` / `--tags` / `--type` / `--external-id` / `--origin`**, each writing the field its identically-named `create-card` flag writes at birth. Additive: an omitted flag leaves its field alone. **The API side was PROBED before any of this was built, not assumed** — the card gated implementation on it, and a response echoing `name` proves only that it is part of the representation (`GET` echoes it too), so each field was asserted by **GET re-read** with `board_id` (422 "prohibited" on update) as the negative control that made the probe able to fail. Two behaviours are worth knowing: **`--tags` REPLACES the whole list** (the API stores `tags` as one list and replaces it wholesale — pass the full intended set to add one; `--triaged` still appends and composes with it), and **`--type` drops any `type:*` tag the card carried** before writing the board's native `card_type_id` or the `type:<alias>` fallback tag. That strip is load-bearing on a **mixed** board, where `list --type <alias>` resolves an alias with no native id through the tag clause: a card retyped to a native type while keeping its old `type:` tag would answer as **two types at once** — the same quiet-wrong the retype exists to fix. Everything resolvable offline (alias spelling, `--external-id` numeric-ness, `--column`, `--swimlane`) is resolved **before any request**, so a typo fails rc 2 having issued none — asserted against a request log with a positive control, not assumed.
- **card#5385** — **`patch` had `--description` but no `--name`, so there was no supported way to retitle a card.** Discharged as the `--name` member of card#5776 above rather than separately: the two would have been the same parser arm, and shipping it alone was what the class card existed to prevent. The consumer discipline the class records is the reason it was a toolkit FR at all — the reporting agent did **not** hand-roll a raw API `PATCH` to fix the names, because that forks a second write path around the wrapper that owns board writes.
- **card#5767** — **a shipped card's `[Unreleased]` entry was a convention enforced by nothing, and 3 of the 24 cards merged since v0.23.1 had no entry at all.** The rule was not merely ungated, it was **unwritten**: `VERSIONING.md` documented only the release-time CHANGELOG update (step 4), so the per-PR obligation existed solely as 21-out-of-24 precedent. The third miss is why the new gate keys on a line-initial bullet rather than a mention: card#5374's only appearance in this file was a **stale forward reference** inside card#5359's entry — *"card#5374 proposes the selftest that would have caught both directions"*, written before #191 shipped it — so a release cut from that file told a reader the gate was proposed, not delivered. Same shape this repo closed three times before by building the tripwire instead of re-fixing instances (card#5355, card#5389, card#5374 itself): a list a human keeps in sync, with no check. **The gate** ([`tests/changelog-card-entry-selftest.sh`](../tests/changelog-card-entry-selftest.sh), its own CI job) derives BOTH sides and hand-lists neither — obligations from commit subjects since the last release tag **plus the PR title** (under squash-merge the title *is* the coming subject, which makes the check pre-merge instead of reddening `dev` after the omission has landed), discharges from the file, scoped to the sections above that tag's own section so an entry under `[0.20.0]` cannot discharge a commit that landed after v0.23.1. A commit with no `card#` token owes nothing, so the exemption for `chore(deps)`/`docs(orientation)` syncs is the absence of a trigger rather than a second registry to maintain. The job takes `fetch-depth: 0`, and the three checkout preconditions are **hard exits before any assertion runs**, because the live leg asserts an absence and each of them can make it answer "" for a reason unrelated to the repo being clean. The load-bearing one is a **missing `## [<last tag>]` stop header** — it widens the scan to the whole file, which errs GREEN silently. The shallow-clone refusal was measured rather than assumed and is a **diagnostic, not a second correctness catch**: a shallow clone yields no wrong answer (either no tag is reachable and `describe` fails, or it is reachable and the range is complete), but a real `--depth 1` clone carrying all 36 tags still resolves none — so without the shallow arm that checkout is told to fetch tags it already has. **Instances fixed in the same change:** card#5371 and card#5374 below, and card#5359's stale forward reference. Weakest property: a green run means no shipped card is undocumented — it says nothing about whether a bullet's prose is accurate.
- **card#5766** — **`kbcard list --swimlane none` (or `0`) filters to the LANELESS cards.** The write side could create a state the read side could not query: `create-card --swimlane none` (card#5671) and `patch --swimlane none` (card#4714, v0.19.0) both deliberately birth or leave a card lane-free, while `list --swimlane none` resolved `none` as a lane *name*, found no `KB_SWIMLANE_*` mapping, and failed rc 2 "swimlane 'none' is not defined on this board". That asymmetry was left open on purpose in #209 — widening what `list` accepts is a different decision from adding a create flag — and routed to the operator who actually runs lanes. **The answer came back as a census, not a preference** (roundtable #205, sola-pm, board 3 / 721 cards): laneless is not a transient state there but a standing **7%** — 50 cards, of which 23 are legitimately lane-free, 1 is genuinely cross-cutting, and **26 carry a `sola-<repo>:` title prefix naming exactly one repo**. The competing "laneless because untriaged" hypothesis was tested and does not survive (two spot-checked members are both tagged `triaged` and sit in `shipped_to_dev`), and a control rules out the prefix being a laneless dialect (the same prefix appears on 25 of 671 *laned* cards — the 52%-vs-3.7% concentration is the anomaly, not the convention). The decisive point is not that those 26 are misfiled — that is *unestablished, and unestablishable*, because no query returns them; they surfaced only as a side effect of answering the question. **The trap this deliberately avoids:** `_kbc_swimlane_value()` maps `none` → JSON `null`, and the list filter treats JSON `null` as *filter absent* — so the naive reuse of the write-side rule returns the **entire board** while looking correct. The filter is therefore transported as a **string** (`--arg`, absent = `""`) rather than JSON (`--argjson`, absent = `null`), which keeps "filter absent" distinguishable from "filter on laneless" and makes the laneless value the projection's own existing key for a lane-free card, `(null | tostring)` == `"null"` — the same key derivation the name-map lookup already uses, not a new comparison. The `none`/`0` spelling is hoisted into one owner, `_kbc_swimlane_is_laneless_ref()`, now shared by all three call sites (canon #5 — extracted at the third real caller, not copied a third time); `0` counts because lane ids are positive, so it never named a real lane, and it must be recognised *before* `swimlane_id()`, which passes any numeric through unvalidated. **Unchanged, and now extended to `list`:** a lane literally *named* `none` is unreachable by name and must be addressed by id; a typo'd lane name still fails rc 2 and enumerates the board's defined lanes. **Validated on a real swimlaned board** (a sandbox board carrying an actual lane, not a synthetic fixture): with one laned and one lane-free probe card present the populations genuinely differ — whole board 9, `--swimlane none` 8, `--swimlane 0` 8, `--swimlane <name>` exactly the laned probe — so the whole-board regression was *observable* there rather than hidden behind a board where every card happens to be laneless; probes hard-deleted, zero residue. All 13 new assertions were driven RED by five targeted mutations (JSON transport restored → the filter returns all 3 of 3 synthetic cards; laneless branch removed → `none` rejected rc 2; the `0` arm dropped; the predicate forced true; the laneless key derivation diverged), each reverted with the binary confirmed byte-identical after.
- **card#5671** — **`kbcard create-card --swimlane <name|id>` births a card directly on a lane** (roundtable #205, sola-pm). `create-card` accepted 16 flags and `--swimlane` was not among them, so it fell to the unknown-arg arm with rc 2 and no write — while `patch` and `list` both advertised `[--swimlane NAME-OR-ID]` two lines away in the *same* usage block, which is what made it reachable by reflex. Minting onto a lane therefore required create-then-patch, leaving a laneless window any lane-keyed reader or automation can observe. The create `POST` honours `swimlane_id` at birth — measured on a swimlaned board and asserted by a GET re-read rather than by the 2xx — so the flag rides the create body directly, with no internal create+patch composition and no laneless window. `--swimlane none` (or `0`) births a card explicitly laneless; an unresolved name fails rc 2 **before any write**, and that strictness deliberately did not change (the defect was the asymmetry that made the flag reachable, not the check that caught it). Shipped in #209; this entry was omitted from that PR and is added here — the gate that would have caught the omission is filed as card#5767.
- **card#5740** (consolidation program, test-harness axis — the class closure card#5740's own fix left open) — **deleting the ten copies of `has()` did not stop an eleventh: the "lives here and nowhere else" contract was a prelude comment, enforced by nothing.** That is precisely the shape Stage B was built to remove (`ci-matrix-parity-selftest`'s `# KEEP THIS LIST IN SYNC`), re-minted one layer down — fixing N copies without the guard that forbids the N+1th leaves the cause in place, so the class was reported closed while its mechanism was intact. `tests/prelude-shadow-selftest.sh` is that guard: it fails when any `tests/*-selftest.sh` re-declares a helper `_selftest-prelude.sh` defines. The helper set is **derived from the prelude**, never restated — a hardcoded list would be this program's own defect a third time — and the deliberate variants (`kb-board-lib-selftest`'s `expect_rc`/`expect_out`, which route failures through the shared `eq`) are allow-listed **by name**, so adding a shadow costs an explicit edit to the guard, which is the review moment the ten silent copies never got. A stale allow-list entry — one naming a shadow that no longer exists, which would silently re-permit a future copy of that name — fails too. **All three bash spellings are recognised** (`has()`, `has ()`, `function has {`): a name-only regex was written first and caught only the first, which would have let the next copy evade the guard by one keystroke while reading as covered. **Test-only — no `bin/` or `hooks/` change, so no vendored surface moves and `promote/action.yml` is untouched.** Every leg was driven red before being trusted: re-adding a local `has()` in each of the three spellings, renaming a sanctioned shadow, and removing `has` from the prelude each took exactly the intended leg red, all mutations reverted and the tree re-verified. Suite 26/26, `shellcheck -S error` clean. Also corrects the prelude's own comment, which said **eleven** selftests had defined `has()` locally — measured at the pre-fix commit, it was eleven *files* including the prelude itself, i.e. **ten** selftests, matching the card and this changelog; the miscount existed nowhere else in the repo.
- **card#5444** — **the consolidation program shaped a dozen changes across this release and left no record in the repo of *why*.** Its reasoning lived only in an agent-local plan file, so the repo carried the outcomes as CHANGELOG entries with nothing stating the invariants a *further* consolidation must not violate — and the single most load-bearing finding (that "empty vs absent" is **three** invariants requiring **opposite** answers on the same input shape, which is why one value-guard cannot own all three) was recoverable only by re-deriving it. Two plan revisions had already got that wrong. **New [`docs/CONSOLIDATION-PLAN.md`](CONSOLIDATION-PLAN.md)**, linked from `README.md`'s Get-started list as the thing to read before changing anything shared across these tools. It records the diagnosis, the ground rules (including *mechanism belongs in the primitive, policy at the caller*, and *add, never replace*), what each stage actually shipped, what was **dropped and why** — the `kb_parse` sibling parser, the comment sweep, and the `promote-released-cards` lib adoption, whose four preconditions are the strongest argument in the program *against* a consolidation — and the claims review disproved, kept rather than quietly deleted. **It is a reconciliation, not a copy of the source plan**, which would have been false on arrival in five ways: the source contradicts itself on the positional axis (its own context section proves `kb_require_value` cannot absorb it, while its Stage C says to extend it there — card#5343 settled it with a *separate* `kb_require_positional`); it describes the dropped lib-adoption stage as live and this document as pending; it prescribes deriving `help-output-selftest`'s `CLIS` from the header idiom, which card#5339 measured as **worse than the status quo** (a required-side derivation fails **open**); its supporting claim that the two host guards are *"identical — verified over 21 hostile URLs × 4 expected-hosts"* does not match `tests/kb-host-guard-selftest.sh`, which compares them over **20 URLs at one expected host** plus the unset/empty fail-closed regimes (identity supported, numbers not); and every one of its `file:line` citations had gone stale across sixteen merged PRs. The new document therefore **cites by symbol, never by line**, and says so as a rule — verified while writing it, when its own draft cited a measured negative result at the call site card#5343 had already moved it away from. Docs only; no tool, test, or vendored surface changes.
- **card#5371** — **`board-session-close`'s "Archive-eligible done cards" leg delegates to the `_kbc-archive-eligible.py` sibling, and nothing asserted the delegation.** `grep -nE 'Archive-eligible|ae_helper|_kbc-archive-eligible'` over the selftest returned zero, with a positive control on the same file returning 3. The leg's rc contract **inverts** the inverse-drift leg's — a missing sibling and a non-zero helper each warn on stderr and leave the ritual's exit code alone, where a failing reconcile hook propagates — and both answers are defensible, which is exactly why the difference needed pinning: nothing stopped the two legs converging. The helper is resolved as a sibling of the bin with no env override, so the cases run a COPY of the bin with a FAKE sibling beside it, making the delegate's rc/stdout/stderr an input rather than an observation. Every "the ritual's rc is UNCHANGED" claim is paired with a presence witness on the same fixture, so the rc channel is known live before any absence claim is made.
- **card#5389** — **`agent-board-toolkit-runtime-check`'s `TOOLS` array was a second hand-maintained enumeration of `bin/`, it under-covered by 3 of 13, and its comment claimed it was derived.** The comment read *"every `bin/` entry of the runtime this script itself belongs to, falling back to the known names when run standalone"*; there was no derivation anywhere in the file (whole file read, not grepped) — the array was always the only tool set, and it was missing `dl-a0-backfill-triaged`, `dl-a1-register-field` and **the check itself**. Both failure legs iterate `TOOLS`, so the three were structurally invisible to the missing-from-PATH warning — whose own comment names the failure it exists to catch as *"the symlink loop predates the tool's release, the v0.12.0 `adopt-to-dl` class"*, which is precisely what `dl-a1-register-field` is — and to the stale-COPY byte-compare. The tool that exists to prove an install is honest could not see 3 of the 13 tools it installs, and said nothing about the gap. **Fixed by completing the list and GATING it, not by deriving it** (the card's (a)-vs-(b) fork, decided on measurement): a derivation is not available where it is needed — the script is standalone by design and runs from `PATH`, where under the copies topology the directory beside it is a general-purpose `~/.local/bin`, not a toolkit `bin/` — and (a) keeps a static fallback anyway, i.e. it is (b) **plus** a directory-sniffing heuristic, with the fallback still free to rot. A static list also behaves BETTER when this script and the runtime it judges are different versions: the ships-it-but-`PATH`-lacks-it warning re-checks `$root/bin/$m` per root, so a superset probe never cries wolf about a tool the judged runtime does not ship, whereas a derivation would have to pick one root's answer. Since the list ships in the same release as the `bin/` it enumerates, asserting set equality in CI gives every released runtime the same answer a derivation would. **The check itself is now in the set** — a stale copy of the checker is the one most worth reporting — resolved through `command -v` like every other tool, so running it by absolute path from a checkout that is not on `PATH` cannot make it report itself as a second runtime. **The gate READS THE SET BACK FROM THE TOOL rather than parsing its source:** on a `PATH` carrying none of the tools every name lands in `missing` and the no-roots arm prints the whole array on stdout, so the assertion is about the set the code ACTUALLY probes and survives any refactor of how the array is spelled — a source grep would instead pin the spelling and could pass over an array the code no longer reads. Driven **red** under three mutations against the live tree: a real bin dropped from `TOOLS` (named by the first direction), a phantom added (named by the second), and the readback channel broken by forcing `--quiet`, which fires both positive controls — the emptiness both absence assertions would otherwise pass on. **What a green run proves — the weakest property:** that two NAME SETS agree. Nothing about whether probing a given name is useful, nor about the legs that consume `TOOLS`. **Canon #5:** the public-`bin/` name rule now has two callers wanting it identically, so it is extracted to `tests/_bin-set-lib.sh` (`_public_bin_names`) and shared with `readme-bin-coverage-selftest.sh`; the other two `bin/` enumerations stay separate and each still states why at its own call site (card#5339's measurement — `no-task-wrapper-selftest.sh` must scan `_`-prefixed files, `help-output-selftest.sh` must skip directories). **Doc-sync:** the false header comment is replaced with what the code does, `runtime-check-selftest.sh`'s stale *"it deliberately does not scan itself"* note is corrected, and the `_public_bins` reference in card#5339's entry above is renamed. **⚠ This WIDENS what the tool reports on a real install** — an install whose symlink loop predates the `dl-*` tools now gets a warn-only install-gap line (rc unchanged), and a copy install carrying a stale `dl-*` or stale checker now fails rc 1 where it read `ok`; `bin/board-snapshot` discards the rc (`|| true`, `exit 0`), so SessionStart sees only the added message. **Filed, NOT fixed here:** `_kb-board-lib.sh`'s currency is never checked under the copies topology — a lib name cannot be on `PATH`, so it cannot enter `TOOLS` and the byte-compare never sees it; a stale copied lib beside byte-current copied tools reports `ok`, reproduced empirically with a positive control (card#5414). `promote/action.yml` is untouched, so no vendored surface moves.
- **card#5339** — **the `CLIS` registry that decides which CLIs get a `--help` assertion was hand-maintained, i.e. the same mechanism that missed a bin in the first place.** card#5334's repair for `agent-board-toolkit-runtime-check` — the one file card#5145's sibling audit never reached — was to APPEND its name to `tests/help-output-selftest.sh`'s `CLIS` array, so the N+1th bin to grow a `--help` was set up to be missed identically; fixing the instance without fixing the registry is canon #5/#7 left half-done. `CLIS` is now one half of a **completeness** assertion: every entry of `bin/` whose source carries `--help` must appear in `CLIS` or in a new `EXCLUDED` list, both directions asserted, and the three `EXCLUDED` bins (`adopt-to-dl`, `dl-a0-backfill-triaged`, `dl-a1-register-field`) each state their reason. **Deriving `CLIS` from the header idiom was rejected as WORSE than the status quo**, and the file says so: a CLI regressing from the `awk` form back to a `sed -n` range would drop OUT of a grep-derived required set and the suite would go green on the exact defect it exists to catch. The completeness form has no such hole — the required set stays hand-written and the derived scan can only ADD names to answer for. That same idiom grep does appear, but only to **challenge** an exclusion — a direction in which it can add a member and never silently drop one — so an `EXCLUDED` bin that later adopts the header form stops being excludable instead of sitting there silently; the three live `CLIS` members are asserted to carry the idiom first, as the presence witness that keeps the three `EXCLUDED` "does not carry it" checks from being a grep that can never match. The scan is the **broadest** matcher available on purpose: no comment filter and not the `-h|--help)` case-arm shape — a bin whose header merely discusses `--help` reads as unaccounted and goes red (noisy in the safe direction), whereas either narrowing could drop a real help arm silently. Directories are skipped because a directory has no source to scan — the **opposite** of the shared `_public_bin_names` (`tests/_bin-set-lib.sh`, extracted from `readme-bin-coverage-selftest.sh` by card#5389), which must not skip them; same directory, two checks, two correct answers, and the file says not to harmonize them. **What a green run proves — the weakest property:** for a `CLIS` member, that its `--help` reproduces that file's own contiguous header exactly (nothing about whether the header's PROSE is accurate); that every bin MENTIONING `--help` is named in one of the two lists — **not** that every bin offering help is, since one dispatching under another spelling entirely (`-?`, a `help` subcommand) mentions no `--help` and stays invisible; and for an `EXCLUDED` member, only that it does not use the header idiom — nothing about whether its help is correct or complete. **CHANNEL was outside this change and is asserted separately by card#5337, below, in this same release:** `adopt-to-dl` printed its usage to **stderr**, which this entry's assertions deliberately did not rule on in either direction, so the exclusion list encodes a FORM and never a verdict on that bug. Each of the three live legs was driven **red** against the real tree — runtime-check dropped from `CLIS` (reported by name), a phantom added to `EXCLUDED`, and `adopt-to-dl` converted to the header idiom — each with its byte change confirmed, alongside fixture-based prove-it-can-fail legs for both set directions and the exclusion challenge. `bin/` and `promote/action.yml` are untouched, so no vendored surface moves. **Doc-sync:** two present-tense claims that the gap was open — in `dl-a0-backfill-triaged-selftest.sh` ("deliberately left alone here") and `ci-matrix-parity-selftest.sh` ("this IS card#5339's defect shape") — were corrected in the same change.
- **card#5374** — **README's "What's here" table is the toolkit's only enumeration of `bin/`, and nothing asserted the two agreed.** It had drifted in BOTH directions, measurably: a phantom row for a tool retired in v0.9.0 survived 23 releases (card#5359), and `agent-board-toolkit-runtime-check` shipped in v0.15.0 with no row at all — found only by comparing the sets by hand, because a grep can find a phantom but never an omission. Since card#5359 removed `ADOPTION.md`'s second copy in favour of pointing at this table, a drifted table is now the whole inventory being wrong with nothing left to contradict it. [`tests/readme-bin-coverage-selftest.sh`](../tests/readme-bin-coverage-selftest.sh) asserts set EQUALITY between the table's row keys and `bin/`, network-free, and joins the CI selftest matrix. Two exclusions, both stated rather than emergent from a pattern: `_`-prefixed `bin/` entries are implementation (the rule is the NAME applied to every directory entry, since `__pycache__` is `_`-prefixed and is a DIRECTORY), and table rows not keyed by `bin/` (today only `promote/action.yml`) are dropped by a named partition step whose live out-of-scope set is printed on every run. Weakest property: this proves the NAME SETS agree, nothing about whether a row's prose is accurate or current.
- **card#5370** (with **card#5372** folded in) — **`board-session-close`'s branch-reality leg had no assertions either — the same silent-stub cause as card#5358, on the OTHER `BSC_WORKTREES` consumer.** `main`'s first loop prints `• <name>: branch=<b>[ (uncommitted changes)]` per checkout, and `grep -rn 'branch=|Local branch reality|uncommitted changes' tests/` returned nothing: the cases that run `main` use a `$HOME` with no checkouts, so `[[ -e "$wt/.git" ]] || continue` skipped every entry and the `git` stub — a silent `exit 0` — was never executed. A stub that never runs and a leg that works are indistinguishable from the suite's seat. This is the leg **card#5227 (Stage E) also lands on**, since deriving the Open-PRs repo set from `BSC_WORKTREES` changes this loop's input too, and card#5358 pinned only the PR side. **15 new assertions** pin: one line per PRESENT checkout, in `BSC_WORKTREES` order, each under its own repo's name; git asked for BOTH the branch and the dirty state of every entry; the ` (uncommitted changes)` suffix present on a dirty tree and **absent** on a clean one in the same run; `branch=?` rendered for BOTH ways the branch can fail to resolve (git answering empty, and git refusing at rc 128); and a checkout-less entry skipped — emitting no line at all and never being *asked about*, which the recording stub's call log proves rather than inferring from silent output. The **checkout tree is reused** from card#5358's fixture (all five `BSC_WORKTREES` entries), but the answering `git` stub lives in its **own** shim dir: `$PRSHIM/git` is a silent `exit 0` on purpose and a git that answers would become an unobserved live input to the Open-PRs and hook-dispatch cases written against silence. Proven with **10 mutations** (label shape, the `?` fallback, the dirty-suffix condition inverted, the dirty probe dropped entirely, the skip guard removed, fail-softness replaced by `|| return 1`, plus four FIXTURE mutations for the witnesses no bin mutation can red by construction) — the union of red labels covers all 15. **Folded in (card#5372):** two pre-existing assertions in the same file shared the label `…and it installed nothing`, so they collapsed to ONE member of any union-of-red-labels proof and either could rot into a decoration invisibly — they now name their own guard (extra-positional / empty-positional), which also restores the intent the file states four lines above them. The assertion count rises by exactly the 15 added (220 → 235) and `uniq -d` over the labels is now empty.
- **card#5358** — **`board-session-close`'s Open-PRs leg had no assertions at all: its selftest installed `gh` as a silent `exit 0` and never checked a thing about it.** The three cases that run `main` use a scratch `$HOME` holding no checkouts, so the leg's `[[ -e "$wt/.git" ]] || continue` guard skipped every entry and the `gh` stub was never even executed — `grep -n 'Open PRs\|gh pr list' tests/` returned nothing. The leg's hardcoded 3-repo list, its `sed "s#^#  ${d}: #"` per-repo label, its missing-checkout skip and its `|| true` swallow were all free to change with a green suite. That matters now because **card#5227 (Stage E) went on to derive that list from `BSC_WORKTREES` by de-duplicating on the remote URL, in this same release** — a refactor that could not have been validated against a leg nothing asserts on. **20 new assertions** in `tests/board-session-close-selftest.sh` pin what the refactor must preserve: exactly which repos are queried, in order, with the same argv; a per-LINE label carrying that repo's own name; a repo with no local checkout skipped; and a failing `gh` swallowed without aborting the repos after it. The fixture creates **all five** `BSC_WORKTREES` entries so the two `-prod` clones' absence from the query is attributable to the LIST rather than to the skip guard, and each absence assertion is paired with a presence witness observed in the same run. The recording `gh` stub (logs `<cwd basename>|<argv>`, emits two PR lines) lives in its OWN shim dir: the earlier cases were written against a silent stub, and a recording one that also prints would become a live input to them the moment anyone gives that `$HOME` a checkout. Proven with **10 mutations** (repo list ±, label dropped, label applied to line 1 only, skip guard removed, `|| true` → `return 1`, stderr un-suppressed, stderr folded into the pipe, leg returning non-zero, plus two FIXTURE mutations for the three witnesses no bin mutation can red by construction) — the union of red labels covers all 20 assertions.
- **card#5357** — **`dl-a0-backfill-triaged` and `dl-a1-register-field` had ZERO test coverage, and both WRITE to a live board.** `grep -rn 'dl-a0-backfill-triaged\|dl-a1-register-field' tests/` returned nothing: neither bin was named by any selftest. dl-a0's `--apply` path PATCHes the tags array of every matched card, and a tag write **replaces the whole array**, so a regression there does not fail loudly — it silently drops the `id:`/`repo:`/`type:` tags every other consumer keys on. dl-a1 **creates and deletes a card**, and two claims in its own header were asserted by nothing: the `409/422 = already-registered` arm that is the entire "a re-run is a clean no-op" guarantee (one `elif` away from a FATAL), and the EXIT trap that tears the throwaway down on the failure paths — the tool's header names the consequence of leaking one, and it is silent until a later mint reads the value. Per canon #4 an uncovered bin cannot be refactored, so this is also the precondition for Stage C touching either. **Two new selftests** — `tests/dl-a0-backfill-triaged-selftest.sh` (70 assertions) and `tests/dl-a1-register-field-selftest.sh` (119) — drive each bin **as a process** (neither is main-guarded: sourcing them RUNS them) against a `curl` stub on PATH, so the seam sits **below** the lib and `kb_load_config`, `fetch_board_cards`, `kb_api`, `kb_api_status`, `kb_by_ref_hit` and the trap all run for real; every assertion is made against the HTTP requests actually issued, not against a faked `kb_api`. dl-a0: dry-run is the default and issues **no** write; the `id:*-pr-*` filter is exercised by a card that must be excluded for each of its four reasons; the PATCH body is asserted **whole**, so sending `{"tags":["triaged"]}` and losing the identity tags is a red; and **both** incomplete-read shapes — a page-1 non-2xx and a short read (`meta.total` exceeding delivered rows, where the fetcher legitimately emits the partial array) — abort with no writes. dl-a1: the documented request sequence and every body on the wire (including that the create body is still **flat**, asserted on the wire rather than in the source text as `no-task-wrapper-selftest` does); 409 and 422 asserted **separately** because they are separate literals in the arm; and the teardown proven to run — clear **before** delete, on the explicit path and on the trap path — after a failed verify, a still-present dl_number, a residual by-ref hit and a failing clear, while **not** re-running after a successful delete. **Every absence assertion is paired with a witness the run cannot destroy:** the stub logs each request before choosing a response, so "no PATCH was issued" is never confused with "the run never reached the API". Where a write-absence assertion could not be made to fail by any single regression it was **deleted, not kept** — dl-a0's page-1-failure leg emits nothing at all, so zero writes there follows from having no targets rather than from the refusal (canon #9); that property is asserted on the short-read leg, where it is live. **All 189 assertions were each observed RED** under 59 targeted mutations, with the union of red labels diffed against the full assertion list and every mutation checked for having changed bytes at all. Three methodology corrections came out of that pass: a mutation must be verified to have *changed* something (a pattern that no longer matches leaves the suite green and reads as proof); **duplicate assertion labels silently collapse a mutation-union proof** — three labels were reused across scenarios, hiding three assertions from the analysis until they were disambiguated; and a card-specific request count (`PATCH /tasks/777.json`) could not see a teardown fired against an **empty** task id, so the assertion that no teardown follows a failed create had to count any `/tasks/` write. New `tests/_kb-api-stub.sh` + `tests/_kb-api-stub-curl.sh` own the shared harness — the scratch board config, the request log, and a `curl` stand-in that honors the caller's own `-w` format (so the `__HTTP__` marker is never hardcoded twice), drains the auth herestring rather than reading the token from argv, emulates `-f` so reintroducing it reds the error-body assertions, and **dies on an option it has not been taught** rather than misparsing it silently. The stub is a real `tests/*.sh` file rather than a heredoc so `shellcheck` sees it, and routing is an **exported bash function** owned by each selftest, so the route tables are shellchecked too. Registration in the `ci.yml` matrix was confirmed load-bearing by watching card#5355's `ci-matrix-parity` job name both files as `unrun` with the entries removed. `bin/` and `promote/action.yml` are untouched, so no vendored surface moves. `tests/help-output-selftest.sh`'s `CLIS` registry still excludes both bins — deliberately, per the v0.23.1 card#5334 rationale (they echo a one-line `$USAGE`, not a header block, so a line-count-equality assertion does not apply); their `--help` surface is now covered behaviourally here instead.
- **card#5356** — **`agent-board-toolkit-drift-check` had one CI exercise, and two of its three legs could not fail in it.** The `drift-check-selftest` job runs `./bin/agent-board-toolkit-drift-check . .` — TOOLKIT and REPO the **same directory** — so **content-drift** `diff -q`s every file against **itself** and can never report drift, and **version-skew** is gated on a `.agent-board-toolkit-version` that the toolkit root does not carry, so the block is skipped entirely. Only **MISSING-LIB** is live there. The green tick on that job therefore certified exactly one thing: that `bin/_kb-board-lib.sh` still ships beside its seven sourcers. Two of three legs were decoration (canon #9) — and the consolidation plan that scheduled this work had named only the drift axis, so the skew leg was found by re-reading the tool rather than by executing the plan. New `tests/drift-check-fixture-selftest.sh` drives the bin **as a process** (it is not main-guarded) against real temp-dir fixtures: a diverged vendored tool, a diverged vendored **lib**, a lib-sourcing bin vendored without the lib, a recorded version disagreeing with `VERSION`, a clean co-vendored copy, an empty repo, a repo-local bin with no toolkit counterpart, and the four bad-invocation forms. **It ADDS a job, it does not replace one:** the self-run's MISSING-LIB leg is the only assertion anywhere about the **shipped** tree, which a temp-dir fixture cannot make — an earlier plan revision said "replace" and review disproved it. **Every rc-0 / "not named" assertion is an assertion of ABSENCE, and drift-check answers OK vacuously for a repo that vendors nothing** — so each is paired with a mutation of the **same directory** whose message is observed present (diverge the tool, then restore it and diverge the lib; add a sourcing bin to the lib-less standalone fixture; write an agreeing version, then remove the file). The skew assertion builds its expected message from `VERSION` read at test time rather than a literal, so it is tied to the authority instead of pinned to a past value, and a hardcoded version in the tool goes red. **All 36 assertions were each observed RED** under 14 targeted mutations — the three legs removed in turn, the drift condition inverted, MISSING-LIB made unconditional and separately made predicate-free, the recorded-version file made mandatory, the version hardcoded, `die()` re-exiting 1, the failure exit collapsed to 0, the walk taught to skip `_`-prefixed files, the not-a-toolkit-tool skip removed, and both fixture **premises** falsified in `bin/kbcard` / `bin/promote-released-cards` — with the union of red labels diffed against the full assertion list to prove none was left uncovered. Two mutation-quality corrections came out of that pass: hardcoding the version to `0.23.1` **is not a mutation** (it equals the current `VERSION`, so nothing changed and the suite stayed green — the value must differ for the assertion to be exercised), and the "a recorded-version file becomes mandatory" mutation is what proves the deliberately-asserted `no recorded version → rc 0` case is a real check rather than a restatement of today's behavior. **The job keeps its name:** `drift-check-selftest` is a **required status check on `main`**, so renaming it would leave that context permanently unreported and every release PR sitting on a check that can never be satisfied — the card offered renaming either side, and the **file** took the distinguishing name (`drift-check-FIXTURE-selftest`) instead. **What a green run proves — the weakest property:** that on temp-dir fixtures each leg reports its own defect by name and exits 1, and a clean fixture exits 0 silently. It says nothing about any real consumer repo, nor about whether a consumer's CI invokes the tool at all. Registration is guarded on arrival by card#5355's `ci-matrix-parity` job — confirmed by watching it name this file as unrun **before** the matrix entry was added. `bin/` and `promote/action.yml` are untouched, so no vendored surface moves.
- **card#5355** — **CI now fails when a `tests/*-selftest.sh` exists that nothing runs.** The `selftest` job's `strategy.matrix.test` list and the contents of `tests/` were aligned **by hand**, behind a `# KEEP THIS LIST IN SYNC` comment — and a comment is not a gate. Adding a selftest and forgetting the matrix line meant the test never ran in CI: permanently, silently, with every PR green and the author seeing it pass locally. Same defect *shape* as card#5339's hand-maintained `CLIS` registry, one layer up — the registry deciding which tests run **at all**. New `tests/ci-matrix-parity-selftest.sh` enforces **one rule with no exception list**: every `tests/*-selftest.sh` must be run by `ci.yml`, either as a matrix entry **or** named in some job's `run:` block. It parses the YAML rather than grepping it (the matrix is structured data; `- foo` matches under any key), and gets its **own top-level job** instead of a matrix entry — a matrix entry would be self-referential, so deleting that one line would disable the guard silently, the exact hole it closes; being named in that job's `run:` is also how it satisfies its own rule. Honest scope: the `unrun` direction is the load-bearing one (nothing else in the repo reports it); the `dangling` direction (a name in `ci.yml` with no file) already fails CI loudly at `bash tests/x.sh` → 127, and is asserted only because it is the other half of the same set comparison. Verified against **three mutations** — a dropped matrix entry, an emptied `jobs` map, and the guard's own `run:` line removed — each reddening the specific assertion that claims to catch it; the emptied-`jobs` case is what proves the **positive control** (both streams non-empty, each carrying a named known member) is not decoration, since every live assertion here is an assertion of *absence* and an extraction that silently yielded nothing would otherwise read as a clean pass. The guard also went red on its first real run, before its CI job existed, correctly naming **itself** as the unregistered test. All three collation points (`sort`, Python's `sorted`, **and `comm` itself**) are pinned to `LC_ALL=C`: `comm` validates input order in the *ambient* locale, and en_US.UTF-8's punctuation-ignoring primary pass orders this suite differently from codepoint order (`kbc-archive-eligible` and `kbcard-field` invert), which produced an unreliable diff until pinned.
- **card#5200** — **`board-session-close` reports checkouts whose `post-checkout` no longer reaches `board-card-start`.** A board-mapped repo can lose its git-hook dispatch while every routine check stays green: `board-card-start` is **fail-soft by contract**, so a missing hook and a working hook are indistinguishable from the operator's seat; the inverse-drift leg sees only the *symptom* (a card that never left its backlog column) and corrects it as ordinary drift, **masking the cause**; and `agent-board-toolkit-drift-check` compares vendored repo *content*, not host hook *wiring*. A new report-only `── Git hook dispatch ──` leg resolves, per local checkout, the directory git **actually** dispatches from and reports a hook that is missing, dangling, non-executable, not reaching the tool, or symlinked into a **different toolkit checkout** (⚠ wiring drift — it still fires — never labelled dead); per repo it reports `core.hooksPath` states that switch dispatch off wholesale, and per host it reports `board-card-start` being off `PATH`. The summary states **inspected vs skipped** counts and never prints an all-clear over zero inspected checkouts. Findings name `install-board-hooks <repo-dir>`; nothing is installed, repaired or mutated, and the ritual's **exit code is unchanged** (still owned by the inverse-drift leg). Placement was chosen against `agent-board-toolkit-drift-check`, which runs in CI where no checkout can ever satisfy a host-wiring assertion. Real `git init` fixture repos cover every state incl. linked worktrees; the two `core.hooksPath` cases assert against the naive `.git/hooks` answer **in both directions**, so simplifying the resolver back to `.git/hooks` goes red.

### Changed
- **card#5776** (same change as above, refactor half) — **`create-card` and `patch` now assemble their top-level task fields through one owner, `_kbc_task_fields`.** This is why five fields could drift onto one writer: each command carried its own field-assembly `jq`, so a field added to `create-card` had no reason to appear in `patch`, and a sixth would have drifted identically. Completes the consolidation `_kbc_build_payload` began for the payload keys (card#4511): payload keys share one assembler, top-level task columns now share this one. Guarded the way the *duplication* has to be guarded rather than the values — the selftest stubs the shared owner with a sentinel key and asserts **both** writers' bodies carry it, so re-inlining the `jq` in either command reds a test that no per-field value assertion would. `--type` alias validation and the `--external-id` numeric guard are likewise hoisted to one owner each. **One observable change for existing callers:** `patch --triaged` no longer re-**sorts** the card's tags. It applied `unique` — which sorts — so an unrelated `--triaged` patch reordered every tag the card carried; dedupe now preserves first-seen order, which is what `create-card` writes, so the two writers agree.
- **card#5740** (consolidation program, test-harness axis) — **`has()` was hand-copied into ten selftests and one copy took its arguments in the opposite order, a divergence that cannot go red.** Reversing the arguments of a literal-substring helper is neither a syntax nor a type error: `has <haystack> <needle>` under a needle-first definition simply tests two unrelated strings and answers `false`, so **every assertion expecting `false` keeps passing** while testing nothing. `tests/kbc-archive-eligible-selftest.sh` had carried the haystack-first spelling since it was written, consistently with its own eight call sites, so neither the file nor the shared prelude could reveal it in isolation. All ten local definitions are deleted in favour of the prelude's single one (hoisted under card#5566, which is why this change adds nothing and only removes); the inverted file's **call sites were audited and swapped to needle-first** rather than its definition flipped, since flipping the definition alone would have silently converted each of its calls into the false-true this card exists to remove. **Test-only — no `bin/` or `hooks/` change, so no vendored surface moves and `promote/action.yml` is untouched.** Measured rather than assumed, in the two halves the card required to stay separate: after the nine conforming copies were removed, **inverting the prelude's argument order took exactly those nine red and left the tenth green** — the tenth still shadowing the prelude with its own definition, which is the evidence that the nine now resolve to the shared one and that the inverted copy was genuinely independent of it. After the tenth was migrated, its three `false`-expecting assertions — the ones that pass vacuously under a wrong argument order, and therefore the only ones a green run cannot vouch for — were each **fed a needle known to be present** and **all three went red**, establishing that they can still fail. Full suite 25/25 green, `shellcheck -S error` clean; the prelude's comment is rewritten, since it described ten copies that no longer exist.
- **card#5566** (consolidation program **Stage C**, flag axis) — **one invariant — "a flag was given without a value" — answered in four spellings, and three of the bins told the operator only that *something* was wrong.** `next-dl`, `dl-a0-backfill-triaged` and `dl-a1-register-field` refused with a bare `$USAGE` line naming none of their flags, while `kbcard`/`adopt-to-dl` had named the offending flag through `kb_require_value` since v0.23.1. Those three now dispatch through the same primitive at all **6** caller-visible call sites — `dl-a0-backfill-triaged --board`, `dl-a1-register-field --board`/`--stage`/`--swimlane`/`--sentinel`, `next-dl --board` — in both the **missing** and the explicitly-**empty** form, which are separate inputs and are asserted separately. **Every one is an rc-2-stays-rc-2 message-text upgrade: no exit code changes on any healthy install.** **⚠ The stage's own headline — "one invariant, TWO exit codes → one" — was UNREACHABLE and is NOT claimed:** the only rc 1 in the corpus is `agent-board-toolkit-runtime-check`'s `"${2:?…}"`, which is excluded by design (it validates `_kb-board-lib.sh`, so it must not source it), so the exit-code divergence **survives, deliberately**; what this retires is the message-quality divergence on the adoptable set. **The scope came from a derived delta table, not the plan's four-row summary**, and the difference was load-bearing: the four rows are **57** individual call sites across 8 files, of which exactly 6 were caller-visibly adoptable (the rest already at target state, already at message parity in the two standalone mirrors, or out of scope), and one of them was **compound**. `next-dl`'s `[[ -n "${1:-}" && -z "$project" ]]` did the value-presence test **and** the "project named twice" mutual exclusion — a `Preserve:` item — in a single condition, so a drop-in `kb_require_value` swap would have **deleted the mutual exclusion and passed every test in the repo**, because `next-dl` had no argument-surface coverage at all. It is **split** into two sequential checks, presence first (the order the `&&` already short-circuited in, now a choice and therefore asserted): the primitive answers presence and names the flag, and the exclusion keeps its own guard and its own generic usage line, since a mutual-exclusion violation is not a value-guard failure. **⚠ One caller-visible change beyond the message text, on a BROKEN install only:** `dl-a0-backfill-triaged` and `dl-a1-register-field` parsed their arguments *before* sourcing the lib, so the source block moved above the arg loop (the ordering `next-dl`/`kbcard`/`adopt-to-dl`/`board-card-start` already had) — a copy vendored **without** `_kb-board-lib.sh` beside it now answers every invocation with the missing-lib refusal at rc 1, `--help` included, where `--help` previously answered rc 0 before the lib was consulted. **`--help` is not the only rc that moves**, and naming it alone understates the flip: with the check below the loop, the missing lib was reported only for an invocation the loop let through, so the loop's own refusals — `--bogus`, a trailing value-taking flag, a stray positional — went **2 → 1** as well (measured lib-less on both binaries, pre and post). The two alternatives were worse: an inline mirror of the primitive is the defect this program exists to remove, and a conditional `[ -r "$KB_LIB" ] && source "$KB_LIB"` evades `agent-board-toolkit-drift-check`'s anchored `^[[:space:]]*source "\$KB_LIB"` probe — deleting a live guard to preserve a refusal path. Both bins' selftests now pin the lib-less behaviour. **New coverage where the hole was:** `tests/next-dl-selftest.sh` gains an argument surface (it previously only lifted `max_int`/`max_dl`), driving the bin as a process against the shared `curl` stub — the value guard in both forms, and the mutual exclusion in all four spellings of the collision, each pinned to rc 2, to the **generic usage line**, to minting nothing and to issuing no request, with every `--board` naming a **resolvable** board on purpose (against an unknown board name the run dies at config resolution anyway and those zeros pass on a deleted exclusion — measured: they did). **Canon #5, in the file this coverage lands in:** that selftest's `has()` — the literal-substring helper — would have been the **11th** hand-rolled copy under `tests/`, in a repo whose consolidation program exists to stop exactly that, so it is deleted and the helper **hoisted into `tests/_selftest-prelude.sh`** (needle-first, the order 10 of the 11 used); this change therefore adds **zero** copies. The 10 standing local definitions shadow the shared one and keep working unchanged as of this change — migrating them is **card#5740**, which lands **in this same release** (entry below) and had to audit the one **argument-inverted** copy (`tests/kbc-archive-eligible-selftest.sh` took haystack first) at its call sites rather than flip it blind, since a blind flip produces a false green. **All 43 new or changed assertions have been observed RED** under **20** targeted mutations, the union of red labels diffed against the full new-label set rather than eyeballed: the value guard reverted to usage-only (per flag), made unfailable, deleted outright, made to name a hardcoded flag, and made to exit 5 instead of 2; the `next-dl` split collapsed back to a bare swap — **8 assertions red, the exact regression this stage was warned about** — plus its value half deleted, its two halves reordered, its value routed to another board, its claim reading the wrong JSON key, and **both** mutual-exclusion arms in turn deleted and then made to answer with the value-guard message instead of the usage line; the missing-lib refusal neutered and made to exit 0; and `bin/kbcard`'s `cmd_field` sub-verb guard deleted, the 20th, which is the mutation that caught the false Preserve claim recorded below. **An earlier revision of this entry excepted one assertion as never-red *by construction*, and the exception does not survive measurement.** It named `a trailing --board does not leak an unbound-variable error` and described it as *"a pre-existing check this change only re-worded"* — two different assertions: that label is `next-dl`'s **new** one (this bin had no argument surface at all before this change, as this entry says above), while the re-worded one is `dl-a0-backfill-triaged`'s `the trailing-flag refusal does not leak an unbound-variable error`. **Both** were re-measured red under mutations already named in the list above: deleting `next-dl`'s value half leaves `project="$1"` reading an exhausted stack, so `next-dl --board` dies with `line 55: $1: unbound variable` (**5** labels red in that file, this one among them), and deleting `dl-a0`'s guard outright does the same at that bin's own `line 41` (**6** labels red, its own included). `${2:-}` holds only while the guard that reads it is *present*, which is exactly what a mutation removes — so no assertion in this change is excepted. **Three zero-request assertions were drafted and dropped rather than shipped**, each with the reason left at the call site: a *trailing* flag reaches no request even with the guard deleted outright (the arg loop's closing `shift` fails on an exhausted stack and `set -e` ends the run), and an empty `next-dl --board` is caught by the `[[ -n "$project" ]]` refusal after the loop — all three are held by something other than the guard under test, so they are decoration (canon #9), while the empty-value zeros beside them are live and are kept. The mutation pass also caught the first draft's **witness lying**: `--board dev claimed against board 42` counted an *unqualified* `/dl-sequence/claim.json`, so it stayed green while the value was routed to a different board — it is board-qualified now, and the fixture gives `alt` and `bridge` **resolvable** board configs precisely so the exclusion cases fail loudly rather than dying at config resolution. All 25 selftests green (`shellcheck -S error` clean, no new findings at default severity either). **⚠ Vendored surface IS affected** — `bin/next-dl`, `bin/dl-a0-backfill-triaged`, `bin/dl-a1-register-field`; `bin/_kb-board-lib.sh` is unchanged and `promote/action.yml` is untouched, so action-pinning consumers see no interface change. **Doc-sync, including two stale claims found in passing and verified before being called stale:** `docs/CONSOLIDATION-PLAN.md` § Status and § Stage C record the landed reality (6 sites not 4 rows, the unreachable exit-code half, the split, and the Preserve items now each named with the test that pins them **and with what that test actually covers** — review measured the first spelling of that claim false, since `tests/kbcard-field-selftest.sh` pinned `kbcard field`'s missing-sub-verb refusal by **rc only**: deleting the guard in `bin/kbcard` left the full 25-file suite green, because an absent sub-verb falls through to the `*)` catch-all which answers rc 2 as well, while `kbcard field` driven as a **process** went from a named rc 2 to a **silent rc 1**. A message assertion now separates the two arms, driven red by that deletion — it is this change's 43rd new assertion and its 20th mutation), the 2026-07-29 deferral ruling is kept and marked superseded with its reason 1 measured smaller than it assumed; `ADOPTION.md` and `docs/INSTALL.md` both said omitting the lib yields *"an inscrutable runtime `source: … No such file`"*, which has been false since v0.11.2 for all seven lib-sourcing bins (six refuse by name at rc 1, `board-card-start` by name at rc 0 by its fail-soft hook contract) — `docs/UPGRADE.md` already recorded the fix, so the two were contradicting a sibling doc, not just the code; `ADOPTION.md` — the front door, and the one place omitting the `--help`/rc facet — additionally dated *"refuse at **startup**"* to v0.11.2, which measurement at that tag refutes for two of the seven: `dl-a0-backfill-triaged`/`dl-a1-register-field` carried the named refusal but placed it **below** the arg loop, so a lib-less `--help` answered **rc 0** there and "at startup" became true for them only at this commit — it now mirrors `docs/INSTALL.md`'s clause, anchoring `v0.11.2` to the message quality and stating the every-invocation fact with its mechanism (the lib is loaded before any argument is read); and the *"all 36 `kbcard` arms"* count is corrected to **37** in all **four** places it was restated — the v0.23.1 and card#5200 entries above, its copy in the root `CLAUDE.md` release table, and a **source comment** in `bin/adopt-to-dl` that the first sweep missed because that sweep was scoped to `*.md` (a clean grep over the wrong file set is not a clean audit). Re-counted at the commit that wrote it, where it was already 37: the 37th is `main()`'s global `--board` arm, outside the per-subcommand loops the figure evidently counted.
- **card#5227** (consolidation program **Stage E**) — **`board-session-close` held a THIRD hardcoded repo list, and three copies of "which repos do I care about" is what let one of them silently drift and lose a repo.** card#5200 hoisted the branch-reality and hook-dispatch legs onto a single `BSC_WORKTREES` — and found, in doing so, that one of those two copies had been silently missing `agent-board-toolkit` entirely. The Open-PRs leg's own list was deliberately left out of that hoist, and the reason is worth keeping: it is a **different semantic** — unique GitHub remotes, not local checkouts, and two worktrees of one repo are two entries in `BSC_WORKTREES` but must be **one** entry in a PR query. So the fix is not "reuse the array"; the query set is now **derived** from `BSC_WORKTREES` by de-duplicating on `git remote get-url origin`, keeping the first **present** checkout of each distinct remote in list order. On the reference host that reproduces the retired literal list exactly — asserted by diffing the live `── Open PRs ──` section of the old and new bins, **byte-identical**, which is the only check in this change that touches real `git remote` output (every other assertion runs over stubbed strings). **The load-bearing decision inverts the naive derivation: an entry whose remote cannot be read keeps its OWN slot and is NAMED — it is never deduped away.** The query set is the *required* side, so dropping an unreadable entry would fail **open** — a clean-looking section that silently covered fewer repos, with no diff anywhere to record it, which is strictly worse than the literal list it replaces (that at least needed a human edit to lose a repo). An extra query is the harmless direction. The warning rides the leg's **own stdout section rather than stderr**, deliberately: the failure direction is safe by construction, so this is not the canon-#9 *"degraded coverage must not read as a clean pass"* class that this file reserves stderr + ⚠ for, and stderr is shared by every leg — a per-leg diagnostic there would couple every other block's stderr assertions to this leg's stub fidelity. **The dedup key is the RAW URL with no normalizer**, and the weakest property is stated at the call site: the dedupe holds for **byte-identical** URLs only (an ssh-vs-https re-clone under-dedupes and lists one repo twice under two labels — visible, labelled, harmless), and it assumes each checkout's `gh` target **is** its `origin`, which a fork remote or `gh repo set-default` could disagree with — both reasons the key is never allowed to DROP a repo, only to merge two that agree exactly. A normalizer was rejected as a new primitive with its own defect surface bought for zero measured benefit. **⚠ One real behavior change, not glossed:** `git remote get-url` needs a checkout, so the `-e .git` filter necessarily runs **before** the remote read — the rule is first *present* checkout. If a repo's primary checkout lost its `.git` while a `-prod` clone kept one, the repo is now queried via the clone where it previously went **unqueried**; better coverage, but a change. **This was only safe to touch because card#5358 (Stage B) gave the leg assertions first**, and that coverage immediately caught two traps: the fixture's `git` stub was a silent `exit 0` that does not answer `remote get-url`, which would hand every entry an EMPTY key and collapse all five checkouts into one group (empty is not "no duplicate" — it is the leg's *unreadable* signal), so the stub now answers per-directory with the two `-prod` clones returning the **same** URL as their siblings, that byte-for-byte identity being the entire input the dedupe acts on; and the fail-closed path needed its own case, **directly paired** with the assertion that the same checkout is absent when its remote IS readable, so it cannot pass on a leg that simply queries everything. Five new assertions; the pre-existing `-prod` absence+witness pairs now test the **dedupe** rather than a list that omitted them, and were driven **red** by mutating the dedupe to a no-op (6 assertions red, rc 1) before being trusted. All 24 selftests green. `bin/board-session-close` is host-local and not vendored, so `promote/action.yml` and the vendored `bin/` surface are untouched. **Out of scope, unchanged:** the card's second smell — operator-specific absolute `$HOME/<name>` paths — which stays blocked on roundtable **#187** (the coord-config `worktrees[]` schema FR).
- **card#5311** — **`--separate-git-dir` checkouts and submodules now INSTALL instead of being refused; the linked-worktree refusal is unchanged.** card#5226 made `install-board-hooks` refuse all three separated topologies with an accurate per-topology message. Two of those three were refused on a rationale that does not apply to them: the decision was *surprising blast radius* — installing silently rewires checkouts the operator never named — which is true of a **linked worktree**, whose hooks dir is shared with the main checkout and every sibling worktree, and false of a `--separate-git-dir` checkout and a submodule, which have exactly **one** work tree each. Those two now install into the directory git actually dispatches from (`<sepgit>/hooks`, `<super>/.git/modules/<name>/hooks`) and report it on **stderr** — never stdout, because `--check`'s only stdout is the target dir and `board-session-close` consumes it. **The disposition moved into one primitive**, `_ibh_install_dir_source <repo> <root> <common-dir>`, rather than staying spelled out at the call site: `bin/board-session-close` **modelled** that call site (`cdir="$root/.git"` under the comment *"it targets `<root>/.git/hooks` and nothing else"`*) to decide whether to offer the installer as a remedy, so this change would have made it tell operators to hand-wire the very repos the installer now fixes. It asks the primitive instead (canon #5) — the second copy being the exact drift class that whole dispatch check exists to catch. **Two pre-existing false claims fixed in passing**, both introduced by card#5226 and both verified against the pre-change binaries before and after: for a linked worktree `_bsc_fix_line` printed *"install-board-hooks would target `<wt>/.git/hooks`"* when the installer had refused outright since #5226 — it targets **nothing** — and the installer view now yields empty stdout + a new **rc 9** so the main-checkout branch (the correct remedy) is reached, with the installer's own refusal quoted where it is not. **One defect found and fixed during the build:** the installer consults the topology disposition *only while `core.hooksPath` is unset* (a set hooksPath wins on every topology), and asking it before reading hooksPath made the installer view report "refused" for a linked worktree that configures one — a checkout the installer services happily; the view now gates on hooksPath exactly as `_ibh_main` does, pinned in both directions. **Deliberately NOT fixed here, filed as card#5434:** `git clone --separate-git-dir=<x>/.git` (basename literally `.git`) is misclassified as a linked worktree by `_ibh_main_checkout` and refused, pointing the operator at a path that is not a checkout — pre-existing since #5226, but this card raises its blast radius from wrong-wording to wrong-**disposition**, and the corrected discriminator is an accept/reject decision of its own. **Tests** extend the card#5226 fixture block with **real** `git init` topologies: a **dispatch proof** that runs a real `git checkout -b` against a marker hook placed in the computed dir before anything is installed (an `-L` presence check alone never establishes that git reads that dir), `--check` stdout captured **without** stderr so the new topology note cannot pollute an equality assert, sub-directory arguments pinning the measurement that git answers the common dir **absolute** for both installing topologies (so no normalization and no `--path-format` version floor is needed), and assertions that nothing lands in the work tree's own `.git/hooks`. **Two test-integrity corrections worth recording:** the `--separate-git-dir` fixture had no commit, and `git checkout -b` on an **unborn HEAD** does not dispatch `post-checkout`, so the dispatch proof was a check that could not pass until a commit was added; and the prove-it-can-fail control initially passed while **never running** — the mutant copy resolved its hook sources at `<dirname $0>/../hooks` and exited before reading any argument, so it "failed" for the wrong reason and an inequality assertion accepted it. The control now reproduces a real toolkit layout and requires a **witness** (the mutant's own diagnostic naming the mutated target) alongside the red assertion. The linked-worktree refusal assertions and the set-`core.hooksPath` positive control survive **verbatim**.
- **card#5343** — **one positional-argument rule was hand-rolled in three bins, and the three copies had already drifted textually.** The rule — *refuse an EMPTY positional by name, refuse a SECOND one* — lived independently in `bin/install-board-hooks`, `bin/adopt-to-dl` and (as of card#5333) `bin/board-card-start`. They agreed behaviourally and disagreed everywhere else: `unknown option:` vs `unknown flag`, `: $arg` vs `'$1'`, `for arg in "$@"` vs `while/shift`. Canon #7 files the duplication itself as the defect at the third copy — the next copy is free to pick any of the three answers. **Hoisted to `kb_require_positional <slot> <arg> <name> [suffix]`** in `bin/_kb-board-lib.sh`, with a documented inline mirror `_ibh_require_positional` in `install-board-hooks`, which is vendored standalone and must not source the lib (the idiom `promote-released-cards` already carries for `host_ok`/`require_value`). **The primitive answers the QUESTION; the caller owns the POLICY** — its own usage line and its own exit status: `board-card-start` refuses at **rc 0** because it runs from a git hook that must never block a checkout (`docs/HOOKS.md`), the two ordinary CLIs at **rc 2**. That rc divergence is justified, not drift, and an owner that also owned the exit would have had to break one of them. `<slot>` is the **destination variable's current value**, not a new seen-flag: refusing an empty positional rather than storing it is exactly what makes `-n "$slot"` a sound "have I seen one?", and `install-board-hooks` had already tried a seen-flag and removed it as unreachable. **Empty is tested FIRST**, as all three copies did, so the corner case *slot already full AND arg empty* still reports the empty — a reorder there would have been a silent user-visible change on a case nobody would have thought to test. **It is deliberately NOT merged into `kb_require_value`** despite the twin naming: inside a function `$#` is the *function's* arity, and `kb_require_value`'s 1-arg shape is contracted with the OPPOSITE answer (`kb_require_value --dl` → rc 1, asserted in `tests/kb-board-lib-selftest.sh`); the flag-value axis and the positional axis need opposite verdicts on the same input shape, and the new function says so at its definition. **⚠ ONE user-visible delta, and only one:** `adopt-to-dl: unexpected extra argument '4242'` becomes `adopt-to-dl: unexpected extra argument: 4242` — the primitive owns the wording, and `: $arg` is what 2 of the 3 already printed, so exactly one message moves. Letting each caller keep its own wording would have deduplicated two comparisons while preserving every drift this card measured. **`--` handling is deliberately UNCHANGED at all three sites** (`install-board-hooks` hard rc 2, `adopt-to-dl` accepts-then-ignores, `board-card-start` a real terminator): unifying it changes what two of them ACCEPT, and bundling an accept/reject change with a hoist puts a behaviour change and a refactor in one reviewable unit. Verified by diffing **18 refusal invocations** across all three bins, old binaries vs new, with each bin's own lib copy beside it — byte-identical on 17, the intended `adopt-to-dl` message on the 18th. **New `tests/kb-positional-guard-selftest.sh`** (network-free, CI-registered) runs ONE assertion matrix against BOTH copies — extract-and-exercise, never a text diff, the shape `tests/kb-host-guard-selftest.sh` uses — and reports *"the two copies DISAGREE"* as its own failure class. Its **prefix equalization** is what makes that a comparison of the **diagnostic** rather than only the rc: the mirror hardcodes `install-board-hooks: ` while the lib copy renders `$(_kb_prog)`, so the test sets `KB_PROG` to that name, the direct analogue of host-guard's `KANBAN_EXPECTED_HOST`/`EXPECT_HOST` pairing. Driven **red** under three mutations against the live tree — the mirror's extra-arg wording drifted (the DISAGREE arm fired), the lib copy's two tests reordered (the empty-wins case fired), and one call site left hand-rolled. **What a green run proves — the weakest property:** that the two implementations agree on the inputs the matrix feeds, and nothing about an input class absent from it. It is also **blind to a call site left hand-rolled** — a bin that calls neither copy passes it, which the third mutation demonstrates — so the file's second half **runs all three bins** on both refusal shapes and asserts rc *and* the first stderr line; those pins are the only thing covering `install-board-hooks`' arm, which had no selftest of its own before this change. `bin/_kb-board-lib.sh` gains a function and no existing signature moves; `promote/action.yml` is untouched, so no vendored interface changes. **Filed, NOT fixed here:** `install-board-hooks -- <dir>` returning a hard rc 2 for a universal shell idiom, which got sharper with card#5333 because `board-card-start`'s refusal now actively teaches operators to reach for `--` (card#5427); `adopt-to-dl`'s decorative `--) ;;` arm is already owned by card#5351; and — from the canon-#7 sibling audit this change ran, which found a **fourth** site the card never named — `agent-board-toolkit-drift-check` **silently discards a third positional** (`drift-check . <repo> IGNORED` → rc 0 `OK`, reproduced live) and names neither of its two positionals when one is empty (card#5429). That is the precise hazard `install-board-hooks`' own arg-loop comment already gives as its reason for refusing an extra positional — written down in one bin, never applied to the sibling. It is deliberately **not** a fourth copy of the fix: `kb_require_positional` is single-slot by construction (`<slot>` IS the destination variable), `drift-check` has two ordered positionals, and refusing an argument it accepts today is an accept/reject change that must not ride a refactor.
- **card#5200** — **`board-session-close`'s existing "Local branch reality" leg now lists 5 checkouts instead of 3.** It and the new dispatch leg read **one** list (`BSC_WORKTREES`) rather than two hand-maintained literals, so the branch/dirty-state lines gained this repo's own checkout and the deploy worktree that owns the on-PATH tools. Additive and read-only — no line was removed — but it is a visible change to a pre-existing section's output.

### Fixed
- **card#5276** — **`kbcard "" <verb>` printed the usage block and exited 0, so a caller checking `rc` read a no-op as a completed write.** `kbcard "$VERB" move --task N --column in_progress` with `VERB` unset moved nothing and reported success. One `[[ -z "$cmd" ]]` answered two different questions — *no arguments*, which is a help request, and *an empty first argument*, which is a failed expansion — and gave both the help answer. They are now discriminated on `$#`: no arguments still prints usage on stdout at rc 0, an empty verb is refused on stderr at rc 2 through the same `kb_require_positional` owner the three sites in card#5200 use, so kbcard's refusal cannot drift from theirs. **This is the fourth position of the empty-vs-absent axis** (v0.23.1 flag values → `core.hooksPath` → the `install-board-hooks`/`adopt-to-dl` positionals → this), and the first one in the tool that already applied the flag-value guard correctly at ~40 arms: the axis is the *shape*, never the file or the argument kind it was last found in. The positional axis was re-derived across every shell bin as part of this fix and every remaining bin was then MEASURED refusing an empty positional at rc 2, not read as doing so; the two bins that consume no arguments at all (`board-snapshot`, `board-session-close`) silently ignore anything passed, which is the extra-positional axis and is recorded on card#5429, not fixed here. The sweep's own method had to be rewritten mid-audit: its first form treated a one-line `die() { …; }` as an unclosed function scope and therefore never printed `agent-board-toolkit-drift-check`'s top-level positional read — a miss in the *scan*, not the tree, and the reason the count it produced was retired rather than corrected. **⚠ `kbcard` now rejects an invocation it previously accepted** — `kbcard "" list` is rc 2 rather than usage-at-rc-0 — the same narrowing, and the same reason, as the v0.23.1 flag-value change. A bare `kbcard` is unaffected.
- **card#5409** — **every guard spelled as a bash bracket RANGE meant something wider than its own error message under a UTF-8 shell.** A bracket range in a bash pattern — `[[ $x =~ ^[0-9]+$ ]]` and glob `case "$x" in *[!0-9]*)` alike — is a **COLLATION** range, not an ASCII range. Measured under `LC_ALL=en_US.UTF-8`: `^[0-9]+$` matches U+0663 ARABIC-INDIC DIGIT THREE and `^[A-Za-z0-9_-]+$` matches U+00E9 / U+2163 / U+FB01; all four are rejected under `LC_ALL=C`. GNU `grep -E` does not widen `[0-9]` in either locale, which is what identifies **bash's engine** rather than the pattern as the cause. **Bounded honestly — this was not a corruption or injection vector.** `kbcard --type`'s stated job is to reject the commas and spaces that would corrupt the tag CSV, and a comma and a space were measured **still rejected** under the wide locale. What was wrong is narrower and real: the guard **admitted input its own error message declares invalid** (`--type 'Ⅳ'` accepted against "must be alphanumeric / '-' / '_'"), and one PARSE site captured a non-ASCII "digit" as a card id — `BOARD-CARD: toolkit#٣` parsed, and `kbcard move --task ٣` was invoked. Three further sites degraded their own diagnostics rather than their verdict: `kb_dl_num` and `native_type_id` got past the pattern and died in bash arithmetic (`[[: ٣: syntax error`), and `kbcard field --field` reached `jq --argjson` and printed a jq parse error in front of the real "not defined on board" message. **The fix is one mechanism, applied at every site:** `kb_ere_match` / `kb_is_uint` in `_kb-board-lib.sh` match under a **function-scoped** `LC_ALL=C` (`local`, so it is restored on return, and `BASH_REMATCH` is deliberately not localized so captures survive) — mechanism in the primitive, refusal **policy** unchanged at each caller, per `CONSOLIDATION-PLAN.md` ground rule 5. `[[:digit:]]`/`[[:alnum:]]` were **not** used: a POSIX class is locale-defined by construction, so it re-poses the same question. Adopted at 17 sites: `kbcard` (`--type`, `--external-id`, `resolve_task`, `swimlane_id`, `native_type_id`, `field --field`), `_kb-board-lib.sh` (`kb_dl_num`, and `fetch_board_cards`' `last_page` + short-read census), `adopt-to-dl` (`<card-id>`, `--issue`), `next-dl` (claim value, claimed DL, board DL max), `dl-a1-register-field` (`--sentinel`, throwaway id). The two standalones that must not source the lib carry **inline mirrors** in the established pattern (as `require_value`/`host_ok` already do): `promote-released-cards` gains `uint_ok`/`uint_csv_ok`, which also collapse four hand-copied numeric globs onto one definition each, and `hooks/agent-dispatch-card-start` gains `_kbads_marker`. **The corpus was swept by SHEBANG and by embedded-shell-in-YAML, never by extension** — 46 files in this tree carry a shell shebang or a `.sh` name where only 30 are named `*.sh`, and the 16-file remainder an extension filter cannot see is where `hooks/agent-dispatch-card-start` and every `bin/` CLI live (51 files swept, counting the 5 YAML); the YAML pass is what found `.github/workflows/auto-tag-version.yml`'s merge-time VERSION gate, which a `*.sh` sweep cannot see (its exposure is **latent, not asserted live**: the hosted runner's locale was not read, and C.UTF-8 measures ASCII-only). **New `tests/locale-range-guard-selftest.sh`** asserts every fixed site under **both** `LC_ALL=C` and `LC_ALL=en_US.UTF-8`, each paired with an ASCII **positive control** that must still be accepted, and positive-controls the **locale itself** first — if the runner's `en_US.UTF-8` does not actually widen `[0-9]`, the UTF-8 half says so loudly instead of passing vacuously. A static backstop covers the adopters no behaviour case can reach (next-dl's three need a live board) by flagging any `[[ =~ ]]` bracket range with no `LC_ALL=C` on that line or the two above it. All 17 sites were **observed RED** under a one-line revert of exactly that site, each restore verified byte-identical. **⚠ This narrows what these tools accept** on non-ASCII input, where the accept-set now matches the wording that was always shown; ASCII behaviour is unchanged at every site (`promote/action.yml` is untouched, but `bin/` and `hooks/` **are** vendored surfaces — re-vendor `_kb-board-lib.sh` with anything that sources it, and re-vendor `promote-released-cards`). **Other engines — re-measured, and NO site in this repo is affected:** GNU **sed** widens `[a-z]` under a UTF-8 locale but does **not** widen `[0-9]`, and the only sed range site (`release-pr-body`'s PR-suffix strip, `s/ *\(#[0-9]+\)$//`) uses `[0-9]`, so it is immune. GNU **grep** widens **neither** class, measured in positive (`^[a-z]+$`) and negated (`[^a-z0-9/#-]`) form against U+00E9 / U+FB01 / U+2163 with a live ASCII control in every cell — so `board-card-start`'s fail-soft branch-name hint is immune too. An earlier revision of this entry stated both engines' behaviour wrongly and inverted between them; the sites were never at risk. **jq**'s Oniguruma and **tr** were measured locale-invariant, and `next-dl`'s `2[0-9][0-9])` HTTP-status arm reads curl's own `%{http_code}`, ASCII by construction.
- **card#5337** — **`adopt-to-dl --help > f` produced an EMPTY `f` at rc 0.** `usage()` baked `>&2` into itself, so the one arm whose entire job is to *emit* help wrote to the diagnostic stream: a caller redirecting stdout got a successful exit and no bytes, which reads as "this tool has no help" rather than as an error. The redirect moved out of `usage()` to the **four error arms** that still want it, because the two uses genuinely differ — an explicitly-requested `--help` is the requested OUTPUT (pipeable, pageable) while usage-after-a-bad-argument is diagnostic — so the channel is the CALLER's to choose and `usage()` is no longer entitled to choose it. **The error paths are byte-identical**: PRE-vs-POST stderr matched byte-for-byte with rc unchanged across five argument shapes (unknown flag, no arguments, two positionals, a non-numeric id, a trailing valueless `--dl`), and stdout was empty on every one of them; `--help` went from 0B stdout / 89B stderr to **89B / 0B**, rc 0 either way. **Why nothing caught it:** `tests/adopt-to-dl-selftest.sh` captures with `2>&1`, merging the streams — it asserts the *text*, and is deliberately channel-agnostic, so it stayed green through the defect and stays green through the fix. `tests/help-output-selftest.sh` said so explicitly and did not rule on channel. It does now, and over the **whole registry** rather than this one bin: every `CLIS` ∪ `EXCLUDED` member is asserted to exit 0 with `--help` on stdout and stderr silent — the six bins were measured green before the assertion was written, so the leg is a record, not an aspiration — driven off `_registry()` so a name added to either list is channel-asserted with no second edit. A second leg is **adopt-to-dl-only, a population of one on purpose**: it guards the regression this change *creates*, since the redirect now lives at four call sites and a later tidy could drop one, putting a diagnostic on stdout with nothing to notice. Both predicates report rc **and both streams as one record**, so every "this stream is silent" assertion carries a "that stream has data" witness measured on the same run — a silent stream is thereby distinguished from a command that produced nothing at all or never executed. **Prove-it-can-fail** uses two fixtures carrying one defect each, each the other's presence witness (a fixture failing both predicates would be indistinguishable from one that does not run): the pre-fix shape fails the help predicate while still satisfying the error one, and a dropped error-arm redirect fails the error predicate while still satisfying the help one. They are synthetic rather than `git show <rev>:bin/adopt-to-dl` — the real pre-fix binary IS a faithful negative control and was run as one by hand, but naming a revision pins the check to a history position a squash-merge invalidates, and the extracted file needs `_kb-board-lib.sh` copied alongside or it exits 1 before reaching any argument. All 11 new assertions were each observed **RED** under a targeted mutation whose byte change was confirmed, including one per registry member so the loop is proven to cover the five bins that are not adopt-to-dl. **Doc-sync:** two present-tense claims that the channel was an open question — in this file's card#5339 and card#5334 entries, both **unreleased**, so they would have shipped alongside their own refutation — were corrected in the same change, as was `help-output-selftest.sh`'s own weakest-property block. **⚠ This changes user-facing output routing:** anything capturing `adopt-to-dl --help` off **stderr** now sees an empty stream (the help is on stdout); error output is unchanged on every path. `bin/` is untouched for every other tool and `promote/action.yml` is untouched, so no other vendored surface moves.
- **card#5359** — **`ADOPTION.md`'s "What you get (`bin/`)" list named a tool retired in v0.9.0, and omitted six that exist.** The line enumerated `board-transition-sync` — retired by #55 (card#3649, superseded by the bridge writeback) and absent from `bin/` since — while closing with *"See `README.md` for the one-line role of each"*, and README's table does **not** list it: an adopter following the pointer finds nothing and cannot tell which of the two docs is wrong. The card scoped the fix to deleting that one name, which would have left the actual defect standing. The line is a **second copy** of README's table, and comparing it name-by-name against `bin/` — the check a stale-name grep can never be, because a grep finds a phantom and never an omission — shows it was also missing **six** live tools (`adopt-to-dl`, `board-card-start`, `install-board-hooks`, `dl-a0-backfill-triaged`, `dl-a1-register-field`, `agent-board-toolkit-runtime-check`). So the enumeration is **removed rather than corrected**: the section now names the tool categories and points at README's table as the single list. `docs/INSTALL.md`'s PATH-install loop was checked as a possible third copy and is already drift-proof — it globs `bin/*` rather than enumerating. **Also fixed in README:** its table was itself missing `agent-board-toolkit-runtime-check` (shipped v0.15.0, documented in `INSTALL.md` §2 and `VERSIONING.md`, never given a row) — a gap that becomes load-bearing the moment that table is the only list. The table and `bin/` now agree 13-for-13, verified in both directions, each direction positive-controlled by injecting the defect it is meant to catch. Docs only; no tool changed. **Now gated:** card#5374 shipped [`tests/readme-bin-coverage-selftest.sh`](../tests/readme-bin-coverage-selftest.sh) in this same release — the selftest that catches both directions.
- **card#5353** — **`docs/HOOKS.md` asserted that the pre-push lint and the auto-move "can never disagree" two lines above the paragraph that refutes it.** card#5333 appended a paragraph narrowing that property to the **grammar** — the argument surface being the one axis the shared matchers do not cover — but left the unqualified summary sentence above it standing, and the same pair sat in `hooks/pre-push`. The un-narrowed form was the one that propagated: `bin/board-card-start` cited it verbatim, so a reader who stopped at the summary got an absolute the subsystem does not provide. All three now say **about the grammar**. A fourth copy in the shipped v0.17.0 entry below is deliberately left alone — this log is the immutable canonical per-version record, and editing a released entry rewrites history. No behaviour change. **Also:** `tests/board-card-start-selftest.sh`'s `KBCARD_API= KBCARD_TOKEN_FILE=` env prefixes introduced **SC1007**, a warning class this repo did not previously carry anywhere. CI gates at `-S error` so nothing was failing, but `ci.yml` states the intent to tighten to `-S warning`; quoting them takes the repo-wide `-S warning` set back to `{SC1090, SC2034}` (measured 5 → 0). Both were raised by the card#5333 adversarial review, which returned after that PR had merged.
- **card#5333** — **`board-card-start <branch> --lint` silently dropped the flag and moved a card for real, and `board-card-start ""` moved the card for whatever branch `HEAD` was on.** Two defects on one unguarded argument surface of the tool that **writes to a live board**. `--lint` was honoured only in position 1 (`[ "${1:-}" = "--lint" ]`), so its most natural mis-spelling — the flag *after* the branch — fell through to the mover: a flag whose entire contract is "no board work" performing the write, **byte-for-byte the defect `install-board-hooks` documents fixing for `--check`**, in a parser that was never revisited when that one was. And `branch="${1:-$(git rev-parse …)}"` fires on **empty** as well as unset, so `board-card-start "$BRANCH"` with `BRANCH` unexpanded did not no-op — it retargeted the move to the current branch, i.e. **a write against a card the caller never named**. That is the empty-value class again — not a new *kind* of source (positionals were swept in this same release), but a **parser that sweep did not reach**: it covered `install-board-hooks` and `adopt-to-dl` and stopped there, leaving the instance sitting in the one tool of the three that **writes to the board**. Arguments are now parsed in a **loop** — `--lint` anywhere, an empty positional / an unknown option / a second positional refused **by name** — with "have I seen a positional?" still answered by the value, which is sound only because an empty one is now rejected outright — the same mechanism as the other two parsers. **The refusals keep the fail-soft contract:** every path still `exit 0`s, so a refusal is a *no-move*, never a non-zero exit — which is why `bcs_skip`/`kb_bcs_log` are deliberately **not** used for them (their wording asserts the branch carries a DL/card token, which an argument refusal has not established), and therefore why an argument refusal is **stderr-only** and never reaches the durable log. The mechanism that binds rc 0 here is **not** `hooks/post-checkout`, which passes **zero** arguments — no refusal arm is reachable from it — through a `>/dev/null 2>&1 || true` wrapper that would swallow a non-zero exit anyway. It is the **operator-authored chained** hook, an explicitly supported topology (HOOKS.md tells operators to chain the toolkit hook into a committed hook by hand; `board-session-close` models its `CHAIN-BROKEN` state and fixtures the unguarded `command -v board-card-start && board-card-start` shape), which carries no `|| true` — and per `githooks(5)`, `post-checkout`'s exit status **becomes** `git switch`/`git checkout`'s (measured on git 2.43: the checkout is not undone, but the command reports failure). **Zero arguments is untouched** and still resolves `HEAD`'s branch — `hooks/post-checkout` passes none, so discriminating `$# -eq 0` from `$1 == ""` was the whole point. **The `-*` arm needed a real end-of-options terminator, and its absence was a live false refusal.** The first cut of that arm was justified by "git rejects branch names starting with a dash", which is **false** and was refuted live on git 2.43: `git check-ref-format refs/heads/-foo` is rc 0 and `git update-ref refs/heads/-foo HEAD` creates the branch — only the `git branch` **porcelain** refuses the name — and a push of it fires `pre-push` with the bare name `-foo`. So `board-card-start --lint -foo` printed `unknown option: -foo — no card moved` on every push of such a branch, while the **mover still moved its card** (`post-checkout` passes no arguments, so it reaches the `git rev-parse HEAD` fallback and never enters the parser) — falsifying HOOKS.md's "the lint and the mover can never disagree". `--` is now a genuine terminator (it sets a flag the loop honors — **not** the decorative `--) ;;` that is accepted and then ignored), everything after it is a positional through the **one** positional owner both sides of the terminator reach, and `hooks/pre-push` passes `--lint -- <branch>`. A fixture-repo selftest covers all nine argument forms **plus `hooks/pre-push` itself** — the call site is exercised with real stdin in git's pushed-ref shape and a real `board-card-start` on `PATH`, because the false refusal was reachable only from there and a parser-only test would leave the hook free to drop the `--` again. The dash-leading branches are **created for real** with `git update-ref` rather than passed as strings, and git's acceptance of them — the premise the whole terminator serves — is itself an assertion, so a future git that rejected the name could not leave every other check passing over a premise that had silently gone false. Its 29 assertions were each watched **red** under a targeted mutation: the flag honoured in position 1 only, the flag accepted then dropped, an empty positional accepted, each refusal arm removed in turn, the `HEAD` fallback removed, the refusals given `install-board-hooks`' rc 2, the fail-soft skip and the lint path made non-zero, lint dispatch disabled, the terminator made **decorative** (`--) ;;` — accepted and ignored), the post-`--` argument re-exposed to the `-*` arm, the hook's `--` dropped (which reds the two call-site assertions and *only* those), the hook made unparseable, and a ref name git genuinely rejects fed to the premise check. The zero-args case doubles as the positive control that a run which *does* reach board work is detectable at all. **Two assertion-quality corrections came out of that pass and are worth recording:** a "no move attempted" check on the *warn-worthy* dash-name could not fail under any mutation (the lint warns only where the grammar does **not** recognize the branch, so such a name can never reach board work) and was moved onto the **correlating** dash-name; and the hook's rc-0 assertion pins that the hook **parses and runs**, not that `board-card-start` returned 0 — the `|| true` and the trailing `exit 0` each independently force rc 0 — which is now stated at the assertion so it is not read as a guarantee it does not give. **Audit:** the remaining positional CLIs are clean for this shape — `agent-board-toolkit-drift-check` requires both positionals non-empty, `runtime-check`'s `--reference` uses `${2:?…}`, and `board-snapshot`'s only positionals are function locals. **⚠ This narrows what the tool accepts:** `board-card-start ""` is a stderr refusal rather than a move against the current branch, `board-card-start <branch> --lint` lints instead of moving, and an unknown flag or a second positional is refused — all still at rc 0, so no caller's exit-status handling changes. A branch name that starts with `-` must now be passed after `--` (the refusal names that fix). **`hooks/pre-push` changed with it**, and the two must move together: on the normal symlink install the hook content tracks the toolkit automatically, but on the Windows/MSYS **copy** topology (INSTALL.md §2) the stale copy still calls the bin without `--`, so the false refusal survives there until `install-board-hooks` is re-run per HOOKS.md's copy procedure. `bin/` is unchanged for every other tool and `promote/action.yml` is untouched, so no other vendored surface moves.
- **card#5334** — **the third `--help` fixed-line-range instance was still live, and it failed in the LEAK direction.** `agent-board-toolkit-runtime-check` printed `sed -n '2,30p' "$0"` — the defect v0.23.1 fixed for both release movers (card#5145), in the one file that card's sibling audit never reached. Its header is **28** lines while the range said 30, so `--help` printed 29 lines and the last one was `set -euo pipefail`: the *inverse* symptom of the movers' truncation, from the same root cause, which is why the suite's central assertion was written as a line-count **equality** rather than a minimum. It now prints the contiguous leading comment block via the movers' `awk` form, and it **joins `tests/help-output-selftest.sh`'s `CLIS`** — so the leak direction, previously only a stated hazard in that file's rationale, now has a real in-repo witness. Adding it also reddened the suite's `Usage:` assertion, which turned out to be reporting a **second** defect: the header explained `--quiet` and `--reference` in prose but carried **no usage synopsis at all**, so `--help` never showed how to invoke the tool; a `Usage:` line was added. The other three `--help` arms were audited and deliberately **not** added to `CLIS`: `dl-a0-backfill-triaged` and `dl-a1-register-field` echo a one-line `$USAGE`, and `adopt-to-dl` echoed a one-line `usage()` — to **stderr** at the time, corrected by card#5337 below in this same release — so none prints a header block and a header-equality assertion does not apply to them; converting them would be an unrequested change to user-facing output.
- **card#5226** — **`install-board-hooks` answered three separated git topologies with one message that was wrong for all of them.** `install-board-hooks <linked-worktree>` (and the same for a `--separate-git-dir` checkout or a submodule) reported `cannot create the hooks directory <root>/.git/hooks` and exited 1 — naming a path git never reads, asserting a cause that is not the problem, and offering no way forward. It is a **reachable** path: this project's own reference host keeps a linked worktree. The installer now resolves the repo's real git **common dir** and refuses when it is not `<root>/.git`, with the message the topology actually needs: a **linked worktree** is told to run `install-board-hooks <main-checkout>` (which wires it too, since the hooks dir is shared by the main checkout and every worktree — installing from the worktree would have changed hook behavior for all of them, the surprising blast radius that decided this disposition); a **`--separate-git-dir`** checkout is told there is no second checkout to run it against and given the dir to symlink into; a **submodule** is told that running it against the superproject would wire a *different repository*, and given `<super>/.git/modules/<name>/hooks`. Only the first has a command to print, which is why one generic message was not an option. **The discriminator named in the report was wrong and is corrected here:** `--git-common-dir` ≠ `--git-dir` is true **only** for the linked worktree — measured on git 2.43, the other two report them *equal* — so a guard built on it would have shipped covering one topology of three; the selftest carries that exact mutation and it reds. Detection is by common dir, never by `.git`-is-a-file (all three share that shape). The refusal is **conditional on `core.hooksPath` being unset**, not on topology alone: a set `hooksPath` wins everywhere, so a worktree that configures one is installable and refusing it would be a false refusal — a second mutation pins that. `board-session-close`'s own topology predicate is retired onto the installer's `_ibh_main_checkout`/`_ibh_common_dir`, so the check and the installer cannot drift apart on which checkout owns a shared hook dir.
- **card#5200** — **a `core.hooksPath` value git cannot expand is reported by a different arm on a different git build, and the disclosure said one of them was unreachable.** On git 2.43.0 such a value is expanded during the *general* config read, so every command in the repo fatals — `rev-parse` included — and the check reports git's own words. On git 2.54.0 `rev-parse` succeeds and only the explicit `--path` read fatals, so the second arm reports it. Both are live. The arm was kept deliberately (folding an unknown read status into "unset" is the guess this check exists to prevent) but was annotated *"the other arm always wins — verified"*, and the test **pinned that arm's exit code**: it passed four adversarial review rounds on one host and failed on CI's first run in a second environment. The disclosure now names the mechanism and both builds and claims no ordering; the test asserts the **guarantee both arms owe** — refused, never folded into "unset", with a cause specific enough to act on — and accepts either route; a `git` shim fixture forces the other arm so **both are exercised wherever the suite runs**, not only on the builds that happen to route to them. Both refusal arms now carry a cause on stdout, so the caller's contract no longer depends on which one fired. The transferable lesson is in the code: a measurement taken in one environment is scoped to it, and "verified" without naming where is how a local artifact gets read as a general property.
- **card#5200** — **`adopt-to-dl`'s four value-taking flags read an empty value as an absent flag, and `--dl ""` minted a SECOND DL.** `kb_require_value` was already sourced and in scope, and `kbcard` rejected the identical input at all 37 of its arms; these four arms were never converted. `adopt-to-dl "$CARD" --dl "$DL"` with `DL` unexpanded therefore took the **mint** path — but `--dl N` exists *only* for the crash-retry-after-mint case, so a retry that meant "re-stamp DL-N" minted a new one instead, **orphaning DL-N and stranding any branch or PR already named for it**: exactly what `_ata_adopt_decision`'s refuse-adopted arm exists to prevent. `--issue ""` silently dropped the co-stamp while still printing "adopted … verified via by-ref". All four now use the shared guard, which also converts a trailing flag with no argument from a bare `shift` failure (rc 1, **no output at all**) into a named diagnostic. **This is the empty-value class in its third position on one axis** — config reads, then positionals, now flag values — and the finding that matters is procedural: the sweep that declared the class closed was run against every bin *except the one being edited*, where the instance sat four lines above the arm it had just hardened. A sibling audit is scoped by the **shape**, and it must include the file the change is in.
- **card#5200** — **an explicitly-empty POSITIONAL argument was invisible, so the next one silently took its place.** `install-board-hooks "" <repo>` installed into `<repo>` at rc 0 — defeating the "an extra positional is rc 2" guarantee in the very parser that had just added it — and `adopt-to-dl "" 4242` adopted card 4242 the same way. Both tracked "have I seen a positional?" by testing the value for emptiness, and an unexpanded shell variable is the way in. **This is the v0.23.1 empty-value class for the third time in this branch**, and it says the earlier sibling audit was scoped to the wrong axis: that sweep covered *sources that can be set-empty* (config reads, env knobs) and correctly found `core.hooksPath`, but v0.23.1's own original instance was **argument parsing**, which the sweep never covered — so the shape survived into a parser written afterwards. The axis for a sibling audit is the **shape** (a value that can be present-but-empty where *presence* is the real question), never the category of source it was last found in. Both parsers now reject an empty positional **by name**, which is what makes their existing value-emptiness test a sound "have I seen one?" — a separate seen-flag was written and removed, because with empties rejected nothing could reach it and a mutation of it could not be made to fail. (That sweep still missed a third position on the same axis — `adopt-to-dl`'s own flag values, in the file it was editing; see the entry above.) **⚠ Both tools now reject invocations they previously accepted** — `adopt-to-dl "" <id>` is rc 2 rather than adopting `<id>` — the same narrowing, and the same reason, as the v0.23.1 flag-value change.
- **card#5200** — **"can git run this hook?" was computed in three places, and each review round found a new fact that had to be added to all of them.** The executable bit was added to the chain path after the direct path already had it; exec-ability was then added to both; and three more one-predicate gaps followed in the same family — a **tab-separated shebang** (`#!<TAB>/bin/sh` and `#!/bin/sh<TAB>` both *run*, and were being reported as a dead hook: a false RED on a healthy repo), an interpreter that is a **directory** (`-x` is true for one), and a first line with **no trailing newline** (`read` returns non-zero while still assigning, so the verdict was discarded). Applying each new fact to both paths, correctly, still left the next one to be applied to both paths. The split was the defect, so the property is now **one predicate — `_bsc_runnable`** — that owns the bit, the file type, the shebang read, the kernel's separator set (space and tab — **only** those two; a POSIX `[[:space:]]` class is a superset that also matches CR/VT/NL/FF, and reading it as "the kernel's whitespace rule" trimmed a broken interpreter name down to a runnable one, so `#!/bin/sh<CR> -e` and `#!<VT>/bin/sh` reported healthy while git failed `cannot exec`), the interpreter's existence/type/bit, and CR/CRLF; both the direct path and the chain path call it and decide nothing themselves. The extraction immediately closed a fourth gap nobody had reported: a **directory** where the hook should be. A hook git cannot run is reported as one `NOT-RUNNABLE` state whose reason text comes from the predicate, so the operator sees which of those facts failed without the report carrying a state per fact. (The intermediate per-fact states this work passed through never shipped.)
- **card#5200** — **a fixture asserted a DEAD hook was "genuinely runnable", and it blocked the correct fix.** The disclosed shebang bound had been stated as a truncation case with a "safe half": that a long shebang line naming a short interpreter "runs normally". Run for real, `#!/bin/sh <500 a's>` dies with `/bin/sh: cannot open aaaa…` — the auto-move is dead while the check says ✓, and the test asserted that as safe. The real bound is broader and was undisclosed: **the shebang argument is never judged**, and twelve bytes are enough (`#!/bin/sh zzz` → check OK, hook dead; `#!/bin/sh -e` → correctly fires). Worst of all, implementing over-long detection reddened *two* assertions, one of which was that false fixture — **a mutation count vouching for a defect**. The false assertion is deleted, the argument bound is disclosed with a control that pins the accepted-argument case, and the same mutation now reds exactly the one true assertion.
- **card#5200** — **a hook that is executable but cannot be EXEC'd reported healthy.** The executable bit is not exec-ability: a hook with **CRLF line endings** or a **missing shebang interpreter** is `-x`, looks perfect, and makes git fail with `fatal: cannot exec '<hook>': No such file or directory` (verified against git 2.43) — so the card auto-move is dead while the check says ✓. CRLF is the native hazard of the Windows/MSYS **copy** install topology this toolkit explicitly supports, making it a supported-platform failure rather than a hypothesis. Detecting it is a **pure read of line 1**, so it is *fixed*, not disclosed — the "closing this would require executing an arbitrary hook" rationale that makes the other two false-OK bounds acceptable does not apply, and disclosing it under that rationale would have stated something untrue. Applied on the direct hook **and** the chained target, because the `-x` check itself had just been fixed in one of those two places only. (Both report through the consolidated `NOT-RUNNABLE` / `CHAIN-BROKEN` states described above; the intermediate token this fix first introduced never shipped.)
- **card#5200** — **a non-canonical path spelling was told its `.git` is a file.** The fix-line compared the installer's target against the dispatch dir as **strings**, but `--git-common-dir` answers relative to the path the caller typed while the classification uses the always-canonical `--show-toplevel`. A **symlinked repo path**, a **trailing slash**, or a **sub-directory of the work tree** therefore fell through to the "linked worktree / --separate-git-dir / submodule" arm, which asserted a falsehood about an ordinary checkout **and** withheld the `install-board-hooks` invocation that in fact succeeds (the installer canonicalizes, so all three are rc 0). Both sides are now canonicalized before comparison, and the fall-through arm reports the two concrete directories instead of asserting a cause.
- **card#5200** — **the fix-line named a command that exits 1, in its MOST COMMON case, because it re-derived `install-board-hooks`' preconditions instead of asking.** The partial copy proved 2 of the installer's 9 preconditions, so every finding whose hook is a regular file — the normal shape of `NO-REACH`, `NOT-RUNNABLE` and `CHAIN-BROKEN`, and a repo carrying its own committed `post-checkout` is the likeliest real cause of a dead auto-move — was told to run a command that hits the installer's clobber refusal. An unwritable hooks directory was a second instance. **The copy was the defect**, and it produced a wrong answer in two consecutive reviews, so the fix is structural rather than two more predicates: `install-board-hooks` gains a **`--check` dry run** that evaluates every precondition in the same function and the same order as a real run, stops before the first write, prints the directory it *would* target on success and its own diagnostic on refusal — and `board-session-close` now **gates on that verdict**. What stays caller-side is the question `--check` cannot answer: whether that target is the directory git *dispatches* from (the installer can succeed while installing where git never looks). A refusal is now reported in the installer's own words with no remedy invented — notably **no `ln -s` suggestion**, which for the clobber case would have told an operator to destroy their own hook.
- **card#5200** — **a hook chaining to a NON-EXECUTABLE toolkit hook reported healthy.** The direct-hook path checked the executable bit from the start (git ignores a non-executable hook), but the one-level chain path did not, so a committed hook that `exec`s a toolkit hook whose bit had been dropped read as `OK` while git's exec failed and the card auto-move was dead — a false green of exactly the class this check exists to eliminate. The chained target is now checked too, and reported as its own `CHAIN-BROKEN` state naming the file it cannot exec.
- **card#5200** — **the remediation line named a command that exits 1 on three real topologies.** A `.git` *file* — linked worktree, `--separate-git-dir` clone, submodule — makes `install-board-hooks <repo>` target an un-creatable `<repo>/.git/hooks`. The line is now derived by comparing the directory the installer would target against the one git dispatches from (the same resolver, one substituted input), so it names the repo only when that provably works, names a sibling checkout that owns the hook dir when one does, and otherwise states plainly that the installer cannot service the checkout and gives the manual `ln -s`. The earlier `*/.git/hooks` path heuristic covered only `git worktree` and silently missed the other two.
- **card#5200** — **a `..`-relative `core.hooksPath` that escapes the work tree was classified as inside it.** The prefix test compared the un-normalized string, so `../shared-hooks` still began with the repo root and drew the in-tree refusal and its hand-chain guidance. A pure lexical normalizer (no filesystem access, so the resolver stays testable) now runs before the classification only — the echoed path is left as git would open it, symlinked components included.
- **card#5200** — **`core.hooksPath` set to an EMPTY value was read as "not configured", by both the new check and `install-board-hooks`.** Verified against git 2.43: an empty value does **not** fall back to `.git/hooks` — git dispatches **no hooks at all** — and `git config --get` separates it from *unset* by **exit status only** (unset → rc 1, empty → rc 0 with empty output). Reading the value's emptiness therefore certified a repo whose card auto-move is dead, and made the installer `ln -sf` into a directory git never reads while reporting success: the exact **#4281 silent no-op** the installer exists to prevent. A single `_ibh_read_hooks_path` reader now returns **presence as a status**, `_ibh_hooks_dir` takes that presence explicitly and returns a distinct "hooks disabled" state, the check reports it with its own remediation (`--unset`, not a re-install), and the installer **refuses** rather than plant a no-op. This is a **sibling of the v0.23.1 empty-flag class** re-minted in a config read; the generalisation — *any source that can express set-empty must have its presence read as a status, never inferred from the value* — is recorded on the reader.
- **card#5200** — **a `~`-prefixed `core.hooksPath` was resolved as a relative path.** `core.hooksPath` is a path-type variable and git expands `~`/`~user` (verified: a hook in `~/<dir>` fires). Reading the raw value produced **both** a false red (a working repo reported dead, and mis-classified as inside the tracked work tree, so the operator was told to hand-chain a hook it did not need) **and** a false green (planting at the literal `<root>/~/<dir>` while the real directory stayed empty). Both callers now read via `git config --path`, which also leaves a relative value unchanged.
- **card#5200** — **a repo git refuses is no longer given a guessed cause.** `install-board-hooks` asserted *"not a git repo"* and the new check *"not a git work tree"* whenever `git rev-parse` failed. Several distinct states land there — a `core.hooksPath` git cannot expand (which fatals **every** command in the repo, the probe included), `dubious ownership` (`safe.directory`), and a genuine non-repo — and each needs a different fix. Both now print **git's own diagnostic**; a wrong-but-specific cause is worse than an honest one.

## [0.23.1] - 2026-07-27

**Patch — an explicitly-empty flag value no longer reads as an absent flag anywhere in the toolkit, `--help` stops truncating, and an unguarded promote says so.** 6 PRs since v0.23.0 (#169–#174). Per `VERSIONING.md § Bump sizing` this is a **patch**: every change is a fix, a doc correction, or an internal test addition — no new `bin/` tool, no new flag, no new capability. **This release DOES touch the vendored surface** (`bin/kbcard`, `bin/_kb-board-lib.sh`, `bin/promote-released-cards`, `bin/release-pr-body`) — consumers who vendor those scripts get the stricter flag handling and the new stderr line; `promote/action.yml` is unchanged, so consumers who only pin the action see no interface change.

**⚠ UPGRADING — the CLIs now reject `--flag ""` where they previously accepted it silently.** This narrows what the tools accept, and it is deliberate: an explicitly-empty value was indistinguishable from the flag being absent, so it selected a **default path** and exited 0. A caller passing an unexpanded shell variable (`--board "$KEY"` with `KEY` unset) was the common way in. Any script relying on `--flag ""` as a way to say "use the default" must now omit the flag instead. Whitespace-only values already died before this release and are unchanged.

### Fixed
- **#169 (card#5144)** — **`promote-released-cards --shipped-stages ""` ran UNGUARDED, with a summary identical to a guarded run.** Every consumer of the variable gated on non-emptiness, so an empty value skipped the validation block (`:100`), the anti-resurrection **source-stage guard** (`:304`), and the `stage-guarded` field in the summary (`:322`) — byte-identical to passing no flag at all. An unexpanded shell variable, an empty CI input, or `--shipped-stages "$STAGES"` with `STAGES` unset therefore produced exactly the failure the guard exists to prevent: a stale or recycled DL/PR stamp resurrecting a declined or backlog card into Released, reported clean. **Reproduced empirically against the selftest fixture before fixing** — flag absent ⇒ rc 0, `wont_do` card promoted (the documented opt-in default); `--shipped-stages 51` ⇒ rc 0, not promoted (guard works); `--shipped-stages ""` ⇒ rc 0, **promoted, unguarded**; `--shipped-stages " "` ⇒ rc 2 (already died). **A correction to the report rides with the fix:** the reporter's stated tell was that the `''` arm at `:103` is *unreachable* and that the code therefore already intended to reject this. The arm is **reachable** — a whitespace-only value normalizes into it and dies today — so the hole was only the *pre-normalization* empty string; the intent reading still holds, but the arm is live code and is now pinned by a test so it is not later deleted as dead. The fix is a `require_value <flag> <value>` primitive that **dispatches on the flag being seen rather than on its value being non-empty**, applied to **all five** value-taking flags (`--dls`, `--base`, `--head`, `--shipped-stages`, `--config`), not just the reported one — the same shape silently switched `--dls ""` from explicit-DL mode to git-range derivation and `--base ""` to local-tag derivation. It also converts a trailing flag with no argument from a bare `set -u` unbound-variable error into a named diagnostic.
- **#171 (card#5146)** — **the same empty-value shape, in every other CLI — found by sibling audit, not by a report.** Filed unprompted from card#5144's audit (canon #7) and approved before implementing. Any flag that consumes `"$2"` and is then dispatched with `[[ -n "$var" ]]` reads an explicitly-empty value as an absent flag, silently selecting the default path and exiting 0. **The instance that matters most is the correlation stamp:** `kbcard patch --task 5137 --dl "$DL"` with `DL` unset sent payload `{}`, PATCHed `{}`, printed a normal success echo, and exited 0 — leaving the card without the ref it was told to carry, and **a card with no `dl_number` / `pr_number` never promotes at release.** The same held for `--pr`, `--pr-url`, `--issue`, `--issue-url`, `--version`, `--column`, `--swimlane`, and `--description`. **The worst is `--board ""`**, which fell through to the *default* board — so an unexpanded `--board "$KEY"` read and wrote **a different board than the caller named**, a wrong-board answer rather than a no-op, demonstrated live against the real tooling before fixing. `kb_require_value` in `bin/_kb-board-lib.sh` is the single owner, adopted by `kbcard` at all **37** value-taking arms; the two self-contained standalones that must not source the lib carry inline mirrors, the same convention already used for `kb_require_https_host`.
- **#170 (card#5145)** — **`--help` printed a fixed line range, so it truncated the header it exists to show.** Both release movers printed their header with `sed -n '2,20p' "$0"` — silently wrong in **both** directions, truncating once the header outgrows the range and leaking code once it shrinks. `promote-released-cards`'s header had grown to **43 lines**, so `--help` stopped at line 20 and never reached `Usage:` (line 35) or the `--shipped-stages` explanation (lines 37–44) — the single block a `--help` user most needs was unreachable. `release-pr-body`'s header is exactly 19 lines, so `2,20p` happened to print all of it: correct today, and it would have begun truncating silently at the first line added. Widening the range was rejected as the fix — it leaks `set -euo pipefail` and the helper functions into user-facing output. Both now print the contiguous leading comment block via `awk 'NR>1 { if (substr($0,1,1) == "#") print; else exit }'`, so output tracks the header at whatever length it reaches. New `tests/help-output-selftest.sh` covers both CLIs from one owner, and its central assertion is a **line-count equality** against the file's own contiguous-comment count, re-derived from the file on every run — an "at least N lines" or "contains `Usage:`" assertion passes while truncating everything after the match, which is precisely how this survived; equality catches truncation **and** over-printing.
- **#173 (card#5152)** — **an unguarded promote now states that it was unguarded, on stderr.** card#5144 made an explicitly-empty `--shipped-stages` die, but the **absent** case still printed a summary indistinguishable from a guarded run, so an unguarded promote read as clean — the same *"looks enabled, isn't"* shape the guard exists to prevent, one layer out, and it also covers the #172 case where an unset repo variable yields no guard at all. **Fixing the summary line was the obvious change and the wrong one:** the summary is byte-identical when the guard is absent by an explicit v0.21.0 fleet-adoption decision (*"absent = byte-identical prior behavior — consumers opt in"*), anything parsing it would break, and a selftest asserts it — overturning that needed its own DL and an argument that could not be made honestly, because the compat property is doing real work. A **separate stderr line** gets the visibility without touching it: the summary stays a stable parse target, and stderr is already where this tool puts its operator-facing findings. The OFF line names **what actually happened** and the remedy, not just a state. Verification asserts both directions plus **that the OFF line is not on stdout**, which would silently break the parse target the change exists to preserve. Split out of card#5148 rather than bundled, because it collided with that compat property and needed its own decision.

### Docs
- **#172 (card#5148)** — **`docs/INSTALL.md` §6a recommended a wiring that silently disables the guard it was enabling.** The snippet wired `shipped-stage-ids: ${{ vars.KANBAN_SHIPPED_STAGE_IDS }}`; an unset variable renders as a blank input, `promote/action.yml` correctly omits the flag, and the anti-resurrection guard is **off** — no error, no warning, and a promote summary identical to a run that never asked for it, while the workflow file still *reads* as guard-enabled to the next person auditing it. The same **"looks enabled, isn't"** shape as card#5144, one layer up, and it was the **recommended** wiring, so the docs walked consumers into it. The stage ids are per-board constants that change only when a board's columns are reconfigured, so the variable indirection bought nothing and cost silent failure: the snippet now carries a **literal** (explicitly marked an example id, not a prescription), and the variable form is demoted to an aside naming the hazard with a preflight mirroring the two already documented directly above it for `KANBAN_API_BASE` / `KANBAN_EXPECTED_HOST`, so an unset variable fails the job instead of quietly downgrading it. **The card's original direction also asked for an action-side `::warning::` on blank; it was dropped after working the mechanism** — `promote/action.yml` cannot distinguish "the operator omitted the input" from "the operator wired a variable that is unset", since both arrive as a blank string, so an unconditional warning would fire at every consumer who has deliberately not adopted an **opt-in** guard, and a check that cries wolf is one the first person it annoys switches off. Only the consumer knows its own intent, which is why the preflight belongs consumer-side; that reasoning is recorded in the doc so the next reader does not re-propose it.
- **#174** — the managed `coord:solo-orientation` block re-synced to coord plugin **v0.24.0** (from v0.23.0) via `orientation-sync.py --apply --role solo --integration dev`. Block-only change; this repo's own addendum outside the markers is untouched. The content delta adds `MIRROR-BEGIN/END` markers around the context-budget chat-gate prose, which are load-bearing: the framework's `chat-gate-mirror-drift.selftest.py` extracts exactly what they enclose and reds if any of the five lockstep copies diverges.

## [0.23.0] - 2026-07-24

**Minor — `board-session-close` gains a read-only "archive-eligible done cards" surfacing leg, and the archive-gate plumbing is factored into a shared lib.** 1 feature PR (#166, card#5094) since v0.22.0, plus a solo-orientation sync to coord v0.23.0 (#165, chore). The new leg reports each board's done-card backlog and gates a bounded sample through the shipped `may_archive` gate so a session-close can surface concrete archive candidates; the shared `kanban_common` locator + GitHub-state resolver are extracted so `_kbc-may-archive.py` and the new leg share one copy (canon #5, `_kbc-may-archive.py` byte-identical). No breaking CLI change; `promote/action.yml` vendor surface unchanged.

### Added
- **#166 (card#5094)** — **`board-session-close` surfaces archive-eligible done cards (read-only).** A new sibling leg — `bin/_kbc-archive-eligible.py`, surfaced under a `── Archive-eligible done cards (consider: kbcard archive #N) ──` section after the inverse-drift check — reports, per configured board (`kanban.boards[]`), **how many terminal (done-lane) cards are not yet archived** and runs the framework's shipped `may_archive` gate over a **bounded sample** of them to show a concrete "safe to archive now" starting point, so the agent can decide to run `kbcard archive #N`. It fetches the one board GET, collects the non-archived cards, filters to those whose stage `lane_type == "done"`, and gates the first `_MAX_DETAIL` with `may_archive(card, resolve, surviving_cards=<all non-archived>)` (a live source with a surviving non-archived twin is correctly reported eligible). The gate makes one `gh` call per backing source and the authoritative per-card gate already runs in `kbcard archive` at archive time, so the sample is bounded to keep the session-close ritual fast — the exact done-count is the actionable signal. It is **READ-ONLY** — it only surfaces; the archive is the agent's call. It fails **loud**: a board with an unset/`REPLACE_ME` id is reported skipped, and an unresolvable `kanban_common` or a board-fetch failure prints a ⚠ to stderr and exits non-zero. In `board-session-close` a **missing sibling is a warning, not a hard failure** (the drift check already ran; advisory). The shared archive-gate plumbing — the version-agnostic `kanban_common` locator and the tri-state GitHub-state `resolve(src)` reader — is **extracted into a new `bin/_kbc-archive-lib.py`** that BOTH the new leg and the refactored `_kbc-may-archive.py` path-load (canon #5: the second consumer extends the primitive instead of siblinging a divergent copy); `_kbc-may-archive.py`'s behavior is byte-identical (its selftest passes unchanged). New network-free `tests/kbc-archive-eligible-selftest.sh` (fake `kanban_common` wrapping the real `may_archive` + a fake board + a per-PR-number fake `gh`) pins the eligible-when-all-terminal, blocked-when-live-and-untwinned, done-lane-only-candidate, and archived-skipped cases, with a prove-it-can-fail that inverting the done-lane filter makes the in-progress case appear. `promote/action.yml` vendor surface unchanged.

## [0.22.0] - 2026-07-23

**Minor — a dispatch-time card-start hook closes the dispatch→branch-creation latency window, plus the promote source-stage guard adopted in the toolkit's own release-promote workflow.** 2 PRs since v0.21.0 (#159, #162), cards #4945/#4962. The headline is a new Claude Code `PreToolUse` hook that moves a card to In Progress the moment a subagent build is dispatched — rather than at the later branch creation. No `bin/` CLI behavior change; `promote/action.yml` vendor surface unchanged.

### Added
- **#159 (card#4945)** — **`hooks/agent-dispatch-card-start` — a dispatch-time "In Progress" card move.** A Claude Code `PreToolUse` hook for the `Agent` (subagent-dispatch) tool that moves a card to In Progress the moment a build is dispatched, closing the latency window that `hooks/post-checkout` (→ `board-card-start`) only closes at branch creation. It is a **peer** of that hook, not a replacement — either can fire first, and the move is idempotent in either order. It acts **only** on an explicit `BOARD-CARD: <board-key>#<card-id>` marker line anchored at line start in the dispatch prompt (`.tool_input.prompt` from the stdin event JSON); a bare card-number scan is deliberately **not** used, because review/report dispatches routinely mention many card ids in prose. Multiple marker lines are each acted on (exact duplicates deduped) and routed per marker to their board key. Transport is the existing `kbcard --board <key> move --task <id> --column in_progress` primitive — kbcard owns board-env/token resolution, so the hook hand-rolls no `curl` and the board-key charset forbids traversal into the `~/.kanban-<key>-board.env` resolve. It is **fail-soft on every path** (exit 0 always — a nonzero `PreToolUse` would block the dispatch): unparseable stdin, no marker, unknown board key, `kbcard` missing, or an API error all diagnose to stderr and continue, and each move is bounded by `timeout` (`KBADS_TIMEOUT`, default 10s). Registration is a **manual operator step** — a `PreToolUse` matcher `Agent` entry in Claude Code `settings.json` (snippet in `docs/HOOKS.md`); it is intentionally **not** installed by `bin/install-board-hooks`, which symlinks git hooks, not Claude Code settings. New `tests/agent-dispatch-card-start-selftest.sh` (PATH-shim `kbcard`, scratch HOME) covers anchor / no-marker / malformed-JSON / unknown-key / multi-marker / dedupe / kbcard-failure cases and joins the CI selftest matrix + a dedicated `ci.yml` job.

### Changed
- **#162 (card#4962)** — **the toolkit's own `release-promote-cards.yml` adopts the v0.21.0 `shipped-stage-ids` source-stage guard.** The workflow consumes the **local** `./promote` composite action (no external pin to bump) and now opts in to the `shipped-stage-ids` input introduced in v0.21.0, passing the tracking board's Shipped-to-Dev stage id. At release-promote time a DL/PR-matched card whose **current** stage is not Shipped-to-Dev is skipped (with a log line) instead of moved — a stale or recycled correlation stamp can no longer resurrect a declined or backlog card into Released. The guard is strictly **opt-in per consumer**: a blank input is byte-identical prior behavior. This applies the guard to the repo's own promotion, mirroring the adoption sibling consumers make via their SHA-pinned action reference.

## [0.21.0] - 2026-07-23

**Minor — the `kbcard field` verb family (custom-field schema read + enum converge-to-set reconcile) + the promote source-stage guard and decline stamp-clear, plus two solo-orientation syncs.** 4 PRs since v0.20.1 (#157, #155, #156, #154), cards #4939/#4940 (roundtable PupFuzz/agent-roundtable#132). `promote/action.yml` vendor surface: **one new optional input `shipped-stage-ids`** — absent = byte-identical prior behavior (proven empirically), so consumers adopt on their own windows. No other CLI behavior change absent the new flags.

### Added
- **#157 (card#4939, roundtable PupFuzz/agent-roundtable#132)** — **`kbcard field` verb family: the allow-listed custom-field/board-schema edit surface replacing raw PATCHes.** `kbcard --board <key> field list` reads the board's custom fields via `GET /boards/<id>/custom_fields.json` (empirically the only field-read surface — the per-id GET is 405), projecting id/key/label/type plus the full `{value,label}` option set for enum/multi_select fields. `kbcard --board <key> field set-options --field <name|id> --options a,b,c` is an **idempotent converge-to-set reconcile** (NOT an append): it PATCHes the whole options array only on drift and no-ops on an identical set — a provisioning preflight can call it with its full derived set and heal drift in one call. Refuses non-enum fields, empty/duplicate values; an unresolved `--field` enumerates the board's defined fields (mirroring `--swimlane`); API error bodies are preserved on failure. **Empirically pinned option-removal semantics** (sandbox-only experiment, stated in `--help` + README): removal is a **definition-only** change — the PATCH returns 200 even with the value in use, and a card holding a removed value keeps it verbatim (orphaned; no clear, no cascade). `add-option`/`rename-option` deliberately deferred to a v2. New network-free `tests/kbcard-field-selftest.sh` in the CI matrix (drift-write and identical-set-no-op mutually pinned; error-body preservation guarded).
- **#155 leg 1 (card#4940)** — **`promote-released-cards` source-stage guard (opt-in)**: `--shipped-stages <ids>` / action input `shipped-stage-ids`. A DL/PR-matched card whose current stage is not in the given set is **skipped + logged** — promotion means Shipped→Released and must never resurrect a declined/backlog card that retained a stale correlation stamp. Input absent = byte-identical prior behavior (proven empirically: identical stdout/stderr/rc/PATCH targets vs the pristine script); a malformed set fails loud (rc 2) before any PATCH. The 0-promoted die-branch deliberately keeps its PR-marker-completeness meaning (a source-stage-skipped DL match cannot disprove it; new red-when-reverted die-branch test).

### Changed
- **#155 leg 2 (card#4940)** — **`kbcard` decline stamp-clear**: both decline call-sites (`move --column wont_do` and `patch --column wont_do`) now clear `payload.dl_number`/`pr_number`/`pr_url` via explicit per-key JSON nulls in the same PATCH, through one shared owner (`_kbc_decline_nulls`) — closing the other half of the stale-stamp promote class at the write-site. `--keep-refs` opts out; an explicit ref flag passed in the declining call wins over the null; non-decline moves are byte-unchanged. (Also fixed en route: a `${1:-{}}` bash brace-matching bug in the shared helper.)

### Docs
- **solo orientation synced to coord v0.20.0** (#156; subagent dispatch REQUIRED + STOP-and-retry on API/limit termination; context-backstop CLEAR auto-fire nudge; Compliance section compressed to owner-pointer) and **v0.17.0** (#154; human-prompt bookends; repo-agnostic subagent dispatch).

## [0.20.1] - 2026-07-21

**Patch — a CI/test-only static regression guard against re-introducing the DL-219 flat-body wrapper, plus a docs sync.** 2 PRs since v0.20.0 (#148, #151). The substantive change is a network-free static test that fails if any `bin/` script re-introduces a `{task: {…}}` v3 write-body wrapper — covering even the standalone-vendored `promote-released-cards`, which can't source the shared lib — so the flat-body cutover can't silently regress. Plus a docs-only solo-orientation stamp sync (coord v0.15.1). No runtime CLI behavior change; `bin/` and `promote/action.yml` vendor surfaces unchanged.

### Added
- **#148** — **`tests/no-task-wrapper-selftest.sh` — a static regression guard against re-introducing the DL-219 `{task: {…}}` write-body wrapper** (card#4811). Follow-on hardening to card#4772: that cutover removed 8 wrapped sends, but two hid for two review rounds because they were hand-built escaped-quote string literals (`"{\"task\":{…}}"`) a plain `"task":` grep never saw. The new network-free selftest statically scans every regular file under `bin/` and fails if any **non-comment** line carries a `task`-keyed body wrapper in any of the three re-introduction shapes — unescaped JSON (`{"task":`), escaped literal (`{\"task\":`), or jq construction (`{task: {…}}` / `{task: $var}`) — via the matcher `("task"\s*:)|(\"task\"\s*:)|((^|[ ({,])task\s*:)`. The jq arm matches the wrapper key alone (not requiring a following `{`), so the brace-less argjson form is caught too; lines whose first non-whitespace char is `#` are excluded (the tree's one legitimate mention is the `# … {task: …}` doc note in `bin/kbcard`). A static test rather than a code helper because `bin/promote-released-cards` is standalone-vendored and cannot source the shared lib — and post-DL-219 there is no shared wrapper primitive left to centralize on — so only a source scan covers every bin uniformly. The test carries its own positive control (three temp fixtures, one per shape, asserted flagged — proving the matcher can fail) and a benign fixture (`task_id`/`to_task_id`/`from_task_id`/`task_links`/`.task-list`/`_action` + a `#`-comment mention, asserted **not** flagged). Added to the CI selftest matrix. No `bin/` or vendor-surface change.

### Docs
- **#151** — **solo-orientation stamp synced to coord v0.15.1** (`CLAUDE_AGENTBOARD.md`, v0.10.0 → v0.15.1). Stamp-only; the solo-orientation text itself is unchanged. No `bin/`/CI/vendor-surface change.

## [0.20.0] - 2026-07-21

**Minor — the v3 task-write API cutover drops the `{task: {…}}` body wrapper (in lockstep with the kanban DL-219 prod deploy), plus a live-source archive gate and two `release-pr-body` syncs.** 5 PRs since v0.19.0 (#143–#147), cards #4772/#4747/#4751/#4761/#4762 (roundtable PupFuzz/agent-roundtable#39/#109/#111). The headline is the unconditional flat-body cutover (#147) — no shim, no version probe, so the tools move in lockstep with the kanban prod deploy carrying DL-219 — riding with `kbcard archive`'s new `may_archive` live-and-untwinned-source gate and the owner-side `release-pr-body` `tag_format` sync. Plus a docs-only solo-orientation sync (coord v0.10.0). `promote/action.yml` vendor surface unchanged.

### Added
- **#146** — **`kbcard archive` gates on the shipped `may_archive` invariant, with an audited `--force` override** (card#4747, roundtable PupFuzz/agent-roundtable#39). Before the archive `PATCH`, `kbcard archive` now consults the framework's shipped `kanban_common.may_archive` primitive (path-loaded version-agnostically, the same ladder `board-session-close` uses for the reconcile hook) via a thin new `bin/_kbc-may-archive.py` shim: it builds a tri-state per-source resolver over the card's backing coordination sources (`pr_number`/`issue_number` by-ref → `gh pr|issue view --json state`, mapping `OPEN`→live, `CLOSED`/`MERGED`→terminal, a read that 404s/errors→unresolvable; an `id:<sid>` provenance tag → unresolvable, since kbcard has no GitHub handle for a coordination stable-id) and passes the board's live cards as `surviving_cards` for twin-awareness. If a backing source is still **live** (or unresolvable — fail-closed) and no surviving twin card keeps it discoverable, the archive is **refused** (rc 1, no `PATCH`) with a drift-vocabulary line (`⚠ archive withheld: card #<id> — <reason>`) — archiving the only card of a live source would hide it from the reconcile/detect backstop. `--force` performs the archive anyway and records an **audited** override (`⚠ archive FORCED past live-source gate: …` to stderr + the kbcard failure log), never a silent bypass. If the `may_archive` primitive cannot be located the gate **fails loud** (refuses, names `--force`) rather than archiving unchecked. A card fetch that returns HTTP 2xx with `.data` **null/absent** (a trashed / permission-limited / edge card) is treated as fail-closed at **both** choke points — the bash `_kbc_archive_decision` short-circuits to `noprimitive` before the board fetch, and the shim rejects an explicit-null `card` — so a null card can never collapse to a source-less `{}` that would false-`ok` and archive a live-and-untwinned card (the card-side sibling of the board input's safe `[]` default). And a **gate-infra failure never blocks `--force`**: if the decision can't be computed (e.g. `python3` absent), `--force` still archives AND still writes the audited override line (`… infra could not run — forced`), so the escape hatch isn't held hostage by the gate it exists to bypass — while the non-force path stays fail-closed (a gate-infra failure refuses). The by-ref GitHub-state read reuses the framework's own `gh_json`/`gh` credential path; the board→repo fallback reads `kanban.boards[]` from the coord config, and a card's own `pr_url`/`issue_url` source is derived via the framework normalizer when a descriptor carries no repo. `--dry-run` is unchanged (offline, no gate evaluation, no mutation). New `tests/kbc-may-archive-selftest.sh` exercises the shim's resolver tri-state over the **real** primitive with a fake `gh` (skipping the primitive-backed cases only where the plugin's `kanban_common` is absent), and `tests/kbcard-selftest.sh` gains the wiring cases (refuse / forced-audit / ok / fail-loud), red-when-reverted (removing the gate lets a blocked card archive). `promote/action.yml` vendor surface unchanged.

### Changed
- **#147** — **Task write bodies are sent FLAT (top-level), dropping the `{task: {…}}` wrapper** (card#4772). The kanban v3 write API unified to take task fields at the top level of the request body and no longer accepts the `{task: {…}}` envelope (kanban DL-219); every task `POST`/`PATCH` these tools issue now sends the task object flat. This is an **unconditional** cutover — no version probe, no wrapped fallback — so the tools must run against a kanban deploy that carries DL-219 (there is no shim; they move in lockstep with the kanban prod deploy). Migrated (8 task-write sites across 5 files, found by enumerating every `/tasks` write rather than grepping for `task:` — two were hand-built escaped-quote literals a name grep slid past): `kbcard` `create-card`/`move`/`patch`; the DL-stamp write **and** the move-to-In-Progress write in `board-card-start`; the tag backfill in `dl-a0-backfill-triaged`; the throwaway-card create + dl-clear in `dl-a1-register-field`; and the stage-move in `promote-released-cards`. Unaffected: `task_links` bodies (already flat, different endpoint) and `_action` bodies (never wrapped). `tests/kbcard-selftest.sh` now asserts the flat body shape (create-card tags/type, the shared create/patch payload, the swimlane setter). `promote/action.yml` vendor surface unchanged.
- **#145** — **`board-session-close` delegates its inverse-drift leg to the shipped `kanban-reconcile.py --detect`** (card#4751, roundtable PupFuzz/agent-roundtable#109 + #111). The bespoke bash inverse-drift check (In-Review cards whose recorded `pr_number` matches no open PR) is retired in favor of the framework-maintained detect hook — the sanctioned conforming implementation of the inverse-drift CONTRACT (the framework's `BOARD-SYNC.md` § "The detect backstop contract"), which previously named this bash as an accepted-divergence conforming implementation "until the solo hook's adoption completes." Both implementations were proven equivalent before the swap: on a synthetic positive control they flag the same two drift sub-cases (stale ref + missing correlation) and suppress the same clean card; on the live boards both report no inverse drift. The delegated hook is a net gain — it adds a **forward** leg (an open PR of a board's repo with no live card carrying its ref) the bash never had, and its inverse leg is repo-qualified `(source, ref)` tuples rather than a flat ref-number set. The hook path is resolved **version-agnostically** (explicit `$KANBAN_RECONCILE_HOOK` override → the coord plugin dir on `$PATH` → the marketplace clone → the newest cached version by `sort -V`) so a plugin update never breaks it, and an unresolvable hook **fails loud** (rc 1, "DID NOT RUN") rather than silently skipping the drift check; a non-zero hook exit propagates so a config/API failure isn't read as a reconciled board. New `tests/board-session-close-selftest.sh` (network-free) covers the resolver ladder and the surfacing / fail-loud / rc-propagation paths (verified red-when-reverted: 3 mutations each red their guarding check). Because a board without `inverse_check_columns` runs the forward leg only and reports `inverse check OFF` at rc 0 — a silent reduction of coverage where `board-session-close` previously keyed success purely on the hook's exit code — the ritual now scans the hook output and **re-emits the OFF state as a loud `⚠` warning on stderr**, naming the affected board key, outside the per-board `(no drift detected)` framing, so the degraded (inverse-off) coverage is never read as a clean pass. A board wholly `skipped` by the hook (absent from `kanban.boards[]`, or lacking a single-repo `repo`) is surfaced the same way. OFF is **not** a hard error (a board may intentionally not arm inverse) — the exit code and forward-leg output are unchanged; only the warning is added. **Adoption note:** the inverse leg is armed by an `inverse_check_columns` entry per board in `coordination.config.json` (`kanban.boards[]`); without it the hook runs the forward leg only and `board-session-close` warns that the inverse-drift check is off for that board.
- **#143** — **`release-pr-body` `tag_format` config knob + 2–4-segment version extraction** (card#4761, roundtable PupFuzz/agent-roundtable#39). Adopted from the agent-board-framework's `templates/release/release-pr-body`, which had evolved ahead of the toolkit copy: a new optional `.tag_format` key in `.release-pr.json` (default `v{{version}}`, preserving the historical behavior byte-for-byte) maps a version to its git tag name for non-`v`-prefixed schemes (`{{version}}`, `release-{{version}}`, …) — and the own-tag exclude in the baseline resolution now derives from it instead of a hardcoded `v` prefix, so a re-run after tagging under a non-`v` scheme no longer reports the current release as its own previous tag. Version-file extraction accepts 2–4 numeric segments (`[0-9]+(\.[0-9]+){1,3}`), so a .NET `Major.Minor.Build.Revision` version (`1.22.1.0`) is no longer silently truncated to `1.22.1`. Both behaviors gain red-when-reverted selftest coverage (verified: 5 checks fail on the pre-sync bin). **Ownership declared** (README): the toolkit owns both release bins (`release-pr-body` + `promote-released-cards`) — tests and release discipline live here; the framework's `templates/release/` copies are mirrors synced at toolkit tags, closing the fork this change reconciles.

### Fixed
- **#144** — **`release-pr-body` display header renders the real tag via `tag_format`** (card#4762). Owner-side follow-on to the card#4761 sync: the PR-body header hardcoded `v${VERSION}` independent of `tag_format`, so a non-`v` scheme showed a `v`-prefixed header while the actual tag differed. The header now renders the tag derived from `tag_format`, and the tag computation is hoisted ahead of the baseline block so it is also live on the explicit `--base` path; under the default `v{{version}}` the output is byte-identical. Red-when-reverted selftest coverage (verified: 3 checks fail on the pre-change bin, including the `--base` path).

## [0.19.0] - 2026-07-19

**Minor — the swimlane write-side lands: `kbcard patch --swimlane` plus a defined-swimlane enumeration on every unresolved name.** 1 PR since v0.18.0 (#140, bundling cards #4714 + #4713, roundtable PupFuzz/agent-roundtable#101 + #100), plus a docs-only solo-orientation sync. Both features activate only on a board env that declares `KB_SWIMLANE_<id>=<name>` entries; `promote/action.yml` vendor surface unchanged.

### Added
- **#140** — **`kbcard patch --swimlane <name|id>` — write-side swimlane setter** (card#4714, roundtable PupFuzz/agent-roundtable#101). Closes the read/write asymmetry left by `list --swimlane` (the read filter): a card could be listed by swimlane but only assigned to one via a raw `swimlane_id` card `PATCH`. The new flag writes the card's top-level `swimlane_id` column (rides the inner task object like `--column`'s `workflow_stage_id`, not the payload merge), resolving a name through the **same** `swimlane_id()` helper `list` already uses — no second resolver. `--swimlane none` (or `--swimlane 0`) is the explicit un-assign form, writing `swimlane_id` null; every other ref resolves name→id (numeric ids pass through) and fails loud (rc 2) on a typo. The write echo surfaces the resulting `swimlane_id` only when `--swimlane` was set (parity with it always showing `workflow_stage_id`), so other patches keep their echo shape. Additive and `patch`-only: `move` stays column-only.

### Fixed
- **#140** — **An unresolved `--swimlane <name>` now enumerates the board's defined swimlanes** (card#4713, roundtable PupFuzz/agent-roundtable#100). `swimlane_id()` printed `swimlane 'X' is not defined on this board` and stopped — with nothing listed, a reader reasonably infers the board has *no* such swimlane (e.g. missing a required lane prefix), and could report that false board fact. The error now appends the id→name map `_kbc_swimlane_map()` already builds (`defined swimlanes: 1=…, 2=…`), mirroring the board-not-found error's "boards found on this box: …" style; a board with no swimlanes declared gets a distinct honest line rather than an empty list. Error text only — the rc-2 loud-fail contract is unchanged, so `list --swimlane` still exits 2 on a typo.

## [0.18.0] - 2026-07-19

**Minor — one new `kbcard` correlation surface plus a fail-loud config error, riding on three same-behavior consolidations.** 5 PRs since v0.17.0 (#133–#137). The feature is issue↔card correlation stamping (`--issue`/`--issue-url`); the fix makes an unconfigured-board error name its own remedy; the remaining three are duplication-removal refactors with byte-identical runtime behavior. No config migration; `promote/action.yml` vendor surface unchanged.

### Added
- **#136** — **`kbcard --issue`/`--issue-url` for issue↔card correlation.** New optional flags on `create-card` and `patch` stamp `payload.issue_number` (a JSON number, matching `pr_number`'s numeric canonicalization) and `payload.issue_url` (which drives the by-ref `source` owner/repo for multi-repo boards, exactly as `pr_url` does), so a card can be correlated to a tracked issue without dropping to a raw per-key payload `PATCH`. The two keys are independent and co-stampable — a card can carry both `issue_*` and `pr_*` with no exclusivity check, the way `dl`/`pr` already coexist. `adopt-to-dl` gains a matching optional `--issue <N>` co-adoption path that co-stamps `issue_number` + a source-qualified `issue_url` in the same atomic write and fail-loud-verifies the `github_issue` by-ref parallel to the DL one. Additive only: omitted flags leave the keys absent (not null), nothing previously accepted is now rejected, and adoption without `--issue` is byte-identical to before. The shared `_kbc_build_payload` helper is extended to carry the new keys, so `issue_number` inherits the same numeric coercion `pr_number` already had rather than a duplicated merge.

### Fixed
- **#133** — **the unconfigured-board error now names its fix, and default-board resolution is documented.** On a box whose primary board env file isn't named `dev`, a bare invocation failed with `board env file not readable: …` and no path forward, reading as broken when it was merely unconfigured for a default. The `rc=2` board-env arm — the one sibling in `kb_load_config` that printed no remedy — now names `--board <name>` / `KBCARD_BOARD_ENV` (context-aware for the bare-default case vs a named board whose file is absent) and lists the `~/.kanban-*-board.env` files that do exist on the box. `INSTALL.md` documents `KBCARD_BOARD_ENV` as the flag-free option and adds the error to troubleshooting. Docs + error text only — the rc, the trigger, and default-resolution behavior are unchanged. Covers every `kb_load_config` consumer (`kbcard`, `adopt-to-dl`, `dl-a0-backfill-triaged`, `dl-a1-register-field`).

### Changed
- **#134** — **`_kbc_build_payload` shared by `create-card` + `patch`.** Four verbatim-duplicated payload-assembly blocks in `bin/kbcard` (the args→JSON merge `jq`, the `version_target` board-capability guard, the DL-canon append, and the `pr_number`/`pr_url` appends) are hoisted into one helper both commands call. Pure same-behavior refactor: the assembled `task.payload` is byte-identical to what each command produced before — numeric-string→JSON-number coercion, the version guard and its warning, and DL canonicalization all preserved; no command's accepted/rejected inputs or emitted payload change.
- **#135** — **selftest harnesses consolidated onto a shared `tests/_selftest-prelude.sh`.** The harness every `tests/*-selftest.sh` duplicated verbatim (`ok`/`bad`/`fails`, `eq`, `expect_rc`/`expect_out`, the required-bin guard, the scratch-dir setup, and the trailing PASS/FAIL summary) moves to one sourced prelude, removing nine copies and normalizing summary wording that had drifted across files. Harness-only: no test case or assertion changed, and all selftests pass identically before and after.
- **#137** — **assorted intra-file + CI deduplication (dedup-audit finding D6), same-behavior throughout.** Single-sources several rules that were each written twice, executable behavior byte-for-byte preserved: a `max_int` "highest int in a stream" helper shared by `next-dl`'s two scans (with new `tests/next-dl-selftest.sh` coverage it previously lacked); the throwaway-teardown request bodies in `dl-a1-register-field`; a shared jq leading-zero normalization across `promote-released-cards`' `dl_number`/`pr_number` fields; and a doubled token-file ladder in `board-snapshot`. On the CI side, `auto-tag-version.yml` now reads the version shape from `.release-pr.json`'s `version_regex` instead of a divergently-anchored hardcoded copy, and the byte-identical per-selftest jobs collapse to a `strategy.matrix` (no required check renamed; `shellcheck`, `drift-check-selftest`, and the composite-action smoke test stay separate). `promote/action.yml` / `_kb-board-lib.sh` unchanged — vendored consumers see only an internal refactor.

## [0.17.0] - 2026-07-18

**Minor — an 8-PR quality release: two roundtable-driven `kbcard`/hook features, a pagination-completeness fix ported into both board movers, a DL-primitive consolidation, and an error-body-preserving `board-card-start` refactor.** 8 PRs since v0.16.0 (#123–#130). Cards #4510/#4513/#4514/#4413/#4617/#4621/#4623/#4637. Two new feature surfaces (`create-card --triaged`, `kbcard list --swimlane`); no config migration; `promote/action.yml` vendor surface unchanged.

### Added
- **#124** — **`kbcard create-card --triaged` flag** (card #4617, roundtable FR PupFuzz/agent-roundtable#86). Mirrors the existing `patch --triaged` onto `create-card` so a card can be born triaged — a plain PM-minted product card that forgot `--tags triaged` otherwise re-surfaced as a false-positive untriaged card every session (the untriaged check keys on a missing `triaged` tag). On a fresh card the tags CSV is built from scratch, so a plain append suffices (no read-merge-write like the `patch` path). Scope is the generic `--triaged` verb only; any PM policy tag stays caller-passed. Works on native-type, tag-typing, and mixed boards.
- **#125** — **`board-card-start` accepts the glued `card<N>` branch spelling + a fail-soft pre-push branch-name lint** (card #4621). The post-checkout auto-move required a separator after `card`, so the natural glued spelling `fix/card4524-slug` matched no grammar — the hook fired, found no token, and the card silently never moved to In Progress. The separator is now optional (`card[-/#]?`), so `card4524`/`card-4524`/`card/4524`/`card#4524`/`#4524` all correlate (still requires the literal `card`/`#` at a token boundary + ≥2 digits, so DL tokens and version numbers stay safe). A new fail-soft `pre-push` lint warns (never blocks; always exit 0) only on a card-ish token the grammar still won't accept (e.g. `card_4524`), reusing the exact matcher the mover uses so the two can't disagree. The widened grammar affects the local hook only; the bridge writeback still requires `card-<id>`/`card#<id>`, which stays the canonical full-lifecycle convention.
- **#130** — **`kbcard list` surfaces `swimlane_id` + a resolved name, and a `--swimlane <name|id>` filter** (card #4637, roundtable FR PupFuzz/agent-roundtable#88). Per-swimlane triage previously dropped to raw-API glue because `list` omitted swimlane. Names resolve from the board env, id-keyed `KB_SWIMLANE_<id>=<name>` (the inverse of `KB_STAGE_<name>=<id>`, since swimlane names are freeform and can't be env suffixes) — zero extra API call. `--swimlane` composes with `--column`/`--type`; a typo'd name errors loud (rc 2), a numeric id passes through. The filter keys on the stringified id because `swimlane_id`'s API type is unverifiable on any reachable board (no board has swimlanes), where a numeric `==` would silently drop a string-typed id.

### Fixed
- **#127** — **`promote-released-cards` now censuses the whole-board read against `meta.total` so a short read can't silently promote a partial board** (card #4513). The mover's inline pagination had drifted from the lib's `fetch_board_cards` — it broke solely on a <200-row page and never consulted `meta.total`, so a server short read terminated the scan early and promoted from an INCOMPLETE board (a card its scan misses is a card it leaves un-promoted). Extracted into a self-contained `fetch_whole_board()` (the standalone must not source the lib) that captures `meta.total`, tracks a pre-dedup running sum, and after dedup distinguishes a genuine undercount (sum < total → DIE, matching the tool's page-cap refuse policy) from a page-boundary dedup artifact (sum ≥ total → warn only).
- **#129** — **`fetch_board_cards` defaults `meta.last_page` to UNKNOWN, not `1`** (card #4623). The lib defaulted `last_page` to `1` and broke unconditionally on `page >= last_page`; a server returning a full 200-row page 1 with neither `meta.last_page` nor `meta.total` present would skip the census and break at page 1 — a silently truncated read. Unreachable against the real Laravel `LengthAwarePaginator` (always emits both), hence latent, but it is the exact miss #4513 already guarded in the co-vendored standalone. Ported for parity: `last_page` is usable only as a positive integer (absent/non-numeric/<1 → unknown), and the secondary break fires only when the server positively declares it — byte-identical behavior on the real API path; a meta-absent read only becomes MORE complete.

### Changed
- **#123** — **`board-card-start` routes its card read + two writes through `kb_api`/`kb_api_status`** (card #4510). The read used a local `get()` and the dl-stamp + move-to-In-Progress writes used `curl -fsS`, which discards the HTTP status and the 4xx/5xx body — exactly the 403-scope-vs-422 indistinguishability the shared lib was fixed to remove (#4337), a fix that never reached these local calls. The "kept local so it sends THIS board's token" rationale was void (`kb_api` reads `KB_TOKEN`, which this hook already set). The two writes now go through a shared `_bcs_patch` helper that captures `KB_HTTP` and durably records a failure via `kb_bcs_log` — the previously-silent failed stamp/move now leaves a trace. Success-path behavior unchanged.
- **#126** — **DL int/format/by-ref-hit consolidated into shared `_kb-board-lib.sh` primitives** (card #4514, dedup-audit finding D5). Three DL-family duplications hoisted: `kb_dl_int_lenient` (the lenient client mirror of the server's `canonicalize('dl')` key, migrated from `adopt-to-dl`, kept a deliberate sibling of the strict `kb_dl_num`), `kb_by_ref_hit` (the "contains card N" predicate centralized from two forks; the lib keeps the object-or-array-tolerant form, so `adopt-to-dl`'s verify safely widens), and one canonical `DL-NNNN` formatter (`kb_dl_canon`) replacing six inline `DL-%04d` literals — `next-dl` gains a fail-loud guard on a computed DL outside the canonical range. `promote/action.yml` consumers unaffected.

### Docs
- **#128** — **`ADOPTION.md`'s "Is this for you?" gate keys on board API access, not instance ownership** (card #4413). The gate read adoption onto instance ownership ("your own kanban board (your own instance)") when the toolkit's real precondition is board API access (an api base + token + board id; it never reads who operates the host) — the parenthetical read successful tenant adopters out of the audience in the doc's first decision paragraph. Reworded to board access, naming the tenant case explicitly; consumer-agnostic.

## [0.16.0] - 2026-07-17

**Minor — three roundtable-driven board-tooling deliverables: `kbcard` card-removal verbs + two `board-snapshot` fixes.** 3 PRs since v0.15.1 (#118–#120). Cards #4479/#4448/#4447. One new command surface (`kbcard archive`/`delete`); the two `board-snapshot` changes are a correctness fix + a perf fix with no config migration.

### Added
- **#118** — **`kbcard archive` + `kbcard delete [--hard]` card-removal verbs** (card #4479, roundtable FR #74 sola-pm). The board API has no HTTP `DELETE` — removal is a non-discoverable top-level PATCH `_action` plus a two-step force-delete — so agents were hand-rolling `curl` to remove a test card. `archive` (`_action:archive`) takes a card off-board as a recoverable live row (keeps its id + `dl_number`); `delete` (`_action:delete`) trashes it recoverably within the retention window (**retains** `dl_number`); `delete --hard` soft-deletes then `POST force-delete.json` for a permanent cascade (**releases** `dl_number`). Thin orchestration over the existing `resolve_task` + `kb_api` primitives (mirrors `cmd_move`/`cmd_patch` — no write-path reimplementation); `--dry-run` + a `--yes` confirmation gate on the destructive paths.

### Fixed
- **#119** — **`board-snapshot`'s `untriaged()` inherited a sibling board's stage ids** (card #4448). `board-snapshot` reads each board's config by **sourcing** its env file (which `export`s `KB_BOARD_ID` + the `KB_STAGE_*` ids); `snap()` scrubbed a hardcoded subset before the next board but `untriaged()` unset only `KBCARD_TOKEN_FILE`, so in an operator shell that had sourced board A, a board B env **omitting** any terminal-stage id (`KB_STAGE_SHIPPED_TO_DEV`/`RELEASED_TO_MAIN`/`WONT_DO`) or `KB_BOARD_ID` silently inherited A's value — B's terminal set went wrong and cards were mis-suppressed or wrongly flagged UNTRIAGED, a **quiet** failure of the "triage is never silently missed" contract. `untriaged()` now scrubs the same full set `snap()` does and gains the `KB_BOARD_ID` guard.

### Changed
- **#120** — **`board-snapshot` fetches each board once, not twice** (card #4447). `snap()` and `untriaged()` were independent subshells that each sourced the env and called `fetch_board_cards` for the **same** board, so every SessionStart pulled each board's ~215KB payload twice (~half the API time; each `curl` is a fresh TLS handshake, so the cost is per-request). At the 10s SessionStart hook timeout a kill leaves the agent with **no** snapshot, and the waste scales with board count. Root cause was the two functions being copies of one setup+fetch (canon #5) — merged into one `board_report()` that fetches once and renders the in-flight snapshot to stdout + the untriaged list to fd 3 (7.24s → 4.85s on the reference multi-board host).

## [0.15.1] - 2026-07-16

**Patch — the v0.15.0 staleness guard now reaches the reader, plus a board-snapshot stage-name fix.** 2 PRs since v0.15.0 (#114, #115). Cards #4393/#4444. Both are fixes; no new surface, no config migration.

### Fixed
- **#114** — **the runtime staleness guard warned into the void** (card #4393). `board-snapshot` ran `agent-board-toolkit-runtime-check --quiet` but its warnings go to **stderr**, while `board-snapshot`'s consumer is a SessionStart hook that surfaces only **stdout** — so a correct, installed guard on a stale pin would have warned where nothing reads (the exact channel trap `board-card-start` already ate). The warning is now folded to **stdout**, where the snapshot itself lands, so the `v0.15.0`-shipped guard actually surfaces a stale pin at SessionStart. Pinned by a test that drives a real stale pin and asserts the warning survives a stderr-dropping consumer, with a positive control proving the same warning vanishes without the fold (dropping the `2>&1` reds 2 checks).
- **#115** — **`board-snapshot` printed a stale hardcoded stage name** (card #4444). The label map hardcoded `In Progress`/`In Review`/`Held` — a private second copy of the board's column names — so a board-side column rename left the snapshot asserting a name the board no longer used (it printed `[Held]` for a column renamed `Blocked`/`Gated`). Names are now sourced from the board via `preload.json` (~2.5KB — `boards/{id}.json` inlines every task at ~220KB, and there is no GET index for `workflow_stages`); selection still keys on the env stage ids (unchanged contract). An unreachable preload or an env id the board does not define degrades to `stage <id>` (the shape the untriaged block already prints) rather than a stale literal — an unnamed stage is honest, a guessed one is how the bug read as working. `kb_api` now honors `KB_CURL_MAX_TIME` (the cap `fetch_board_cards` already honored), so the new preload call cannot reintroduce the SessionStart-stall the cap prevents; unset = no cap, every existing caller unaffected.

### Docs
- **#114** — **`VERSIONING.md` gains the missing DEPLOY step** (release flow step 11). The flow previously ended at the tag — a tag is not a deploy, so `v0.15.0` shipped while the on-PATH runtime silently stayed at `v0.14.0`. Step 11 now advances the pinned runtime, **re-runs the symlink loop** (a symlink tracks its target's content, not the *set* of tools — a release that ADDS a bin never lands on PATH otherwise), runs the guard, and exercises one real command; it also documents the bootstrap rule (a guard shipped *in* the artifact it guards cannot force its own first run).


## [0.15.0] - 2026-07-15

**Minor — a quiet-wrong sweep: three read/diagnostic surfaces that answered plausibly-wrong instead of failing loud, one new guard that judges what EXECUTES, and the UPGRADE walk restored.** 10 PRs since v0.14.0 (#101–#110). **No breaking change; one new tool.** Consumers who vendor: re-vendor `promote-released-cards` (#110, diagnostic-only) and `_kb-board-lib.sh` (#103/#106).

### Added
- **#108** — **`bin/agent-board-toolkit-runtime-check`: verification that judges what EXECUTES, not the checkout** (cards #4361 + #4351). "Patch the checkout, then grep the checkout" certifies the wrong artifact on two live-observed topologies: real-file **copies** (MSYS/Windows `ln`, manual `cp`) and a **pinned worktree** (PATH detached at a tag that never advances — a maintainer box false-greened under the toolkit's own advisory). The check resolves each on-PATH tool through its symlink chain and **fails loud** on: a pinned runtime behind the newest tag (fetching tags first — a never-fetching pin is stale-blind), stale copies vs a reference checkout (`cmp`), and **mixed runtimes** (a partial upgrade where tools disagree about the lib they source). Honest UNKNOWN ≠ ok: an unverifiable copies topology **warns**, never passes silently. A tool the runtime ships but PATH lacks warns (found a real gap on the first run). `board-snapshot` runs it `--quiet` at SessionStart, closing #4351's silent-pin exposure (the tools keep working, just older — no symptom). 7-topology decision-matrix selftest + CI job.

### Fixed
- **#107** — **`kbcard show` emitted `stage: null` for a card that IS in a stage** while `list` mapped the real id (reported by an integrator; reproduced same-second). `null` is an assertion, not an absence — it read as "the card never moved" and caused a false "automation is broken" escalation downstream. `show` now maps `stage` from `workflow_stage_id` and resolves `column` by reverse `KB_STAGE_*` lookup, **omitting** any key it cannot populate. Shape-independent (correct whether the serializer omits the keys or emits nulls). New `kbcard-selftest` + CI job.
- **#109** — the same shape on the **write** echoes (card #4390): `create-card`/`patch` fabricated `payload: null` on boards whose serializer omits the key. One `_kbc_write_echo` primitive replaces two divergent inline filters; a server-**sent** null still passes through.
- **#103** — **`fetch_board_cards` lost the API error body** in the v0.8.2 lib extraction (`curl -fsS`): a 403 token-scope failure and a 422 validation failure were indistinguishable in the failure log (`curl-rc=22`). Restored `HTTP-<status> <body>` logging + loud-mode stderr; the selftest stub emulates `-f` so a regression reds.
- **#106** — **the short-read backstop no caller could reach** (card #4338): `fetch_board_cards` detected `total > read_n`, warned INCOMPLETE, then returned **0** — so `next-dl` minted a DL from a read the lib had just declared incomplete (its whole exit-code contract exists to prevent DL re-mints, DL-154). Now a distinct **rc 4** (partial data still emitted), with the pre-dedup page sum distinguishing a real undercount from a **page-boundary dedup artifact**. `next-dl`/`dl-a0` refuse it via their existing branches; `kbcard list`'s documented never-print-truncated contract now actually holds; `board-snapshot` still displays partials.
- **#105** — **`release-pr-body` computed the release baseline from the LOCAL `main` ref** (card #4348), which the documented release flow never updates — so it drifted a full release per cycle and the "generated — do not hand-edit" body authoritatively listed already-shipped PRs as new. Now fetches `origin/<main>` (`--tags` is load-bearing: release tags point at main-only merge commits a dev-side fetch never auto-follows); an unfetchable origin **fails loud** (exit 2) naming `--base <tag>` as the offline override — never a silent stale fallback. New fixture-repo selftest + CI job.
- **#110** — `promote-released-cards`' local dry-run derives its baseline from local tags and can over-report the shipped range; a stderr NOTE names it (suppressed under `GITHUB_ACTIONS`, where the CI invocation is remote-truth by construction). Diagnostic-only (card #4389).

### Docs
- **#104** — **`docs/UPGRADE.md` §6 walk-forward contract restored** (card #4345): the required `KANBAN_EXPECTED_HOST` action sat under a stale "Unreleased (after v0.8.2)" heading (it shipped in **v0.9.0** — pinned by tag inspection, not inferred) and **v0.9.0–v0.13.0 had no entries at all**, so an upgrader on v0.8.2..v0.12.x walked forward and found nothing. Reconstructed per-version with citations; no-action releases get explicit one-liners so a gap is never ambiguous.
- **#102** — Windows/MSYS **copy-not-symlink** note (reported by an integrator's Windows adopter): `ln -s` produces copies on native mingw64, silently changing the upgrade contract; INSTALL.md §2 now states it, plus the pinned-topology sibling and the verify-what-`~/.local/bin`-resolves-to rule.
- **#101** — HOOKS.md's bridge correlation-grammar row tracks the agent-webhook-bridge **v0.57.0** correlation grammar — the `card-<id>` dash alias + DL-shaped boundary (`\bcard[-#](\d+)`) — version-scoped, with the `<type>/card-<id>-<slug>` branch convention it enables.


_(empty after each tagged release; accumulates as feature PRs land on dev)_

## [0.14.0] - 2026-07-15

**Minor — a security fix in the `api_base` host guard, plus the restoration of the board-env token precedence that v0.8.2 silently broke.** 2 PRs since v0.13.0 (#98, #97). Cards #4346/#4325.

**Read before upgrading — two consumer-visible changes.** (1) **[vendor]** a repo that vendored `promote-released-cards` **must re-vendor** to get the security fix; the vendored copy carries its own guard and does not track the toolkit. **[release-CI]** SHA-pinned action consumers get it with the pin bump (`promote/action.yml` is unchanged). (2) **[host]** if you set `KBCARD_TOKEN_FILE` in **both** a board env and `~/.kanban-host.env`, the **board's** token now wins where the host's silently did since v0.8.2 — a live change in *which credential is sent*. Full actions: `docs/UPGRADE.md` § v0.14.0.

### Security
- **SECURITY — the `api_base` host guard accepted a URL that sends the bearer token to an attacker-controlled host.** `kb_require_https_host` (`_kb-board-lib.sh`) and its standalone mirror `host_ok` (`promote-released-cards`) terminated the URL authority at **`/` alone**. RFC 3986 ends it at the first of **`/`, `?`, or `#`** — so with a delimiter placed before an `@`, the userinfo strip (`${h##*@}`) reached past it and lifted a fake host out of the query/fragment:

  ```
  api_base: https://evil.example#@kanban.your-host.tld
  guard parses host -> kanban.your-host.tld  -> ACCEPTED
  curl connects to  -> evil.example          -> bearer token sent to the attacker
  ```

  Because `api_base` is read from the **committed, PR-editable** `.release-pr.json`, a pull request alone was enough: for `board-card-start` a maintainer merely checking out the branch leaked their kanban token, and for `promote-released-cards` a repo running promote on untrusted config would leak the `KANBAN_WRITEBACK_TOKEN` CI secret. Neither required any access to `$HOME` or `git config`. Both copies now terminate the authority at `[/?#]`. Verified end-to-end against a listener standing in for the attacker: the pre-fix hook delivered the bearer token, the fixed hook sends nothing and names the real host in its refusal. Legitimate values — subdomains, `:port`, real `user:pw@` userinfo, queries — are unaffected.

  It survived because the guard had **no decision coverage**: it was wired and ran on every checkout, but nothing had ever asked it to judge a hostile URL. New `tests/kb-host-guard-selftest.sh` (+ CI job) pins the matrix and additionally asserts the two copies agree — they are sync-paired by comment only, and both carried the identical defect.

  **[vendor] Action required:** `promote-released-cards` changed, so a repo that **vendored** it must re-vendor (UPGRADE §3) to get the fix. **[release-CI]** consumers of the SHA-pinned composite action get it with the pin bump — `promote/action.yml` itself is unchanged.
### Fixed
- **A board env's `KBCARD_TOKEN_FILE` is honored again — the documented precedence is restored.** `kbcard` honored it from v0.4.2 through v0.8.1 (`v0.8.1:bin/kbcard:440`, *"after host + board env are sourced, so a config-file `KBCARD_TOKEN_FILE` is honored"*). The **v0.8.2 shared-library extraction** consolidated six divergent copies of the config resolution onto the two that had the source order backwards (`next-dl`, `dl-a1-register-field`) and **silently regressed** it: from v0.8.2 through v0.13.0 a board-env `KBCARD_TOKEN_FILE` was ignored and the host/default token was sent to every board. The docs were right; the code drifted. One ladder now serves every tool — **board > host > ambient > `~/.kanban-dev-token`** — as a property of source order (host sourced first, board second) rather than six hand-rolled precedence tests. **Operator-visible where a board env *and* the host env both set `KBCARD_TOKEN_FILE`:** the board's token now wins, changing which credential is sent — see the `docs/UPGRADE.md` v0.14.0 entry.
- **A board env that sets the board-independent `KBCARD_API` is refused, loudly.** Same inverted order, same silence: v0.4.2–v0.8.1 ignored one (as INSTALL.md documented, because `API` was frozen before the board env was sourced), while v0.8.2–v0.13.0 sourced the board env *first* and so silently **honored** it — quietly re-pointing the tool at another host. `kb_resolve_env` now returns rc 4 and names the offending file; `next-dl` no longer swallows that message (it muted stderr, leaving a bare "config incomplete" while losing both the atomic DL claim and the board's `dl_number` seed — the DL-154 re-mint collision class).
- **`board-snapshot` no longer goes dark on a host with no `~/.kanban-dev-token`.** It read one token up-front and `exit 0`'d when that file was missing, so a host using only per-board tokens rendered **nothing**. It now reads each board's token as it renders that board, honoring that board's `KBCARD_TOKEN_FILE`; a board whose token is unreadable reports itself instead of blanking the entire snapshot.
- **`board-card-start` attributes a failed card read to its actual cause.** `get()` used `curl -f`, so an unreachable API, a rejected token (401/403), and a server error were all an empty body — every one reported as **"card … does not exist"**, sending an agent to hunt a missing card instead of a broken token. Each cause is now distinct in the durable diagnostic log. (The status is returned on stdout: `$(get …)` is a subshell, so a global set inside it cannot reach the caller.)

### Changed
- **`board-card-start`: per-board tokens are a host-local opt-in, and `git config kanban.board-id` outranks `.release-pr.json`.** The hook consults a board env's `KBCARD_TOKEN_FILE` **only** when the repo's board id came from the repo-local `git config`; a board id from the committed, PR-editable `.release-pr.json` keeps the host/default token (identical to prior behavior). Reordering the two sources alone would not have closed this — they are documented mutually-exclusive populations, so the inversion only helps the population that never had the exposure; gating the *capability* on the trusted source is the actual fix. The committed value is now digits-validated (`^[0-9]+$`) before it keys a board-env lookup.
- **Reading a board env is now clean-slate** (`kb_board_env_get`): every key is unset before the file is sourced, so a key the file does not set reads as **empty** instead of inheriting whatever is in the environment. Board envs `export` their keys, so in an operator shell that sourced one board's env, that board's values are live for every later tool. Scope, precisely: stage ids are unique to one board, so an inherited id never matches another board's cards — the reachable cases are `board-snapshot` (a board env with no `KB_BOARD_ID` would render a *different* board's cards under this board's label, or abort the subshell under `set -u`) and a board env missing a required stage id (the guard would pass on an inherited value and PATCH a stage that isn't on that board). `board-snapshot` now also says so (`• <label>: (env … sets no KB_BOARD_ID)`) rather than silently rendering nothing.
- **`board-card-start` resolves its board env once**, using it for both the stage ids and the token — the two lookups were separate matchers, free to disagree about which file wins when two board envs claim the same `KB_BOARD_ID`. A duplicate `KB_BOARD_ID` now warns (which file wins is arbitrary either way; being silent about it was the defect), and the stage ids are read newline-delimited so an unset optional `KB_STAGE_HELD` cannot shift another id into its place.
- **`kb_load_host_token`'s `gated`/`unconditional` mode flag is gone**, replaced by `kb_load_host_env`. The two modes differed only because the gate conflated "source the host env" with "let the host's `KBCARD_API` win"; separating those makes one behavior serve both callers. It also fixes the gated bug where a stray ambient `KBCARD_API` skipped the host env entirely, dropping the host's `KBCARD_TOKEN_FILE` — `board-snapshot` was the only `gated` caller, so it was the only tool affected.

### Added
- **`tests/kb-board-lib-selftest.sh` + a CI job** — network-free unit checks for the shared config resolution under a scratch `HOME`: the full token ladder in all four cases, the `KBCARD_API` refusal and its message, the failure return codes, the board-env matcher (no-match, a leaked `KB_BOARD_ID` false-match, duplicates, an unparsable env), and token paths containing spaces. The ladder is a property of source *order*, which is precisely how it regressed unnoticed for five minor versions — nothing exercised it.

## [0.13.0] - 2026-07-14

**Minor — `board-card-start` adoption hardening + Windows/Git-Bash curl portability.** 1 PR since v0.12.2 (#94). No config migration; back-compat (absent-config behavior is identical). Consumer-visible: the token auth mechanism, a new board-id resolution fallback, `core.hooksPath` support, and a durable diagnostic log. Cards #4281/#4289/#4290; correlation roundtable #30/#32/#34.

### Fixed
- **#94** — **Windows/Git-Bash portability: every kanban API call now works on native mingw64 curl.** The bearer token was fed to `curl` via `-H @<(…)` process substitution, which needs a `/dev/fd` named pipe that native mingw64/Git-Bash curl cannot open (rc=26) — so every API call fail-softed. All **8 call-sites** — the shared `_kb-board-lib.sh` (`kb_api`/`kb_api_status`/`fetch_board_cards`), `board-card-start`, `next-dl`, and the standalone `promote-released-cards` — now feed the token via a stdin herestring (`-H @- <<<`), which redirects a regular temp file onto fd 0. This keeps the token out of `argv` (unchanged security property) **and** off persistent disk (bash writes the herestring temp `0600` and unlinks it before exec), and additionally fixes a latent `promote-released-cards --retry` bug (the old process-sub pipe was non-seekable, so a retried request could not re-read the header). Vendored surface: `promote-released-cards` changes an internal auth-feed line, but `promote/action.yml` is unchanged — a SHA-pinned action consumer sees only the internal fix.
- **#94** — **`install-board-hooks` honors `core.hooksPath`.** It hardcoded `<repo>/.git/hooks/post-checkout`; when a repo sets `core.hooksPath` (gitleaks, the pre-commit framework, Husky, many Windows setups) git dispatches hooks only from there, so the install was a silent no-op (symlink created, install reports success, hook never fires). It now installs into `<core.hooksPath>/post-checkout`, and refuses — with operator guidance — a `core.hooksPath` that resolves inside the tracked work tree, where a machine-specific absolute symlink would show as a work-tree change and break on other clones.

### Added
- **#94** — **`board-card-start` resolves its board id without a `.release-pr.json`.** A repo that does not cut releases can bind itself to a board with a repo-local, uncommitted `git config kanban.board-id <id>` — so it adopts the hook without adding a committed, token-adjacent `api_base` surface (the board id is not a secret; `api_base` still resolves from `~/.kanban-host.env`). `.release-pr.json` `promote.board_id` still wins when present (back-compat).
- **#94** — **`board-card-start` durable diagnostic log.** When a branch carries a DL/card token but the move does not happen for an infrastructure reason (no resolvable board id, an unloadable token/host, an untrusted `api_base`, unresolved stage ids, an unreachable board, a named `card#N` that does not exist, or a pinned card), the hook prints the reason and appends it to `~/.cache/agent-board-toolkit/board-card-start.log` (`KB_BCS_LOG` overrides the path) — the installed hook wrapper discards stderr, so the log is the durable record. Still always `exit 0`. Genuine no-ops (no token), already-advanced cards, and the card-id board-scope guard stay silent (no cry-wolf).

### Changed
- **#94** — the host-scrub placeholder detector recognizes the RFC-2606/6761 reserved forms (`.invalid`, `.test`, `.localhost`, the bare `.example` TLD) in addition to `example.{com,net,org}`, each anchored to a host-label boundary so a real host that merely *contains* the substring (e.g. `kanban.latest-corp.com`) is not misread as a placeholder.

## [0.12.2] - 2026-07-14

**Patch — `docs/HOOKS.md` clarifies that un-parking a pinned card is intentionally NOT mirrored by the local hook (docs only; no bin/CI/vendored-surface change).** 1 PR since v0.12.1 (#91).

### Changed
- **#91** — `docs/HOOKS.md`: document that un-parking a pinned card is **owned by the real-time push-path mover, not the local `board-card-start` post-checkout hook**, by design. A push-path mover can override a deliberate pin and promote a pinned card from an opt-in unpark stage set on branch-cut *because it has a durable compensating override-alert surface*; a post-checkout hook's only surface is stderr — effectively silent when an agent drives git, never persisted — so it lacks the durable alert that would make reversing a deliberate pin safe. The local hook therefore keeps refusing pinned cards. Documentation only: `bin/*`, `promote/action.yml`, `examples/*`, and CI are byte-identical to v0.12.1, so a consumer that pins the action or vendors the scripts sees no functional change.

## [0.12.1] - 2026-07-13

**Patch — new agent-facing DL-counter recovery runbook (docs; one comment-only `bin/next-dl` change).** 1 PR since v0.12.0 (#88).

### Added
- **#88** — `docs/DL-COUNTER-RECOVERY.md`: an agent-facing, **API-driven** runbook for recovering a stranded per-board DL counter — the scenario where `next-dl` mints numbers in a far-too-high range because the board's monotonic DL high-water mark was pushed above the real sequence (e.g. by a hard-deleted or trashed high-DL card). Written for an operator/agent who drives a board **over its API token with no server shell**: diagnose (inspect the sequence + list refs), exhaustively sweep every trashed/junk pinner (not just the top one), force-delete each, then reset the counter — mirroring the board's own console recovery path via the board API's DL-sequence inspect/reset endpoints. `README.md` gains an index pointer to the runbook.

### Changed
- **#88** — `bin/next-dl` gains a **comment-only** pointer to the recovery runbook for the "minting far-too-high numbers" symptom. No behavior change — the executable path is byte-identical. `promote/action.yml`, all other `bin/*`, and `examples/*` are byte-identical to v0.12.0, so a consumer that pins the action or vendors the scripts sees no functional change.

## [0.12.0] - 2026-07-12

**Minor — new `bin/adopt-to-dl`: the pull-into-build adoption seam for card-first boards.** 1 PR since v0.11.4 (#85).

### Added
- **#85** — `bin/adopt-to-dl <card-id> --repo <owner/name> --board <name> [--dl N]`: stamps an existing *plain* board card with `payload.dl_number` + a source-qualified placeholder `pr_url` in **one atomic write**, then **fail-loud-verifies** the card resolves via the kanban `by-ref?system=dl&ref=N&source=<repo>` correlation — so a card crosses from manual ownership into the bridge writeback's PR-lifecycle authority with its `source` resolving **at adoption time**, not silently at the next `bridge:check`. Thin orchestration over existing primitives (no write-path reimplementation): `next-dl` mints the DL (atomic server-side claim, echoed to the caller **before** the write so a crash mid-seam is retried with `--dl N`), and `kbcard patch --dl … --pr-url …` performs the single-request `dl_number`+`pr_url` PATCH (kanban per-key payload merge). An **already-adopted guard** refuses to re-mint over a card that already carries a `dl_number` (which would orphan the old DL and strand any branch/PR named for it); `--dl N` re-stamps idempotently for a crash-retry. The by-ref verify **lowercases the source** (the kanban server stores and looks up `source` lowercased), so a mixed-case `--repo` verifies correctly. Sibling bins (`next-dl`, `kbcard`) resolve from the script's own directory, not `$PATH`. Ships with a network-free pure-logic selftest (`tests/adopt-to-dl-selftest.sh`) wired into CI as the `adopt-to-dl-selftest` job; `shellcheck -S error` now also covers `tests/`. `adopt-to-dl` is the write-site adoption seam **for CLI-adopted (card-first) boards** — a board whose `dl_number` is server-derived by a bridge writeback does not need it. It `source`s `bin/_kb-board-lib.sh` (a co-vendored dependency for vendor-by-copy consumers). All other `bin/*`, `promote/action.yml`, and `examples/*` are byte-identical to v0.11.4, so a consumer that pins the action or vendors existing scripts sees only the new bin. Card #4020.

## [0.11.4] - 2026-07-11

**Patch — release-notes reframe to be board/setup-agnostic (docs only; no change to any vendored/shared or CI surface).** 1 PR since v0.11.3 (#82).

### Changed
- **#82** — reframed the v0.11.3 release notes (CHANGELOG entry + recent-releases row) to describe the change and its consumer impact in board/setup-agnostic terms, rather than referencing the maintainer's own board/card specifics. Documentation only — `bin/*`, `promote/action.yml`, `examples/*`, and all CI workflows are byte-identical to v0.11.3, so a consumer that pins the action or vendors the scripts sees nothing new. Card #3895.

## [0.11.3] - 2026-07-11

**Patch — the toolkit dogfoods its own release-promote automation (internal CI only; no change to the vendored/shared surface).** 1 PR since v0.11.2 (#78).

### Added
- **#78** — a `release-promote-cards.yml` workflow in the toolkit's own CI that, on a release main-push, moves the release's shipped tracking cards to the released stage — using the existing (unchanged) `promote-released-cards` script via the local `./promote` composite action, with board/stage/api read from the repo's own `.release-pr.json`. **No change to any vendored/shared file** (`bin/*`, `promote/action.yml`, `examples/*` are byte-identical to v0.11.2), so a consumer that pins the action or vendors the script sees nothing new. Reusable pattern: any consumer can run `promote-released-cards` (or `uses: <owner>/agent-board-toolkit/promote@<sha>`) on their own release, pointed at their own board via their own `.release-pr.json` — the script derives the shipped ref set (DL tokens + PR numbers) from `git log <prev-tag>..HEAD` and moves cards matched by `payload.dl_number`/`pr_number`.

## [0.11.2] - 2026-07-10

**Patch — fail-loud + document `_kb-board-lib.sh` as a required co-vendored dependency of the lib-sourcing bins (card #3894, from a peer integrator).** 1 PR since v0.11.1 (#74).

### Fixed
- **#74** — the 6 bins that `source` the shared `bin/_kb-board-lib.sh` had **no existence guard**, so a vendor-by-copy that omitted the lib died with a raw `source: …/_kb-board-lib.sh: No such file` instead of a message naming the fix. Now: the **5 interactive bins** (`kbcard`, `next-dl`, `board-snapshot`, `dl-a0-backfill-triaged`, `dl-a1-register-field`) fail **loud** (`exit 1` + a self-naming message pointing at INSTALL.md §3 / co-vendoring); **`board-card-start`** (a git post-checkout hook — must never block a checkout) keeps its **soft** `exit 0` but the silent skip now prints a diagnostic to stderr; and **`agent-board-toolkit-drift-check`** grows a **MISSING-LIB probe** (anchored `source "$KB_LIB"` match, so standalone tools — `promote-released-cards`, `release-pr-body`, drift-check itself — are correctly excluded) that fails a lib-sourcing bin vendored without the co-located lib. Docs (ADOPTION.md / INSTALL.md / UPGRADE.md) now state the lib is a required co-vendored dependency of those bins. `promote-released-cards` is deliberately standalone and needs no lib.

## [0.11.1] - 2026-07-10

**Patch — `kbcard` + `board-card-start` payload writes rely on the kanban per-key merge; drop the stale wholesale-replace read-merge-write (card #3867).** 2 PRs since v0.11.0 (#70 fix + #69 docs).

### Fixed
- **#70** — `kbcard` (`patch`) and `board-card-start` (the DL-stamp write-site) assumed `PATCH /tasks/{id}.json` replaces `task.payload` **wholesale** and did a read-merge-write of the full payload. That premise is stale: the kanban v3 API **merges `task.payload` per-key** (kanban #2180). Both now PATCH **only the changed keys** — dropping an unnecessary GET (kbcard) and, more importantly, the **lost-update race** #2180 was designed to prevent (a concurrent edit to another custom field between the read and the PATCH was clobbered by the stale full-payload write). `tags` read-merge-write is intentionally **retained** (`tags` is a top-level array with no per-key merge — only `payload` got #2180; same label, opposite correctness). Verified on the prod API: a delta `dl_number` PATCH preserved sibling `pr_number`/`origin`. Card #3867.

### Changed
- **#69** — synced the solo-agent orientation doc (`CLAUDE_AGENTBOARD.md`) to coord v0.2.253 (finish-to-next self-drive guidance). Docs only.

## [0.11.0] - 2026-07-09

**Composite GitHub Action for `promote-released-cards` — GitHub-Actions consumers pin a SHA instead of vendoring a copy.** PR #66 (card #3768).

### Added
- **#66** — `promote/action.yml`: `promote-released-cards` is consumable as a SHA-pinned **composite GitHub Action** (`uses: <owner>/agent-board-toolkit/promote@<sha>  # vX.Y.Z`) — the preferred path for GitHub-Actions consumers (INSTALL.md §6a). Thin wrapper: all logic stays in the script; inputs `writeback-token` / `expected-host` / `api-base` (runtime injection into the checked-out `.release-pr.json`) / `dls` / `dry-run`. Drift becomes impossible rather than detected, presence is guaranteed, and dependabot's `github-actions` ecosystem bumps the pin — replacing the manual re-vendor ritual for these consumers. Vendoring + drift-check (§6b) remains the documented path for non-Actions consumers (PM-project vendors per the §8 amendment). New `promote-action-selftest` CI job guards the wrapper's wiring (embedded shellcheck + fail-closed `uses: ./promote` smoke). Corrects the #2615 Decision #3 consumption model for kanban-dev's own two consumers; consumer-side cutover tracked per repo (kanban DL-195 / bridge DL-180 corrections).

## [0.10.0] - 2026-07-09

**Card-automation restoration — `board-card-start` is revived and hardened.** PRs #58–#63. The post-checkout hook's branch→In-Progress automation was dead on every install; this release fixes the root cause and completes the framework #112/#113 contracts for the local mover. **Operator: `~/.kanban-host.env` must now export `KANBAN_EXPECTED_HOST` for `board-card-start` (see INSTALL.md §3) — one host-level setting activates every repo.**

### Fixed
- **#61** — `board-card-start` falls back to `$KB_API` when the committed `.release-pr.json` `api_base` is a host-scrubbed placeholder (card #3753). The hook read the api base **only** from `.release-pr.json`, which is a scrubbed `*.example.com` placeholder post host-scrub — so it hit a dead host and fail-softed silently, and no card ever auto-moved to In Progress. It now detects the RFC-2606 placeholder and uses the real host it already resolves from `~/.kanban-host.env` (`KBCARD_API`); a genuinely-real committed host is used as-is. Verified live.

### Added / Changed
- **#60** — `board-card-start`: a `DL-NNN` that resolves to no card **falls through** to a card-id token, and **stamps `payload.dl_number`** when selected via card-id with a DL named in the branch (card #3726, framework #112). Closes the dead-end where a decision-logged-but-unstamped card never moved; the card-id regex now also recognizes the bridge's `card#<id>` grammar.
- **#59** — `board-card-start` auto-moves a **Held** card to In Progress on a genuine branch **creation** (reflog-detected, ≤15s), with a `no-automove`/`block_reason` opt-out (framework #113 Held-automove contract, toolkit half).
- **#63** — `docs/HOOKS.md` codifies the **single-token correlation naming convention** (card #3755): branch `<type>/<card-id>-slug` + `card#<id>` (or `DL-NNN`) in the PR title drives both movers with zero manual stamping; flags the bare-`#<id>`-doesn't-match-the-bridge gotcha.
- **#58** — `kbcard`: URL-encode the `resolve_task` query + fail loud on a non-numeric `--external-id` (was silently mis-resolving).

### Fixed (release tooling)
- **#62** — `promote-released-cards` retries transient 5xx (`curl --retry`) to ride the deploy maintenance window (card #3700). Byte-identical to the kanban + bridge vendored copies.

### Changed (dependencies)
- **#23** — `actions/checkout` 6.0.2 → 7.0.0.

## [0.9.0] - 2026-07-08

### Removed
- **#55** — `bin/board-transition-sync` retired (#3649). Three defects (whole-command DL grep moving unrelated Released cards on PR-body citations; exact-string match silently inert vs the zero-padded `DL-%04d` canonical form; cross-board first-match on a false uniqueness assumption) — reported upstream, framework-confirmed never-shipped-there. Superseded by the bridge writeback (bridge DL-174 fixed 1:1-board correlation; put the PR's own `DL-NNN` in the title). **Operator action: remove the PostToolUse hook entry** — see UPGRADE §6.

### Added
- **#55** — `board-session-close`: In-Review-without-open-PR reconcile invariant (#3651) — flags any In-Review card whose `pr_number` matches no open PR; first live run caught three cards stale for 2–4 weeks. The toolkit's own repo joined the branch/PR loops.
- **#54** — UPGRADE.md §6: complete version-by-version upgrade actions (v0.4.1 → dev), source-verified per tag.

### Security
- **#52** — writeback bearer token kept out of argv (process-table exposure) + promote `api_base` host validation (#3569/#3570).
- **#53** — baked infra host removed from the shared toolkit; `KANBAN_EXPECTED_HOST` is REQUIRED (fail-closed) for the guarded promote path. **Operator action:** set the repo/org variable — see UPGRADE §3/§6.

### Changed
- **#51** — VERSIONING.md names the auto-tag workflow; stale changelog comment fixed.

## [0.8.2] - 2026-07-06

**Shared board library extraction (`bin/_kb-board-lib.sh`): the config/API/pagination/DL-canon logic duplicated across the toolkit's `bin` scripts is collapsed to one sourced source, plus a silent board-read truncation fix.** PRs #46, #47, #48. **First release under the bridge-parity release infrastructure** (`VERSIONING.md`, `docs/CHANGELOG.md`, `.release-pr.json`, `CLAUDE.md § Recent releases`).

### Changed

- **Extracted `bin/_kb-board-lib.sh` as the single shared board library (#46).** The per-board config load, kanban API client, paginated board read, and DL canonicalization were each reimplemented across `kbcard`, `board-snapshot`, `next-dl`, `dl-a0-backfill-triaged`, `dl-a1-register-field`, and `board-card-start`; the second real caller exists, so per canon #5 the primitive is extracted to one sourced lib. Behavior-preserving.

### Fixed

- **Board reads now paginate via `fetch_board_cards` — no silent truncation (#47, DL-A0).** A single-page board GET silently dropped cards past the first page on a large board; the shared read now follows pagination so a full board is returned, failing loud on a partial read rather than reporting a truncated set as complete.

### Docs

- Agent Board Framework solo orientation (#48).
