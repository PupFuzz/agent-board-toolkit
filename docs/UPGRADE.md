# agent-board-toolkit — upgrade an existing install

Use this when a new toolkit version is available. Three surfaces upgrade differently: the **agent host** (symlinks — trivial), **product repos that vendored a tool** (need a re-vendor + drift-check), and **composite-action consumers** (a SHA-pin bump, dependabot-automated — see the §3 callout).

## 1. See what you have vs what's available

```bash
cat ~/agent-board-toolkit/VERSION                 # installed
git -C ~/agent-board-toolkit fetch --quiet && git -C ~/agent-board-toolkit show origin/HEAD:VERSION   # available
```

## 2. Upgrade the agent host (symlink installs — nothing else to do)

If you installed per INSTALL §2 (symlinks into `~/.local/bin`), the host tracks the source automatically:

```bash
git -C ~/agent-board-toolkit pull --ff-only
hash -r
cat ~/agent-board-toolkit/VERSION                 # confirm the new version
kbcard list --column backlog | jq 'length'  # smoke test -> a number on stdout, plus one stderr line
                                            # `… <M> of <N> board cards matched` (the filter's denominator,
                                            # printed on every filtered read — not an error)
```
If any **new** tool was added, re-run the symlink loop from [`INSTALL.md`](INSTALL.md) §2 to pick it up, then `hash -r`. **The loop is not repeated here on purpose** — INSTALL §2 is its one owner. The copy that stood in this spot was one of **four** in this repository, and all four carried the same defect: they linked *every* `bin/` entry, including a directory, which `ln -sf` follows into rather than replaces. Four copies is how a correction reaches one of them. Run §2's block as written; do not retype it from memory.

> **`pull --ff-only` upgrades this checkout only while its HEAD is on a branch.** That is the INSTALL §1 **Option A** clone topology — the recommended one, and the one this section assumes. If the checkout has been **detached at a tag** — by a §5 rollback, by a deliberately pinned second checkout, or by a `checkout v<version>` line copied out of a release note — the command above answers `You are not currently on a branch.` and exits **1** instead of upgrading. Return it to the tracking branch first (`git -C ~/agent-board-toolkit checkout main`), then pull. The pin's real cost is quieter than that error, though: an install nobody pulls simply never sees the next release. `agent-board-toolkit-runtime-check` is the guard for exactly that (`board-snapshot` runs it at SessionStart), and its remediation line **branches on your topology** — it names `pull --ff-only` for a checkout on a branch and the pin-advancing `checkout <tag>` only for one that is already detached.

## 3. Upgrade a product repo that vendored a tool (INSTALL §6b)

> **Composite-action consumers (INSTALL §6a) skip this section entirely** — their upgrade is the SHA-pin bump (`uses: …/agent-board-toolkit/promote@<sha>  # vX.Y.Z`), which dependabot PRs automatically. Nothing is vendored, so there is nothing to re-vendor or drift-check.

A vendored copy does **not** update with the host pull — re-vendor it deliberately, in a branch, and let the drift-check confirm:

```bash
cd <repo> && git checkout -b chore/bump-agent-board-toolkit
cp ~/agent-board-toolkit/bin/promote-released-cards bin/promote-released-cards   # re-copy each vendored tool
cp ~/agent-board-toolkit/bin/_kb-board-lib.sh bin/                              # + the shared lib IF you vendored a lib-sourcing bin — WHICH bins those are is derived, not listed: see the box below
cat ~/agent-board-toolkit/VERSION > .agent-board-toolkit-version                       # record the new version
~/agent-board-toolkit/bin/agent-board-toolkit-drift-check ~/agent-board-toolkit .            # -> "drift-check: OK"
git add bin/promote-released-cards .agent-board-toolkit-version
git commit -m "chore: bump vendored agent-board-toolkit to $(cat ~/agent-board-toolkit/VERSION)"
# open a PR per the repo's normal flow; CI re-runs the drift-check as a guard.
```

> **A stamped bin changes on EVERY release, so re-copy it on every bump.** A bin carrying a line-initial `ABTK_TOOL_VERSION=` is re-stamped with each release's version, so its bytes differ from any other release's even when nothing else in it changed, and the drift-check reports it as `DRIFT` until it is re-copied. Name the set for the version you are vendoring: `grep -l '^ABTK_TOOL_VERSION=' ~/agent-board-toolkit/bin/*`.

> **Which bins need `_kb-board-lib.sh` beside them is DERIVED, never listed here.** Name the set for the version you are actually vendoring:
> ```bash
> grep -lE '^[[:space:]]*source "\$KB_LIB"' ~/agent-board-toolkit/bin/*
> ```
> That is the same anchored pattern `agent-board-toolkit-drift-check`'s `MISSING-LIB` probe triggers on, and `tests/drift-check-fixture-selftest.sh` asserts as a premise that it still matches a real sourcer and still misses a real standalone — so it cannot rot silently while the checks keep passing. The list that stood here could and did: it named **six** bins while the tree answered **nine** — three short, and the ones it missed are the newest, which is the direction a hand-kept list always fails in. A consumer following it vendored a bin without the lib and got an rc-1 `shared lib not found` refusal on **every** invocation of that bin, `--help` included. The complement is derived the same way: anything `bin/` holds that the command above does not list is standalone and needs no lib. `tests/lib-set-derivation-selftest.sh` is what keeps a list from growing back here — it reds if this box stops publishing a derivation that runs, or starts naming members instead.

> **⚠ Re-vendoring `promote-released-cards` from a host-guarded version? You must also add `KANBAN_EXPECTED_HOST`.** The guarded script — the version that validates `.release-pr.json`'s `api_base` against `$KANBAN_EXPECTED_HOST` before sending the writeback token (see [`INSTALL.md`](INSTALL.md) §6b + [`README.md`](../README.md)) — **requires** `KANBAN_EXPECTED_HOST` in the promote-CI env and has **no baked default**. A re-vendor that copies the new script but does **not** add the variable makes the **next promote run fail closed**: the token is never sent and tracking-card promotion is skipped (with a loud CI error). The **`drift-check` will NOT catch this** — it verifies the script matches the toolkit, not that your consuming workflow supplies the env. So in the SAME re-vendor PR, add it to the promote step's env, alongside `KANBAN_WRITEBACK_TOKEN`:
> ```yaml
> KANBAN_EXPECTED_HOST: ${{ vars.KANBAN_EXPECTED_HOST }}   # your kanban host, e.g. kanban.example.com
> ```
> and set the variable once (out-of-band from the PR-editable `.release-pr.json`):
> ```bash
> gh variable set KANBAN_EXPECTED_HOST --repo <owner>/<repo> --body "<your-kanban-host>"
> ```
> If your promote workflow also supplies `api_base` from a variable, set `KANBAN_API_BASE` the same way — from the version that added it, the script reads that variable **directly** and in preference to `.promote.api_base`, so a vendored copy gets the same channel the composite action's `api-base` input uses. Both are host-validated against `KANBAN_EXPECTED_HOST`; the guard accepts that host or a subdomain of it.

> **Why a re-vendor + check instead of a submodule?** It keeps each repo self-contained for CI (no submodule checkout) while `agent-board-toolkit-drift-check` makes silent divergence impossible — the check fails CI if `bin/<tool>` no longer matches the toolkit at the recorded version. If you prefer one literal copy, a git submodule of `agent-board-toolkit` is the supported alternative; then "upgrade" is `git submodule update --remote` and the drift-check is unnecessary.

## 4. Verify after upgrade

```bash
agent-board-toolkit-drift-check ~/agent-board-toolkit <repo>   # each vendored repo -> "drift-check: OK"
kbcard show --task <some-id> | jq .id              # host -> the id, no error
```

## 5. Compatibility

- **Patch/minor** (`x.y.Z` / `x.Y.z`): backward-compatible — config files unchanged; host pull + re-vendor is all that's needed.
- **Major** (`X.y.z`): may change a config key or a tool's flags. The version's release notes list any required config migration **before** you bump. Read them, migrate `~/.kanban-*-board.env` / `.release-pr.json` as directed, then upgrade.
- **Required config added with the promote host-guard.** The version that introduces the fail-closed `api_base` host validation adds **one required** promote-CI variable — `KANBAN_EXPECTED_HOST`, with **no default**. Any repo that vendors `promote-released-cards` must add it to its promote-workflow env on the re-vendor that pulls the guarded script (see §3), or the promote step fails closed on the next release. This affects **only** repos that run the promote workflow; the agent-host symlink install (§2) is unaffected. `.release-pr.json` and `~/.kanban-*-board.env` are otherwise unchanged.
- **Rollback on the host is a deliberate, temporary pin — not the mirror image of the upgrade.** `git -C ~/agent-board-toolkit checkout <previous-tag>` **detaches HEAD**: the install stops advancing, and §2's `git pull --ff-only` exits 1 until you return it with `git -C ~/agent-board-toolkit checkout main`. That is the pinned topology INSTALL §2's warning box tells you to stay out of, so undo the pin once the reason for it is gone. While it stands, `agent-board-toolkit-runtime-check` fails loud every session — intended, not noise: you are knowingly behind. (Do not substitute `git reset --hard <previous-tag>` to keep HEAD attached; that diverges the branch from its upstream and breaks `--ff-only` too.) Repos roll back by reverting the vendor-bump PR. No *state* is stored in the toolkit, so no data is ever at risk — the topology change is the part that persists.

## 6. Version-specific upgrade actions

§1–§5 are the mechanics (pull, re-vendor, drift-check). This section is the **content**: the changes that require an upgrader to **do** something (set a var, add a file, re-run a loop, deploy in a particular order, or knowingly accept a changed behavior), from the coverage floor named in this section's **Coverage floor** note below through its last `### v…` heading. Feature additions that need no action are omitted from an entry's bullets. **Find your installed version (`cat ~/agent-board-toolkit/VERSION`) and walk forward from the next entry — each entry is cumulative.**

> **Every release has an entry; a release that asks nothing of you says so.** A release requiring no action carries a **No upgrade action** line rather than being left out, so a gap in this walk can only ever mean a doc defect — never "nothing was required". *No upgrade action* means nothing **beyond** the routine mechanics above: §2's `git pull` on the host, and §3's re-vendor + drift-check in any repo that vendored a tool which changed in that release. Keeping this section complete is a release-time obligation, not a courtesy — `VERSIONING.md` § The §6 upgrade-action rule owns it, and the release-artifact gate asserts it.

**Audience tags** (an entry may carry more than one):

- **[host]** — an agent host with symlink installs (INSTALL §2).
- **[vendor]** — a product repo that copied a `bin/` tool into its own tree (INSTALL §6b / §3 above).
- **[release-CI]** — a repo whose CI runs the promote workflow (via a vendored copy or the §6a composite action; the tool is usually `promote-released-cards`).

> **Coverage floor** is **v0.4.1**. **v0.4.0** was the first tag and has no earlier release to upgrade from, so it has no entry. Releases **v0.4.x–v0.8.1** predate `docs/CHANGELOG.md` (which starts at [0.8.2]); the actions below were reconstructed from the git history (release commits + PR titles/bodies) and verified against the actual script source at each tag. Where the history did not record an operator action for a change, that is stated rather than invented.

### v0.4.1

- **[host] board-snapshot board roster became config-driven.** The set of boards `board-snapshot` scans is now read from `~/.kanban-snapshot-boards` (or the `KANBAN_SNAPSHOT_BOARDS` env override) as `<name>:<label>` lines, instead of hardcoded literals. It **defaults to the `dev`+`bridge` pair**, so a stock kanban-dev host needs no action. **If your host snapshots a different set of boards, create `~/.kanban-snapshot-boards`** with your `<name>:<label>` lines, or the snapshot will only cover the default pair.
- **[host] optional `KB_STAGE_WONT_DO` board-env key.** A "Won't Do" terminal column is now supported by `kbcard` (`--column wont_do`) and by `board-snapshot`'s terminal/untriaged detection. Both read `KB_STAGE_WONT_DO` from `~/.kanban-<name>-board.env`. **Only needed if your board has such a column** — set `KB_STAGE_WONT_DO=<stage_id>` in that board's env to use it; leaving it unset simply means that column isn't recognized as terminal.
- **[host] `kbcard --version` no longer 422s on a board without the version field.** It now writes `version_target` only when `KB_CF_VERSION_TARGET` is a real custom-field id (not `000`/unset), and warns instead of failing. Transparent — no action.
- `next-dl` gained `--board <name>` + `KB_DL_CHECKOUT_GLOBS`, and `board-card-start` now correlates by card-id as well as `DL-NNN`. Both are transparent improvements — no action.

### v0.4.2

- **[host] token-resolution order fixed — verify after upgrade.** `kbcard` now honors a board-env `KBCARD_TOKEN_FILE` in the correct precedence. If you had worked around the old order (e.g. an ambient `KBCARD_TOKEN` in your shell), **run the §2 smoke test** after upgrading to confirm your token still resolves. *(Scope correction, made in v0.14.0: this entry originally also named `board-snapshot`, which was never true — `board-snapshot` resolved its token before sourcing any board env at every version up to v0.13.0, so a board-env `KBCARD_TOKEN_FILE` never reached it. It honors one as of **v0.14.0**. The `kbcard` half was accurate here, then silently regressed in v0.8.2 — see the **v0.14.0** entry.)*
- **[host] doc correction: `KBCARD_API` is host-env only.** It is resolved **before** the per-board env is sourced, so a `KBCARD_API` placed in a `~/.kanban-<name>-board.env` file is **ignored**. If you had put it there, **move it to `~/.kanban-host.env`** (INSTALL §3). No code change forces this, but a board-file `KBCARD_API` silently does nothing. *(Accurate as written for v0.4.2–v0.8.1. v0.8.2 inverted the source order and began silently **honoring** a board-env `KBCARD_API`; as of **v0.14.0** it is **refused loudly** rather than either ignored or honored — see the v0.14.0 entry for the current rule.)*

### v0.4.3 — v0.4.4

- **[host] `kbcard list --column` undercount fixed (v0.4.3), then an argv-overflow + truncation regression fixed (v0.4.4).** Before v0.4.3, `kbcard list --column X` silently stopped at the first page; the fix paginates it, and v0.4.4 repairs a follow-on regression on large boards. **Awareness only, no action** — but any automation that keyed off the old (undercounted) numbers will now see the true, larger counts. There is no config change.

### v0.5.0

- **[host] two new tools — re-run the symlink loop.** `dl-a1-register-field` and `dl-a0-backfill-triaged` were added to `bin/`. A symlink install won't pick up a *new* tool automatically, so **re-run the INSTALL §2 link loop** after pulling (§2 above points at it; it is not repeated there).
- **[host] `dl-a1-register-field` needs a one-time board setup.** It registers the `dl_number` custom field on a DL board and real-surface-verifies the server's `system=dl` by-ref index. *(Correction, made in the release that shipped card#6517: at v0.5.0 through v0.29.0 this tool registered that field as **`number`**, and reported a pre-existing definition of any type as an idempotent success. Every toolkit writer of `dl_number` stamps the canonical `DL-NNNN` **string**, so a board stood up by those versions rejects every `kbcard --dl` write with `422 Must be a number.` — see that release's entry below for the check, and for the in-place `field retype` remedy. If you ran this tool on any version up to v0.29.0, verify with `kbcard --board <name> field list`.)* **Run it once per board that mints DLs** (`dl-a1-register-field --board <name>`); it is idempotent (a re-run is a clean no-op). `next-dl`'s atomic-claim path depends on that field existing. `dl-a0-backfill-triaged` is a one-shot sweep that backfills the `triaged` tag onto pre-existing adapter-owned cards — run it once per board if you rely on the untriaged-discovery check over a legacy card corpus.
- **[host] loud-on-cap / fail-closed posture (FR-2/FR-3).** A board read that hits the pagination cap now **errors loudly** instead of silently returning a truncated set, and ambiguous states fail closed. This is the intended hardening, **but** any wrapper that previously consumed a silently-capped partial result will now see a **non-zero exit** — update wrappers that swallowed the old (wrong) success.

### v0.6.0 — v0.6.1

- **[release-CI] cause-aware promote exit + shift-left card-coverage check (v0.6.0).** The promote step now distinguishes failure causes on its exit code and checks card coverage earlier in the release. **[vendor] re-vendor `promote-released-cards`** (§3) to pick it up; a release whose cards aren't covered may now be flagged earlier. No config change.
- **[vendor] `promote-released-cards` jq argv-overflow fixed on large boards (v0.6.1).** The paged board is now accumulated via stdin rather than argv (fixes `jq: Argument list too long`). If your board is large, **re-vendor `promote-released-cards`** (§3). No config change.

### v0.7.0

- **[host] `kbcard --type` behavior change — undeclared aliases now TAG instead of ERRORING.** Before v0.7.0, `--type` accepted a fixed set of six aliases (`dl|release|request|fr|bug|idea`) and **errored** on any other alias or on an alias not defined for the board. Now `--type` is config-driven: it resolves **any** `KB_TYPE_<ALIAS>` key from the board env to that native `card_type_id`, and for an alias with **no** native id (or when `KB_TYPING_MODE=tags` is set) it applies a `type:<alias>` **tag** instead — it no longer errors. **Action:**
  - To type cards **natively** on your board, add `KB_TYPE_<ALIAS>=<card_type_id>` keys (uppercased, `-`→`_`, e.g. `KB_TYPE_TECH_DEBT`) to `~/.kanban-<name>-board.env`.
  - To force **tag** mode board-wide, set `KB_TYPING_MODE=tags` in that env.
  - Be aware: because an unknown alias no longer errors, a **typo'd `--type` now silently produces a `type:<typo>` tag** rather than a hard failure. Don't rely on the old error to catch mistyped types.
- **[host] `board-snapshot` pagination fix.** In-flight and untriaged counts were short past the default page; they now paginate. Awareness only — snapshot counts may rise. No action.

### v0.8.0

- **[host] `next-dl` stops masking a failed atomic claim.** It now surfaces a present-but-failed server-side DL claim (non-zero exit) instead of silently falling through, and paginates its offline header-scan fallback. Intended hardening — **update any wrapper that assumed `next-dl` always succeeded**; it may now exit non-zero where it previously masked the failure.
- **[host] `kbcard --pr-url`** was added to `patch` + `create-card` (sets `payload.pr_url`). Opt-in new capability — no action.

### v0.8.1

- **[host/vendor] DL number pad width changed 3 → 4 (`DL-NNN` → `DL-NNNN`).** Newly minted or normalized DLs render zero-padded to four digits (`DL-0001`). **No migration required:** the width is purely cosmetic — every reader extracts the digits and compares **numerically**, so pre-existing 3-padded cards and `## DL-NNN` decision-log headers stay valid and mixed widths coexist. Awareness only.
- **[host] `kbcard --dl` is normalized at the write-site** (#3400): a `--dl` value is canonicalized to `DL-NNNN` and a non-DL shape (a `pr_url`, a version string, an out-of-range overflow) is now **rejected** at write time. Awareness — a malformed `--dl` errors where it may previously have been stored as-is.

### v0.8.2

- **[vendor] NEW file dependency: `bin/_kb-board-lib.sh`.** The shared config/API/pagination/DL-canon library was extracted, and **at v0.8.2** six tools `source`d it: **`kbcard`, `board-snapshot`, `next-dl`, `dl-a0-backfill-triaged`, `dl-a1-register-field`, `board-card-start`**. ⚠ **That six is a fact about v0.8.2 and nothing later** — it is recorded here because this is a version-specific entry, and the set has grown since. **Never read it as the current set:** §3 above carries the derivation that answers for the version in your hand. Consequences:
  - **[vendor] critical:** a product repo that vendored **any lib-sourcing tool** must now **also vendor `bin/_kb-board-lib.sh` alongside it**. A vendored tool is a plain file copy; it resolves the lib next to itself, so a re-vendor that copies only the single old tool file will **break at runtime** (`_kb-board-lib.sh: No such file or directory`). In the same re-vendor PR (§3), `cp ~/agent-board-toolkit/bin/_kb-board-lib.sh bin/_kb-board-lib.sh`, add it to the commit, and record it — the drift-check verifies each file against the toolkit but will **not** add the missing lib for you.
  - **Exception — `promote-released-cards` is deliberately standalone** and does **not** source the lib (it intentionally duplicates the host-guard so it can be vendored as one self-contained file). If the only tool you vendor is `promote-released-cards`, you do **not** need `_kb-board-lib.sh`.
  - **[host] symlink installs are unaffected.** Each tool resolves the lib via `readlink -f` back to the toolkit's real `bin/`, so a symlinked tool finds `_kb-board-lib.sh` in the source checkout automatically — no separate action (re-running the §2 loop is harmless but not required for the lib).
- **[host] board reads now paginate via `fetch_board_cards` — no silent truncation** (#47, DL-A0). Awareness only — a full large board is returned where a page could previously be dropped.
- The bridge-parity release infrastructure (`VERSIONING.md`, `docs/CHANGELOG.md`, `.release-pr.json`) landed here. These are **toolkit-repo-internal** — a consumer needs no action. (`.release-pr.json` is per-consumer-repo config you already maintain per §4; nothing about its schema changed.)

### v0.9.0 — security hardening

- **[release-CI/vendor] fail-closed `KANBAN_EXPECTED_HOST` host guard — REQUIRED new promote-CI variable, no default** (#52/#53). `promote-released-cards` (and `board-card-start`) now validate `.release-pr.json`'s `api_base` against `$KANBAN_EXPECTED_HOST` **before** sending the writeback token, and there is **no baked default** — an unset var makes the guard **fail closed** (token never sent, promotion skipped with a loud CI error). **This is the one action an upgrader must not miss when re-vendoring the guarded `promote-released-cards`.** The exact steps — adding `KANBAN_EXPECTED_HOST` (and, if your workflow injects `api_base` from a variable, `KANBAN_API_BASE`) to the promote step's env and setting the repo/org variable — are in the **§3 warning box** above and summarized in **§5**. The drift-check will **not** catch a missing var (it checks the script, not your workflow env). **[host]** symlink installs that don't run the promote workflow have no CI action here — but note `board-card-start` gained the same guard at this version, so the **host-level** `KANBAN_EXPECTED_HOST` (in `~/.kanban-host.env`) becomes load-bearing at **v0.10.0**, which revives that hook — see the next entry.
- **[host] `bin/board-transition-sync` RETIRED — remove its `PostToolUse` hook entry from `~/.claude/settings.json`** (#55, #3649). The hand-rolled single-card mover had three defects (2026-07-08 incident, reported upstream): it grepped the first `DL-NNN` from the whole `gh pr create` command (PR *bodies* citing historical DLs moved unrelated Released cards), matched the card `dl_number` by exact string (silently inert against the zero-padded `DL-%04d` canonical form), and scanned boards in a fixed order on a false cross-board-uniqueness assumption. The bridge writeback supersedes it (correct title/branch-only extraction, numeric compare, repo-routing; bridge DL-174 fixed the 1:1-board correlation) — put the PR's own `DL-NNN` in the PR **title** and the bridge moves the card on opened/merged. **Action:** delete the hook block referencing `board-transition-sync` from `~/.claude/settings.json` `hooks.PostToolUse`, and remove any stale `~/.local/bin/board-transition-sync` symlink/copy.
- **[host/vendor] token kept out of argv** (#52) and **baked infra host scrubbed from the toolkit** (#53). The writeback bearer token is no longer passed on the command line (it stays out of the process table), and the previously-baked host literal was removed — which is *why* `KANBAN_EXPECTED_HOST` now has no default. Both are transparent to operators beyond the required-var action above; **no separate action.**

### v0.10.0

- **[host] REQUIRED for local card automation: export `KANBAN_EXPECTED_HOST` from `~/.kanban-host.env`** (INSTALL §3). This release revives `board-card-start` — the post-checkout branch→In-Progress automation was **dead on every install** before v0.10.0 (the hook read `api_base` only from the committed `.release-pr.json`, which is a host-scrubbed placeholder; #61 falls back to the real host from `~/.kanban-host.env`). The revived hook runs the v0.9.0 host guard, which has **no baked default** — and because a checkout hook must never block a checkout, the failure mode is **fail-soft**: without the var the hook exits 0, warns only on stderr, and **no card ever moves**. One host-level export activates card automation for every repo on the host. Symlink hosts that never installed the hook, and repos that only run promote in CI, are unaffected.
- **[host] the revived hook's behavior — accept or opt out per card.** A genuine branch **creation** now auto-moves a **Held** card to In Progress (#59; opt out per-card with a `no-automove` tag or a `block_reason`). A branch `DL-NNN` that resolves to no card **falls through** to a card-id token and **stamps `payload.dl_number`** on the selected card (#60); the bridge's `card#<id>` grammar is now recognized alongside bare card ids. No config change — but if you had parked cards in Held relying on the hook staying dead, review them.
- **[vendor] re-vendor `promote-released-cards`** (§3) to pick up transient-5xx retry (#62) — promote runs no longer fail on a deploy maintenance window. No config change.

### v0.11.0

- **[release-CI] optional migration: `promote-released-cards` as a SHA-pinned composite action** (#66, INSTALL §6a). GitHub-Actions consumers can replace the vendored copy with `uses: <owner>/agent-board-toolkit/promote@<sha>  # vX.Y.Z` — drift becomes impossible and dependabot bumps the pin. **No required action** — vendoring + drift-check (§6b) remains fully supported; migrate at your convenience.

### v0.11.1 — v0.11.4

- **No action.** v0.11.1 switches `kbcard`/`board-card-start` payload writes to per-key delta PATCHes (the kanban server already merges `task.payload` per-key, so this is transparent); v0.11.3 (toolkit-internal CI) and v0.11.4 (release-notes reframe) change nothing a consumer touches.
- **[vendor] v0.11.2 awareness — a missing `_kb-board-lib.sh` is now loud.** The five interactive lib-sourcing bins fail with a self-naming message (and `agent-board-toolkit-drift-check` grows a MISSING-LIB probe that fails CI) when vendored without the co-located lib. The lib has been a required co-vendored dependency **since v0.8.2** — if this newly fails for you, the vendored tool was already broken at runtime; fix it by co-vendoring the lib (§3), not by pinning back.

### v0.12.0

- **[host] new tool `bin/adopt-to-dl` — re-run the symlink loop** (INSTALL §2) to pick it up; a symlink install never picks up a *new* tool automatically. The tool itself is opt-in (the pull-into-build adoption seam for card-first boards — stamps an existing plain card with a freshly minted `payload.dl_number`); a board whose `dl_number` is bridge-derived doesn't need it. **[vendor]** it sources `_kb-board-lib.sh` — co-vendor the lib if you vendor it (v0.8.2 rule).

### v0.12.1 — v0.12.2

- **No action.** Docs releases (the DL-counter recovery runbook; the HOOKS.md pinned-card clarification). The one `bin/next-dl` change is comment-only — every executable path, `promote/action.yml`, and `examples/*` are byte-identical to v0.12.0.

### v0.13.0

- **[host] if any repo sets `core.hooksPath`, re-run `install-board-hooks` there.** Before v0.13.0 the installer hardcoded `.git/hooks/post-checkout`; on a repo with `core.hooksPath` (gitleaks, pre-commit, Husky, many Windows setups) git dispatches hooks only from that path, so the install **reported success but the hook never fired**. The installer now targets `core.hooksPath` (and refuses, with guidance, a hooksPath inside the tracked work tree). If your hook has silently never moved a card on such a repo, this is why — re-run the installer after upgrading.
- **[vendor] re-vendor `promote-released-cards`** (§3): the bearer token is now fed to curl via a stdin herestring instead of `-H @<(…)` process substitution. **Required if any promote run executes on native mingw64/Git-Bash curl** (the old form fail-softed every API call there, rc=26); recommended regardless — it also fixes a latent `--retry` bug (the old non-seekable pipe meant a retried request couldn't re-read the auth header). **[release-CI]** SHA-pinned action consumers get it with the pin bump; `promote/action.yml` itself is unchanged.
- **[host] optional: `git config kanban.board-id <id>`** (repo-local, uncommitted) binds a repo with no `.release-pr.json` to a board for `board-card-start` — adopt the hook without adding a committed `api_base` surface. Opt-in; existing installs unchanged. Failed-but-attempted moves are now durably logged to `~/.cache/agent-board-toolkit/board-card-start.log` (`KB_BCS_LOG` overrides) — check there first when a card didn't move. Awareness only.

### v0.14.0

- **[vendor/release-CI/host] SECURITY — re-vendor `promote-released-cards`; the `api_base` host guard could be bypassed.** The guard that validates `.release-pr.json`'s `api_base` before the bearer token is sent terminated the URL authority at `/` alone, where RFC 3986 ends it at the first of `/`, `?` or `#`. An `api_base` of `https://evil.example#@your.real.host` therefore parsed as `your.real.host` and was **accepted**, while curl discarded the fragment and sent the token to `evil.example`. Since `api_base` lives in a **committed, PR-editable** file, a pull request alone was sufficient — no access to the victim's `$HOME` or git config. Both copies of the guard (`bin/_kb-board-lib.sh`'s `kb_require_https_host` and `bin/promote-released-cards`' standalone `host_ok`) are fixed.
  - **[vendor] Action:** a repo that **vendored `promote-released-cards`** must **re-vendor it** (§3) — the vendored copy carries its own guard and does not track the toolkit. Until you do, that repo's promote workflow keeps the bypassable guard, and its exposure is the `KANBAN_WRITEBACK_TOKEN` CI secret. Exposure requires your promote workflow to run against untrusted config (e.g. `pull_request_target`); a workflow triggered only on `push` to a protected branch is not exposed, but should still re-vendor.
  - **[release-CI] Action:** if you consume the **SHA-pinned composite action** (§6a), bump the pin — `promote/action.yml` is unchanged, so you get the fix in `bin/promote-released-cards` with the bump alone.
  - **[host] No action** — a symlink install picks it up on `git pull`.
  - Legitimate `api_base` values are unaffected: subdomains, `:port`, real `user:pw@` userinfo, and queries all still pass. If a *valid* `api_base` of yours now refuses, that is a bug — report it.
- **[host] a board env's `KBCARD_TOKEN_FILE` is honored again — verify your per-board tokens.** For `kbcard`, `next-dl`, `dl-a0-backfill-triaged`, `dl-a1-register-field` and `adopt-to-dl` this is a **restoration, not a new rule**: `kbcard` honored a board-env `KBCARD_TOKEN_FILE` from v0.4.2 through v0.8.1, and the v0.8.2 shared-library extraction silently inverted the source order, so from **v0.8.2 through v0.13.0** it was **ignored** and the host-level (or default `~/.kanban-dev-token`) token was sent instead — to *every* board. For **`board-snapshot` this is genuinely new** (it never honored a board-env token at any version), and **`board-card-start` is the one exception to the ladder** — it honors a board's token only for a repo whose board id is host-local (see its own entry below). The precedence:
  > **a board env's `KBCARD_TOKEN_FILE` > the host env's > an ambient one > `~/.kanban-dev-token`**

  > ⛔ **SUPERSEDED — the fourth rung is gone (card#7245, see `[Unreleased]`).** The ladder above is the **v0.14.0** rule and is kept here as the record of that release. `~/.kanban-dev-token` is **no longer a default**: a board for which none of the first three tiers declares a token file is now **refused**, not fallen back. Being one file shared by every board that had not set its own is exactly what let a single overwrite take every board down at once. `docs/INSTALL.md` §3 states the current rule — three declared tiers plus the coord credential store's pointer, added by card#7316 — and is where to read it; do not act on this line.

  **Action — only if you set `KBCARD_TOKEN_FILE` in BOTH a board env and `~/.kanban-host.env`.** For those boards the **board's** token now wins where the host's silently did since v0.8.2. That is the documented intent, but it is a live change in *which credential is sent*: confirm each board's token file is the one you want that board to use (`kbcard --board <name> list` per board is the quickest check). If you only ever set one of the two, nothing changes.
- **[host] `board-snapshot` renders per-board tokens — and no longer goes dark without `~/.kanban-dev-token`.** It read **one** token up-front and exited silently when that file was missing, so a host that uses only per-board token files displayed **nothing** at all. It now reads each board's token as it renders that board, and a board whose token is unreadable reports itself (`• <label>: (token file unreadable: …)`) instead of blanking the whole snapshot.
- **[host] a board env that sets `KBCARD_API` is now REFUSED, loudly.** The API base is board-independent. v0.4.2–v0.8.1 ignored a board-env `KBCARD_API`; v0.8.2–v0.13.0 silently **honored** it (the same inverted order as above), so a stray value in a board env quietly re-pointed the tool at another host. `kbcard` / `next-dl` / `dl-a0-backfill-triaged` / `dl-a1-register-field` / `adopt-to-dl` now **refuse** it with a message naming the file, instead of guessing which behavior you meant. **Action:** if any `~/.kanban-<name>-board.env` sets `KBCARD_API`, move it to `~/.kanban-host.env` (INSTALL §3). Refusal is **not uniform** — know what you are looking at: `kbcard` / `dl-a0-backfill-triaged` / `dl-a1-register-field` / `adopt-to-dl` **exit 2 and will not run** until you move it, while `next-dl` **warns, skips the board check, and still mints from its offline scan** (it is fail-soft by contract). For `next-dl` the message is therefore your *only* signal that it silently lost both the atomic claim and the board's `dl_number` seed — do not ignore it. `board-snapshot` and `board-card-start` never read a board env's `KBCARD_API` at all, so they ignore it as they always have.
- **[host] `board-card-start` — per-board tokens are a host-local opt-in.** The hook consults a board env's `KBCARD_TOKEN_FILE` **only** when the repo's board id came from a repo-local `git config kanban.board-id`. When the board id comes from the committed `.release-pr.json`, the hook keeps using the host/default token exactly as before — a committed, PR-editable file must not be able to select which board's credential the hook sends. `git config kanban.board-id` now also takes precedence over `.release-pr.json`'s `promote.board_id` (in practice the two are mutually-exclusive populations, so this rarely decides anything). **No action** — an install that sets no per-board token behaves identically.
- **[host] `board-card-start` reports *why* a card did not move.** A card fetch that failed on an unreachable API, a rejected token (401/403), or a server error used to be reported as **"card … does not exist"** — sending you to hunt a missing card instead of a broken token. Each cause is now named distinctly in the diagnostic log. Awareness only; no action.

### v0.15.0

- **[host] new tool `bin/agent-board-toolkit-runtime-check` — re-run the symlink loop.** It judges what actually **executes** rather than the checkout, catching the two live-observed topologies a "patch the checkout, then grep the checkout" verification certifies wrongly: real-file **copies** (native mingw64 `ln`, a manual `cp`) and a **pinned runtime** (`PATH` resolving into a checkout detached at a tag that never advances). It also warns on a mixed runtime — a partial upgrade where the on-`PATH` tools disagree about the lib they source — and reports an unverifiable topology as UNKNOWN rather than passing it silently. A symlink install never picks up a *new* tool by itself, so **re-run the INSTALL §2 link loop** after pulling (§2 above points at it; it is not repeated there).
- **[host] a short board read now exits 4 — it used to exit 0, so check anything that wraps these tools.** `fetch_board_cards` detected `total > read_n`, warned INCOMPLETE, and then returned **0**, so a caller could act on a read the lib had just declared incomplete (`next-dl` minted a DL from one). It now exits **4** with the partial data still emitted, and `next-dl` / `dl-a0-backfill-triaged` refuse it. **Update any wrapper that ignored the exit code of a board read, or that treats every non-zero as fatal** — rc 4 means "partial, and it says so".
- **[vendor] re-vendor `bin/_kb-board-lib.sh` — and `bin/promote-released-cards` if you copied it.** The lib carries the rc-4 backstop above and the restored API error body (a 403 token-scope failure and a 422 validation failure were indistinguishable in the failure log after the v0.8.2 extraction). The `promote-released-cards` change is diagnostic-only: a *local* dry-run derives its baseline from local tags and can over-report the shipped range, so it now says so on stderr (suppressed under `GITHUB_ACTIONS`, where the invocation is remote-truth by construction).

### v0.15.1

- **No upgrade action.** Two fixes on host tools: the v0.15.0 staleness guard's warning now reaches its reader (it was written to **stderr**, and the SessionStart hook that consumes `board-snapshot` surfaces only stdout — a correct, installed guard that nobody could see), and `board-snapshot` renders each board's own column names instead of a hardcoded `In Progress`/`In Review`/`Held` map that a board-side rename silently falsified. `_kb-board-lib.sh` also gains an **opt-in** `KB_CURL_MAX_TIME` per-request cap; unset — the default — is byte-identical behavior, and it should be set around a *read*, never exported process-wide over the bins that write (a timed-out POST/PATCH is ambiguous, not failed).

### v0.16.0

- **No upgrade action.** New `kbcard archive` and `kbcard delete [--hard]` verbs (the board API has no HTTP `DELETE`; these replace the hand-rolled `curl` that removal previously required) plus two `board-snapshot` fixes — a board no longer inherits a **sibling board's** stage ids in its untriaged count, and each board is fetched once per run instead of twice. Untriaged counts may change on a host that snapshots more than one board: that is the fix landing, not a regression.

### v0.17.0

- **[host] `install-board-hooks` now installs TWO hooks — re-run it in every repo that has it.** `hooks/pre-push` joins `hooks/post-checkout`: a **fail-soft** advisory that warns when a branch name references a card in a spelling the auto-move grammar will not accept (e.g. `card_4524`), reusing the mover's own matcher so the two can never disagree. It never blocks a push and always exits 0. An existing install carries only the post-checkout symlink — `install-board-hooks <repo>` again to pick the second one up (still non-destructive: it refuses to clobber a repo's own non-symlink hook).
- **[host] the branch→card grammar widened, so a branch that moved nothing may now move a card.** The separator after `card` is optional: `card4524`, `card-4524`, `card/4524`, `card#4524` and `#4524` all correlate. It still requires the literal `card`/`#` at a token boundary and ≥2 digits, so DL tokens and version numbers stay safe. **Awareness** — if you were relying on the glued spelling being inert, opt the card out with a `no-automove` tag or a `block_reason`. The bridge writeback is unchanged and still wants `card-<id>`/`card#<id>`.
- **[host] optional: name your swimlanes in the board env.** `kbcard list --swimlane <name|id>` resolves names from **id-keyed** `KB_SWIMLANE_<id>=<name>` entries in `~/.kanban-<name>-board.env` (see `examples/kanban-board.env.example`); without them the filter still works by numeric id, and a board declaring none says so rather than printing an empty list. Also new and opt-in: `create-card --triaged`.

### v0.18.0

- **No upgrade action.** New optional `--issue` / `--issue-url` correlation flags on `kbcard create-card` and `patch` (they stamp `payload.issue_number`/`payload.issue_url`), an unconfigured-board error that now names its own fix instead of reading as breakage, and three same-behavior consolidations (payload assembly, the selftest prelude, assorted intra-file dedup). No config migration.

### v0.19.0

- **No upgrade action.** `kbcard patch --swimlane <name|id>` closes the write side of the read filter that shipped in v0.17.0 (`--swimlane none`/`0` un-assigns), and an unresolved swimlane **name** now enumerates the board's defined lanes instead of leaving a reader to infer the board has none. Both read the same optional `KB_SWIMLANE_<id>=<name>` board-env entries recorded under **v0.17.0**; no existing invocation changes.

### v0.20.0 — deploy-ordering release

- **[host/vendor/release-CI] the v3 flat task-write cutover is UNCONDITIONAL — these tools must run against a kanban carrying DL-219.** Every task `POST`/`PATCH` now sends the task object **flat** (top-level), dropping the `{task: {…}}` envelope. There is **no version probe, no wrapped fallback and no shim**, so this release moves in lockstep with the kanban deploy: **do not advance a host, or re-vendor, past v0.19.0 until the kanban your tools write to serves DL-219** — and once that deploy lands, a copy still sending the wrapped body is the one that breaks. Eight write sites across `kbcard` (create-card/move/patch), both `board-card-start` writes, `dl-a0-backfill-triaged`, `dl-a1-register-field` and `promote-released-cards` are affected, so **[vendor] re-vendor every one of those you copied**, in the same window. `task_links` / `_action` bodies are unaffected.
- **[host] `kbcard archive` can now refuse.** It gates on the framework's shipped `may_archive` invariant and **refuses (rc 1)** to archive the only card of a still-live, untwinned coordination source; `--force` overrides and is audited, and an unlocatable `may_archive` primitive fails loud rather than archiving anyway. Accept the refusal, or pass `--force` knowingly.
- **[host] `board-session-close`'s inverse-drift leg moved to the framework's `kanban-reconcile.py --detect`, and it can now tell you the check is OFF.** The hook is resolved version-agnostically (`$KANBAN_RECONCILE_HOOK` → the coord plugin dir on `$PATH` → the marketplace clone → the newest cached version), and an **unresolvable** hook is now rc 1 `DID NOT RUN` instead of a silent skip. **Action, if you want the inverse leg armed:** give each board an `inverse_check_columns` entry in `coordination.config.json`'s `kanban.boards[]`. Without it the hook runs the forward leg only and the ritual prints a loud `⚠` naming the board — OFF is a legitimate choice, and the warning exists so degraded coverage is never read as a clean pass.

### v0.20.1

- **No upgrade action.** Test/CI only: a network-free static guard that fails if any `bin/` file re-introduces the `{task: {…}}` wrapper the v0.20.0 cutover removed — including the standalone-vendored `promote-released-cards`, which cannot source the shared lib — plus a docs stamp sync. **No file under `bin/`, `hooks/`, `promote/` or `examples/` changed at this release**, so there is nothing to re-vendor.

### v0.21.0

- **[host] declining a card now CLEARS its refs — pass `--keep-refs` if you were relying on them staying.** Both decline call-sites (`kbcard move --column wont_do` and `patch --column wont_do`) null `payload.dl_number`, `pr_number` and `pr_url` in the same PATCH, closing the write-site half of the stale-stamp class: a recycled ref could otherwise resurrect a declined card into Released at the next promote. `--keep-refs` opts out, an explicit ref flag passed in the declining call still wins, and non-decline moves are byte-unchanged.
- **[release-CI] optional anti-resurrection guard: `--shipped-stages <ids>` / the `shipped-stage-ids` action input.** A DL/PR-matched card whose current stage is outside the given set is skipped and logged instead of promoted. **Opt-in — absent is byte-identical to the previous behavior** (proven empirically), so adopt on your own window; a malformed set is rc 2 before any PATCH. **[vendor]** re-vendor `promote-released-cards` (or bump the action pin) to have the flag at all.
- **[host] new `kbcard field list` / `field set-options` verbs** — a custom-field schema read plus an idempotent converge-to-set enum reconcile (never an append). Opt-in; one semantic worth knowing before you use it: option **removal is definition-only**, so a card keeps a removed value orphaned — no clear, no cascade.

### v0.22.0

- **[host] optional new dispatch-time card move — registering it is a manual step.** `hooks/agent-dispatch-card-start` is a Claude Code **`PreToolUse`** hook for the subagent-dispatch tool: it moves a card to In Progress the moment a build is dispatched, closing the window `hooks/post-checkout` only closes at branch creation. It is that hook's **peer, not its replacement** — either may fire first and the move is idempotent — and it acts **only** on a line-start `BOARD-CARD: <board-key>#<card-id>` marker in the dispatch prompt (a bare card-number scan is deliberately not used; prose mentions many ids). **`install-board-hooks` does NOT install it** and never will: it symlinks git hooks, and this is a `settings.json` entry. **Action, if you want it:** add the `PreToolUse` `Agent`-matcher block from [`HOOKS.md`](HOOKS.md) to your Claude Code `settings.json`. Fail-soft on every path (it always exits 0 — a non-zero `PreToolUse` would block the dispatch). Skip all of this if you do not dispatch subagents.

### v0.23.0

- **No upgrade action.** `board-session-close` gains a **read-only** "archive-eligible done cards" leg: per configured board it reports the un-archived done-card count and runs a bounded sample through the shipped `may_archive` gate to show concrete candidates. It surfaces, never archives — the authoritative per-card gate stays in `kbcard archive`. Its two new files are `_`-prefixed **siblings**, resolved beside the real `board-session-close` through `readlink -f` rather than looked up on `PATH`, so they need no symlink loop.

### v0.23.1

- **[host/vendor] the CLIs now REJECT `--flag ""` where they silently accepted it.** An explicitly-empty value was indistinguishable from an absent flag, so it selected a **default path** and exited 0 — an unexpanded shell variable was the usual way in. **Any caller using `--flag ""` to mean "use the default" must omit the flag instead**; whitespace-only values already died and are unchanged. Two of the shapes this closes show the cost: `kbcard patch --dl "$DL"` with `DL` unset PATCHed `{}` and reported success (leaving a card without the ref it needs to ever promote), and `promote-released-cards --shipped-stages ""` ran **unguarded** with a summary byte-identical to a guarded run. **[vendor] the vendored surface IS affected** — re-vendor `bin/kbcard`, `bin/_kb-board-lib.sh`, `bin/promote-released-cards` and `bin/release-pr-body`; `promote/action.yml` is unchanged, so action-pinning consumers see no interface change.
- **[release-CI] check how your promote step wires `shipped-stage-ids`.** `docs/INSTALL.md` §6a used to recommend `shipped-stage-ids: ${{ vars.KANBAN_SHIPPED_STAGE_IDS }}`; an **unset** variable renders blank, the action correctly omits the flag, and the promote then runs **unguarded** while the workflow still reads as guard-enabled. The recipe is now a literal, with the variable form demoted to an aside carrying a preflight. **If you copied the variable form, either set the variable or inline the ids.** Relatedly, an unguarded promote now announces itself on **stderr** (the summary is deliberately byte-identical-when-absent, by an explicit v0.21.0 compat decision).

### v0.24.0

- **[host] four narrowings — each closes a wrong input that was answered at rc 0.** (1) `kbcard "" <verb>` printed usage and exited **0**, so a caller checking `rc` read a no-op as a completed write; an empty first argument is now a failed expansion, not a help request. (2) An explicitly-empty **positional** is refused by name across the bins that take one — it was previously invisible and let the next argument take its place, so `install-board-hooks "" <repo>` installed into `<repo>`. (3) `board-card-start` honours `--lint` in any position and refuses an empty/unknown/extra argument; it previously dropped a non-leading `--lint` and moved a card for real. (4) Guards spelled as bash bracket **ranges** are pinned to ASCII, so under a UTF-8 shell they no longer admit the wider set the locale's collation allowed. **Re-check any script that passed an empty argument deliberately.** One further observable: `patch --triaged` no longer re-**sorts** the card's tags.
- **[host] `install-board-hooks` now INSTALLS into topologies it used to refuse — re-run it there.** A `--separate-git-dir` checkout and a submodule are supported (the linked-worktree refusal is unchanged), and the hook-health rules were rewritten around real failure modes: an **empty** `core.hooksPath` (git then dispatches *no* hooks at all — it does not fall back to `.git/hooks`), a `~`-prefixed or `..`-escaping one, a non-canonical path spelling, and a hook that carries the executable bit but cannot be exec'd (CRLF endings, a missing shebang interpreter). **If the installer ever refused one of your repos, or a card quietly stopped moving in one, re-run it now** — and note `board-session-close` gained a leg that reports checkouts whose `post-checkout` no longer reaches `board-card-start`, which is how you find the rest.
- **[vendor] a broad re-vendor release.** Eleven `bin/` files changed, including `_kb-board-lib.sh`, `kbcard`, `next-dl`, `promote-released-cards`, `board-card-start`, `adopt-to-dl`, `install-board-hooks` and both `dl-a*` bins — re-vendor whichever you copied (§3), remembering the lib for any lib-sourcing bin. `promote/action.yml`'s vendor surface is unchanged. New capability, no action: `kbcard patch --name / --tags / --type / --external-id / --origin` can now correct the five fields that were previously settable only at birth (`--tags` **replaces** the whole list; `--type` strips any `type:*` tag before writing the native id), plus `create-card --swimlane` and `list --swimlane none|0`.

### v0.25.0

- **[host/vendor] one narrowing: `agent-board-toolkit-drift-check` refuses a third positional.** `agent-board-toolkit-drift-check <toolkit> <repo> <anything-else>` previously exited **0** with `drift-check: OK`, reading the first two arguments and dropping the rest without a word — a green drift report about something other than what was asked, in the tool that *is* the drift gate. It now exits **2** naming the extra argument, and an empty-positional refusal says **which** slot is empty. Every documented invocation passes exactly two positionals; fix any caller of yours that passes more.
- **[host] new tool `bin/release-artifacts-check` — re-run the symlink loop** if you want it on `PATH` (same reason as v0.15.0: a symlink tracks its target's content, not the set of tools).
- **[release-CI] optional: adopt the `release-artifacts/` composite action.** It asserts that every member of your `.release-pr.json` `artifacts` array actually moved in a release PR — present in the PR's own changes, still existing at head, and carrying the version being shipped — where that array was previously only *printed* as a `- [ ]` checklist. SHA-pinned on the same terms as `promote`; the recipe is `docs/INSTALL.md` §6c, and see **v0.26.0** for the `types:` line that recipe was missing. A repo that declares no `artifacts` array is unaffected: the check reports that nothing was declared and exits 0.
- **[host] `board-card-start` gained `-h`/`--help`** (previously the only usage surface a hand-typed invocation reached was the unknown-option refusal, whose remedy read as advice to target a branch literally named `--help`). No action.

### v0.26.0

- **[host] `next-dl --peek` now answers from the board's DL counter, not an offline scan — stop treating the old answer as authoritative.** `--peek` took its maximum over DL numbers that had reached a **card**, so a number already claimed from the counter but never stamped was invisible to it and `--peek` handed back a number the server had already issued (measured on a real board: `--peek` said `DL-0220` while the counter's next was **222**). A DL is a correlation key, so a reused one silently merges two unrelated work items. `--peek` now reads the board's non-consuming DL-sequence counter and falls back to the offline scan only where that endpoint is **absent** (404/unreachable ⇒ pre-endpoint behavior, unchanged). **Two live changes:** a board whose kanban serves the route starts reporting different, authoritative numbers — if your `--peek` and a direct counter read disagree, your `next-dl` predates this fix — and a **present-but-errored** endpoint is now rc 1 with the cause on stderr, where it previously answered a plausible wrong number from the floor. The default (claim) path is untouched.
- **[release-CI] optional: `card_token_regex` in `.release-pr.json`, and `promote-released-cards --cards`.** If your commit subjects say `card#NNNN` rather than `DL-NNN`, `release-pr-body`'s `## Card coverage` section derived an unconditionally empty manifest and reported clean **without checking anything**. Add a `"card_token_regex"` key — and **do not simply re-spell `ref_token_regex`**: its numeric part is matched against `payload.dl_number`, so under a `card#` spelling `card#42` correlates with whichever card carries **DL-42**. The two keys are two id spaces (`ref_token_regex` → `payload.dl_number`; `card_token_regex` → the card's own `id`). Adopting neither key is byte-identical, but note that `examples/release-pr.json.example` now sets it, so a config copied from that template has the coverage section on.
- **[release-CI] if you copied the `INSTALL.md` §6c release-artifacts recipe before this release, add `edited` to its `types:`.** Retargeting a PR's base fires `edited`, not `synchronize`, and the gate's verdict is a function of the base (the merge base drives both the classification and the asserted diff) — so a stale verdict survived a base retarget. The same omission was fixed in this repo's own gate.
- **[vendor] re-vendor `bin/next-dl`, `bin/promote-released-cards` and `bin/release-pr-body`.** `promote/action.yml`, `bin/kbcard` and `bin/_kb-board-lib.sh` are unchanged this release.

### v0.27.0

- **[host] new tool `bin/dependabot-deploy-reconcile` — re-run the symlink loop.** Same structural reason this section gives every time a release adds a bin: a symlink tracks its target's *content*, not the *set* of tools, so a new tool never lands on `PATH` without the loop. `agent-board-toolkit-runtime-check` gains the new tool in its `TOOLS` set and will print a warn line naming exactly that until you re-run it — that line is the check doing its job, and the re-symlink it prescribes is the fix. The tool itself is host-local and read-only (it asks whether the fix a *closed* Dependabot alert claims is actually present in what is deployed and running); it writes only its own JSON artifact under `~/.cache/coord/dependabot-reconcile/`.
- **[host] two new preflight entries in [`INSTALL.md`](INSTALL.md) §0 — neither is fatal.** `ps`: absent, the new tool reports an instrument failure rather than a wrong answer. Coreutils `timeout`: absent — the default on macOS, and possible under an MSYS install — `board-session-close`'s advisory legs still run, each printing a one-line warning that it is running **unbounded**. Install coreutils if you want the 60s per-leg bound.
- **Awareness, no action:** `board-session-close` grows a `── Dependabot fixed-alert vs deployed-tree reconciliation ──` section and runs both advisory legs under that 60s wall-clock bound (a killed leg prints a ⚠ and still never blocks the close). The archive leg's missing-sibling warning was reworded in the same extraction — relevant only if something of yours matches on its exact text.

### v0.28.0

- **[host] RE-RUN `install-board-hooks <repo>` ON EVERY SEAT — this is the one action in this release that is about a state you may already be in.** The hooks are installed by symlink, and that symlink IS the upgrade contract: a `git pull` in the toolkit checkout reaches every installed hook only because each entry points at the source. On a symlink-incapable seat the POSIX-emulation layer does not fail `ln -s` — measured on a consumer's Windows seat (Developer Mode off, non-elevated), it returns **rc 0 having substituted a COPY** — so the install reported plain success while that seat ran hook copies no upgrade would ever reach again, and **the two outcomes were byte-identical in every log this tool writes**. The *state* is readable after the fact exactly where the shell's own readers can be trusted: `test -L` answering **false** on an installed hook is conclusive — that entry is a copy no `git pull` will ever update (pair a **true** with `readlink` against the toolkit's hook source before concluding the good direction — a symlink to the wrong target is still broken). On the emulation-layer seat class this release exists for, that cheap read is NOT available: under `winsymlinks:lnk` MSYS `test -L` calls a `.lnk` shortcut a symlink, so the shell reader reports exactly the false clean the probe was built to close, and there only a fresh run — whose probe reads through native git — answers. What is never readable after the fact, on any seat, is which code path *produced* a copy: a deliberate `--allow-copies` and the old silent substitution leave identical residue. If you probe capability by hand rather than re-running the installer, **probe at the repo's own path, never in a temp dir** — `mktemp -d` lands on `/tmp`, often a different filesystem (tmpfs), and a probe there says nothing about the filesystem the hooks actually live on. From this release the installer probes the target directory first and either installs unchanged (**CAPABLE**), refuses at **rc 1** (**NOT_CAPABLE**, naming the opt-in), or refuses at **rc 4** (**INDETERMINATE** — reported distinctly, because folding "we could not determine" into "we know you cannot" would re-mint exactly the information loss this closes). A seat that re-runs and succeeds was fine; a seat that re-runs and refuses was not, and now knows.
- **[host] `--allow-copies` is a permit, not a force — and it exits 3, never 0.** On a capable seat it changes nothing. Where the probe refuses NOT_CAPABLE it installs copies, prints `COPY dst <= src` per hook, and states the re-install obligation those copies carry (a copy does not track the source, so every toolkit upgrade needs the install re-run by hand). It deliberately does **not** unblock INDETERMINATE: a known degradation is a thing you can accept, an unknown state is not. If you script around this tool, note the new exit codes — a caller that tested only `rc != 0` is unaffected, one that enumerated codes is not.
- **[host] `install-board-hooks --check` is no longer write-free.** It runs the same probe, which creates and removes two `.ibhp*` entries in the hooks directory — or, where that directory does not exist yet, in the nearest existing ancestor, which for an out-of-tree `core.hooksPath` is a directory outside the repository. They are removed on every path including a signal. If you invoked `--check` specifically because it touched nothing, that assumption no longer holds.
- **[host] new tool `bin/board-stats` — re-run the symlink loop.** The same structural reason this section gives every time a release adds a bin: a symlink tracks its target's *content*, not the *set* of tools, so a new tool never lands on `PATH` without the loop. `agent-board-toolkit-runtime-check` gains `board-stats` in its `TOOLS` set and will print a warn line naming exactly that until you re-run it — the check doing its job, and the re-symlink it prescribes is the fix. The tool itself is a read-only per-board stock + flow report; its flow half reads the changelog event log rather than `last_stage_move`, which is a snapshot and cannot answer a flow question.
- **[release-CI] re-pin the `release-artifacts/` composite action, and know that an ORDINARY PR can now red on it.** `release-artifacts-check` used to read the set it judges a PR by — and the keys that classify the PR — out of `.release-pr.json` **at head**, which is a file the PR under judgment can edit; four distinct shapes let a PR weaken its own verdict leaving no trace. The declared set and `version_file`/`version_regex` now come from the config at the **fork point**, so a head edit may only WIDEN the set. The consequence you must plan for: **a member declared at the fork point and absent from head fails in its own right, on a release PR and an ordinary one alike.** The one narrowing channel is a new optional `retired_artifacts` array at head, which logs a `RETIRED:` line instead of failing — a member listed in **both** arrays is a config error. A fork point with no usable config (the adoption PR that creates it, an unparseable or key-less one) is a `::warning::` and a fallback to head's keys, never a refusal. Your `.release-pr.json` must be **committed at head**, since the config is read from the commit and not from the working tree.
- **[vendor] re-vendor `bin/kbcard`, `bin/_kb-board-lib.sh`, `bin/install-board-hooks`, `bin/board-session-close`, `bin/release-artifacts-check`, `bin/agent-board-toolkit-runtime-check`, `bin/dependabot-deploy-reconcile` + `bin/_dependabot-reconcile.py`** — and `bin/board-stats` if you want the new tool. `bin/next-dl`, `bin/promote-released-cards`, `bin/release-pr-body`, `bin/board-card-start` and `promote/action.yml` are unchanged this release; **`release-artifacts/action.yml` IS changed** (see the release-CI entry above).
- **Awareness, no action.** `kbcard` gains `comment` / `comments` — posting a card comment had no verb, so every caller hand-rolled the API call; `comment --task <id-or-ext> --content …` prints the created comment id and `comments --task <id-or-ext>` reads them back. `kbcard list` now projects `tags` and `external_id` (an additive widening of the projected field set — a consumer parsing the old columns is unaffected), and a filtered read names its denominator. `board-session-close`'s "do these two path spellings name the same directory?" test answered by comparing the spellings as **text** on any host whose `readlink` lacks `-m` (a GNU coreutils extension, so macOS/BSD/busybox), and is now decided by identity — if you run it on such a host, expect it to start answering correctly where it previously reported a spurious mismatch. `dependabot-deploy-reconcile` binds its read affirmatively to the RUNNING process tree.

### v0.29.0

- **[host] `next-dl` now REFUSES (rc 1) to mint when a CONFIGURED board cannot be read at all** — where it previously minted from the local floor. The offline `max+1` fallback takes its maximum over local `## DL-NNN` headers **and** the board's stamped `dl_number`s, and it already refused on the paginator's rc 2, 3 and 4 — a read that was not *whole*. rc 1 was the exception: **nothing** of the board was read (no response, a non-2xx status, or a 2xx carrying no card array), and that one branch fail-softed, handing back a number derived from a floor it knew was incomplete. A DL is a correlation key, so a number minted below the real maximum silently merges two unrelated work items. **What this changes for you:** a seat whose config names a board that is unreachable, renamed, or authenticated with a stale token now gets a refusal with the reason on stderr where it previously got a plausible number. That is the tool reporting a broken board entry — fix or drop the entry rather than routing around the refusal. There is no flag to restore the old fail-soft, and the three rcs that already refused are unchanged.
- **[host] NO `PATH` symlink loop this release — stated rather than omitted, because every recent release owed one.** A symlink tracks its target's **content**, not the **set** of tools, so a release that ADDS a `bin/` file never lands it on `PATH` without the loop — that is why v0.15.0, v0.25.0, v0.27.0 and v0.28.0 each asked for it. **v0.29.0 adds no `bin/` file.** `kbcard search`, `kbcard field create` / `delete` / `retype` and `--name-file` / `--description-file` are new **verbs and flags inside `kbcard`**, a tool your existing symlink already points at, so §2's `git pull` in the toolkit checkout is the whole upgrade for them. `agent-board-toolkit-runtime-check`'s `TOOLS` set is unchanged and will not print a missing-tool warn line.
- **[vendor] a broad re-vendor — and `bin/_kb-board-lib.sh` is in it, so copy the lib TOGETHER with any bin you took.** Ten `bin/` files changed: `_kb-board-lib.sh`, `kbcard`, `next-dl`, `adopt-to-dl`, `dl-a0-backfill-triaged`, `dl-a1-register-field`, `board-card-start`, `board-snapshot`, `board-stats` and `promote-released-cards`. The tolerant response parse those bins now call (`kb_parse_resp`) lives in the lib, so a new bin beside an old lib fails at runtime with `kb_parse_resp: command not found`. The lib has always been a co-vendoring requirement (`ADOPTION.md`, [`INSTALL.md`](INSTALL.md) §6b, and the drift check's `MISSING-LIB` probe) — this is the first release where a stale copy breaks a **working** command rather than a new one. `promote/action.yml` and `release-artifacts/action.yml` are unchanged, so action-pinning consumers see no interface change.
- **Awareness, no action — four reads stop answering an unreadable response with a confident value.** A `2xx` whose body these tools cannot parse no longer kills them at `jq`'s exit 5: every single-card / single-field read and every write echo refuses in its own tool's words at that tool's own documented rc, and three routes to a **wholesale tag wipe** close with it — `patch --tags` / `--type` / `--triaged` replaces the tag list wholesale, and a `null` `.data`, a `tags` **object** and `tags: false` each merged into a PATCH that destroyed every tag the card held at **rc 0** without jq ever faulting. The whole-board paginator stops reporting an unreadable **page-1** `2xx` as an empty board, and an unreadable **page > 1** stops ending the scan as if it were a short page (in both whole-board paginators and in `board-stats`'s changelog window) — `promote-released-cards` was promoting from the truncated list. And under `KB_FETCH_LOUD` the paginator sent curl's stderr to the **path** `/dev/stderr`, which re-opens the file behind the caller's fd 2 `O_TRUNC` on every page fetch: `kbcard list 2>run.log` lost everything written before the first page, and that log now survives. If you have scripts that treated an empty `kbcard list` as "this board has no cards", they were reading a failed fetch the same way — those now get a non-zero rc and a reason.
- **Awareness, no action — the new `kbcard` surface.** `search <query>` is a board-wide free-text read that prints the card's **full description** per hit, so a term matched in a body is visible (`list`'s fixed projection drops `description`, so grepping a body through `list` answered 0 no matter what the board held). `field create` / `delete` / `retype` extend the field-schema write side beyond `set-options`: `retype` is a thin call on the server's atomic type-conversion route (no delete, no recreate, no per-card cast — `placeholder`, `mask`, `multiline`, `is_filterable` and `position` survive), and `delete` is fail-closed on the carriers it can see, refusing at rc 2 without `--orphan-values` and printing an `N of M board cards carry <key>` denominator on every run. `--name-file` / `--description-file` take free text from a file instead of `argv`, which matters because **the caller's shell expands an inline `--description "…"` before `kbcard` is started** — a `$(…)` or a backtick sitting in a report someone else wrote runs as a command on the invoking box, and no in-tool check can see it after the fact. If you pipe externally-authored text into these tools, switch to the `-file` forms.
- **Awareness, no action — `field set-options`' option-removal semantic is now a property of YOUR kanban server, and the `--help` text says so.** It previously pinned one answer: the removal PATCH is accepted (HTTP 200) and a card holding the dropped value is left stranded. That is still true of a kanban without its DL-242 change (absent from every kanban release up to and including v0.41.0). A server carrying it refuses the same PATCH **422** when the removal would strand a value the current definition still accepts. Two limits are stated with it, because both are easy to overstate: the check is a **delta** — a carrier whose value the current definition already rejects is skipped by design — and the board-**import** route still narrows unchecked, so it is not the case that all narrowing is now checked. `kbcard`'s behaviour is unchanged either way: it propagates the non-2xx with the server's error body.

### v0.30.0

- **⚠ SECURITY [host/vendor/release-CI] — an api base carrying `user:password@` was printed VERBATIM by ten error paths, four of them into durable on-disk logs; anything already emitted is leaked, so ROTATE it (card#7500).** That input is accepted *by design* — the host guards judge the HOST and nothing else, and `https://u:pw@<expected-host>/api/v3` has been a pinned **accept** row since the guard was written — so nothing about which bases are allowed moves here, and the hostile matrix (`https://good.host@evil.example/`, the `#@` and `?@` authority splits) refuses exactly as before. The defect was at the **render**. Six of the ten sites reach stderr, and therefore the CI log of the run that just failed; **four reach a file** — three of the paginator's failure lines go to `$KB_LOG_FILE`, which `kbcard` defaults to **`~/.kbcard-failures.log`** (override `$KBCARD_LOG_FILE`), and `board-card-start`'s two go to `$KB_BCS_LOG`, default **`~/.cache/agent-board-toolkit/board-card-start.log`** — where a password persists until something deletes it. All ten are ERROR paths, i.e. they fire exactly when an operator is most likely to paste the output into a ticket. **Action, and upgrading is not it:** grep those two paths and your own CI logs for a credential, rotate anything you find, and truncate the files — upgrading stops the *next* emission and cannot unprint the last. After upgrading, the only observable difference is that such a base reads `https://***@host/api/v3` wherever it is printed; a base with no userinfo is byte-unchanged, so an ordinary refusal looks exactly as it did. **[vendor] `bin/_kb-board-lib.sh`, `bin/board-card-start` and `bin/promote-released-cards` move as a SET** — the `kb_redact_url_userinfo` primitive lives in the lib and both bins call it, so a new `board-card-start` beside an old lib fails at runtime with `kb_redact_url_userinfo: command not found`. No exit code, request, verdict or accepted input changes.
- **[host] ⚠ TWO fail-closed refusals — configure for both BEFORE upgrading, or the board tools on that seat start refusing (card#7245).** Both were minted after a test harness overwrote the reference host's live credential config wholesale, took every board on the box down at once, and was caught by nothing.
  - **Action 1 — every board must NAME a token file.** The trailing `:-$HOME/.kanban-dev-token` default is **removed** at all four resolution sites. A board for which neither its own `~/.kanban-<name>-board.env`, nor `~/.kanban-host.env`, nor the invoking environment declares `KBCARD_TOKEN_FILE` is now **refused** — `kb_resolve_env` returns a new **rc 7**, which `kbcard` / `dl-a0-backfill-triaged` / `dl-a1-register-field` / `adopt-to-dl` surface as exit 2, `board-snapshot` reports per board while still rendering the others, `board-stats` puts on that board's failure list, and `board-card-start` skips the move and stays fail-soft. The refusal names the file to edit and the exact line to add. The **ambient** tier is deliberately kept: `KBCARD_TOKEN_FILE=… kbcard …` is a declaration by the invoker, where the baked default was a declaration by nobody. If you never set the variable anywhere, add it to `~/.kanban-host.env` (or per board) before you pull — or adopt the store pointer in the next entry.
  - **Action 2 — `KANBAN_EXPECTED_HOST` must be set in `~/.kanban-host.env` on every seat.** A new preflight judges the `KBCARD_API` the tools actually resolve, not just the committed `.release-pr.json` `api_base` the two older guards judged, and it runs **before the token file is even located**: an undeclared or mismatched host is **rc 6** (exit 2 in the four bins above; `next-dl` skips its board check; `board-stats` exits 1; `board-snapshot` prints a `SKIPPED` line carrying the variable, the file and the refused host, and still exits 0). **It refuses when nothing is declared** — a guard keyed only on *declared-and-mismatched* would have been silent through the exact write it exists to catch. The predicate is the **host alone**, not the scheme, so an `http://127.0.0.1` board stays legitimate. The variable was already documented as required here, but only for the checkout hook; it is now required for every tool.
- **[host] optional — a host that runs the coordination framework can point the toolkit at the credential store instead of keeping a second copy of the token (card#7316).** The resolver gains a **fourth, last** tier under the three declared ones (board env > host env > ambient > **store pointer**): `${COORD_CREDENTIALS:-~/.config/coord/credentials.ini}`'s `[kanban] api_token_file`. **Pointer-only** — an inline `[kanban] api_token` is **refused**, with the message naming the pointer form; no store value ever reaches an output stream, and a "pointer" that has the shape of a credential is refused before it can be echoed. It is **board-independent** (one pointer, so every board that declares nothing resolves the same file), needs no Python, and a host with no store is unaffected in every observable way — point `COORD_CREDENTIALS` at a non-existent path to disable the tier deliberately.
- **[host] re-run the `PATH` symlink loop — this release ADDS five files under `bin/`.** The structural reason this section gives every time: a symlink tracks its target's *content*, not the *set* of tools. New: `gh-code-search` (a `search/code` wrapper that refuses to report a count GitHub flags as incomplete — `total_count: 0` on a private repo is byte-identical to a real zero), `gitignore-secret-family-check`, `release-tag-check`, `_shellcheck-pinned` (with the `.shellcheck-version` declaration at the repo root) and `_kbc-stale-blocker.py`. `agent-board-toolkit-runtime-check` gains **three** of the five in its `TOOLS` set — `release-tag-check`, `gh-code-search` and `gitignore-secret-family-check` — and will print a warn line naming exactly those until you re-run the loop: the check doing its job, and the re-symlink it prescribes is the fix. The other two (`_shellcheck-pinned`, `_kbc-stale-blocker.py`) are internal helpers that set does not track, so **nothing will warn you about them** — link them with the same loop.
- **[host] ⚠ a DL board stood up by v0.29.0 or earlier is ALREADY broken; this release stops new ones being broken and refuses to certify the old ones (card#6517).** `dl-a1-register-field` — the tool §6 `v0.5.0` tells you to run once per board that mints DLs — declared `dl_number` as a **`number`** field. Every `dl_number` writer in this toolkit (`kbcard --dl`, `next-dl`, `adopt-to-dl`) stamps the canonical `DL-NNNN` **string**, and a number-typed field answers that with `422 {"message":"Must be a number."}`, so such a board rejected every `--dl` write from its first real use — while the setup tool's own step 2 wrote its sentinel as a bare JSON number, the one shape no writer produces, and passed. **Action, per DL board you stood up:** read the declared type (`kbcard --board <name> field list`) and, where it is not `string`, convert it in place with **`kbcard [--board <name>] field retype --field dl_number --to string --restamp-dl`** — that verb is a thin call on the server's atomic type-conversion route, so the definition and every stored value move in one transaction with no delete and no recreate. **Do not "declare everything string":** `pr_number` / `issue_number` must stay declared **`number`**, because `kbcard` coerces a numeric value to a JSON number and a string-typed field rejects it with `Must be a string.` The setup tool now refuses (rc 1) before creating anything on a board whose declared type is not `string`, prints that same remedy, and no longer reports a `409`/`422` as `idempotent OK` on the strength of the status alone. Its `--sentinel` default moves `999000001` → **`999001`** (the old 9-digit value is outside what `kb_dl_canon` can render) and now accepts a `DL-`prefixed value. ⚠ **The remedy command is derived from `field retype`'s contract, not from a run against a wrongly-typed board** — provisioning one is `403` for a non-admin token.
- **[host] `dl-a1-register-field` is runnable on a board with no swimlanes, where it previously could not run at all (card#7464).** It put a fabricated `swimlane_id: 1` on its throwaway card and was refused `422 {"message":"The selected swimlane is not on this board."}` — and no value existed that you could pass instead, because a board with no lanes defines no id. The key is now **omitted** from the create body unless you name a lane; `--swimlane N` and `KB_A1_SWIMLANE=N` are honoured verbatim, unchanged, when either is set. **No action** beyond re-reading §6 `v0.5.0` if that `422` is why you never completed setup.
- **[host/release-CI] `promote-released-cards` can be pointed at a real host for the first time — set `KANBAN_API_BASE` (card#7494).** The tool refused **unconditionally** on this repo's committed config and on every consumer's: `.promote.api_base` is where `KANBAN_WRITEBACK_TOKEN` is sent and the file naming it is PR-editable, so the value is host-validated against `$KANBAN_EXPECTED_HOST` before the token leaves the process — but that guard has an env channel precisely so a PR cannot move it, while the `api_base` it is checked against is (correctly) a scrubbed `kanban.example.com` placeholder with **no** env channel. Constraint and target could therefore never agree, and every run died at `refusing to send token — api_base 'https://kanban.example.com/api/v3' is not https:// on '<host>'`. The only two ways to run it were to commit a real host into a vendored repo or to carry a permanent uncommitted edit to a security-sensitive file. It took the release flow's card-coverage step with it, since `release-pr-body`'s `## Card coverage` section runs this tool and renders **nothing** when it exits non-zero. **Action:** export `KANBAN_API_BASE` (host env for a seat; a repo/org **variable** for Actions, like `expected-host`) — it overrides `.promote.api_base`, and **unset or empty is the committed value, byte-identical to before**. The guard is not weakened and its posture improves: both sources feed ONE resolution and ONE `host_ok` call, so an env-supplied base off the expected host refuses identically with nothing sent, and with target *and* constraint both out-of-band a PR editing `.release-pr.json` can now move **neither**. Every run names the CHANNEL the base came from on stderr — never the base itself, because `host_ok` legitimately accepts one carrying userinfo and echoing it on the happy path would print a credential into every CI run log.
- **[host] ⚠ NARROWING, every lib-sourcing tool: a LEADING-ZERO number is now refused (card#6912).** `kb_is_uint` accepted any run of ASCII digits and handed it to bash arithmetic, which reads a leading-zero literal as **base 8**: `010` compared unequal to `10`, and `08` died mid-guard with a raw `bash: [[: 08: value too great for base` on the caller's stderr. `0`, `1`, `42`, `100` accept exactly as before; `00`, `08`, `007`, `010` and every other zero-padded spelling now get that tool's own ordinary refusal at its own documented rc. **Action only if you feed zero-padded numbers to these tools** — a card/task ref, an external id, a swimlane id, a custom-field ref, a GitHub issue number, a page size — including one read off a spreadsheet column that pads.
- **[host] `next-dl` announces a fallback it used to take silently, and `--require-counter` refuses it outright (card#7214).** When the board's DL-sequence counter could not answer, `next-dl` printed a number from the offline `max+1` scan at **rc 0 with an empty stderr** — indistinguishable, on every channel a caller can read, from a server-side atomic claim, which is how two allocators get handed the same DL by the tool whose purpose is to prevent that. Each of the four causes now prints its own line (config did not resolve / endpoint unreachable / endpoint not deployed / answered but carried no number) followed by a disposition. **Action:** nothing is required — the default path is unchanged — but if you script around `next-dl`, pass **`--require-counter`** where an atomic claim is load-bearing, and expect stderr output on the fallback path where there was none.
- **[host] three operator-facing reports now carry the incompleteness ON the numbers, so expect `≥` prefixes and floor markers you have not seen before (card#6365, card#7228, card#7235).** `board-snapshot` renders `in-flight ≥200`, `≥200 UNTRIAGED` and `… +≥194 more` when the board read was page-capped or short, and — the part that matters — the INCOMPLETE signal moved to **stdout**, because the SessionStart hook that consumes this script discards stderr. `board-stats` does the same for its **stock** counts (nine numbers per board) and its **flow** counts (fifteen on the fixture board). `board-stats --format json` is deliberately **unchanged**. **Awareness, not action** — unless you parse these renders, in which case a `≥`-prefixed cell is new input.
- **[host] `agent-board-toolkit-runtime-check`'s report is now BUFFERED (card#6911).** Nothing appears until the run ends, and it can spend up to 10 s per runtime in `git fetch`. Progressive output is what let a truncating reader (`| head -1`, a pager you quit) kill the writer mid-report and hand back **rc 141 with 0 B** in place of the verdict; it now answers rc 1 with its full stderr through `head -n 0`. `--quiet`, the exit codes and the text are unchanged.
- **[host] the documented `PATH` install loop CHANGED — copy the new one; the old one planted a symlink cycle inside your own checkout (card#7170).** `ln -sf` does not replace a destination that is a symlink-to-directory: it **follows** it and creates the link inside the target, at rc 0. A directory in `bin/` (a `__pycache__/` left by a python helper — closed at source by card#6871 and at CI by card#7207) was linked onto `PATH` on run 1 and, on run 2, linked back through that `PATH` entry into the source. Invisible to `git status`, because the directory is gitignored. The recipe now refuses any `bin/` entry that is not a regular file and links with **`ln -sfn`**, which closes the same defect at the destination end. **Action: use the loop as `docs/INSTALL.md` §2 now writes it** — directories, dangling symlinks, FIFOs and sockets stop being linked and each refusal is named on stderr. If you have already run the old loop twice, check for `bin/__pycache__/__pycache__` and remove it.
- **[host] re-run `install-board-hooks <repo>` if a seat ever refused with `readers-disagree-shell-does-not-track`, or if you pinned one to `--allow-copies` (card#6572).** The capability probe's rule — a read that FAILS is the measurement not happening, not a measured absence — covered only its git reader; the shell reader threw away the distinction with `|| :`, so an unreadable entry scored a mechanism nobody had measured and the printed remedy was the copies opt-in, i.e. a permanent pin to hooks no `git pull` will ever reach. Both readers are now `unmeasured` until their own read succeeds. A seat that installed cleanly is unaffected.
- **[vendor] re-vendor `bin/board-snapshot` — a runtime guard that DID NOT RUN was rendering byte-identically to one that found nothing (card#7305).** The runtime-staleness leg ended `agent-board-toolkit-runtime-check --quiet 2>&1 || true`, and that guard buffers its entire report into a single write at the end (card#6911, deliberately — progressive output is what let a truncating reader destroy its verdict). Under `--quiet` silence IS the clean signal, so a run that dies before that write produces exactly what a current runtime produces. Measured through the real bin: a healthy run and a SIGKILLed one both gave **0 bytes at caller status 0**. An `EXIT` trap makes the flush unconditional on every *ordinary* death, but a signal cannot be trapped, and the guard's own header already accepts that a full filesystem yields the right rc having printed nothing. The leg now reads the status and adds a line **only where the guard said nothing** — a run carrying its own `✗`/`⚠` is neither softened nor given a second line, a current runtime stays silent, and `board-snapshot` still exits 0 and still never blocks. **No host action:** if your runtime is current the output is byte-identical.
- **[vendor] a broad re-vendor — 22 of the 26 files under `bin/` are new or changed, and `bin/_kb-board-lib.sh` is one of them.** The token-file and api-host refusals above both live in the lib, so **copy the lib TOGETHER with any bin you took**; a new bin beside an old lib is the failure mode this has already produced once. Only **`adopt-to-dl`, `dl-a0-backfill-triaged`, `dependabot-deploy-reconcile` and `agent-board-toolkit-drift-check`** are untouched — `agent-board-toolkit-drift-check` names the ones your tree actually holds. Two changes reach a vendored copy without any new flag: `kb_api` now returns a distinct **`$KB_API_RC_TRANSPORT`** for *the request did not complete* while keeping **rc 1** for *the server answered non-2xx* (card#6680 — every existing `|| …` / `if !` bucket behaves byte for byte as before, but a caller that wants the distinction must branch on the **name**, never the number), and 46 of 47 `<producer> | grep -q` sites are migrated to a pipeline-free whole-line match because `grep -q` exits on match, its writer then dies on the closed pipe, and `pipefail` promoted that into a **reported non-match** (card#7175).
- **[release-CI] ALL THREE composite actions move this release — re-pin every one, and know that the third is new.** `release-artifacts/action.yml` changes in **description only** — but the script it runs did NOT stand still, so a pin left where it is keeps the old behaviour: `release-artifacts-check` stops classifying a real release PR as *version unchanged* when the version file carries a version-shaped line above its version line (card#6488 — one prepended `# was 0.1.0` made every declared member's leg silently not run, at **rc 0**; on an equal classifying value the two ends' matched-value **sets** are now compared, exactly one new value at head names the version being shipped, several is rc 2), and it no longer carries a second, wider hardcoded version shape beside `version_regex` (card#7208 — that stage could only ever narrow the config's match, and truncated a 4-segment version to 3). **New: `release-tag-check/action.yml`** (card#7203), the SHA-pinnable surface for the untagged-release gate, whose tag name is derived from `.release-pr.json`'s **`tag_format`** by the one tool that reads that key — a repo that sets `tag_format` gets the tag its scheme produces, and a repo that does not is byte-identical. `docs/INSTALL.md` §6d is the recipe. **`promote/action.yml` CHANGED too, so re-pin all three** (card#7494): it keeps `api-base` as an interface but stops **rewriting `.promote.api_base` into the checked-out `.release-pr.json` with `jq`** and passes the value as `$KANBAN_API_BASE` instead. That rewrite was the workaround for the defect above, reachable only by Actions consumers, and it is removed rather than left standing beside its own fix — two ways to set one value is how they drift, and mutating a security-sensitive file in your workspace mid-job is a side effect no caller asked for. A blank input is still the committed value, so an absent override is byte-identical.
- **[release-CI] the promote workflow no longer reports a release as shipped before its tag exists (card#6579).** `auto-tag-version.yml` and `release-promote-cards.yml` fire on the same `push: main` as independent runs with no `needs:` between them, so promote won the race on **every** release; on one real merge the tag push was refused `403` while promote reported success, and the board asserted a shipped release for 17m27s against a tag that did not exist. Promote now waits for the tag and **refuses to stamp Released if it never appears**. **Consequence to plan for: a red promote run now means *check whether the tag exists*, not *the board write failed*.**
- **Awareness, no action — the rest.** `board-session-close` gains a fourth read-only leg, `── Stale blocker citations ──`, reporting which OPEN card's own text still says it is blocked behind a card that has since become terminal (card#7113; advisory, never blocks the close). **`bin/gitignore-secret-family-check` is a new bin and a new CI job** (card#7036, whose commit subject names only the `.gitignore` edit; first pass #264) — it derives which of a repo's `.gitignore` rules are secret-bearing and asserts the backup FAMILY is covered, not just the name: `.env` being ignored says nothing about `.env~`, `.env.bak`, `.env.dpkg-old` or the emacs `#.env#` wrapper, each holding a byte-identical copy of the same secret with nothing red anywhere. It **fails closed** — a run deriving zero secret-bearing rules refuses at **rc 2** rather than reporting a sweep it never made — and it creates no file in the repository, because `check-ignore --no-index` answers about pathnames and so never plants the file it hunts. It joins this repo's `selftest` matrix as its own job; nothing a consumer vendors changes, but the bin is on the symlink loop's list above. ⚠ **If you hand-edit your own `.gitignore` after reading this: `#*#` is a COMMENT** — escape the leading hash (`\#*#`) or the rule silently does nothing, which is how the first pass of this fix was incomplete and what the guard caught. `bin/gh-code-search` exists because `GET /search/code` answers HTTP 200 with `total_count: 0` on a private repository for a term that certainly exists, byte-identically to a real zero (card#6884). CI's shellcheck is pinned by `.shellcheck-version` and resolved through `bin/_shellcheck-pinned`, which prints on stderr which binary ran, so a local sweep certifies the same program the gate runs (card#6619). The python entry points and CI's own syntax gate stop minting `bin/__pycache__` inside the directory you symlink onto `PATH` (card#6871, card#7207). `release-pr-body` emits its `## Card coverage` section only when it actually measured — **a generated body carrying no coverage section means the check did NOT run, not that it passed** — and the inert `title_prefix` config key is deleted (card#7038).

### v0.30.1

- **⚠ [vendor] `bin/_kb-board-lib.sh` moves as a SET with `bin/board-card-start` and `bin/next-dl`.** The one-decorated-integer rule is now a lib constant, `KB_JQ_REF_CANON`, which both bins prepend to their jq programs. A new bin beside an old lib does **not** fail at jq — it dies earlier, at `KB_JQ_REF_CANON: unbound variable` under `set -u` (measured on both bins). Same co-vendoring requirement as v0.30.0's `kb_redact_url_userinfo`; `promote-released-cards` is deliberately **not** in the set — it is vendored standalone and must not source the lib, so it carries a byte-identical inline copy that a selftest pins against the lib's.
- **⚠ ACTION IF YOU MINT CORRELATION REFS — `kbcard --pr` / `--issue` refuse three input classes they used to accept**, at **rc 2, before any request**: a value that is not ONE decorated integer (`1.5`, `2026-08-23`, `PR 12 of 34`), a **negative** value, and **zero**. Well-formed decorated values are unchanged — `85`, `#178`, `PR-085` all still pass, and an interior hyphen is decoration, not a sign. If a script pipes an unvalidated field into these flags, it now fails loudly where it previously stamped a value no reader could correlate.
- **Awareness, no action — three READERS stop answering with a fabricated ref.** `promote-released-cards` concatenated every digit run in a card's stored `dl_number` / `pr_number` / `id`, so a card holding `1.5` correlated against shipped **PR 15** — a real but unrelated pull request — and the sweep PATCHed that card to a terminal stage. `board-card-start` took the **first** run, so `1.5` answered to `DL-1` and a checkout **moved the wrong card**; it is the only over-matching member of the class. `next-dl`'s board floor scan split a multi-run stamp, so one `2026-08-23` stamp raised the DL floor to `2026` and permanently burned every number beneath it. All three now correlate to **nothing** on a malformed value, which is the fail-safe direction: an un-promoted card is recoverable by hand, a wrongly-promoted one is not. **A well-formed board is byte-identical** — proven as a differential against the pre-fix expressions, not asserted.
- **Awareness, no action — two tools stop reporting an unresolvable ref as a clean empty range.** `release-pr-body` and `promote-released-cards` each accepted an explicit `--base` / `--head` naming a ref the repo does not have and answered with an empty bundle at **rc 0**; both now refuse at **rc 2** naming the flag and the value. ⛔ The predicate peels to a commit (`<ref>^{commit}`) and that is load-bearing: `git rev-parse --verify -q` exits **0** for a full-length hex string naming no object, so the bare spelling passed a deleted-ref sha straight through. `release-pr-body` additionally resolves a **defaulted** head from `origin`, because a lagging local `dev` silently dropped PRs from the bundle **and** from the manifest that decides which cards get promoted.
- **Docs only — `kbcard search`'s usage text and `README.md` stopped claiming comment text is not searched.** It has been searched since the server added its comment arm; the paragraph also quoted a live-board hit count as its worked example, which had already drifted twice and is now removed rather than refreshed.

### v0.31.0

- **[host] re-run the `PATH` symlink loop — this release ADDS one file under `bin/`: `run-coverage-check`.** The structural reason this section gives every time: a symlink tracks its target's *content*, not the *set* of tools, so a newly added bin never reaches `PATH` without the loop. It **is** a member of `agent-board-toolkit-runtime-check`'s `TOOLS` set, so that guard prints a warn line naming it until you re-run the loop — the check doing its job, and the re-symlink it prescribes is the fix.
- **Awareness, no action — `run-coverage-check` is REPORT-ONLY and gates nothing (card#7615).** It separates *a check that never ran* from *a check that passed*, which nothing in the fleet could tell apart: GitHub creates no workflow runs at all for a PR whose merge ref it cannot compute, `mergeStateStatus` does not distinguish the two, and for a `paths:`-filtered workflow zero runs is the normal, correct state. It derives which workflows should have produced a run on a PR's head and reports each as RAN / SKIPPED / MISSING, plus UNMEASURED (something it needed was unreadable — never folded into "covered") and OUT-OF-SCOPE. `run-coverage-check --repo <owner/name> --pr <N> [--ref <git-ref>]`; needs `gh` (read scope) and `jq`; rc 0 covered, rc 1 at least one MISSING, rc 2 the run stands behind nothing. **It issues only GET requests, posts nothing, sets no status, and is deliberately not wired into any workflow and not a required status check** — making it one changes what gates a merge, which is a decision for the repo's owner. ⭐ Where your plan offers branch protection, **a required status check is the better fix and closes the class**; reach for this tool for the two cases that structurally cannot use one (no protected branches available, and a `paths:`-filtered workflow, which can never be required because a correct skip would deadlock every PR that does not touch its paths).
- **[vendor] re-vendor `bin/kbcard` for its new read surface — both additions are opt-in and no existing caller's output moves (card#7694).** `search` gains **`--brief`** (the header lines only, the *same bytes* the block form's header already carries, one renderer owning that line) and **`--count`** (one `<M> of <N>` line on stdout, no bodies); neither changes which cards match, and passing **both is rc 2 before any request**. New verb **`kbcard stages`** prints the stage id → column name map as JSON — the join `list` needs, since `list` projects `stage` as a native numeric id while every write flag takes a column NAME. ⚠ **`stages` reads THIS CALLER'S OWN CONFIG, not the server:** the pairs are the `KB_STAGE_*` variables of `~/.kanban-<board>-board.env`, no request is issued, and the ids are therefore per-install — which is the argument for reading them at run time instead of pinning one. A board env mapping **no** stage is **rc 1 with nothing on stdout** (a board-env gap, `docs/INSTALL.md` §3b — not a board with no columns), and a `KB_STAGE_*` whose value is not a stage id is **named on stderr and left off**, which includes the `000` placeholder a copied board-env template still carries.
- **[release-CI/vendor] re-vendor `bin/release-pr-body` for the new optional `version_extract_cmd` key — a config that does not set it is byte-identical (card#7599).** Set it when your version lives **inside a code file**, where `version_regex` must match surrounding context to be unambiguous: the version was resolved as the *whole first match*, so `"version_regex": "'version' => '[0-9.]+'"` rendered the version as `'version' => '0.43.0'` on a real release. The key is a repo-relative path to an executable whose **stdout is the version**, taken verbatim; empty output, more than one line, or a non-zero exit is **rc 2**, never a silent fall-back to `version_regex`. Point it at the extractor your repo already owns for its tagging workflow rather than adding a capture group — two implementations of "what version is this repo at" is the actual defect. ⛔ **Keep `version_file`/`version_regex` set even when you set this key:** `release-artifacts-check` (§6c) classifies release PRs by them and deliberately does **not** read `version_extract_cmd` — its classification compares the version at two refs, which a command running in one checkout cannot answer, and executing a script the PR under review names would hand that PR code execution inside the job judging it. `bin/release-artifacts-check` changes in **comments only** (no executable line moves) and needs no re-vendor for correctness. `docs/INSTALL.md` §4 owns when to set the key and the full trust boundary (an argv[0], not a command line: exec'd with no shell, must be inside the repo, never resolved through `$PATH`, must exist and be executable).
- **Docs only, no action — the two shipped `examples/*.env.example` files stopped asserting a token-tier ladder that v0.30.0 had already falsified (card#7616).** `kanban-host.env.example` said *"There is no tier below this"* and `kanban-board.env.example` restated the same three-tier list, both stating that a board no tier names a token file for is refused — false since card#7316 (v0.30.0) added the coord credential store's `[kanban] api_token_file` **pointer** as a fourth tier beneath them. The restated list is **deleted rather than refreshed**, and both comments now point at `docs/INSTALL.md` §3b, which owns the precedence — a copy of that list is exactly what went stale when a tier was added below the ones it named. Nothing about how your configuration resolves changes; if you copied either example before upgrading, the comment in **your** copy is still the stale one.

### v0.31.1

- **No upgrade action.** Nothing under `bin/`, `hooks/`, `promote/`, `release-artifacts/`, `release-tag-check/` or `examples/` changed at this release, so there is nothing to re-vendor and no reason to re-run the `PATH` symlink loop — the whole content is this repository's own CI plus documentation. The substance is a new **`ci-gate`** job in this repo's `.github/workflows/ci.yml` (card#8261): a single aggregating status context that `needs:` every other job in that file and runs under `if: always()`, so one context stays correct while the `selftest` matrix beneath it is edited by ordinary pull requests. **⭐ Awareness, and the part worth carrying to your own repos — the pattern, not a file:** an enumerated branch-protection `contexts` list standing in front of a matrix that pull requests edit rots in **both** directions and only one of them is loud (a name dropped from the matrix never reports again and deadlocks every PR; a name **added** to it is required by nothing at all, silently). ⛔ `if: always()` is the load-bearing half of the aggregator — without it the job is *skipped* the moment a needed job fails, and a skipped required check does **not** block a merge, so the gate would read green exactly when the suite is red. This repository's own required-check settings are an operator action on its protected branches and are not something an upgrade performs or inherits. **Also docs-only:** three comments in this tree asserted that a particular job or matrix-entry name "is a required status check on `main`" — read live and false, and each now says so; and `VERSIONING.md` step 8 names where the required-context set is declared. If you vendored none of the above, this release is a no-op for you.

### v0.32.0

- **[release-CI] ⚠ BREAKING CONFIG CHANGE — declare `.promote.source` in your `.release-pr.json`, or `promote-released-cards` refuses at rc 2 before any board read (card#8421).** There is deliberately **no default**: `dl_number` and `pr_number` are **repo-scoped** counters, so repo A's PR #15 and repo B's PR #15 are different pull requests wearing the same number, and the previous permissive default correlated on the bare number **while writing cards into a terminal stage** — on a board tracking one repo that is harmless, on a board tracking several it promotes another repo's cards. A reporting peer measured one release's 15 shipped PRs matching **24** cards across three lanes, including cards already sitting at a released stage on a different repo's version line. **Single-repo boards want `"source": "*"`** — the explicit declaration that the board tracks one repo, not a wildcard that re-opens the hole. A `--source <owner/repo>|*` override (and a matching optional `source` input on `promote/action.yml`) beats the config for one run. Values are canonicalised the way the server does (trim, cap 255, ASCII-lowercase), and a URL, an scp-style `git@host:owner/repo`, a `.git` suffix, a glob like `owner/*`, a bare name, extra path segments and embedded whitespace are each refused — every one of them a spelling that would look configured while correlating to nothing. **Nothing GitHub-legal regresses:** this narrows a CONFIG surface, not a repository name.
- **[release-CI] the three keys a release correlates on are now read for correctness, and two of the three checks are rc 2 (card#8538).** `bin/release-artifacts-check` **refuses a `promote`-declaring config that omits `promote.source`, `ref_token_regex` or `card_token_regex`** — previously rc 0 and unread, the tool's own header naming them as keys that "stay head-read and unguarded" — naming the key *and the tool that reads it*, unless the key is declared absent **by name** in a new top-level `"unset_correlation_keys": [...]` array (logged as an `UNSET:` line, exactly like `retired_artifacts`; a key both declared and listed is itself a config error, as is a name that is not one of the three). `bin/promote-released-cards` **refuses a resolved `.promote.source` that is not `$GITHUB_REPOSITORY`** (case-folded on both sides, naming the declared value, the channel it came from and the repo the run is actually in; it does not fire on `"*"`). `bin/release-pr-body` **reports an empty manifest over a non-empty range** in a new `## Correlation gaps` body section and on stderr, instead of omitting a footer indistinguishable from one that legitimately had nothing to say. **Why this is an upgrade action and not just news:** with `card_token_regex` absent, `release-pr-body` used to emit a **0-id** `shipped-cards` manifest at rc 0 under the line *"All shipped refs have a tracking card"* — a clean answer produced by never having asked. If your config omits a key on purpose, add it to `unset_correlation_keys` in the same change that takes this release.
- **[vendor] re-vendor `bin/kbcard` — every mutating verb now reads its write back, and the exit ladder gains a third outcome (card#8556).** PREVIOUSLY a mutating verb issued its write, saw a `2xx` status class, and printed a success built out of what it **sent** rather than out of a read of what the server now holds — so a mutation the server did not apply reached the caller as applied. `delete --hard` is the severe member: a false success there says a DL ref is released. The ladder is now **`0`** applied and the read agrees with what was asked · **`1`** NOT APPLIED and KNOWN (refused, or a read-back that MEASURED the mutation missing — reported as *HARD FAILURE*, quoting what the board holds) · **`2`** bad invocation, nothing sent · **`3` UNVERIFIED WRITE** — the request went out and the confirming read could not be made (did not complete, refused `401`/`403`, or a `2xx` nothing could be read out of); nothing claims it landed and nothing claims it did not. ⭐ **`3` is a THIRD outcome, not a rename of `1`:** every state that was `1` and is still knowable is still `1` — asserted in the suite, because an `rc 3` arm that swallowed a definite refusal would turn every known failure into "we cannot say", the opposite of the fix. **A caller testing `non-zero` is unaffected and needs no change.** A caller that branches on `$? -eq 1`, or that must not act on an unconfirmed write, wants an explicit `3` arm — that is the whole of what this asks of you. The population of mutating verbs is not a figure in this entry: `tests/readback-before-success-census.sh` re-derives it.
- **[vendor] re-vendor `bin/kbcard` for its new `unlink` verb as well — `kbcard unlink --link-id N --on ID-OR-EXT` (card#8545).** The toolkit could CREATE a card link and never remove one, so a mislinked relation was **permanent** with the shipped tools: a `blocks` link later ruled to owe no work is a blocker the blocked work cannot discharge, and every gate count derived from links stays wrong by one, forever — the only remedy on offer being prose alongside the link contradicting it. The verb reports a removal **only when it has read the link's absence back**; the API's `204` is the server's claim, not the removal.
- **[vendor] re-vendor `bin/adopt-to-dl` — a narrowed `--repo` accept set, and a corrected message on the new rc (card#8421, card#8556).** `--repo` is now judged by `kb_is_repo_slug` (charset **and** shape) instead of a shape-only `^[^[:space:]/]+/[^[:space:]/]+$` that accepted anything carrying one `/` and no whitespace — measured, **16 further spellings**, including `git@github.com:acme/widget`, `acme/*`, `acme/widget.git`, a non-ASCII name, and any name carrying `+ : ~ @ % , = ! ' " $` or a backtick; all are now refused at rc 2 before anything is stamped, and every one of them already failed a layer later and less legibly. Separately, `adopt-to-dl` stops saying *"card not adopted"* on a `kbcard` rc that means nobody read the board: on `3` it names the stamp UNVERIFIED, says the DL is already claimed, and points at the idempotent re-run — the old sentence would have sent an operator to re-adopt a card that may already carry the stamp.
- **[vendor] re-vendor `bin/run-coverage-check` — `--repo owner/name.git` is refused at rc 2 (card#8421).** PREVIOUSLY accepted by a bare shape regex, interpolated into a `repos/<slug>/…` request path, and answered **404** by GitHub. NOW refused at the flag, before any request is made. `.git` is a git-remote suffix and is never part of a repository name in a REST path, so every value this refuses already failed.
- **[host] ⚠ `agent-board-toolkit-runtime-check` can now go rc 1 on a seat that upgraded cleanly — that is the new check firing, not a regression (card#8376).** v0.31.0's coord-store credential rung stopped a seat from minting a *second* plaintext token copy, but shipping the rung does not DELETE the copy an operator already made: both kept working, this very check reported healthy, and the shadowed one is a live credential nothing rotates. The check now judges the seat's **credential topology** beside its runtime topology — resolving the ambient `KBCARD_TOKEN_FILE`, `~/.kanban-host.env`, every `~/.kanban-*-board.env` and the store's `[kanban] api_token_file` pointer under the documented precedence, **plus the literal glob `~/.kanban-*-token`**, which is what makes it true of the state [`INSTALL.md`](INSTALL.md) §3b promises it catches (migrating onto the pointer means dropping the line that named the old copy, so the leftover file is declared by nothing). Files are compared **by digest over the content their READERS see, not their raw bytes** — every consumer takes a token file through `$(cat …)`, which strips trailing newlines, so §3b's `printf '%s'` recipe and the same secret an editor terminated are ONE credential. **The same credential in two files ⇒ `✗` and rc 1**, naming the file to delete, the file to keep (the store's pointer wins) and the declaration to drop. **A readable token file no board resolves, holding a different token ⇒ `⚠` and rc 0** — reported, not gated, because its innocent twin is a supported install. Your action is to act on the line: delete the shadowed copy and drop the declaration that named it. A `~/.kanban-dev-token.bak` is outside the glob and is not reported.
- **[host] no `PATH` symlink loop is owed on the usual ground — this release adds NO file under `bin/`.** Re-running it is harmless and never wrong, but a symlink install picks up every change above from its target's content. The re-vendor items are for repos that took a **copy** of a tool (INSTALL §6b), which is the topology this section's `[vendor]` tag addresses.
- **Awareness, no action — a comment is not a guard, and four vendored standalones were held to the library by one (card#8529).** `docs/CONSOLIDATION-PLAN.md` § Stage D *decided* against consolidating the tools that are vendored standalone and must not source `bin/_kb-board-lib.sh`, choosing **guarded duplication** — but two guard families had no comparison at all, so a fix landing in one copy and missing its twin shipped a guard that was right in the tool and wrong in the lib **with a green suite, because nothing compared them**. `tests/mirror-pair-parity-selftest.sh` now drives those families row by row against the lib, with the copy set **derived from the tree** so a fifth copy is driven the day it lands, and each block carries a control on a **real** `bin/` copy mutated in memory rather than on a fixture the test wrote. One false sync claim is corrected in the process: `promote-released-cards`'s `uint_ok` said it mirrored `kb_is_uint`, and the two have **never** agreed — `kb_is_uint` refuses a leading zero and the glob accepts one, so `007` was a board id in one and not the other. ⭐ **The carry-away for your own repos is the shape, not the file:** where you have decided on duplication, the decision owes a test that compares the copies; the comment that asks a future reader to keep them in sync is the thing that failed.
- **Docs and test-only, no action:** three shipped surfaces described `tests/prelude-shadow-selftest.sh`'s reach in their own words and all three were **wider than its predicate** — it compared *names*, so a seventh hand-spelling under any other name left it at rc 0 (card#8548); `tests/changelog-card-entry-selftest.sh` told a branch cut **before** a release fold that its entry was missing when the 3-way merge had cleanly moved it under the folded `## [X.Y.Z]` heading, and an author who believed it shipped a duplicate — it now names the move instead (card#8442, and ⛔ the right answer to that red is to move the bullet back, never to file a second one); four wall-clock assertions started their clock in the **test**, so the tool's own startup sat inside the interval being judged — measured at **21 of 30 runs red** on a loaded host, which reported the box as a defect in the tool (card#8485, card#8533); and `.github/pull_request_template.md` gained the `Built:` and `Coordinated in:` fields the coord plugin declares required on every impl-repo PR body, as a pointer to the owning section rather than a copy of its semantics (card#8536).

### v0.33.0

- **[vendor] re-vendor `bin/kbcard` — it is the ONLY vendorable file this release moves (card#9169, card#9171, card#9173, card#9213).** Nothing else under `bin/`, `hooks/`, `promote/`, `release-artifacts/` or `release-tag-check/` changed, and the `bin/` **set** is byte-for-byte the same set as at v0.32.0 — **no new tool, so no `PATH` symlink loop is owed** and `agent-board-toolkit-runtime-check` gains no member in its `TOOLS` set. A symlink install picks every change below up from its target's content; the re-vendor is for a repo that took a **copy** (INSTALL §6b).
- **[host] ⚠ `kbcard stages` is no longer safe to run with stderr merged into stdout (card#9171).** `cmd_stages` now emits a provenance line on stderr on **every successful call**, so `kbcard stages 2>&1 | jq .` dies at `jq: parse error` on line 1 where it previously parsed. **stdout is byte-for-byte unchanged** — redirect stderr separately (`2>/dev/null`, or to a file) wherever you pipe the JSON. The reason the line exists is the reason it could not go on stdout: the verb issues **no request** and names each stage by its `KB_STAGE_*` variable's own suffix, lowercased, so `KB_STAGE_HELD=102` prints `{"id":102,"name":"held"}` whatever the board calls stage 102 — a peer seat wrote a board-resolved sprint filter keyed on this install's word for a column and got an empty lane that read as a finished sprint. Widening stdout with a caveat key would have broken the `{id, name}`-and-no-other-key join `list`'s `stage` projection depends on, in order to warn the one consumer that never reads the help. The rc-1 no-stage-mapped path does not emit it.
- **[host] OPTIONAL — declare `KB_USER_<SEAT>=<kanban user id>` in each board env to address seats by NAME (card#9169).** The new `kbcard patch --assign <seat|user-id>` resolves a seat name through this map, and `list`/`show` render the seat name for a mapped id. Name-keyed with the id as the value, the same shape as `KB_STAGE_*`/`KB_TYPE_*`; a seat name carrying a hyphen is declared with **underscores** (an env-var suffix cannot hold one) and both spellings resolve. Find the ids at `GET <api-base>/users.json` → `.data[] {id,name}`. **Without these vars you lose the NAME, not the feature** — `--assign` still takes a numeric user id and the read verbs still print the raw `assigned_user_id`. ⛔ An **unresolvable seat name is rc 2 before any request**, never a silent no-op, and `kbcard` looks nowhere else for this map: it is a standalone CLI and never reads another product's agent config. `examples/kanban-board.env.example` carries the commented block.
- **[host] a board token whose role lacks `task.update` now has a way to fail that is worth recognising (card#9169).** Writing an assignee needs that permission; a token without it gets an HTTP policy refusal **every time**, so retrying or editing the card changes nothing. The tool names it as an INSTALL FAULT rather than a card problem and points at INSTALL §3b — fix the role/token for that board.
- **Awareness, no action — two read projections WIDEN (card#9169).** `kbcard list` now projects `assigned_user_id` and `assignee` per card, and `kbcard show` gains an `assignee` annotation. Both are **additive**: `assigned_user_id` is always the board's raw id (null when unassigned), while `assignee` is the seat name only where this board env maps that id — on `show` an unresolvable annotation is **omitted rather than emitted null**, because an emitted `"assignee": null` on a card that IS assigned reads as "nobody is working this". A consumer selecting named keys is unaffected; one asserting an exact key set is not.
- **Awareness, no action — `kbcard patch` can refuse where it previously could not, but only on the new flags (card#9169, card#9213).** `--assign` over a **different** seat's live claim is **rc 1 with the holder NAMED and NO PATCH ISSUED** — that refusal is the point of the flag, since a card whose stage never moved is indistinguishable from an unclaimed one and two seats pull the same work. `--steal` is the opt-out and says whose assignment it took; `--unassign` is deliberately not gated the same way, because `kbcard` has no notion of which seat is running it and gating it would make `--steal` routine. `--block-reason "…"` / `--block-reason-file PATH` write the board's top-level `block_reason` — the field a sprint burn-down's "Blocker / next action" column renders and that **no writer has ever set**, not this CLI and not the web UI, so that column rendered blank on every row and read as "nothing is blocked" — and **`--unblock` clears it**. Every one of these is a NEW flag: an omitted flag leaves the field exactly as it was, and no existing invocation changes shape or exit code.
- **Test-only and docs, no action:** `tests/kbcard-contract-selftest.sh` is new and guards the `stages` contract copies in `bin/kbcard`'s help and in `README.md` **against the executable**, not against each other (card#9173 — the card#9171 fix had already updated the bin and left README describing the pre-change output, and nothing mechanical could have caught it: `help-output-selftest.sh` asserts help structure and never opens README, `readme-bin-coverage-selftest.sh` asserts a README row exists rather than that any prose agrees with anything, and both were green over the drifted tree). `docs/INSTALL.md` §2 stopped asserting that the coord plugin's `install-linked-bin.sh` leaves its source glob unguarded — read live on plugin **v0.50.0**, that arm carries a `[ -f ]` filter and names every skipped entry, and the cited card is released; ⭐ the reason it mattered is that a peer repo had **sourced a hazard of its own from this document**, so a stale doc here was manufacturing findings elsewhere. §2's own `[ -f "$t" ]` and `-n` **stay** — the recipe is run by hand and inherits nothing from that arm.

### v0.34.0

- **[vendor] re-vendor every tool this release changes, and copy `bin/_kb-board-lib.sh` TOGETHER with any lib-sourcing bin you vendored.** The changed set under `bin/` and `hooks/` is `bin/_kb-board-lib.sh`, `bin/board-card-start`, `bin/board-stats`, `bin/gh-code-search`, `bin/kbcard`, `bin/promote-released-cards`, `bin/release-artifacts-check`, `bin/release-pr-body`, `hooks/agent-dispatch-card-start` and `hooks/pre-push` — re-print it with `git diff --name-only v0.33.0 v0.34.0 -- bin/ hooks/`. The lib gains functions that the changed lib-sourcing bins now call (`kb_is_blank`, card#9337; `kb_owner_tag_write`, `kb_card_tags` and `kb_coord_config_path`, card#9436), so a v0.34.0 bin beside a v0.33.0 lib fails with `<function>: command not found` the first time it reaches one — on that code path, not at startup, so a smoke test that never reaches it passes. Which bins source the lib is derived by the §3 box above, not listed here. `promote-released-cards`, `release-pr-body` and `release-artifacts-check` stay standalone. **The `bin/` set is unchanged** — every path above is a modification, none is added or removed — so **no `PATH` symlink loop is owed** and `agent-board-toolkit-runtime-check` gains no `TOOLS` member; a symlink install picks everything below up from its target's content.
- **[vendor] re-vendor `bin/release-pr-body` on this release and on EVERY later one (card#9335).** It now carries a line-initial `ABTK_TOOL_VERSION='<version>'` stamp that each release re-stamps, so `agent-board-toolkit-drift-check` reports `DRIFT bin/release-pr-body` on any bump that does not re-copy it, even when the release did not otherwise change the tool. The new `release-pr-body --tool-version` prints the toolkit version the file itself is, from a copy with no checkout beside it. **Bootstrap bound:** every copy older than this release refuses the flag as `unknown arg '--tool-version'` at rc 2 — that answer means "older than v0.34.0", never "current". `--version` (the input naming the release) is unchanged.
- **[host] [vendor] ⚠ OUTPUT CONTRACT CHANGE — `release-pr-body`'s stdout is now the PR body and nothing else (card#9248, DL-224).** `## Correlation gaps` and `## Card coverage` are no longer body sections: the same content arrives on **stderr** as `release-pr-body: correlation gap: …` and `release-pr-body: card coverage: …` lines, and a successful run ends stderr with one `announce:begin` … `announce:end` block (version, tag, SHA-pinned range, shipped refs and card ids). A coverage line still appears **only** when the check could run, so a run with no coverage line did not measure coverage. **Do, in this order:** (1) **keep the streams apart** — `release-pr-body … > body.md 2>&1` now writes the diagnostics and the announce block INTO the PR body; (2) **keep stderr open** — `2>&-` fails the run at rc 1; (3) switch anything that read `## Correlation gaps` or `## Card coverage` out of a generated body to the prefixed stderr lines. Readers known to go stale when they consume this release, each owned by its own repo: `agent-webhook-bridge`'s `VERSIONING.md` (the release-flow card-coverage table) and `kanban-board`'s `VERSIONING.md` (the release-checklist card-coverage bullet); card#9248 also names the coord plugin's `skills/release-pr/SKILL.md` (`grep -n 'leg A'` over that file re-prints the passages, if your installed version still has them) and the comment above `ALLOWED_H2` in its `pr-body-lint.py`. The body also stops saying *"no card can be promoted from this release"* when both manifests are empty — `promote-released-cards` never read a manifest, so that sentence was false.
- **[host] OPTIONAL but the lint speaks until you do — seed `KB_CARD_ID_FLOOR` in each board env (card#9342, DL-223).** The `pre-push` hook's `board-card-start --lint` now reports a branch whose card id is **below** the board's floor — most likely a GitHub issue/PR number, which moves no card on checkout and closes none at merge. It never blocks a push, leaves the exit status unchanged and reads no board. **Until a board is seeded, every push of a branch carrying a card id prints `card-id floor not seeded`** — deliberately; a repo whose board cannot be resolved, a board id with no board env, and a malformed seed each print a `card-id floor NOT CHECKED` line naming the missing piece. **Do:** `export KB_CARD_ID_FLOOR=<at or below the board's lowest card id, archived cards included>` in each `~/.kanban-<name>-board.env`; `docs/INSTALL.md` §3b owns the derivation and its limit — `kbcard list` returns live cards only, so its minimum is an **upper bound** on a safe seed, and a lower card archived at seeding time and unarchived later would be accused, so seed below any archived card you know of and never round up. The floor is a magnitude heuristic: too LOW a seed yields false cleans, never a false accusation. A seat whose hooks were installed as copies (`install-board-hooks --allow-copies`) re-runs `install-board-hooks <repo>` to receive the new `pre-push`.
  > ⛔ **SUPERSEDED — the floor leg is gone (card#9570 — see its `[host]` entry in the section below this one, for the release that ships it).** Do not seed `KB_CARD_ID_FLOOR`: nothing reads it once you are past this release. The bullet above is kept as the record of v0.34.0.
- **[host] the card-start hooks now stamp the seat owner tag `owner:<project>/<seat>` (card#9436).** `bin/board-card-start` (post-checkout) and `hooks/agent-dispatch-card-start` (through the new `kbcard move --stamp-owner`) stamp it in a separate tag write **after** a confirmed In Progress move; the move itself is still a stage-only `PATCH`, so a refused tag write never refuses the move. **Do:** make `COORD_AGENT` — and `COORD_CONFIG` unless the coord config is at its default path — present in the environment the hooks run in (`docs/INSTALL.md` §3c has the one-line check), and give every install its own distinct `project` value where installs share a board. Without them cards still move, unstamped, and each move says why. A hook paired with a `kbcard` older than `--stamp-owner` says so and retries the move without the flag. README § The seat owner tag owns every case in which no tag is stamped.
- **[host] [vendor] ⚠ BEHAVIOUR CHANGE on terminal `kbcard` moves (card#9436).** `released_to_main` is now terminal, beside `shipped_to_dev` and `wont_do`. A `move --column` / `patch --column` to any of them **clears `assigned_user_id` in the move** — new for `released_to_main`, and a move carrying it needs the token's update permission — then reads the card and removes every `owner:*` tag in a separate `PATCH` when it carries one. `--stamp-owner` beside a terminal column is rc 2 before any request.
- **[release-CI] ⚠ `promote-released-cards` now removes the owner tags from every card it moves (card#9436)** — a fresh card read per moved card, and a `PATCH {tags}` for each one carrying an `owner:*` tag. **A promote token without update permission gets a printed refusal for each such card**; the move, the counts, the summary line and the exit code are unchanged. Composite-action consumers receive this through the pin bump (`promote/action.yml` itself is unchanged); a vendored copy re-vendors.
- **[release-CI] `promote-released-cards` refusal lines change wording (card#9301).** A move the board refuses now reads `move failed (left in place) — HTTP <status>, server said: <excerpt>` (the bearer token masked before the excerpt is bounded), and the board read's refusal carries the same detail; curl's separate, card-less `curl: (22) …` line is gone. **A request that never completed now reads `move NOT CONFIRMED`** instead of `move failed (left in place)`, because the server may have applied it — re-read the card before re-running or hand-moving it. **Which answers count as success did not change** (the boundary stays 400; a 3xx still passes through as before, and a 503 is still retried). Update anything that greps promote logs for `curl: (22)` or treats every `left in place` line as the whole failure set.
- **[host] [vendor] ⚠ ACCEPTANCE CHANGES in `kbcard`, each asked and granted — a wrapper that ignores `kbcard`'s exit status will now report success and change nothing, so check the status.**
  - **A visually blank `--name` / `--description` is rc 2 before any request, on the inline half as well as the file half (card#9222).** A blank `--description` used to **CLEAR** the field at rc 0; the deliberate clear is now **`patch --clear-description`** (an explicit JSON `null`; mutually exclusive with both halves of the `--description` pair; `create-card` does not take it). A blank `--name` moves from an rc 1 HTTP 422 to rc 2. Any value with text is byte-identical on the wire.
  - **A visually blank `--origin` / `--version` / `--pr-url` / `--issue-url` is rc 2 before any request (card#9338).** It used to be rc 0 and **clear** the field — for `--pr-url` / `--issue-url` also detaching the card from its repo's by-ref `source`, after which a repo-qualified promote skips it. (A card carrying a `payload.repo` that is a string containing `/` is unaffected: it outranks every URL, so the source survives the clear and the promote still sees it.) The deliberate clear is now **`patch --clear FIELD[,FIELD…]`** over `origin`, `version`, `pr-url`, `issue-url` (card#9420) — refused rc 2 for an unknown field, for a field named beside its own setter, and for a second `--clear`.
  - **A visually blank `--tags` on `create-card` / `patch` is rc 2 before any request (card#9421).** On `patch` it used to be sent and **replace the card's whole tag list**. A blank *member* (`"a, ,b"`) is not refused and still goes out as its own element.
  - **"Blank" is exactly six ASCII characters — space, tab, CR, LF, VT, FF — decided under `LC_ALL=C`, in one lib predicate (card#9337).** On a UTF-8 seat, a U+2003-only or U+3000-only value at `kbcard search`, `board-stats --since` and `gh-code-search <query>` moves from refused to **content** (the one widening; the verdict is now the same on every seat). A `\v`-only or `\f`-only value on `kbcard`'s free-text flags is rc 2 on every seat. A non-ASCII-blank-only value (U+00A0, U+2003, U+3000) is content and is sent.
  - **`--origin`, `--version`, `--pr-url` and `--issue-url` are sent as JSON strings exactly as typed (card#9423).** Previously a value with text was rewritten at rc 0: `1.10` went out as a number, `nan` as `null` (a **clear**), a leading newline as `""` (a clear), `"a\r\nb"` as `"a\r"`, and `"foo\nversion_target=evil"` **wrote an extra key**. On a board that declares `version_target` or `origin` as `number`, a non-canonical numeric string (`00123`, `1e3`) is now refused by the server where it used to land as a number. `--pr` / `--issue` keep their typing, but a value containing a newline is **no longer split at it** (it stays inside that key), and a ref is trimmed of surrounding space/tab/CR/LF **before the number attempt only**, so `" 123 "` goes out as `123` on jq-1.7 and jq-1.8 alike (measured on jq-1.7 and jq-1.8.1; jq-1.6 not measured). `--dl` is unchanged.
- **[host] [vendor] `kbcard` stops reporting a landed write as `UNVERIFIED WRITE` (rc 3) on a card past 131,072 B, and `kbcard archive` runs its live-source gate on one (card#9482).** A long-lived card that accreted comments hit Linux's single-argument cap in `jq`, so `comment` / `archive` / `delete` returned rc 3 for writes that landed — the answer a caller re-posts on — and `archive` without `--force` died before its gate. Exit codes and gate policy are unchanged. Two malformed-body diagnostics change, both still refusals: a re-read carrying two JSON texts is still rc 3 under `kbcard`'s own message, and under `archive` such a card is now refused as `noprimitive` (*archive withheld*, rc 1) before the board is fetched.
- **Awareness, no action — new opt-in flags beyond the clearers above:** `kbcard move --stamp-owner` (card#9436) and `release-pr-body --tool-version` (card#9335). An invocation that omits them is unchanged.
- **Docs, template and test-only, no action:** `.github/pull_request_template.md`'s `Built:` / `Coordinated in:` rows are now unbulleted at column 0 with unquoted values, because the coord plugin's line-anchored plane-1 audit read the bulleted rows as ABSENT — a repo that copied that block copies the fix; no gate reads the template (card#9344). `release-artifacts/action.yml` changes in description only, and `bin/release-artifacts-check` rewords two refusal-row texts and comments with no verdict change (card#9248). `examples/kanban-board.env.example` gains the commented `KB_CARD_ID_FLOOR` block. §6's intro stops restating its own coverage span (card#9343); `tests/board-stats-selftest.sh` no longer reds under a C/POSIX locale; README documents that a `kbcard` move `PATCH` is stage-only unless the mover adds a key; and the coord orientation block is re-synced to plugin v0.52.0.

### v0.35.0

- **[vendor] ⚠ re-vendor every tool this release changes, and copy `bin/_kb-board-lib.sh` beside every lib-sourcing bin among them — the lib gains `kb_card_pinned` and `kb_card_start_stage_verdict`, and the changed lib-sourcing bins require both (card#9556, DL-226; card#9756).** Print the changed set with `git diff --name-only v0.34.0 v0.35.0 -- bin/ hooks/`; which bins source the lib is derived by the §3 box above, not listed here. Paired with a v0.34.0 lib, those bins refuse rather than guess, and write nothing: `kbcard move --card-start` exits `rc 2` with a line naming the missing function and its rc, and `board-card-start` puts the same kind of line in its durable log and exits 0 — so after a partial re-vendor a checkout moves no card and stamps nothing, and a dispatch hook calling such a `kbcard` moves no card. **The `bin/` set is unchanged** — `git diff --name-status --diff-filter=AD v0.34.0 v0.35.0 -- bin/` prints nothing — so **no `PATH` symlink loop is owed** and `agent-board-toolkit-runtime-check` gains no `TOOLS` member; a symlink install picks everything below up from its target's content.
- **[vendor] upgrade `bin/kbcard` before, or together with, `hooks/agent-dispatch-card-start` (card#9556).** The hook now moves a card only through `kbcard move --card-start`, and unlike `--stamp-owner` it **never drops that flag to get the move through**: paired with a `kbcard` older than the flag, it prints a line naming the upgrade and leaves the card where it is.
- **[vendor] re-vendor `bin/release-pr-body` on this release, as on every one (card#9335).** Its line-initial `ABTK_TOOL_VERSION` stamp is re-stamped each bump, so `agent-board-toolkit-drift-check` reports `DRIFT bin/release-pr-body` on any bump that does not re-copy it, even when nothing else in the tool changed.
- **[host] ⚠ every board env the dispatch hook runs against must map `KB_STAGE_BACKLOG` and `KB_STAGE_PRIORITIZED` (card#9556, DL-226).** `kbcard move --card-start` resolves both stage ids **before any request** and refuses at `rc 2` when either is missing — a guard that cannot be applied refuses rather than falling through to the unguarded move it exists to remove — so a board env missing either one stops moving cards the hook moved before this release. The hook relays kbcard's `column '<name>' is not defined on this board` line, which names the key to add to `~/.kanban-<name>-board.env`. A board that maps no `held` stage is unaffected.
- **[host] ⚠ BEHAVIOUR CHANGE — the dispatch hook moves a card to In Progress only from Backlog or Prioritized, and never a pinned one (card#9556, DL-226; card#9756).** A card that is Held, already In Progress, In Review, Shipped, Released or declined, or pinned (a non-empty `block_reason` or a `no-automove` tag), is left where it is: `rc 0`, nothing written — no move and no seat owner tag — and a `kbcard: move --card-start` line on stderr saying why, which the hook relays. Re-dispatching work on a card already In Progress therefore no longer re-stamps the owner tag. A card that could not be READ is `rc 1` with nothing written, never a policy refusal. The guard judges the card's pin and stage only, not which board the id belongs to. **The guard's card read runs inside the same per-move `KBADS_TIMEOUT` bound (default 10 s) as the move and the owner-tag write**, so a slow board that used to finish inside it can now be killed — `timeout` exits 124 and the seat sees only the generic `kbcard move failed` line; on a board you know is reachable, raise `KBADS_TIMEOUT` first.
- **[host] a branch that carries a card id and was last checked out BEFORE this release has no board-verdict record, so every push of it prints `board verdict NOT RECORDED` until you check it out again (card#9568, DL-225).** No config is owed. `git checkout <branch>` re-fires `post-checkout` even on the branch you are already on, and that writes the record — under `$(git rev-parse --git-common-dir)/agent-board-toolkit/board-verdict/`, never tracked, shared by linked worktrees. A record is written only when a checkout's `post-checkout` reaches the on-`PATH` `board-card-start`, so re-check each checkout's hook install; `board-session-close`'s git-hook dispatch check reports one that does not. The lint never blocks a push and its exit status is unchanged. A seat whose git hooks were installed as copies (`install-board-hooks --allow-copies`) re-runs `install-board-hooks <repo>` to receive the new `pre-push`.
- **[host] `KB_CARD_ID_FLOOR` is now ignored — delete it from each `~/.kanban-<name>-board.env` when convenient; nothing breaks if it stays (card#9570; DL-225 supersedes DL-223's floor leg).** The `pre-push` lint's card-id floor leg is removed, and the board verdict recorded at checkout (card#9568) is the only id-space signal at push: `board-card-start --lint` no longer reads the key and prints no `card-id floor …` line (`not seeded`, `NOT CHECKED`, `BELOW`), nor the ` Meanwhile the card-id floor leg (…): …` suffix after an undecided board-verdict line. This reverses v0.34.0's seed instruction above — do not seed the key on a new board. `examples/kanban-board.env.example` drops its `KB_CARD_ID_FLOOR` block.
- **Awareness, no action — a new opt-in flag:** `kbcard move --card-start` (card#9556) applies the work-start guard above to any caller that passes it; beside any column but `in_progress` it is `rc 2` before any request. Every `kbcard move` invocation that omits it is unchanged.
- **Awareness, no action — `kbcard comment`'s `UNVERIFIED WRITE` diagnostic changes wording (card#9714).** It no longer says a comment leaves the card's `updated_at` untouched; the verb verifies a comment write from the echoed comment id, as before. Its rc and control flow are unchanged — update only something that matches the old text.

### v0.35.1

- **[vendor] ⚠ re-vendor `bin/kbcard`, `bin/adopt-to-dl` and `bin/_kb-board-lib.sh` TOGETHER — a re-vendor that misses the lib refuses EVERY `kbcard patch` and EVERY adoption (card#9846).** Print the changed set with `git diff --name-only v0.35.0 v0.35.1 -- bin/ hooks/`; which bins source the lib is derived by the §3 box above, not listed here. The number/URL pair check both bins now call lives in the lib (`kb_ref_pairs_alone` / `kb_ref_pair_verdicts`, with the constant `KB_JQ_REPO_FROM_GH_URL`) and its preflight runs on **every** `patch`, whatever the call writes, so a new bin beside a v0.35.0 lib is `rc 2` on every `kbcard patch` and `rc 1` on every `bin/adopt-to-dl` run — nothing written, no DL minted, and a line naming the missing function. `bin/promote-released-cards` is unchanged; its `repo_from_gh_url` is the text the lib's constant copies, held identical by `tests/mirror-pair-parity-selftest.sh` § 5. **The `bin/` set is unchanged** — `git diff --name-status --diff-filter=AD v0.35.0 v0.35.1 -- bin/` prints nothing — so **no `PATH` symlink loop is owed** and `agent-board-toolkit-runtime-check` gains no `TOOLS` member; a symlink install picks the changed content up from its target.
- **[vendor] re-vendor `bin/release-pr-body` on this release, as on every one (card#9335).** Its line-initial `ABTK_TOOL_VERSION` stamp is re-stamped each bump, so `agent-board-toolkit-drift-check` reports `DRIFT bin/release-pr-body` on any bump that does not re-copy it, even when nothing else in the tool changed.
- **[host] ⚠ ACCEPTANCE CHANGE, operator-approved 2026-09-18 — `kbcard patch` that writes ONE half of a number/URL pair now reads the card first and REFUSES at `rc 2`, with nothing written, when the stored other half names a different ref (card#9837, card#9846).** Both pairs and both directions: `--pr N`, `--issue N`, `--pr-url U` and `--issue-url U`, each sent without its counterpart. Before this release those four wrote at `rc 0` and left the card naming one ref by number and another by URL — and the board derives a card's by-ref `source` from the URL, not the number, so `promote-released-cards` in the repo the stale half names correlated on it. **The fix in your scripts is to pass both halves**, e.g. `--pr <N> --pr-url https://github.com/<owner>/<repo>/pull/<N>`. A refusal names the missing flag, the derived `owner/repo` and the number, and never echoes a stored or given URL. Proceeds unchanged and needs no edit — the whole set, which `docs/CHANGELOG.md` [0.35.1] card#9846 owns in full: both halves in one call; the counterpart `--clear`ed in the same call (a key present as null replaces the stored half, so nothing is left to diverge from); no stored counterpart; a counterpart naming the same number; a STORED `…/pull/0` / `…/issues/0` placeholder; and a `wont_do` decline without `--keep-refs`, which nulls both `pr` keys; and a STORED number of `0` in any spelling (it names no ref, so no URL written over it diverges from anything — `kb_ref_pair_verdicts`' own `stored-zero` arm, separate from "no stored counterpart"). Two cases proceed but are no longer silent: a stored or given value that is not a parseable GitHub PR/issue URL, and a stored number that names no one number, each now print a notice saying the check could not be made. A card that cannot be READ is `rc 1` with nothing written — a new failure mode for a call that previously issued no read. `kbcard create-card` is untouched. **Two arms are narrower than "different number" and are the ones most likely to surprise a working script** (operator rulings, card#9846): a GIVEN `…/pull/0` / `…/issues/0` placeholder over a stored real number is refused, and a `commit` / `tree` / `blob` URL on either side, stored or given, over a real number on the other is refused — card#9837 wrote the stored-side case with a notice.
- **[host] `bin/adopt-to-dl` now refuses (`rc 1`) a card whose `pr_number` already names a real PR, BEFORE it mints a DL — on the `--dl N` retry path too (card#9846).** Its stamp is `--dl … --pr-url <…/pull/0>`, which is exactly the write refused above, and a refusal at the stamp would orphan the DL `next-dl` had already claimed. A card with no `pr_number`, a `0`, or one naming no single number adopts as before.
- **Awareness, no action — the pair-guard refusal MESSAGE TEXT is shorter than the one v0.35.0 never shipped and than card#9846's own first wording (card#9918, card#9935).** The clauses explaining who the board would attribute the card to and what a release would promote are gone from every refusal arm of both pairs, on an operator ruling: a refusal states what is refused and what to pass. Every verdict, exit code, request and notice arm is unchanged. ⛔ **A caller that matched a refusal on wording past the flag names matches nothing now** — `attributes the card to`, `would promote it` and `move its by-ref source` are no longer printed; match the flag names and `NOTHING WAS WRITTEN` instead. Nothing in this bullet needs action on a seat upgrading from v0.35.0, where none of these messages existed.
