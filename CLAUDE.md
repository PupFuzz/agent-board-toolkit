# agent-board-toolkit

Repo-specific SME knowledge lives here (add as it accumulates).

## Releasing

Versioning + release process: see [`VERSIONING.md`](VERSIONING.md). Full per-version log: [`docs/CHANGELOG.md`](docs/CHANGELOG.md). Release PRs are cut with the `coord:release-pr` skill; the deterministic body generator + card-promotion tooling read [`.release-pr.json`](.release-pr.json) (board 12).

## Recent releases (last 10)

Filled in by the release PR that produces each version tag — which also **trims the oldest row back to 10**. This table is the ergonomic snapshot, deliberately truncated; [`docs/CHANGELOG.md`](docs/CHANGELOG.md) is the canonical per-version log and is **never** truncated, so nothing is lost by trimming here.

| Version | Date | Highlights |
| --- | --- | --- |
| v0.36.0 | 2026-09-24 | **Minor — `kbcard` gains a `reorder` verb for the board's rank-within-a-column route that nothing in the fleet could reach, a release promote can be told to REFUSE a card whose work is not finished (new bin `bin/card-completeness`), a checkout stops moving a card in a repo that did not ASK for it, and three movers stop reporting a write they never read back.** The PRs in `v0.35.1..v0.36.0` are enumerated in `docs/CHANGELOG.md` [0.36.0] and in the release PR's generated `## Bundled` table. Per `VERSIONING.md` § Bump sizing this is a **minor**: a new `bin/` tool, a new `kbcard` verb and new flags on two tools. **⚠ UPGRADE ACTIONS — all in `docs/UPGRADE.md` §6 v0.36.0:** **[host] [vendor] RE-RUN THE `PATH` SYMLINK LOOP** — this release ADDS a bin (`git diff --name-status --diff-filter=AD v0.35.1 v0.36.0 -- bin/` prints it) and `agent-board-toolkit-runtime-check` gains `card-completeness` as a `TOOLS` member, so it warns until you do. **[vendor]** re-vendor the changed set (`git diff --name-only v0.35.1 v0.36.0 -- bin/ hooks/`) and copy `bin/_kb-board-lib.sh` WITH any lib-sourcing bin — the lib gains `kb_confirm_card` and `board-card-start` asks for it **before it writes anything**, so beside a v0.35.1 lib a checkout moves no card and stamps no `dl_number`, exits 0, and says so only in `~/.cache/agent-board-toolkit/board-card-start.log`; vendor `bin/card-completeness` beside `bin/promote-released-cards` if you use `--require-complete`; re-vendor the stamped `bin/release-pr-body` as on every bump (card#9335). **[host] ⚠ BEHAVIOUR CHANGE (card#9845):** `hooks/post-checkout` moves a card only in a repo armed with `git config kanban.automove-on-checkout true` — **unset means OFF**, so a symlink host disarms every repo the moment it pulls this release; an unarmed repo also stops recording the board verdict and stops stamping `payload.dl_number`, which is release-grade (the DL-correlated movers then silently no-op on that card). **[release-CI] ⚠ BREAKING (card#10176):** a `promote/action.yml` boolean input spelled anything but `true`/`false` now refuses at rc 2 — grep consuming workflows for `dry-run:` and `require-complete:` and lower-case them BEFORE re-pinning; a workflow passing `dry-run: 'True'` is making real board writes today. **[release-CI] ⚠ (card#9938):** `promote-released-cards` rules a move from a READ-BACK, so one un-applied card among 27 promoted ones turns a green step RED at `rc 1`, and the new `rc 3` UNVERIFIED is not a failure — read `promote-released-cards --help` § EXIT CODES, now the one published statement of the ladder. **[host] ⚠ (card#10230):** `bin/next-dl` refuses at `rc 1` where it used to mint a DUPLICATE DL — an undecodable `2xx` or a dead transport on the consuming claim, and an offline mint against a board holding a card whose `payload` is not a JSON object; under `--require-counter` the two CLAIM-ROUTE inputs move `rc 4` → `rc 1` because they abort above the degrade decision, while the payload one is `rc 4` before and after (it sits below that decision, so strict never reaches the scan), and `--peek` is untouched. **Added:** `kbcard reorder` writes `position` and only `position` and rules on the collection the board returns (card#10346); `promote-released-cards --require-complete` + its oracle, opt-in and byte-identical when absent, needing `pull-requests: read` (card#10176). **Fixed:** `board-card-start` and `promote-released-cards` report their writes from a re-read, not from the `2xx` (card#10029, card#9938); the changelog gate's duplicate-entry leg scopes to the `###` heading it claimed and judges a squash-merged PR on the diff it will LAND, refusing that basis on the merge-commit classes (card#10337, card#10338); the read-outcome gate's population reaches the operator-run checks under `tests/` (card#10311). **Docs/tests:** `release-pr-body`'s card-coverage preconditions are declared and source-checked (card#10039); the by-ref `source` rule and the true-message/wrong-next-action class each get a derived population (card#9957, card#10105); `tests/framework-mirror-check.sh` checks the framework's `templates/release/` mirror against the tag it claims (card#9939). |
| v0.35.1 | 2026-09-19 | **Patch — `kbcard patch` stops writing HALF of a number/URL pair: `--pr`, `--issue`, `--pr-url` and `--issue-url`, each sent without its counterpart, now refuse at `rc 2` when the card's stored other half names a different ref, and the refusal says what is refused and what to pass and nothing about what a release would then promote.** No new `bin/` tool, no new flag, no new capability — fixes, CI and docs only — so per `VERSIONING.md` § Bump sizing this is a **patch**. The PRs in `v0.35.0..v0.35.1` are enumerated in `docs/CHANGELOG.md` [0.35.1] and in the release PR's generated `## Bundled` table. **⚠ UPGRADE ACTIONS — all in `docs/UPGRADE.md` §6 v0.35.1:** **[vendor]** re-vendor `bin/kbcard`, `bin/adopt-to-dl` and `bin/_kb-board-lib.sh` TOGETHER — the pair check both bins call now lives in the lib (`kb_ref_pairs_alone` / `kb_ref_pair_verdicts`, with `KB_JQ_REPO_FROM_GH_URL`) and its preflight runs on every `patch`, so a new bin beside a v0.35.0 lib refuses **every** `kbcard patch` at `rc 2` and **every** adoption at `rc 1`, nothing written and no DL minted (card#9846); the `bin/` **set** is unchanged, so **no `PATH` symlink loop**; re-vendor the stamped `bin/release-pr-body` as on every bump (card#9335). **[host] ⚠ acceptance changes, each asked and granted (card#9837, card#9846):** the four single-half writes above fail `rc 2` where they wrote at `rc 0`, and `patch` now issues a read that can fail `rc 1` on a call that previously issued none — pass both halves. Two arms are narrower than "different number": a GIVEN `…/pull/0` / `…/issues/0` placeholder over a stored real number, and a `commit`/`tree`/`blob` URL on either side over a real number on the other. **[host]** `bin/adopt-to-dl` refuses (`rc 1`) a card whose `pr_number` already names a real PR **before** it mints a DL, so a refusal at the stamp can no longer orphan the DL `next-dl` claimed (card#9846). **Awareness:** the refusal message drops the attribution and promotion clauses from every arm of both pairs on an operator ruling — a caller matching on `attributes the card to`, `would promote it` or `move its by-ref source` matches nothing now (card#9918, card#9935). **CI/docs:** this repo runs the coord PR-body linter on every PR, reporting-only (#371), and the coord solo-orientation block is re-synced to plugin v0.55.0 (#373, #375). |
| v0.35.0 | 2026-09-17 | **Minor — the `pre-push` lint repeats the board's OWN verdict on a branch's card id, recorded at checkout, in place of the `KB_CARD_ID_FLOOR` magnitude guess (now removed), and the dispatch hook stops pulling a finished or pinned card back to In Progress through a new opt-in `kbcard move --card-start` guard.** The PRs in `v0.34.0..v0.35.0` (#364–#368), enumerated in `docs/CHANGELOG.md` [0.35.0]. **⚠ UPGRADE ACTIONS — all in `docs/UPGRADE.md` §6 v0.35.0:** **[vendor]** re-vendor every changed tool (`git diff --name-only v0.34.0 v0.35.0 -- bin/ hooks/`) and copy `bin/_kb-board-lib.sh` WITH any lib-sourcing bin (the set is derived in `docs/UPGRADE.md` §3) — the lib gains `kb_card_pinned` / `kb_card_start_stage_verdict`, and beside an older lib the card-start paths refuse and write nothing (card#9556, card#9756); upgrade `kbcard` with or before `hooks/agent-dispatch-card-start`; the `bin/` **set** is unchanged, so **no `PATH` symlink loop**. **[host] ⚠** every board env the dispatch hook runs against must map `KB_STAGE_BACKLOG` and `KB_STAGE_PRIORITIZED`, or the move refuses at rc 2 (card#9556). **[host] ⚠ behaviour change:** the dispatch hook leaves a pinned card, or one outside Backlog/Prioritized, where it is — including one already In Progress, which it no longer re-stamps, and the guard's card read shares the hook's `KBADS_TIMEOUT` bound — raise it on a slow board (card#9556, card#9756). **[host]** a card-id branch last checked out before this release prints `board verdict NOT RECORDED` on push until checked out again (card#9568, DL-225). **[host]** `KB_CARD_ID_FLOOR` is ignored — delete it when convenient (card#9570). **Docs:** `kbcard comment`'s `UNVERIFIED WRITE` diagnostic stops claiming a comment leaves `updated_at` untouched (card#9714). |
| v0.34.0 | 2026-09-14 | **Minor — the blank-value refusals in `kbcard` get their deliberate clearers (`patch --clear <field>`, `patch --clear-description`), the card-start hooks stamp a seat owner tag, the `pre-push` lint catches a branch named for an issue/PR number instead of a card, and `release-pr-body` keeps builder diagnostics out of the body and can say which version it is.** The PRs in `v0.33.0..v0.34.0` (#345–#361), enumerated in `docs/CHANGELOG.md` [0.34.0]. **⚠ UPGRADE ACTIONS — all in `docs/UPGRADE.md` §6 v0.34.0:** **[vendor]** re-vendor every changed tool (`git diff --name-only v0.33.0 v0.34.0 -- bin/ hooks/`) and copy `bin/_kb-board-lib.sh` WITH any lib-sourcing bin — the lib gains functions those bins now call; the `bin/` **set** is unchanged, so **no `PATH` symlink loop** and no new runtime-check `TOOLS` member; re-vendor the now-stamped `bin/release-pr-body` on every bump from here on (card#9335). **[host] ⚠ `release-pr-body` output contract (card#9248, DL-224):** `## Correlation gaps` / `## Card coverage` moved to prefixed stderr lines plus an `announce:begin`…`announce:end` block — never `2>&1` into the body, never `2>&-`. **[host] optional — seed `KB_CARD_ID_FLOOR`** per board env, or every push of a card-id branch prints `card-id floor not seeded` (card#9342, DL-223). **[host]** the owner tag needs `COORD_AGENT` (and `COORD_CONFIG` off the default path) in the hooks' environment (card#9436). **[release-CI]** `promote-released-cards` now removes `owner:*` tags from the cards it moves (a token without update permission prints a refusal per card) and its refusal lines carry the HTTP status and server body, with a never-completed request reading `move NOT CONFIRMED` (card#9436, card#9301). **⚠ Acceptance changes in `kbcard`, each asked and granted:** a visually blank `--name`/`--description` (card#9222), `--origin`/`--version`/`--pr-url`/`--issue-url` (card#9338) or `--tags` (card#9421) is rc 2 before any request — a blank `--description` used to CLEAR at rc 0; "blank" is six ASCII characters under `LC_ALL=C` in one lib predicate, which on a UTF-8 seat moves a U+2003/U+3000-only value at `search`/`board-stats --since`/`gh-code-search` from refused to content (card#9337); the four payload flags are sent as JSON strings exactly as typed, ending numeric coercion, `nan`→`null` clears and newline key injection (card#9423). **⚠ Behaviour change:** `released_to_main` is now a terminal `kbcard` column, so a move there clears the assignee and removes `owner:*` tags (card#9436). **Fixed:** a landed write on a card past 131,072 B no longer reads back as rc 3, and `archive`'s gate runs on one (card#9482). **Docs:** the PR template's audit rows sit unbulleted at column 0 so plane 1 reads them (card#9344); §6's intro stops restating its coverage span (card#9343). |
| v0.33.0 | 2026-09-12 | **Minor — `kbcard patch` gains the writers for two board fields nothing in the fleet could set (`assigned_user_id`, `block_reason`), and `kbcard stages` stops printing this install's own config in the exact shape of a board read.** 4 card-carrying PRs since v0.32.0 (#339–#342) plus two docs-only PRs (coord orientation re-sync #337, `docs/INSTALL.md` §2 #338). **⚠ UPGRADE ACTIONS — three, all in `docs/UPGRADE.md` §6:** **[vendor] re-vendor `bin/kbcard`** — it is the ONLY vendorable file this release moves, and the `bin/` **set** is unchanged, so **no new tool and NO `PATH` symlink loop is owed** and `agent-board-toolkit-runtime-check` gains no `TOOLS` member; **[host] ⚠ `kbcard stages` is no longer safe to run with stderr merged into stdout** — `kbcard stages 2>&1 \| jq .` now dies at `jq: parse error` on line 1, because the provenance line below rides every successful call (redirect stderr separately; **stdout is byte-for-byte unchanged**); and **[host] optional — declare `KB_USER_<SEAT>=<kanban user id>`** in each board env to address seats by name and render `assignee`, the same name-keyed shape as `KB_STAGE_*`/`KB_TYPE_*`. **Added — the claim detector (card#9169, #340):** a card whose **stage never moved** is indistinguishable from an unclaimed one, so two seats pull the same work and neither finds out; the board has a native field that answers it — `assigned_user_id` — and nothing in the fleet wrote it. `patch --assign <seat\|user-id>` / `--unassign` write it, and **assigning over a DIFFERENT seat's claim is `rc 1` with the holder NAMED and NO PATCH ISSUED** — that refusal *is* the detector, and it works exactly where the column cannot help. `--steal` is the loud opt-out and names both ends; `--unassign` is deliberately **not** gated the same way, because `kbcard` has no notion of WHICH seat is running it (the map resolves names to ids, never "me") and gating it would make `--steal` routine, which is the one thing that would stop the loud line meaning anything. The read side widens with it: `list` projects `assigned_user_id` **and** `assignee`, `show` gains the `assignee` annotation, and an id this board env cannot map renders raw rather than failing or guessing. **Added — a rendered column with no writer (card#9213, #341):** a sprint burn-down's **"Blocker / next action"** column reads the board's top-level `block_reason`; the v3 API accepts it on an ordinary task write and the column has existed since the initial tasks migration, but **no writer was ever built** — not this CLI, not the board's web UI — so every open card on every board carried a null there, the column rendered blank on every row, and an operator handed that page concluded nothing was blocked while the blockers sat in **card comments**, which that column cannot read. A working renderer with no writer is worse than an absent column, because a blank cell is indistinguishable from a measurement. `patch --block-reason "…"` / `--block-reason-file PATH` sets it (the prose-pair convention every other free-text flag follows — the file form exists because an inline value is expanded by the caller's shell before `kbcard` starts) and **`--unblock` clears it**; ⛔ the clearer is not optional dressing, since a reason that can be set and not unset goes stale in a structured field. **Fixed — a LOCAL alias printed in the shape of a board read (card#9171, #339):** `cmd_stages` issues no request — it iterates the caller's `KB_STAGE_*` variables and names each one by `stage_name()`, which returns **the variable's own suffix, lowercased**, so `KB_STAGE_HELD=102` prints `{"id":102,"name":"held"}` whatever the board calls stage 102. A peer seat did the obvious correct thing — read the stages with the board tool, write a config from the result — and produced a sprint filter keyed on `held` for a board whose stage 102 is `blocked_gated`; its fatal-on-unmatched rule is the only reason that cost one run instead of a silently empty lane reading as a finished sprint. **The `⚠ IT READS THIS CALLER'S OWN CONFIG` warning had been in `--help` since 2026-08-25 and could not have helped — a consumer piping the JSON never opens the help**, so the declaration now rides the OUTPUT, on **stderr** rather than as a third row key: stdout's `{id, name}`-and-no-other-key contract is the join `list`'s `stage` projection depends on, and widening it would break every consumer in order to warn the one that never reads. **Fixed — the contract was stated on two reader-facing surfaces and the card#9171 fix had already missed one (card#9173, #342):** it lives in `bin/kbcard`'s `Usage:` header (which IS the rendered help) and again in `README.md` § `kbcard stages`, and #339 updated the bin while leaving README describing the pre-change output contract. **Nothing mechanical could have caught it** — `help-output-selftest.sh` asserts help STRUCTURE per bin and never opens README, `readme-bin-coverage-selftest.sh` asserts each bin owns a README ROW rather than that any prose agrees with anything, and both ran green over the drifted tree. Neither copy is deleted (the two readers differ: one has the tool installed, one is reading before install); instead `tests/kbcard-contract-selftest.sh` guards each copy **against the EXECUTABLE**, not against the other copy, and found one live divergence in the writing — the help block never carried the stderr-merge hazard above. **Docs:** `docs/INSTALL.md` §2 stopped asserting a coord-plugin gap that is **closed** — a false present-tense claim about ANOTHER project's code, which a peer repo had already consumed, re-derived and reported as a hazard of its own; the recipe's `[ -f "$t" ]` guard and `-n` **stay**, with a live reason (this block is run by hand and inherits nothing from that arm) replacing the retired one (#338). |
| v0.32.0 | 2026-09-06 | **Minor — a mutating verb no longer reports a success it never read back (new **`rc 3` UNVERIFIED WRITE**), `kbcard` can finally REMOVE a link, and the keys a release correlates on stop being unread — including a now-REQUIRED `.promote.source`.** 12 substantive PRs since v0.31.1 (#322–#323, #325–#334; #321 and #324 are coord orientation-doc syncs). **⚠ UPGRADE ACTIONS — three, all in `docs/UPGRADE.md` §6:** declare `.promote.source` in every `.release-pr.json` (`"*"` on a single-repo board) or `promote-released-cards` refuses at rc 2; re-vendor the changed bins; and expect `agent-board-toolkit-runtime-check` to go **rc 1** on a seat still holding a duplicate token file. **No new `bin/` tool**, so no `PATH` symlink loop is owed on that ground. **Read-back before success (**card#8556**, #333):** a mutating verb printed a success built out of what it SENT, so a write the server did not apply reached the caller as applied — `delete --hard` the severe member, where a false success says a DL ref is released. Every mutating verb now confirms by READING, and the exit ladder gains a third outcome: `0` applied-and-read, `1` NOT applied and KNOWN, `2` bad invocation, **`3` UNVERIFIED WRITE** — sent, but the confirming read could not be made. **`3` is additive, not a rename of `1`** — every knowable failure is still `1`, and a caller testing `non-zero` is unaffected; one that must not act on an unconfirmed write tests for `3`. The population is not a figure here — `tests/readback-before-success-census.sh` re-derives it. **New verb (**card#8545**, #328):** `kbcard unlink --link-id N --on ID-OR-EXT` — the toolkit could CREATE a link and never remove one, so a mislinked `blocks` relation was permanent and every gate count derived from links stayed wrong by one, forever. It reports a removal only after reading the link's absence back; the API's `204` is a claim, not the removal. **⚠ BREAKING CONFIG CHANGE (**card#8421**, #322):** `dl_number`/`pr_number` are repo-scoped counters and `promote-released-cards` correlated on the bare number while writing into a TERMINAL stage — on a multi-repo board it promoted another repo's cards (a peer measured one release's 15 shipped PRs matching 24 cards across three lanes). `.promote.source` is now required with no default; `"*"` is the explicit single-repo answer, plus a `--source` override and a matching `promote/action.yml` input. Two sibling narrowings ship with it: `run-coverage-check --repo owner/name.git` and `adopt-to-dl --repo` (now `kb_is_repo_slug`, charset AND shape — 16 further spellings refused at the flag rather than 404'd a layer later). **⚠ Three more accept/reject changes (**card#8538**, #330):** `release-artifacts-check` refuses a `promote`-declaring config that omits a correlation key (rc 2, unless declared absent by name in the new `unset_correlation_keys` array); `promote-released-cards` refuses a resolved `.promote.source` that is not `$GITHUB_REPOSITORY`, before any board read; and `release-pr-body` reports an empty manifest over a non-empty range in a new `## Correlation gaps` section instead of a silence indistinguishable from having nothing to say. **Credential topology (**card#8376**, #323):** shipping v0.31.0's coord-store rung did not DELETE the plaintext copy an operator already made — both kept working, runtime-check reported healthy, and the shadowed one is a live credential nothing rotates. `agent-board-toolkit-runtime-check` now resolves every source a token can come from and compares them **by digest over the content their READERS see** (`$(cat …)` strips trailing newlines, so an editor-terminated file and the `printf '%s'` recipe are ONE credential). Same secret in two files ⇒ `✗` **rc 1**, naming the file to delete and the one to keep; a different token no board resolves ⇒ `⚠` rc 0. **Guards that now compare instead of asserting (**card#8529**, #334):** four standalone-vendored tools carry inline copies of lib guards by a *decided* guarded-duplication policy, held together by a `keep the two in sync` comment — and a comment is not a guard. `tests/mirror-pair-parity-selftest.sh` drives the copy families that had no comparison against the lib row by row, with the copy set derived from the tree so a fifth is driven the day it lands; a false sync claim in `promote-released-cards` (two spellings that had NEVER agreed on `007`) is corrected. **Fixed:** three shipped surfaces described `prelude-shadow-selftest.sh`'s reach more widely than its predicate — it compared NAMES (**card#8548**, #332); `changelog-card-entry-selftest.sh` told a pre-fold branch its entry was missing when the merge had moved it under the folded heading, and an author who believed it shipped a duplicate — it now names the move (**card#8442**, #325); and four wall-clock assertions started their clock in the TEST, so the tool's own startup sat inside the bound being judged — measured at 21/30 red on a loaded host (**card#8485**, #326; **card#8533**, #329). **Docs:** the PR template gained the `Built:` / `Coordinated in:` fields the coord plugin declares required, by pointer rather than by copy (**card#8536**, #327); and the single-card read is documented as six spellings that disagree on what a card is (#331). |
| v0.31.1 | 2026-08-31 | **Patch — the repository read as protected on every surface an operator inspects while NO CI result was required to merge; one aggregating status context now stands in front of the whole suite.** 2 commits of substance since v0.31.0 (#318 plus the coord v0.44.0 solo-orientation doc sync #317). **No new `bin/` tool, no new flag, no new capability — nothing under `bin/`, `hooks/`, `promote/`, `release-artifacts/`, `release-tag-check/` or `examples/` moved at all**, so per `VERSIONING.md` § Bump sizing this is a **patch** and `docs/UPGRADE.md` §6 carries the no-action line. **Added — `ci-gate` in `ci.yml`** (**card#8261**, #318): `main` and `dev` carried the whole fleet branch-protection model — `enforce_admins`, blocked force-pushes and deletions, the merge-method rulesets — with `required_status_checks` at **`null`**, read live on both. The obvious fix was not available: a protection `contexts` list is a hand-maintained registry standing in front of a `selftest` matrix that **ordinary pull requests edit** (`run-coverage-check-selftest` was added three PRs earlier), and such a list rots in **both** directions with only one loud — a name dropped from the matrix never reports again and deadlocks every PR, while a name **added** to it is required by nothing at all. `ci-gate` `needs:` every other job in the file and asserts on the collected `needs.<job>.result`, so it is the one context that does not move when the matrix does. ⛔ **`if: always()` is the load-bearing half** — without it the job is *skipped* the moment a needed job fails, and a skipped required check does **not** block a merge, so the gate would be satisfied precisely when the suite was red: strictly worse than the honest `null` it replaces. **The ruling on the three non-success results**, stated in the job's own comment: `failure` and `cancelled` fail (a cancelled run measured nothing — there is no verdict to inherit, and `always()` rather than `!cancelled()` is what keeps the job alive to say so), and **`skipped` fails too** — nothing in the file can be skipped today, so that arm is a ruling about the next edit rather than about this tree, because reading it as a pass would let a future `if:` drop a leg out of the merge gate with no signal anywhere. Results are read from `toJSON(needs)`, **derived from the `needs:` list rather than re-enumerated**, so a job added there is gated with no second edit — a hand-written `needs.a.result != 'success' || …` chain would be card#5910's subset-reading guard again. **`tests/ci-gate-selftest.sh`** two-way-compares the `needs:` list against every top-level job in `ci.yml`, asserts `if: always()` and the absence of a `name:` (which would silently rename the required context), disposes of every job in the whole workflow directory into one of three buckets, and drives the **shipped** verdict step — extracted by step name — over planted `needs` payloads; **all 25 assertions were each observed RED** under 16 targeted mutations. ⛔ **This release changes no repository setting.** Setting `required_status_checks` is an admin operation on a protected branch, is the operator's, and is correct only after `ci-gate` has reported at least once — a required context that has never reported blocks everything. **Docs:** three comments asserting that a job or matrix-entry name "is a required status check on `main`" were read live and are false (true when written, dropped when the 2026-08-30 fleet model replaced the protection object without carrying the contexts over); each now records what it used to say, why it is wrong, and the one name that is load-bearing. `VERSIONING.md` step 8 gains the pointer to where that context set is declared. Vendored surfaces — every `bin/` file and all three composite actions — are **untouched**. |
| v0.31.0 | 2026-08-25 | **Minor — three new surfaces: a check that tells *never ran* apart from *passed*, two terse `kbcard` reads, and an extractor key for a version that lives inside a code file.** 4 substantive PRs since v0.30.1 (#310, #312–#314; #311 is an orientation-doc sync). **⚠ UPGRADE ACTION: re-run the `PATH` symlink loop** — `bin/run-coverage-check` is a NEW bin and a member of `agent-board-toolkit-runtime-check`'s `TOOLS` set, so that guard warns until you do. **New tool:** `run-coverage-check` derives which workflows SHOULD have produced a run on a PR's head and reports RAN / SKIPPED / MISSING / UNMEASURED — GitHub creates no runs at all for a PR whose merge ref it cannot compute, and for a `paths:`-filtered workflow zero runs is the normal state, so *never ran*, *correctly skipped* and *passed* were one reading. **⛔ Report-only — it gates nothing, is wired into no workflow, and a required status check is the better fix wherever the plan allows one** (**card#7615**, #314). **New flags + verb:** `kbcard search --brief` / `--count` and `kbcard stages`, all opt-in with no existing output moved — `stages` exposes the existing `stage_name` map so a consumer joins `list`'s numeric `stage` at run time instead of pinning a per-install id (**card#7694**, #313). **Fixed:** `release-pr-body` rendered the version as `'version' => '0.43.0'` for a repo whose `version_regex` must carry context; the new optional `version_extract_cmd` names the extractor such a repo already owns, and a config that does not set it is byte-identical (**card#7599**, #310). **Docs:** two `examples/*.env.example` files stopped asserting a three-tier token ladder card#7316 had falsified — the restated list deleted, both now pointing at `docs/INSTALL.md` §3b (**card#7616**, #312). |
| v0.30.1 | 2026-08-24 | **Patch — one rule, applied at the mint site and at every reader, for "which pull request or decision does this stored value name".** 8 substantive PRs since v0.30.0 (#300–#307). No new tool, no new flag, no new capability — fixes/docs only. **⚠ TWO UPGRADE ACTIONS:** (1) **`bin/_kb-board-lib.sh` co-vendors with `board-card-start` and `next-dl`** — the rule is now the lib constant `KB_JQ_REF_CANON`, and a new bin beside an old lib dies at `KB_JQ_REF_CANON: unbound variable` under `set -u`, *before* jq (measured). `promote-released-cards` stays standalone with a byte-identical inline copy, pinned against the lib's by a selftest. (2) **`kbcard --pr`/`--issue` now refuse a non-single-integer, a negative, and zero** at rc 2 before any request — `85`, `#178`, `PR-085` unchanged, an interior hyphen is decoration not a sign. **Fixed:** three readers stopped fabricating a ref — `promote-released-cards` concatenated digit runs so `1.5` correlated to shipped **PR 15** and the sweep PATCHed that card to a terminal stage (**card#7587**, #306); `board-card-start` took the FIRST run so `1.5` answered to `DL-1` and a checkout **moved the wrong card**, the only over-matching member of the class (**card#7592**, #307); `next-dl`'s floor scan split a multi-run stamp, burning every DL beneath `2026` (same PR). **card#7536** (#304/#305) closed the MINT site. **card#7535** (#303) and **card#7525** (#301) stopped `promote-released-cards` and `release-pr-body` reporting an unresolvable explicit `--base`/`--head` as a clean empty range at rc 0 — the `^{commit}` peel is load-bearing, since `git rev-parse --verify -q` exits 0 for a hex string naming no object. **card#7517** (#300) resolved `release-pr-body`'s defaulted head from `origin`, because a lagging local `dev` dropped PRs from the bundle AND from the promote manifest. **Docs:** **card#7539** (#302) removed a `kbcard search` claim that was verified false and a live figure that had drifted twice. Well-formed data is byte-identical throughout, proven as a differential against the pre-fix expressions. 53/53 selftests, shellcheck clean (pinned 0.9.0). |
| v0.30.0 | 2026-08-23 | **Minor — the credential a board tool sends must be one somebody DECLARED and the API host it resolves must be one somebody NAMED, the toolkit's own DL-board setup tool stops standing boards up broken and becomes runnable on a board with no swimlanes, five files land under `bin/`, every operator-facing number that can be built from a partial read now carries its floor on the number itself, a SECURITY fix stops ten error paths echoing a credential the api base legitimately carries, and `promote-released-cards` becomes runnable against a real host at all.** 39 PRs since v0.29.0 (#259–#298, less this release's own #295): thirty-six card-carrying, plus two docs-only solo-orientation syncs (coord v0.37.0 #259, coord v0.41.0 #290) and one card-less `.gitignore` widening (#264) whose generalisation is card#7036. **⚠ UPGRADING — five host actions, a SECURITY rotation the upgrade does NOT perform for you, a broad re-vendor and a re-pin of ALL THREE composite actions (`docs/UPGRADE.md` §6 v0.30.0). Take the rotation first (card#7500): rotate any credential an api base already printed, because upgrading stops the next emission and cannot unprint the last:** **re-run the `PATH` symlink loop** (this release ADDS five `bin/` files — `gh-code-search`, `gitignore-secret-family-check`, `release-tag-check`, `_shellcheck-pinned`, `_kbc-stale-blocker.py` — and `agent-board-toolkit-runtime-check` gains three of the five in its `TOOLS` set, so it warns for `release-tag-check`/`gh-code-search`/`gitignore-secret-family-check` but not for the two underscore-prefixed helpers); **[host] every board must now NAME a token file** (card#7245 — the baked `~/.kanban-dev-token` default is removed at all four resolution sites and an undeclared board is refused at a new rc 7), **[host] `KANBAN_EXPECTED_HOST` must be set in `~/.kanban-host.env` on every seat** (card#7245 — a new preflight refuses an api host nobody declared, *including* when nothing is declared, which is the arm that catches the incident: one write took every board on the reference host down at once and nothing saw it), and **[host] a DL board stood up by ≤v0.29.0 is ALREADY broken** (card#6517 — `dl_number` declared `number` rejects the `DL-NNNN` **string** every writer sends; convert in place with `kbcard [--board <name>] field retype --field dl_number --to string --restamp-dl`, and leave `pr_number`/`issue_number` declared `number`), and **[host] set `KANBAN_API_BASE` if you want `promote-released-cards` to run at all** (card#7494 — the committed `.promote.api_base` is a scrubbed placeholder with no env channel while its `KANBAN_EXPECTED_HOST` guard had one, so target and constraint could never agree and every run died refusing to send the token, taking `release-pr-body`'s `## Card coverage` step with it; unset or empty stays byte-identical). **⚠ One further narrowing:** `kb_is_uint` refuses a LEADING ZERO, so `010`/`08`/`007` are refused where bash previously read them as base 8 or died mid-guard (card#6912). **Added:** `bin/gh-code-search` (#260, card#6884) — `GET /search/code` answers **HTTP 200 `total_count: 0`** on a private repository for a term that certainly exists, byte-identically to a real zero, so the ordinary `--jq '.total_count'` spelling hands back a `0` that reads as a finding; the contract is `error / partial / complete` carried by the exit code, never a count. `bin/gitignore-secret-family-check` (#292, card#7036) — `.env` being ignored says nothing about `.env~`, `.env.bak`, `.env.dpkg-old` or the emacs `#.env#` wrapper (found live on a peer install as a 4,339-byte `.env~`, mode 600, nine days in the tree); which rules are secret-bearing is DERIVED from the `.gitignore` itself, the affix table names the producer each entry was read off, and it fails **closed** — a run deriving zero rules is rc 2, not a pass. Fixed at the primitive as ONE universal affix block, and ⚠ **`#*#` in a `.gitignore` is a COMMENT** unless the hash is escaped, which is how the first pass (#264) was incomplete and what the guard caught. `bin/release-tag-check` + `release-tag-check/action.yml` (#274/#289, card#6579/card#7203) — the third composite action, whose tag name is derived from `.release-pr.json`'s `tag_format` rather than a hardcoded `v` prefix (measured on the pre-change binary: with `tag_format: release-{{version}}` it polled 300 s for a tag that cannot exist and refused the release). `bin/_shellcheck-pinned` + `.shellcheck-version` (#279, card#6619) — the gate ran whatever `ubuntu-latest` shipped and printed no version, and the two ends of that drift disagree at ERROR severity (measured: SC2218 on 0.9.0, clean on 0.11.0). `bin/_kbc-stale-blocker.py` (#267, card#7113) — a fourth read-only `board-session-close` leg reporting which OPEN card's own text still says it is blocked behind a card that has since become TERMINAL; the predicate was measured against the live corpus (682 cards, 3 boards) before it was written, and a bare `#N` is deliberately not a citation (it flags 27 of 48 open cards). `next-dl --require-counter` (#278, card#7214) and a fourth token tier, the coord credential store's `[kanban] api_token_file` **pointer** (#293, card#7316 — pointer-only, an inline `api_token` is refused, no store value reaches any output stream, and a host with no store is unaffected in every observable way). **Changed:** the `PATH` install loop itself (#277, card#7170) — `ln -sf` **follows** a symlink-to-directory instead of replacing it, so the documented recipe linked `bin/__pycache__` onto `PATH` on run 1 and, on run 2, planted `bin/__pycache__/__pycache__ -> bin/__pycache__` inside the consumer's checkout, at rc 0, invisible to `git status`; the recipe now refuses any non-regular-file entry and links with `ln -sfn`, under one owner, with a selftest that executes both shipped copies against a fixture so a third copy reds. `agent-board-toolkit-runtime-check`'s report is now BUFFERED (#280, card#6911) — progressive output is what let `\| head -1` kill the writer and return **rc 141 with 0 B** in place of the verdict. `board-snapshot` (#275, card#6365) and `board-stats`' stock (#283, card#7228) and flow (#284, card#7235) sections put the floor marker on **every** number — `in-flight ≥200`, `≥200 UNTRIAGED`, `… +≥194 more` — because a number survives being quoted out of its line and a ⚠ several lines above it does not; `board-stats --format json` is deliberately unchanged. **Fixed:** ⚠ **SECURITY — an api base carrying `user:password@` was echoed verbatim by ten error paths, four of them into durable on-disk logs** (#298, card#7500) — that input is *accepted by design* (the host guards judge the host and nothing else, and the accept row has been pinned since the guard was written), so the defect was at the RENDER, not the verdict: no verdict moves and the hostile matrix refuses exactly as before. One primitive, `kb_redact_url_userinfo`, applied where the base is RESOLVED rather than at ten call sites, masks the whole userinfo to `***` and keeps host/port/path/query/fragment byte-identical; it reads its authority boundary from `kb_url_host`, which is load-bearing — `https://evil.example#@good.host` has NO userinfo, so a redactor cutting at the last `@` would rewrite the one hostile input the guard exists to refuse into a message naming the protected host. The population was RE-DERIVED by USE rather than by grepping `$api`, which found six sites beyond the four filed, including the paginator's three durable log lines where the base is a PREFIX of the rendered value — the shape a name-grep structurally cannot see — and `tests/url-userinfo-render-selftest.sh` now re-derives that population every run so the eleventh message inherits the redaction. `promote-released-cards` is pointable at a real host for the first time (#297, card#7494 — see UPGRADING; the composite action's `jq` rewrite of `.release-pr.json` goes with it). `board-snapshot`'s runtime-staleness leg stops rendering a guard that DID NOT RUN byte-identically to one that found nothing (#296, card#7305 — the leg ended `… --quiet 2>&1 \|\| true` and the guard buffers its whole report to one final write, so a SIGKILLed run and a healthy run both produced **0 bytes at status 0**, measured through the real bin; the fix reads the status and adds a line only where the guard said nothing, and still never blocks). `dl-a1-register-field` on both halves (#291/#294, card#6517/card#7464 — see UPGRADING); `kb_api` stops answering *the request did not complete* and *the server answered 500* with the same rc (#270, card#6680 — rc 1 still means the server answered, so every existing `\|\| …` bucket is byte-identical; the transport case takes a named `$KB_API_RC_TRANSPORT`); `release-artifacts-check` stops reading a real release PR as "version unchanged" when the version file carries a version-shaped line above its version line (#272, card#6488 — every declared member's leg silently did not run, at **rc 0**, needing no config edit, which made it the cheapest evasion of the whole gate) and `bin/` stops declaring a second, wider version shape beside `version_regex` (#282, card#7208); `release-promote-cards.yml` refuses to report a release as shipped while its tag is missing (#274, card#6579 — on the v0.28.0 merge the tag push was refused 403 while promote reported success, and the tracking board asserted a shipped release for 17m27s); 46 of 47 `<producer> \| grep -q` sites migrate to a pipeline-free whole-line match (#273, card#7175 — `grep -q` exits on match, its writer dies on the closed pipe, and `pipefail` promotes that into a reported NON-match at rc 0); the python entry points (#271, card#6871) and CI's own syntax gate (#288, card#7207) stop minting `bin/__pycache__` in the directory a consumer symlinks onto `PATH`; `install-board-hooks`' capability probe brings BOTH readers under the failed-read rule (#269, card#6572 — an unreadable entry scored a mechanism nobody measured and the printed remedy pinned the seat to hook copies); `release-pr-body` emits `## Card coverage` only when it measured (#265, card#7038 — **a body with no coverage section means the check did NOT run**). **Tests/CI:** guards for the read-outcome-collapse class (#276, card#7210), the hand-enumerated-population class (#261/#262, card#6645), the `pull_request` trigger contract (#268, card#6099), a superseded `[Unreleased]` entry standing beside its replacement (#281, card#7227) and the changelog region's floor (#285, card#7293) and its duplicate leg's scope (#287, card#7303), plus whole-suite `$HOME` containment (#286, card#7245 — the test-tree half of the two narrowings above, after a harness overwrote the reference host's live credential config). **All three composite actions move — re-pin every one:** `promote/action.yml` CHANGED (card#7494 — `api-base` survives as an interface but the `jq` rewrite of the checked-out `.release-pr.json` is gone, replaced by `$KANBAN_API_BASE`, so the action no longer mutates a security-sensitive file in the consumer's workspace mid-job); `release-artifacts/action.yml` changes in DESCRIPTION only, but the script it runs DID change; `release-tag-check/action.yml` is new. |

<!-- `CLAUDE_AGENTBOARD.md` is LOCAL-ONLY and untracked (`.gitignore`): the coord plugin's `coord:init-solo` places it in each agent machine's checkout and its orientation sync keeps it current. A fresh clone has no such file, so this import resolves to nothing there. -->
@CLAUDE_AGENTBOARD.md
