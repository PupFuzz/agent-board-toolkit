# agent-board-toolkit

Repo-specific SME knowledge lives here (add as it accumulates).

## Releasing

Versioning + release process: see [`VERSIONING.md`](VERSIONING.md). Full per-version log: [`docs/CHANGELOG.md`](docs/CHANGELOG.md). Release PRs are cut with the `coord:release-pr` skill; the deterministic body generator + card-promotion tooling read [`.release-pr.json`](.release-pr.json) (board 12).

## Recent releases (last 10)

Filled in by the release PR that produces each version tag — which also **trims the oldest row back to 10**. This table is the ergonomic snapshot, deliberately truncated; [`docs/CHANGELOG.md`](docs/CHANGELOG.md) is the canonical per-version log and is **never** truncated, so nothing is lost by trimming here.

| Version | Date | Highlights |
| --- | --- | --- |
| v0.29.0 | 2026-08-18 | **Minor — `kbcard` gains a board-wide `search` verb and the `field create`/`delete`/`retype` write side, externally-authored free text can finally be handed over from a file the caller's shell never expands, and four reads that answered an unreadable response with a confident wrong value now refuse.** 10 PRs since v0.28.0 (#247–#256): nine card-carrying plus one docs-only `docs/UPGRADE.md` §6 entry for v0.28.0 (#247). **⚠ UPGRADING — one action plus a re-vendor (`docs/UPGRADE.md` §6 v0.29.0):** the **`PATH` symlink loop is NOT owed** — this release adds no bin, and every new verb and flag lands inside `kbcard`, whose existing symlink tracks its target's *content* (it is the *set* of tools a symlink cannot track, which is why an added bin owes the loop and this release does not); **[host] `next-dl` now REFUSES (rc 1) to mint when a configured board cannot be read at all** (card#6631), where it previously minted from the local `max+1` floor it knew was incomplete — a seat with a stale board config or an unreachable board sees a refusal where it used to get a number, and a DL is a correlation key; and **[vendor] re-vendor `bin/_kb-board-lib.sh` TOGETHER with any bin you copied** — the tolerant parse those bins now call lives in the lib, so a new bin beside an old lib fails at runtime with `kb_parse_resp: command not found`. **Added:** `kbcard search <query>` (#255, card#6771) — the API has always matched `name` + `description` on `GET /tasks/search.json`, but the only board-wide read VERB was `list`, whose fixed projection drops `description`, so **grepping a card body through `list` answered 0 no matter what the board held** — a confident wrong answer, not an error. `search` prints one block per hit (header line, then the card's **full description**), because reusing `list`'s projection would hide the field the search matched on and move the same miss one layer over; `--column`/`--type` filter through the **same** select as `list` — extracted as `_kbc_card_filter` at this second caller rather than copied — and carry the same `<M> of <N>` stderr denominator, so a filter that removed every match reads distinctly from a query that matched nothing. `kbcard field create / delete / retype` (#250, card#6525) — `retype` is a **thin call** on the server's atomic type-conversion route (`POST …/change-type.json` moves the definition and every stored `tasks.payload` value in ONE transaction with the search index rebuilt inside it and no delete, so `placeholder`, `mask`, `multiline`, `is_filterable` and `position` survive — which a delete-and-recreate could not), so the verb issues no delete, no recreate and no per-card cast; `delete` is **fail-closed on the carriers it can SEE**, refusing at rc 2 without `--orphan-values` and printing its `N of M board cards carry <key>` denominator on every run, so a census that RAN and returned zero is distinguishable from one that never ran (the read is live and non-archived, and the entry says so). `kbcard --name-file` / `--description-file` (#252, card#6648) — `--description "…"` forced externally-authored text to be typed inline, where **the caller's shell expands it before `kbcard` is started**, so a `$(…)` or a backtick sitting in a report someone else wrote runs as a command on the invoking box and `argv` already holds its output; the fix is a **source that never meets a shell**, not a validator (an in-tool `$(`-sniffing check could not detect the thing it names), and all three free-text pairs now resolve through one owner. **Fixed:** the unreadable-`2xx` class, four instances. A `2xx` whose body these tools cannot read no longer kills them at `jq`'s exit 5 — every single-card/single-field read and every write echo refuses in its own tool's words at that tool's own documented rc — and **three routes to a wholesale tag wipe close with it** (#248, card#6426): `patch --tags/--type/--triaged` replaces the tag list wholesale, and `{"data":null}`, a `tags` **object** (jq's `map` iterates its values), and `tags: false` each merged into a PATCH that destroyed every tag the card held, at **rc 0**, without jq ever faulting — so `.data` is now required to be an object and `tags` an array, shape-tested before the wire. `fetch_board_cards` stops answering an unreadable page-1 `2xx` with an **empty board** (#249, card#6594 — measured through the real bin: `RC=0 STDOUT=[[]] STDERR=[]`, byte-identical to what a genuinely empty board gives), and an unreadable page **> 1** stops ending the scan as if it were a short page, in both whole-board paginators and in `board-stats`'s changelog window (#254/#256, card#6630 — three instances, the population derived from "every loop that issues repeated requests to accumulate one result set" rather than recalled; `promote-released-cards`'s `fetch_whole_board` is a MOVER and was promoting from the truncated list). `fetch_board_cards` no longer destroys the caller's own stderr log (#251, card#6661) — under `KB_FETCH_LOUD` it sent curl's stderr to the **path** `/dev/stderr`, which re-opens the file behind the caller's fd 2 `O_TRUNC` on every page fetch, so `kbcard list 2>run.log` lost everything written before the first page. `promote/action.yml` and `release-artifacts/action.yml` vendor surfaces unchanged. |
| v0.28.0 | 2026-08-14 | **Minor — `install-board-hooks` measures whether the seat it is installing onto can actually keep a symlink and REFUSES rather than report a healthy install over hook copies, `kbcard` gains the card-comment verbs, `bin/board-stats` arrives, and the release-artifact gate stops reading the verdict surface out of a file the PR under judgment can edit.** 11 PRs since v0.27.0 (#234–#244): nine card-carrying plus two docs-only solo-orientation syncs (coord v0.34.0 #235, v0.35.0 #243). **⚠ UPGRADING — four actions (`docs/UPGRADE.md` §6 v0.28.0):** re-run the `PATH` symlink loop (this release ADDS a bin — a symlink tracks its target's *content*, not the *set* of tools); **re-run `install-board-hooks <repo>` on every seat**, because a seat that installed under the old behaviour may already be running copies no `git pull` has ever reached and nothing in the old logs distinguishes that from success; know that `--check` is no longer write-free (it runs the probe, which creates and removes two `.ibhp*` entries); and **[release-CI] re-pin the `release-artifacts/` action** — an *ordinary* PR that drops a declared `artifacts` entry now reds unless the removal is acknowledged in a `retired_artifacts` array. **Added:** `bin/install-board-hooks`'s `_ibh_symlink_probe` + `--allow-copies` (#244, card#6527) — the hooks are installed by symlink and that symlink IS the upgrade contract, but on a symlink-incapable seat the POSIX-emulation layer does not fail `ln -s`: measured on a consumer's Windows seat it returns **rc 0 having substituted a COPY**, and the two outcomes were byte-identical in every log this tool writes. The probe tests the property the contract needs — the entry must be a symlink **and** still deliver the source's content after the source is REPLACED (a new inode, which is what `git pull` does) — before the first write, and disposes on three verdicts: CAPABLE installs symlinks (byte-identical to before), NOT_CAPABLE refuses at **rc 1**, INDETERMINATE refuses at **rc 4** and is reported distinctly (folding "could not tell" into "cannot" would re-mint the information loss this closes). `--allow-copies` is a **permit, not a force** — a no-op on a capable seat, it prints `COPY dst <= src` per hook, exits **3** and deliberately does NOT unblock INDETERMINATE. `bin/board-stats` (#236, card#5981) — a per-board stock + flow report whose flow half reads the changelog event log rather than `last_stage_move`, which is a snapshot and cannot answer a flow question. `kbcard comment` / `kbcard comments` (#237, card#6051) — posting a card comment had no verb, so every caller hand-rolled the API call; `comment` prints the created comment id, `comments` reads them back. **Changed:** `release-artifacts-check` reads the declared set **and** the classification keys (`version_file`/`version_regex`) from the **fork point** instead of head (#242, card#6055) — the file was editable by the PR being judged, so four distinct shapes let a PR weaken its own verdict without leaving a trace; a head edit may now only WIDEN the set, and the one narrowing channel is a declared, logged `retired_artifacts` key. `kbcard list` projects `tags` + `external_id`, and a filtered read names its denominator (#234, card#5779); `dependabot-deploy-reconcile` binds its read affirmatively to the RUNNING process tree (#238, card#6442). **Fixed:** `board-session-close`'s "do these two path spellings name the same directory?" test answered by raw string compare on any host whose `readlink` lacks `-m` (a GNU extension) — now decided by identity (#241, card#5281). **Docs:** `docs/UPGRADE.md` §6 backfilled v0.15.0→v0.27.0 and made a release obligation with an explicit no-action line, gated by the artifacts array (#240, card#5764); one owner for the release-artifact gate's read inventory (#239, card#6160). `promote/action.yml` vendor surface unchanged; `release-artifacts/action.yml` CHANGED. |
| v0.27.0 | 2026-08-11 | **Minor — a new host-local instrument that asks whether the fix a CLOSED Dependabot alert claims is actually present in what is deployed and RUNNING, wired as a fourth advisory leg of `board-session-close`.** 2 PRs since v0.26.0 (#230–#231): one feature (#231, card#6277) plus a docs-only solo-orientation sync to coord v0.33.0 (#230). **⚠ UPGRADING — re-run the `PATH` symlink loop (VERSIONING.md step 11):** this release ADDS a bin, and a symlink tracks its target's *content*, not the *set* of tools, so `dependabot-deploy-reconcile` never lands on `PATH` without it — and `agent-board-toolkit-runtime-check`, which gains it in its `TOOLS` set, prints a new warn line naming exactly that (the check doing its job; the fix is the re-symlink it prescribes). `docs/INSTALL.md` §0 gains two non-fatal preflight entries, `ps` and coreutils `timeout`. **Added:** `bin/dependabot-deploy-reconcile` + `bin/_dependabot-reconcile.py` (#231, card#6277) — an alert closes when its fix reaches the default branch, while production serves something cut by a separate process, so a repository reading 0-open can be serving the vulnerable version with no symptom anywhere; the tool implements **one leg** (an installed npm dependency tree, resolved from the MCP config that *launches* the deployed server rather than a hardcoded path) and compares each alerted package's installed version against the alert's own `first_patched_version` under SemVer 2.0 precedence, with `vulnerable_version_range` deliberately never parsed (a range-parsing bug is a silent false negative; the `installed >= first_patched_version` proxy can only over-report). Fail-closed vocabulary — `FIXED_CONFIRMED` / `STILL_EXPOSED` / `UNDECIDABLE` / `UNVERIFIABLE`, a row for every alert including the ones the leg does not cover, and an unreadable directory read is `UNVERIFIABLE` rather than a decidable fact about the deployment (one `dir_fault` primitive at all four directory reads). Findings ride the report, not the exit code: rc 0 = ran and disposed, rc 1 = instrument failure, rc 2 = usage; an unconfigured host is *not configured*, not broken (one line, no artifact, rc 0). Read-only apart from its JSON artifact under `~/.cache/coord/dependabot-reconcile/`. **Changed:** `board-session-close` gains a `── Dependabot fixed-alert vs deployed-tree reconciliation ──` section and runs **both** advisory legs through one extracted `_bsc_advisory_leg` helper under a **60s** wall-clock bound (a kill prints a ⚠ and still never blocks the close; where coreutils `timeout` is absent each leg runs unbounded and says so), which also reworded the archive leg's missing-sibling warning — relevant only to something matching its exact text. **Known residuals, both bounded and tracked:** a manual invocation whose stdout is piped to a reader that exits first escapes the backstop as a `BrokenPipeError` at rc 120 (the session-close leg captures output, so it is unaffected — verified rc 0), and three arms of the nested-copy classifier are correct but carry no fixture that reds them (exercised by hand — FIFO, unix socket, symlink-to-chardev, `ELOOP` — each recorded a fault as designed). `promote/action.yml` and `release-artifacts/action.yml` vendor surfaces unchanged. |
| v0.26.0 | 2026-08-10 | **Minor — `next-dl --peek` stops handing back an already-allocated DL number, and a repo whose commit subjects say `card#NNNN` can finally have its release-PR card-coverage gate fire.** 5 PRs since v0.25.0 (#224–#228). **⚠ UPGRADE FIRST IF ANYONE USES `next-dl --peek` TO CHOOSE A DL NUMBER** (#228, card#6232): `--peek` answered from an offline scan of DL numbers that had reached a *card*, so a number already **claimed** from the board's DL counter but never stamped was invisible to it and `--peek` returned a number the server had already issued — one per burned claim, and a DL is a correlation key, so a reused one silently merges two unrelated work items. The scan is a floor, not the truth; only the counter knows the claimed-but-unstamped numbers. `--peek` now reads the board's non-consuming DL-sequence counter, falling back to the scan only where that endpoint is **absent** (404/unreachable ⇒ pre-endpoint behaviour, unchanged), while a present-but-errored endpoint is now rc 1 with the cause on stderr rather than a plausible wrong number from the floor. Both routes are one `dl_sequence_call` primitive instead of two copies. The default (claim) path is untouched. **Added:** `.release-pr.json` gains an optional `card_token_regex`, and `promote-released-cards` an opt-in `--cards` (#227, card#5877) — `release-pr-body`'s `## Card coverage` derived its manifest from `ref_token_regex` alone, so in a repo whose subjects moved to `card#NNNN` the manifest was unconditionally empty and the section reported clean **without checking anything**; re-spelling the existing key was the worse fix (its numeric part is matched against `payload.dl_number`, so `card#42` moved whichever card carries DL-42). The two keys are two **id spaces**: `ref_token_regex` → `payload.dl_number`, `card_token_regex` → the card's own `id`. Additive — a repo adopting neither key sees byte-identical output; card ids are never derived from commit subjects on the MOVE path (`--cards` takes them explicitly, strictly numeric). **Fixed (CI/process):** the `changelog-card-entry` gate moved into its own workflow so a PR-**title** edit re-runs it (#225, card#6062) — `ci.yml`'s default `pull_request` types exclude `edited`, so adding a `card#NNNN` token after the last push left the gate answering about the pre-edit title; `ci-matrix-parity-selftest.sh` generalised from `ci.yml` to the workflow directory filtered by trigger in the same change. The release-artifact gate's own trigger and the `INSTALL.md` §6c recipe both consumers copy now carry `edited` for **base** retargets (#224, card#6054), and two surfaces stop claiming artifact contents are read at both ends — they are read at head only. **Docs:** `.github/dependabot.yml` records why this repository's Dependabot *security* updates are off as a permanent posture (#226, card#6169) — alerts stay enabled; the reasoning generalises to any repo whose default branch carries a version/tag invariant, and the setting is inherited by nothing. Changed vendorable files: `bin/next-dl`, `bin/promote-released-cards`, `bin/release-pr-body`, plus a description-only correction to `release-artifacts/action.yml`; `promote/action.yml`, `bin/kbcard` and `bin/_kb-board-lib.sh` unchanged. |
| v0.25.0 | 2026-08-07 | **Minor — the declared release-artifact set becomes an assertion instead of a checklist, and `board-card-start` gains `-h`/`--help`.** 5 PRs since v0.24.0 (#217–#221). **⚠ UPGRADING — one narrowing:** `agent-board-toolkit-drift-check <a> <b> <extra>` previously exited **0** reading the first two arguments and dropping the rest silently; it now exits **2** naming the extra argument, and an empty positional refusal names **which** slot is empty (#219, card#5429). **Added:** `bin/release-artifacts-check` + the `release-artifacts/` composite action (#221, card#5910) — `.release-pr.json`'s `artifacts` array is the declared **must-move-together** release set (`VERSION`, the CHANGELOG's `[X.Y.Z]` section, this table's row), and its only consumer was `release-pr-body`, which *printed* it as a `- [ ]` checklist; the new tool **asserts** each member against the PR's own changes — the fork point `merge-base(base, head)` drives both the version read and the diff, so a base branch moving after the fork can neither spuriously satisfy the moved-leg nor mask a real release PR as "version unchanged" — reporting **every** failing member, classifying release PRs by version **value** (safe on every PR, no `paths:` filter), and failing **closed** (rc 2) on an unreadable version file or unresolvable merge base. Adopted by this repo in `release-artifacts-gate.yml`, binding via VERSIONING.md's "wait for ALL CI checks" step. The `promote-action-selftest` flag-parity check now **derives** the wrapper's flag list instead of enumerating three literals, and a sibling job gives the new action the same derived guard — the same subset-of-a-set class, closed rather than re-minted. `board-card-start -h`/`--help` (#220, card#5354) — previously the only usage surface a hand-typed invocation reached was the unknown-option refusal, whose remedy text read as advice to target a branch literally named `--help`. **Fixed:** `release-pr-body` no longer exits 1 after writing a correct body when the commit range has no ref-token match (#218, card#5874 — a trailing `[ -n … ] && printf` leaked its status past `set -e`; unconditional for this repo since subjects moved to `card#NNNN` while `ref_token_regex` still says `DL-`); every surface recommending `git checkout <tag>` now states which topology it is correct for — on the recommended clone install it detaches HEAD, so `git pull --ff-only` then exits 1 and the install silently stops advancing (#217, card#5876), including `agent-board-toolkit-runtime-check`'s own remediation, which now branches on topology. `promote/action.yml` and the board-writing CLIs unchanged. |
| v0.24.0 | 2026-08-04 | **Minor — the five write-once-at-birth card fields become patchable, swimlanes gain their write + laneless-read sides, and a dozen surfaces nothing asserted on get gates that were watched to fail.** 37 PRs since v0.23.1 (#178–#214). **⚠ UPGRADING: four narrowings**, each closing a wrong-input-answered-at-rc-0 path — `kbcard "" <verb>` (#214, card#5276) exited **0** printing usage, so a caller checking `rc` read a no-op as a completed write; an explicitly-empty **positional** was invisible and let the next argument take its place, `install-board-hooks "" <repo>` installing into `<repo>` (#195/#180, card#5343/card#5200); `board-card-start` dropped a non-leading `--lint` and moved a card for real (#183, card#5333); and bracket-**range** guards are pinned to ASCII rather than the wider set UTF-8 collation admitted (#203, card#5409). Also observable: `patch --triaged` no longer re-**sorts** tags. **Added:** `kbcard patch --name / --tags / --type / --external-id / --origin` (#212, card#5776/card#5385) — each writes the field its identically-named `create-card` flag writes at birth, so a card minted wrong no longer stays wrong; the API side was **probed** first with `board_id` (422 prohibited) as the negative control that made the probe able to fail, and two semantics are pinned in `--help`: `--tags` **replaces** the whole list, `--type` **strips** any `type:*` tag before writing the native id. `create-card --swimlane` (#209, card#5671, roundtable #205) and `list --swimlane none|0` (#210, card#5766) — the write side could previously create a state the read side could not query. A `board-session-close` leg reporting checkouts whose `post-checkout` no longer reaches `board-card-start` (#180, card#5200), and CI gates for the things a human kept in sync by hand: a shipped card's `[Unreleased]` entry (#211, card#5767 — 3 of 24 cards since v0.23.1 had none), the `CLIS` `--help` registry (#192, card#5339), README's `bin/` table (#191, card#5374), an orphaned `tests/*-selftest.sh` (#185, card#5355), plus real assertions for the Open-PRs (#188), branch-reality (#190) and archive-eligible (#198) legs and the two board-**writing** dl-adoption bins (#187). **Changed:** the positional-argument rule (#195), the missing-flag-value guard (#205) and the `BSC_WORKTREES` repo list (#200) each collapse onto one owner; `install-board-hooks` now **installs** into `--separate-git-dir` checkouts and submodules instead of refusing (#196, card#5311). **Fixed:** the card#5200 hook-health cluster — a hook that is executable but not exec'able (CRLF, missing interpreter), a `~`/`..`/empty `core.hooksPath`, a non-canonical path spelling, and a remediation line that named a command exiting 1 on three real topologies. `promote/action.yml` vendor surface unchanged. |
| v0.23.1 | 2026-07-27 | **Patch — an explicitly-empty flag value no longer reads as an absent flag anywhere in the toolkit, `--help` stops truncating, and an unguarded promote says so.** 6 PRs since v0.23.0 (#169–#174); all fixes/docs, so **patch** per `VERSIONING.md § Bump sizing`. **⚠ UPGRADING: the CLIs now reject `--flag ""` where they silently accepted it.** An explicitly-empty value was indistinguishable from an absent flag, so it selected a **default path** and exited 0 — an unexpanded shell variable was the common way in. Callers using `--flag ""` to mean "use the default" must omit the flag instead; whitespace-only values already died and are unchanged. **Vendored surface IS affected** (`bin/kbcard`, `bin/_kb-board-lib.sh`, `bin/promote-released-cards`, `bin/release-pr-body`); `promote/action.yml` is unchanged, so action-pinning consumers see no interface change. **#169 (card#5144):** `promote-released-cards --shipped-stages ""` ran **unguarded** with a summary byte-identical to a guarded run — skipping validation, the anti-resurrection source-stage guard, and the `stage-guarded` summary field, so a stale/recycled DL/PR stamp could resurrect a declined card into Released and report clean; reproduced empirically across four inputs before fixing, and the reporter's "unreachable arm" diagnosis was corrected (the arm is reachable via a whitespace-only value; only the pre-normalization empty string was the hole). Fixed by a `require_value` primitive dispatching on **flag-seen**, applied to all five value-taking flags. **#171 (card#5146):** the same shape in every other CLI, found by sibling audit rather than report — `kbcard patch --dl "$DL"` with `DL` unset PATCHed `{}` and exited 0, leaving a card without the ref it needs to ever promote, and `--board ""` fell through to the **default board**, a wrong-board answer rather than a no-op; one `kb_require_value` owner adopted at all 37 `kbcard` arms, with inline mirrors in the two standalones that must not source the lib. **#170 (card#5145):** `--help` printed a fixed `sed -n '2,20p'` range, truncating a 43-line header so `Usage:` was unreachable; now prints the contiguous comment block, asserted by **line-count equality** re-derived from the file (a "contains Usage:" assertion is what let this survive). **#173 (card#5152):** an unguarded promote now says so on **stderr** — not by changing the summary, which is byte-identical-when-absent by an explicit v0.21.0 compat decision that anything parsing it depends on. **#172 (card#5148):** INSTALL §6a recommended `${{ vars.KANBAN_SHIPPED_STAGE_IDS }}`, which renders blank when unset and silently disables the guard while the workflow still reads as guard-enabled; now a literal, with the variable form demoted to an aside carrying a preflight. An action-side blank-warning was considered and dropped — the action cannot distinguish an omitted input from an unset variable, so it would cry wolf at every deliberate non-adopter of an opt-in guard. **#174:** solo-orientation block synced to coord v0.24.0. |
| v0.23.0 | 2026-07-24 | **Minor — a read-only "archive-eligible done cards" surfacing leg for `board-session-close`, plus a shared archive-gate lib.** 1 feature PR (#166, card#5094) + a solo-orientation sync (#165). **Added:** `bin/_kbc-archive-eligible.py` — surfaced under an `── Archive-eligible done cards ──` section after the inverse-drift check, it reports each configured board's done-card backlog count and gates a **bounded sample** through the framework's shipped `may_archive` gate to show concrete "safe to archive now" candidates (the exact count is the signal; the authoritative per-card gate stays in `kbcard archive`). READ-ONLY — surfaces, never archives; a missing sibling is a non-blocking warning; fail-loud on an unresolvable `kanban_common`/board-fetch. The shared `kanban_common` locator + tri-state GitHub-state resolver are **extracted into `bin/_kbc-archive-lib.py`** that both the new leg and the refactored `_kbc-may-archive.py` path-load (canon #5; `_kbc-may-archive.py` byte-identical, selftest green). New `tests/kbc-archive-eligible-selftest.sh` (network-free, real `may_archive` over faked I/O) + prove-it-can-fail, CI-registered. Verified live read-only against the 3 real boards. `promote/action.yml` vendor surface unchanged. |
| v0.22.0 | 2026-07-23 | **Minor — a dispatch-time card-start hook + the promote source-stage guard adopted in the toolkit's own release-promote workflow.** 2 PRs (#159, #162), cards #4945/#4962. **Added:** `hooks/agent-dispatch-card-start` (#159, card#4945) — a Claude Code `PreToolUse` hook for the `Agent` (subagent-dispatch) tool that moves a card to In Progress the moment a build is dispatched, closing the dispatch→branch-creation latency window that `post-checkout` only closes at branch creation (its idempotent peer, not a replacement). Acts **only** on a line-start `BOARD-CARD: <key>#<id>` marker in the dispatch prompt (a bare card-number scan is deliberately avoided — prose mentions many ids); multi-marker + multi-board routed per marker, exact duplicates deduped; transport is the existing `kbcard move` primitive (no hand-rolled curl); **fail-soft on every path** (exit 0 always — a nonzero `PreToolUse` would block the dispatch — `timeout`-bounded per move, jq/unparseable-safe, unknown-board-key skipped). Registration is a manual `settings.json` `PreToolUse` `Agent`-matcher step (`docs/HOOKS.md`); `install-board-hooks` is untouched by design (it symlinks git hooks, not Claude Code settings). Selftest joins the CI matrix + a dedicated `ci.yml` job. **Changed:** the toolkit's own `release-promote-cards.yml` adopts the v0.21.0 `shipped-stage-ids` source-stage guard (#162, card#4962) with the tracking board's Shipped-to-Dev stage id — a DL/PR-matched card outside that stage is skipped + logged at promote time, so a stale/recycled ref stamp can no longer resurrect a declined/backlog card; consumes the local `./promote` composite action (no pin to bump), strictly opt-in per consumer. 16 selftests green; `promote/action.yml` vendor surface unchanged. |
| v0.21.0 | 2026-07-23 | **Minor — the `kbcard field` verb family + the promote source-stage guard and decline stamp-clear, plus solo-orientation syncs (coord v0.17.0 → v0.20.0).** 4 PRs (#157, #155, #156, #154), cards #4939/#4940 (roundtable #132). **Added:** `kbcard --board <key> field list` (custom-field schema read incl. full enum option sets) + `kbcard --board <key> field set-options --field <f> --options a,b,c` — an **idempotent converge-to-set reconcile** (never an append; no-ops on an identical set; refuses non-enum/empty/duplicate; unresolved `--field` enumerates defined fields; error bodies preserved), with the **empirically pinned removal semantic** in `--help`/README: option removal is definition-only (200; a card keeps a removed value orphaned — no clear, no cascade); `add-option`/`rename-option` deferred to v2 (#157, card#4939). `promote-released-cards --shipped-stages <ids>` / action input `shipped-stage-ids` (opt-in): a DL/PR-matched card outside the set is skipped + logged — promotion never resurrects a declined/backlog card off a stale stamp; absent = byte-identical (proven empirically); malformed set rc 2 before any PATCH (#155 leg 1, card#4940). **Changed:** both `kbcard` decline call-sites clear `dl_number`/`pr_number`/`pr_url` per-key in the same PATCH via one shared owner; `--keep-refs` opts out; explicit flag wins (#155 leg 2). **Docs:** solo orientation → coord v0.20.0 (#156) after v0.17.0 (#154). 15 selftests green; `promote/action.yml` gains only the optional input — absent = unchanged. |

@CLAUDE_AGENTBOARD.md
