# agent-board-toolkit

Repo-specific SME knowledge lives here (add as it accumulates).

## Releasing

Versioning + release process: see [`VERSIONING.md`](VERSIONING.md). Full per-version log: [`docs/CHANGELOG.md`](docs/CHANGELOG.md). Release PRs are cut with the `coord:release-pr` skill; the deterministic body generator + card-promotion tooling read [`.release-pr.json`](.release-pr.json) (board 12).

## Recent releases (last 10)

Filled in by the release PR that produces each version tag — which also **trims the oldest row back to 10**. This table is the ergonomic snapshot, deliberately truncated; [`docs/CHANGELOG.md`](docs/CHANGELOG.md) is the canonical per-version log and is **never** truncated, so nothing is lost by trimming here.

| Version | Date | Highlights |
| --- | --- | --- |
| v0.18.0 | 2026-07-19 | **Minor — one new `kbcard` correlation surface plus a fail-loud config error, on three same-behavior consolidations.** 5 PRs (#133–#137). **Added:** `kbcard --issue`/`--issue-url` (#136) stamp `payload.issue_number` (a JSON number, like `pr_number`) + `payload.issue_url` (drives the by-ref `source` owner/repo for multi-repo boards, like `pr_url`) on `create-card`/`patch`, so a card correlates to a tracked issue without a raw per-key `PATCH`; the keys are independent + co-stampable with `pr_*`, and `adopt-to-dl` gains a matching `--issue <N>` co-adoption path (co-stamps + verifies the `github_issue` by-ref). Additive only — omitted flags leave the keys absent. **Fixed:** the unconfigured-board error now names its fix (#133) — the `rc=2` `kb_load_config` board-env arm was the one sibling with no remedy; it now names `--board`/`KBCARD_BOARD_ENV` and lists the `~/.kanban-*-board.env` files that exist, and `INSTALL.md` documents default-board resolution (docs + error text only; rc/trigger unchanged). **Changed (same-behavior refactors):** `_kbc_build_payload` hoisted so `create-card`+`patch` share one payload assembler (#134); every `tests/*-selftest.sh` harness consolidated onto `tests/_selftest-prelude.sh` (#135); assorted intra-file + CI dedup (#137, D6) — a shared `max_int` in `next-dl` (with new selftest), single-sourced teardown bodies + jq normalization, `auto-tag-version.yml` reading `version_regex` from `.release-pr.json`, and the per-selftest CI jobs collapsed to a `strategy.matrix`. `promote/action.yml` / `_kb-board-lib.sh` unchanged. |
| v0.17.0 | 2026-07-18 | **Minor — an 8-PR quality release (#123–#130).** Cards #4510/#4513/#4514/#4413/#4617/#4621/#4623/#4637. **Added:** `kbcard create-card --triaged` (#124, roundtable FR #86) — mirrors `patch --triaged` so a card is born triaged instead of re-surfacing as a false-positive untriaged every session; `board-card-start` accepts the glued `card<N>` branch spelling + a fail-soft pre-push branch-name lint (#125, card #4621) — `card4524`/`card-4524`/`card/4524`/`card#4524` all now auto-move to In Progress (bridge writeback still needs `card-<id>`); `kbcard list` surfaces `swimlane_id` + a resolved name and a `--swimlane <name\|id>` filter (#130, roundtable FR #88) — id-keyed `KB_SWIMLANE_<id>=<name>` env, zero extra API call, composes with `--column`/`--type`. **Fixed:** `promote-released-cards` censuses the whole-board read against `meta.total` (#127, card #4513) so a server short read can't silently promote a partial board; `fetch_board_cards` defaults `last_page` to UNKNOWN not `1` (#129, card #4623) — parity with the standalone, a meta-absent full page-1 no longer truncates. **Changed:** `board-card-start` routes its read + two writes through `kb_api`/`kb_api_status` (#123, card #4510) — a failed dl-stamp/move is no longer silent (403-vs-422 body preserved, durably logged); DL int/format/by-ref-hit consolidated into `_kb-board-lib.sh` primitives (#126, card #4514, dedup D5). **Docs:** `ADOPTION.md` gate keys on board API access not instance ownership (#128, card #4413) — tenants qualify. `promote/action.yml` vendor surface unchanged. |
| v0.16.0 | 2026-07-17 | **Minor — three roundtable board-tooling deliverables.** 3 PRs (#118–#120), cards #4479/#4448/#4447. **Added `kbcard archive` + `kbcard delete [--hard]`** (#118, roundtable FR #74 sola-pm): the board API has no HTTP `DELETE` (removal is a non-discoverable PATCH `_action` + a two-step force-delete), so agents hand-rolled `curl`; `archive` = off-board recoverable live row (keeps `dl_number`), `delete` = trashed/recoverable (retains `dl_number`), `delete --hard` = soft-then-force-delete permanent cascade (releases `dl_number`); thin orchestration over `resolve_task`+`kb_api`, `--dry-run` + `--yes` gate. **Fixed `board-snapshot` sibling-board stage-id inheritance** (#119, card #4448): `untriaged()` unset only `KBCARD_TOKEN_FILE`, so a board env omitting a `KB_STAGE_*` terminal id or `KB_BOARD_ID` silently inherited a previously-sourced board's value — a quiet failure of the "triage never silently missed" contract; now scrubs the full set `snap()` does + gains the `KB_BOARD_ID` guard. **Perf: `board-snapshot` fetches each board once** (#120, card #4447): `snap()` + `untriaged()` each re-fetched the same ~215KB board payload per SessionStart (~half the API time; per-request TLS cost) risking a no-snapshot kill at the 10s hook timeout — merged into one `board_report()` (canon #5), 7.24s → 4.85s on the reference host. |
| v0.15.1 | 2026-07-16 | **Patch — the v0.15.0 staleness guard now reaches its reader + a board-snapshot stage-name fix.** PRs #114, #115. **Fixed (#114, card #4393):** `board-snapshot` ran `agent-board-toolkit-runtime-check --quiet` but the guard warns on **stderr** while the SessionStart hook consuming `board-snapshot` surfaces only **stdout** — so a correct guard on a stale pin warned into the void. Folded to stdout; a stale-pin test + positive control pin it (dropping the `2>&1` reds 2 checks). **Fixed (#115, card #4444):** `board-snapshot`'s hardcoded `In Progress`/`In Review`/`Held` label map was a private second copy of the board's column names, so a column rename made it print `[Held]` for a renamed `Blocked`/`Gated` column. Names now source from the board via `preload.json` (~2.5KB); selection still keys on env stage ids (unchanged); an unreachable preload or undefined id degrades to `stage <id>`, not a stale literal. `kb_api` now honors `KB_CURL_MAX_TIME` so the new preload call can't reintroduce the SessionStart stall the cap prevents. **Docs (#114):** `VERSIONING.md` gains the missing **DEPLOY step** (flow step 11) — a tag is not a deploy, which is why v0.15.0 shipped while the on-PATH runtime stayed at v0.14.0; step 11 advances the pin, re-runs the symlink loop (a symlink tracks content, not the *set* of tools), runs the guard, exercises one real command. Cards #4393/#4444. |
| v0.15.0 | 2026-07-15 | **Minor — a quiet-wrong sweep + a guard that judges what EXECUTES.** 10 PRs (#101–#110). **New tool `agent-board-toolkit-runtime-check`** (#108, cards #4361/#4351): resolves the on-PATH tools through their symlink chain and fails loud on a stale pin (tags fetched first — a never-fetching pin is stale-blind), stale real-file copies (`cmp` vs reference), or mixed runtimes; unverifiable = named UNKNOWN, never silent-ok; `board-snapshot` runs it `--quiet` at SessionStart. **Fixed, all one shape — a surface answering plausibly-wrong instead of failing loud:** `kbcard show`'s `stage: null` on a staged card (#107, roundtable #53 — cost a peer a false escalation) + the same fabrication on the write echoes (#109); `fetch_board_cards` losing the API error body so 403 and 422 were indistinguishable (#103); the short-read backstop that warned INCOMPLETE then returned 0, letting `next-dl` mint from a read the lib had declared incomplete (#106 — now rc 4, with a pre-dedup census separating real undercounts from page-boundary dedup artifacts); `release-pr-body`'s baseline read from the stale LOCAL main ref, listing already-shipped PRs as new (#105 — now fetches origin, fails loud offline). **Docs:** UPGRADE.md §6's cumulative walk restored across v0.9.0–v0.13.0 (#104 — the required `KANBAN_EXPECTED_HOST` action was unreachable under a stale 'Unreleased' heading); Windows/MSYS copy-topology note (#102); HOOKS.md tracks the agent-webhook-bridge v0.57.0 correlation grammar (#101). **[vendor] re-vendor `promote-released-cards` (#110) + `_kb-board-lib.sh` (#103/#106).** Cards #4333/#4337/#4338/#4345/#4348/#4351/#4361/#4387/#4389/#4390. |
| v0.14.0 | 2026-07-15 | **Minor — an `api_base` host-guard security fix + the restoration of board-env token precedence broken since v0.8.2.** PRs #98, #97. **Security (#98):** both copies of the guard that validates a config-supplied `api_base` before the bearer token is sent terminated the URL authority at `/` alone, where RFC 3986 ends it at the first of `/?#` — so `https://evil.example#@your.host` parsed as `your.host` and was **accepted** while curl sent the token to `evil.example`. Since `api_base` lives in the committed, PR-editable `.release-pr.json`, a pull request alone sufficed. Both copies now terminate at `[/?#]`; new `kb-host-guard-selftest.sh` + CI job pin a hostile-URL matrix against **both** and assert they agree. **[vendor] must re-vendor `promote-released-cards`**; `promote/action.yml` unchanged. **Precedence (#97):** the v0.8.2 lib extraction collapsed six divergent config resolvers onto the two with the source order backwards, so a board-env `KBCARD_TOKEN_FILE` was ignored for five minor versions and the host/default token went to every board. Restored as one ladder (**board > host > ambient > default**) by source order; a board-env `KBCARD_API` is now refused loudly; `board-snapshot` reads a token per board (it used to render **nothing** on a per-board-token host); `board-card-start` names why a card didn't move instead of always saying "does not exist", and gates per-board tokens on a host-local board id. **[host] verify per-board tokens if you set `KBCARD_TOKEN_FILE` in both a board env and the host env.** Cards #4346/#4325. |
| v0.13.0 | 2026-07-14 | **Minor — `board-card-start` adoption hardening + Windows/Git-Bash curl portability.** PR #94. **Portability:** all 8 kanban-call sites (shared `_kb-board-lib.sh`, `board-card-start`, `next-dl`, `promote-released-cards`) feed the bearer token via a stdin herestring (`-H @- <<<`) instead of `-H @<(…)` process substitution — works on native mingw64/Git-Bash curl (where the `/dev/fd` path can't be opened, rc=26), keeps the token out of argv **and** off persistent disk, and fixes a latent non-seekable `--retry` bug; `promote/action.yml` unchanged (SHA-pinned consumers see only the internal fix). **`board-card-start`:** resolves board id from a repo-local uncommitted `git config kanban.board-id` when there's no `.release-pr.json` (adopt without a committed `api_base` surface); loud + durable-logged (`~/.cache/agent-board-toolkit/board-card-start.log`) on an attempted-but-failed move while genuine no-ops stay silent; widened+anchored host-scrub placeholder detector (RFC-2606/6761 reserved forms). **`install-board-hooks`:** honors `core.hooksPath` (install into it; refuse a tracked-worktree hooksPath with guidance) instead of a silent no-op. Back-compat: absent-config identical. Cards #4281/#4289/#4290. |
| v0.12.2 | 2026-07-14 | **Patch — `docs/HOOKS.md` clarifies un-parking a pinned card is intentionally NOT mirrored by the local hook (docs only; no bin/CI/vendored-surface change).** PR #91. A push-path mover can override a deliberate pin (opt-in unpark stage set on branch-cut) *because it has a durable compensating override-alert surface*; the local `board-card-start` post-checkout hook has only stderr (effectively silent when an agent drives git, never persisted), so it deliberately does NOT mirror the override and keeps refusing pinned cards. `bin/*` / `promote/action.yml` / `examples/*` / CI byte-identical to v0.12.1 — pin/vendor consumers see no functional change. |
| v0.12.1 | 2026-07-13 | **Patch — agent-facing DL-counter recovery runbook (docs; one comment-only `bin/next-dl` change).** PR #88. New `docs/DL-COUNTER-RECOVERY.md`: an **API-driven** runbook for recovering a stranded per-board DL counter (`next-dl` minting far-too-high numbers because the monotonic high-water mark was pushed above the real sequence by a hard-deleted/trashed high-DL card), written for an operator/agent driving a board over its API token with no server shell — diagnose, exhaustively sweep every junk pinner, force-delete each, reset. `bin/next-dl` gains a comment-only pointer to it (executable path byte-identical); `README.md` gains an index row. `promote/action.yml`, all other `bin/*`, and `examples/*` byte-identical to v0.12.0 — pin/vendor consumers see no functional change. Card #4045. |
| v0.12.0 | 2026-07-12 | **Minor — new `bin/adopt-to-dl`: the pull-into-build adoption seam for card-first boards.** PR #85. Stamps an existing plain card with `payload.dl_number` + a source-qualified placeholder `pr_url` in one atomic write (reusing `next-dl` for the mint + `kbcard patch` for the write — no reimplementation), then fail-loud-verifies the card resolves via the kanban `by-ref?system=dl&ref=N&source=<repo>` correlation — so `source` resolves at adoption, not silently at the next `bridge:check`. An already-adopted guard refuses to re-mint over a card already carrying a `dl_number` (would orphan it + strand its branch/PR); `--dl N` re-stamps idempotently for a crash-retry; the verify lowercases the source so a mixed-case `--repo` resolves. Net-new bin (sources `_kb-board-lib.sh`); all existing `bin/*`/`promote`/`examples` byte-identical to v0.11.4, so pin/vendor consumers see only the new bin. Network-free selftest wired into CI. Card #4020. |

@CLAUDE_AGENTBOARD.md
